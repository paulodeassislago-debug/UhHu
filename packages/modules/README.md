# packages/modules — fronteira reservada

Fronteiras futuras: `platform` | `lab` | `lib` | `note` | `plan` | `prof`.

Não implementar antes da spec própria de cada módulo.
Cada módulo é dono das próprias regras e tabelas.
