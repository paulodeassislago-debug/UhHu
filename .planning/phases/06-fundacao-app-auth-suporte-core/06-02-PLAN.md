---
phase: 06-fundacao-app-auth-suporte-core
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/lab/package.json
  - apps/lab/tsconfig.json
  - apps/lab/app.json
  - apps/lab/babel.config.js
  - apps/lab/metro.config.js
  - apps/lab/.npmrc
  - apps/lab/README.md
  - apps/lab/app/_layout.tsx
  - apps/lab/app/index.tsx
  - apps/lab/app/login.tsx
  - apps/lab/app/register.tsx
  - apps/lab/app/projects.tsx
  - apps/lab/app/project/[id].tsx
  - apps/lab/src/api/client.ts
  - apps/lab/src/api/auth.ts
  - apps/lab/src/api/projects.ts
  - apps/lab/src/api/lab.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - User abre o app na web e no tablet sem erro de build e navega entre as telas do esqueleto
    - App consome somente o CORE via client tipado derivado dos schemas compartilhados sem cópia local de tipos
    - Bundle web não contém segredos e typecheck strict passa com any proibido
  artifacts:
    - path: apps/lab/package.json
      provides: Scaffold Expo do Lab
      contains: expo
    - path: apps/lab/src/api/client.ts
      provides: Fetch wrapper tipado do CORE
      contains: apiFetch
    - path: apps/lab/app/_layout.tsx
      provides: Navegação entre telas do esqueleto
      contains: Stack
  key_links:
    - from: apps/lab/src/api/client.ts
      to: packages/contracts
      via: import type de DTOs e schemas Zod sem duplicar tipos
      pattern: from '@uhhu/contracts'
    - from: apps/lab/app/_layout.tsx
      to: apps/lab/src/api/client.ts
      via: telas chamam o client, nunca fetch direto nem banco
      pattern: apiFetch|authApi|projectsApi
---

<objective>
Tirar o app do zero: scaffold Expo em apps/lab (web + nativo) com navegação do esqueleto e client tipado do CORE em definição única.

Purpose: Base executável para auth (06-03) e estados/gates (06-04); prova que web e tablet abrem sem erro de build.
Output: App Expo que abre na web e no Expo Go, 7 rotas placeholder navegáveis, client tipado sem cópia de tipos.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fundacao-app-auth-suporte-core/06-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/03-lab-spec-v1.md
@dev-docs/07-core-contract.md
@dev-docs/08-security-baseline.md
@apps/lab/README.md
@packages/contracts/src/index.ts
@packages/contracts/src/auth.ts
@packages/contracts/src/projects.ts
@packages/contracts/src/lab.ts
@pnpm-workspace.yaml
@tsconfig.base.json

<interfaces>
From packages/contracts (definição única — NÃO copiar tipos no app):
```typescript
// auth.ts
export const loginSchema = z.object({ email: emailSchema, password: z.string().min(1).max(128), rememberMe: z.boolean().optional().default(true) });
export const patCreateSchema = z.object({ email: emailSchema, password: z.string().min(1).max(128), deviceName: patDeviceNameSchema });
export interface PublicUser { id: string; name: string; email: string; role: UserRole; createdAt: string; }
// projects.ts
export const createProjectSchema = z.object({ title: z.string().trim().min(1).max(200), researchQuestion: z.string().trim().max(2000).optional(), description: z.string().trim().max(5000).optional() });
export interface ProjectDTO { id: string; title: string; researchQuestion: string | null; description: string | null; status: ProjectStatus; createdAt: string; updatedAt: string; referenceSearchId: string | null; }
// lab.ts
export interface SearchDTO { id: string; projectId: string; term: string; filters: SearchFilters; sources: ExecutableSource[]; createdAt: string; updatedAt: string; }
```
API base (dev-docs/07 §11 + código real): `POST /api/v1/auth/login|logout|register`, `POST /api/v1/auth/token`, `GET /api/v1/auth/me`, `GET|POST /api/v1/projects`, `GET|PATCH /api/v1/projects/:id`, `GET /api/v1/lab/*`. Envelope de erro PT-BR: `{ error: { code, message, details, requestId } }` + header `x-request-id`. Nenhuma UI acessa PG/storage/externo direto (AGENTS.md).
Decisões CONTEXT: D-05 Expo Go (+web) sem EAS na fase 6; D-06 Android primeiro; SDK/estrutura a critério do executor com TS strict + `any` proibido.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: scaffold Expo em apps/lab com navegação do esqueleto</name>
  <files>apps/lab/package.json, apps/lab/tsconfig.json, apps/lab/app.json, apps/lab/babel.config.js, apps/lab/metro.config.js, apps/lab/.npmrc, apps/lab/README.md, apps/lab/app/_layout.tsx, apps/lab/app/index.tsx, apps/lab/app/login.tsx, apps/lab/app/register.tsx, apps/lab/app/projects.tsx, apps/lab/app/project/[id].tsx</files>
  <read_first>
    - apps/lab/README.md (fronteira reservada — será substituída pelo scaffold)
    - pnpm-workspace.yaml (apps/* já incluído)
    - tsconfig.base.json (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes — estender, nunca enfraquecer)
    - dev-docs/10-lab-esqueleto-telas.md §1 (mapa de navegação) e §2-§4 (login/convite, projetos, cabeçalho+abas)
  </read_first>
  <action>Scaffoldar o Lab do zero per UI-01 + D-05/D-06, sem EAS/store/iOS:
  1. Resolver versão vigente: rodar `npm view expo version` e `npm view expo-router version` na hora da execução e pinar as EXATAS instaladas no SUMMARY (não chutar SDK 49/50 do passado; pesquisador dispensado porque research pulado — o executor confirma a vigente). Criar app com `npx create-expo-app@latest apps/lab-tmp --template blank-typescript` ou equivalente, depois mover o conteúdo para apps/lab PRESERVANDO o diretório (não deletar apps/lab, só substituir o README de fronteira). Alternativa aceitável: montagem manual de package.json + app.json + babel + metro se o template conflitar com pnpm — documentar a escolha no SUMMARY.
  2. Dependências mínimas: `expo`, `expo-router`, `react`, `react-native`, `react-native-web`, `expo-secure-store`, `expo-device`, `zod` (mesma major do contracts). Usar `expo-router` com typed routes para as 7 rotas placeholder: `/` (redirect para /login ou /projects conforme sessão), `/login` (§2: email+senha+entrar+link convite), `/register` (token de convite), `/projects` (§3: lista + CTA criar), `/project/[id]` (§4: cabeçalho pergunta+status + abas Estratégias/Comparação/Corpus com contador vivo placeholder), mais 2 placeholders de aba aninhada se o router exigir arquivos (ex.: `project/[id]/strategies.tsx`, `project/[id]/corpus.tsx`) — total navegável sem erro de build. Cada tela placeholder exibe título + texto de mapeamento CORE (ex.: "Lista de Projetos — GET /api/v1/projects") e link para a próxima (navegação real, não botão morto).
  3. TypeScript strict: apps/lab/tsconfig.json com `extends: ../../tsconfig.base.json`, `strict: true`, `noEmit: true`, includes de `app/**` + `src/**`; tipos de domínio SOMENTE via `import type { ... } from '@uhhu/contracts'` (UI-02 antecipado aqui na navegação: nenhum `interface Project {...}` local). `any` proibido: garantir que `eslint.config.mjs` raiz já barra `@typescript-eslint/no-explicit-any` (não criar exceção para apps/lab).
  4. pnpm workspace: garantir `apps/lab/package.json` com `name: "@uhhu/lab"`, scripts `typecheck: tsc --noEmit -p tsconfig.json`, `lint: eslint .`, `test: vitest run`, `start: expo start`, `web: expo start --web`; dependência `"@uhhu/contracts": "workspace:*"`. Se Metro reclamar de symlinks pnpm, criar `apps/lab/.npmrc` com `node-linker=hoisted` + `symlink=true` APENAS nesse diretório (não tocar no .npmrc raiz se existir). app.json com `name: UhHu Lab`, `slug: uhhu-lab`, `scheme: uhhu-lab`, `platforms: ["ios","android","web"]`, `experiments.typedRoutes: true`.
  5. Atualizar apps/lab/README.md: trocar a fronteira reservada por descrição do scaffold (como rodar web `pnpm --filter @uhhu/lab web`, como abrir no Expo Go via tailnet, que D-05 dispensa EAS aqui).
  6. Prova de build: `pnpm --filter @uhhu/lab exec tsc --noEmit` exit 0 e `pnpm --filter @uhhu/lab exec expo export --platform web` (ou `npx expo export --platform web`) gera `apps/lab/dist/` sem erro. Tablet validado via Expo Go (instruções no SUMMARY; sem build EAS, sem conta, sem assinatura per D-05).
  </action>
  <acceptance_criteria>
    - `test -f apps/lab/package.json && grep -c '"@uhhu/contracts": "workspace' apps/lab/package.json` >= 1
    - `grep -c "extends.*tsconfig.base" apps/lab/tsconfig.json` >= 1 e `grep -c '"strict": false\|"strict": 0' apps/lab/tsconfig.json` == 0
    - `ls apps/lab/app/*.tsx apps/lab/app/project/*.tsx 2>/dev/null | wc -l` >= 5 (rotas placeholder existem)
    - `grep -rn "interface Project\|interface Search\|type ProjectDTO" apps/lab/app apps/lab/src 2>/dev/null | grep -v "import type" | wc -l` == 0 (sem cópia local de tipos)
    - `pnpm --filter @uhhu/lab exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit && ls apps/lab/app/*.tsx</automated>
  </verify>
  <done>Expo abre na web (export ok) e no Expo Go, navegação entre placeholders funciona, TS strict verde, zero tipo de domínio duplicado</done>
</task>

<task type="auto">
  <name>task 2: client tipado do CORE em definição única</name>
  <files>apps/lab/src/api/client.ts, apps/lab/src/api/auth.ts, apps/lab/src/api/projects.ts, apps/lab/src/api/lab.ts</files>
  <read_first>
    - packages/contracts/src/index.ts (o que é reexportado — fonte única)
    - packages/contracts/src/auth.ts (loginSchema, patCreateSchema, PublicUser)
    - packages/contracts/src/errors.ts (ERROR_CATALOG, buildEnvelope — formato PT-BR)
    - apps/core-api/src/routes/auth.ts linhas 237-300 (contrato real de login/token: 201 com {token, ...info} no PAT, cookie httpOnly no login)
    - apps/lab/app/_layout.tsx criado na task 1 (onde o client será consumido)
  </read_first>
  <action>Implementar UI-02 (SOMENTE o CORE, sem backend próprio) com fetch wrapper único:
  1. Em apps/lab/src/api/client.ts: `API_BASE_URL` vindo de `process.env.EXPO_PUBLIC_API_BASE_URL` com fallback `http://127.0.0.1:3000` em dev (NUNCA hardcodar URL de prod; NUNCA segredo com prefixo EXPO_PUBLIC — só base URL pública). `apiFetch<T>(path, opts)` com: `credentials: 'include'` SEMPRE (web cookie httpOnly); header `Authorization: Bearer <PAT>` injetado via getter assíncrono SOMENTE no nativo (o token vive em SecureStore — ver 06-03; aqui o client recebe `getToken: () => Promise<string|null>` por injeção, sem importar SecureStore direto para manter web testável); `x-request-id` gerado por `crypto.randomUUID()` quando disponível; `Content-Type: application/json`; parse do envelope de erro `{error:{code,message,requestId}}` para `ApiError` tipado com `code`, `message` PT-BR verbatim, `requestId`, `status`; Zod na fronteira para bodies de saída (usar os schemas do contracts para montar login/register/PAT bodies — nunca montar objeto à mão sem parse).
  2. Em apps/lab/src/api/auth.ts: `authApi.login(input: LoginInput): Promise<PublicUser>` (POST /api/v1/auth/login), `authApi.register(input: RegisterInput)`, `authApi.me(): Promise<PublicUser|null>` (GET /auth/me, 401→null sem lançar), `authApi.logout()` (POST /auth/logout, 204), `authApi.issuePat(input: PatCreateInput): Promise<{token:string} & PersonalAccessTokenInfo>` (POST /api/v1/auth/token, raw circula UMA vez — repassar sem logar). Todos os tipos de entrada/saída via `import type` de @uhhu/contracts; view models de apresentação SÓ com mapeamento explícito se precisar (ex.: `AuthViewModel {user, expired}` com função `toAuthViewModel` documentada).
  3. Em apps/lab/src/api/projects.ts: `projectsApi.list/listById/create/update(referenceSearchId incl.)` com ProjectDTO de contracts; update aceita `{title?, researchQuestion?, description?, status?, referenceSearchId?}` espelhando updateProjectSchema. Em apps/lab/src/api/lab.ts: stubs finos para fases 7-9 (searches/runs/results/corpus/export/sources/compare) que chamam os paths reais do contrato §11 mas retornam os DTOs de contracts sem lógica de negócio (o Core garante filtro/dedup; o client nunca filtra localmente para decidir elegibilidade).
  4. Proibições AGENTS.md: zero `any`/`as any`/`@ts-ignore` (usar `unknown` + narrowing no parse de JSON); zero `localStorage` para privilégio/owner; zero import de `postgres`/drizzle/storage externo; URL/endpoint sempre dos paths do contrato (não inventar `/api/v2/...`).
  </action>
  <acceptance_criteria>
    - `grep -c "from '@uhhu/contracts'" apps/lab/src/api/*.ts` >= 4 e `grep -rn "interface PublicUser\|interface ProjectDTO\|interface SearchDTO" apps/lab/src 2>/dev/null | wc -l` == 0
    - `grep -c "import type" apps/lab/src/api/*.ts` >= 4 (tipos via import type)
    - `grep -rn ": any\|as any\|@ts-ignore\|Array<any>" apps/lab/src apps/lab/app 2>/dev/null | wc -l` == 0
    - `grep -c "credentials.*include" apps/lab/src/api/client.ts` >= 1 e `grep -c "EXPO_PUBLIC_API_BASE_URL" apps/lab/src/api/client.ts` >= 1
    - `grep -c "localStorage" apps/lab/src -r 2>/dev/null | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit && grep -rn "as any\|: any" apps/lab/src apps/lab/app | wc -l</automated>
  </verify>
  <done>Client chama o CORE com tipos compartilhados, cookie no web + Bearer injetado no nativo, erros PT-BR tipados, zero tipo duplicado e zero any</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → API | Toda resposta é dado hostil até validada; client nunca decide privilégio |
| web bundle → navegador | Bundle é público; nenhum segredo com prefixo EXPO_PUBLIC além da base URL |
| nativo SecureStore → memória | PAT em repouso só em SecureStore; nunca em AsyncStorage/localStorage/log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | web bundle | mitigate | só EXPO_PUBLIC_API_BASE_URL é pública; grep de segredos no bundle em 06-04; nenhum token hardcoded |
| T-06-02-02 | Tampering | tipos locais divergentes | mitigate | definição única em @uhhu/contracts + `import type`; teste grep barra interface duplicada |
| T-06-02-03 | Spoofing | client decide owner | mitigate | ownerId nunca enviado nem lido do storage; servidor deriva do ator; guards do app são UX |
| T-06-02-04 | Information Disclosure | logs do client | mitigate | raw do PAT e cookies nunca logados; ApiError carrega só code/message/requestId |
| T-06-02-05 | Denial of Service | Metro/pnpm quebra build | accept | .npmrc local hoisted só em apps/lab; sem EAS; Expo Go como validação manual documentada |
</threat_model>

<verification>
- `pnpm --filter @uhhu/lab exec tsc --noEmit` exit 0
- `expo export --platform web` gera dist sem erro
- Greps de tipos: zero interface de domínio local, >=4 imports de @uhhu/contracts com import type
- Grep `any`: zero ocorrências em app/src
</verification>

<success_criteria>
- UI-01: web e Expo Go abrem, 7 rotas placeholder navegáveis sem erro de build
- UI-02: todo dado do CORE passa pelo client tipado derivado dos schemas compartilhados, sem cópia local
</success_criteria>

<output>
After completion, create `.planning/phases/06-fundacao-app-auth-suporte-core/06-02-SUMMARY.md`
</output>
