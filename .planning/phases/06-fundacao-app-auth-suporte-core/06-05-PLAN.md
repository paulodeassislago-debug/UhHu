---
phase: 06-fundacao-app-auth-suporte-core
plan: "05"
type: execute
wave: 4
depends_on:
  - 06-04
gap_closure: true
gap_source: 06-HUMAN-UAT.md (teste humano real 11/09/2026 — lista de Projetos não rola, web + nativo)
files_modified:
  - apps/lab/app/projects.tsx
  - apps/lab/app/login.tsx
  - apps/lab/app/register.tsx
  - apps/lab/app/project/[id].tsx
  - apps/lab/src/ui/__tests__/scroll-containers.test.ts
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - User rola a lista de Projetos até o fim na web e no nativo (24 itens de DEV acessíveis, nenhum conteúdo cortado sem scroll container)
    - Telas de formulário (login/register) e detalhe do projeto não cortam conteúdo em telas pequenas (scroll vertical presente)
    - Gate tripwire falha se uma tela com lista voltar a renderizar sem ScrollView/FlatList
  artifacts:
    - path: apps/lab/app/projects.tsx
      provides: Lista de projetos rolável com virtualização
      contains: FlatList
    - path: apps/lab/src/ui/__tests__/scroll-containers.test.ts
      provides: Tripwire contra regressão de scroll
      contains: ScrollView|FlatList
  key_links:
    - from: apps/lab/app/projects.tsx
      to: apps/lab/src/ui/Empty.tsx
      via: lista vazia continua renderizando Empty (ListEmptyComponent ou branch preservado)
      pattern: Empty
    - from: apps/lab/app/projects.tsx
      to: apps/lab/src/ui/ErrorBanner.tsx
      via: erro continua com banner + repetir após a troca para FlatList
      pattern: ErrorBanner|onRetry
---

<objective>
Corrigir o gap do UAT humano: lista de Projetos sem rolagem (View+map) + hardenecer as demais telas + tripwire de regressão.

Purpose: A fase 6 não fecha aprovada com conteúdo inacessível abaixo da dobra; o bug passa em vitest/expo-export, então a correção inclui um gate que falha se o padrão voltar.
Output: Telas roláveis, teste tripwire verde, bundle web reexportado, SUMMARY com auditoria rápida.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fundacao-app-auth-suporte-core/06-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/08-security-baseline.md
@apps/lab/app/projects.tsx
@apps/lab/app/login.tsx
@apps/lab/app/register.tsx
@apps/lab/app/project/[id].tsx

<interfaces>
Evidência do UAT (11/09/2026, Chrome headless via CDP): body.scrollHeight=1668 vs clientHeight=493, body.overflowY=hidden, nenhum elemento com overflow auto/scroll. Causa: projects.tsx usa `<View style={{flex:1,...}}>` + `items.map(...)` sem ScrollView/FlatList. No nativo, View também não rola — conteúdo abaixo da dobra inacessível.
Regra: toda lista (`.map` sobre dados) usa FlatList (virtualização; 24+ itens) ou ScrollView; toda tela de formulário/detalhe usa ScrollView como raiz rolável para não cortar em telas pequenas. Preservar estados §11 (Empty/ErrorBanner/CardSkeleton) e textos verbatim do esqueleto. Zero `any`; `import type` para tipos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: rolagem real em projects + hardening das demais telas</name>
  <files>apps/lab/app/projects.tsx, apps/lab/app/login.tsx, apps/lab/app/register.tsx, apps/lab/app/project/[id].tsx</files>
  <read_first>
    - apps/lab/app/projects.tsx (lista View+map a converter; preservar Empty/ErrorBanner/CardSkeleton e link /project/[id])
    - apps/lab/app/project/[id].tsx (abas placeholder; checar conteúdo estático a embrulhar)
    - apps/lab/app/login.tsx e apps/lab/app/register.tsx (formulários; checar teclado/overflow)
  </read_first>
  <action>
    1. Em apps/lab/app/projects.tsx: trocar o `View` + `items.map(...)` por `FlatList` com `data={items}`, `keyExtractor={(p) => p.id}`, `renderItem` renderizando o MESMO card (Link /project/[id] + pergunta truncada + status), `contentContainerStyle={{ padding: 24, gap: 12, flexGrow: 1 }}`, `ListHeaderComponent` com o título "Projetos", `ListEmptyComponent` reusando o branch Empty existente (manter textos verbatim), `ListFooterComponent` com o botão "Nova pesquisa (em breve — fase 7)" + link voltar. Loading/erro/redirecionamento/vazio-zero-itens mantêm os branches atuais (só o branch de lista vira FlatList). Cabeçalho sticky não é requerido.
    2. Em apps/lab/app/login.tsx e apps/lab/app/register.tsx: embrulhar o conteúdo em `ScrollView` com `contentContainerStyle={{ flexGrow: 1, padding: 24, gap: 12 }}` + `keyboardShouldPersistTaps="handled"` (formulário não pode cortar em tela pequena nem esconder o submit atrás do teclado no submit por toque).
    3. Em apps/lab/app/project/[id].tsx: embrulhar cada branch de conteúdo estático em `ScrollView` com o mesmo contentContainerStyle (abas/estratégias/corpus chegam nas fases 7-9 já roláveis).
    4. Não mudar contrato, rotas, auth, textos §11 ou navegação — só contêiner de rolagem. Verificar que nenhum `items.map` direto em `View` resta nas telas: `grep -rn "\.map(" apps/lab/app/ | grep -v FlatList | wc -l` == 0 para listas de dados.
  </action>
  <acceptance_criteria>
    - `grep -c "FlatList" apps/lab/app/projects.tsx` >= 3 (import + uso + props)
    - `grep -c "ScrollView" apps/lab/app/login.tsx apps/lab/app/register.tsx "apps/lab/app/project/[id].tsx"` >= 3 (um por tela)
    - `grep -rn "\.map(" apps/lab/app/ | grep -v "FlatList\|renderItem" | wc -l` == 0 (nenhuma lista fora de contêiner rolável)
    - `grep -c "Empty\|ErrorBanner\|CardSkeleton" apps/lab/app/projects.tsx` >= 3 (estados §11 preservados)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit</automated>
  </verify>
  <done>Lista de Projetos rola na web e no nativo; formulários e detalhe não cortam conteúdo; estados §11 intactos</done>
</task>

<task type="auto">
  <name>task 2: tripwire de regressão + gates + reexport + auditoria</name>
  <files>apps/lab/src/ui/__tests__/scroll-containers.test.ts</files>
  <read_first>
    - apps/lab/src/api/__tests__/client.test.ts (padrão de teste do app: vitest, zero `any`, sem rede real)
    - apps/lab/package.json (script `test: vitest run` cobre src/**; confirmar que o novo teste entra no glob)
  </read_first>
  <action>
    1. Criar apps/lab/src/ui/__tests__/scroll-containers.test.ts (vitest, sem rede, zero `any`): lê os fontes das telas via `readFileSync` + `new URL(..., import.meta.url)` e asserta: projects.tsx importa e usa `FlatList` de react-native; login.tsx, register.tsx e project/[id].tsx usam `ScrollView`; nenhum `.map(` de lista fora de FlatList/renderItem nas telas. Comentário no topo explicando o motivo (gap UAT 11/09/2026: View+map corta conteúdo; vitest/expo-export não pegam layout).
    2. Rodar gates do app: `pnpm --filter @uhhu/lab run typecheck`, `lint`, `test` — todos exit 0. `grep -rn ": any\|as any\|@ts-ignore" apps/lab/src apps/lab/app | wc -l` == 0.
    3. Reexportar bundle web (`expo export --platform web`) + grep de segredos em dist == 0 (mesmo padrão 06-04 task 2 item 4).
    4. Auditoria adversarial rápida do diff (arquivo-por-arquivo: o FlatList expõe algum dado novo? keyExtractor usa id opaco? nenhum log/token adicionado?) como seção do SUMMARY. Sem autoaprovação.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @uhhu/lab run typecheck && pnpm --filter @uhhu/lab run lint && pnpm --filter @uhhu/lab run test` todos exit 0 (inclui o tripwire novo)
    - `test -f apps/lab/src/ui/__tests__/scroll-containers.test.ts`
    - `grep -rE "uhhu_pat|COOKIE_SECRET|DATABASE_URL" apps/lab/dist/ 2>/dev/null | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test 2>&1 | tail -n 8</automated>
  </verify>
  <done>Tripwire verde impede regressão do padrão View+map; gates verdes; bundle reexportado sem segredos</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FlatList renderItem → dados | mesmos DTOs já exibidos; nenhum campo novo exposto |
| teste lê fonte | teste de build-time sobre código versionado; não executa dado de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | card na FlatList | mitigate | renderItem exibe os mesmos campos do card atual; keyExtractor usa id opaco |
| T-06-05-02 | Denial of Service | lista longa sem virtualização | mitigate | FlatList virtualiza (24+ itens de DEV); ScrollView só em conteúdo curto/estático |
| T-06-05-03 | Tampering | tripwire que mente | mitigate | teste asserta import+uso reais, não só presença de string solta; typecheck valida o fonte |
</threat_model>

<verification>
- typecheck + lint + testes do app exit 0 com zero `any` (inclui tripwire)
- `expo export --platform web` ok + grep de segredos em dist == 0
- Nenhum `.map` de lista fora de FlatList nas telas; ScrollView em login/register/[id]
</verification>

<success_criteria>
- UI-05: lista de Projetos rolável e integral na web e no nativo (gap UAT fechado no código; re-teste humano pendente no UAT)
- UI-04: gates verdes mantidos + tripwire novo impede regressão
</success_criteria>

<output>
After completion, create `.planning/phases/06-fundacao-app-auth-suporte-core/06-05-SUMMARY.md`
</output>
