---
phase: 05-prova-headless
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/core-api/src/auth/pat.ts
  - apps/core-api/src/auth/requireAuth.ts
  - apps/core-api/src/auth/session.ts
  - apps/core-api/src/routes/auth.ts
  - apps/core-api/src/capabilities.ts
  - apps/core-api/src/routes/lab.ts
  - apps/core-api/src/routes/projects.ts
  - apps/core-api/src/plugins/rateLimit.ts
  - tests/integration/pat-auth.test.ts
autonomous: true
requirements: [CORE-02, CORE-05]
must_haves:
  truths:
    - "CLI obtém PAT via login por API sem cookie e lista PATs ativos, revoga um e revoga todos"
    - "Bearer PAT acessa as mesmas rotas lab/projects com o mesmo 404 IDOR e o mesmo envelope PT-BR"
    - "Toda rota lab/projects passa por execute() — não existe chamada direta a lib fora do mapa de handlers"
    - "Emissão de PAT compartilha lockout 5→15min e rate-limit do login; fora do escopo continua 404 idêntico"
  artifacts:
    - path: "apps/core-api/src/auth/pat.ts"
      provides: "issue/list/revoke/touchPat/resolvePat com sliding 30d"
      exports: ["issuePat", "listPatsForUser", "revokePat", "revokeAllPats", "resolvePat", "touchPat"]
    - path: "apps/core-api/src/auth/requireAuth.ts"
      provides: "Bearer-first + cookie fallback, 401 genérico, ActorContext pat"
      contains: "authMethod: 'pat'"
    - path: "apps/core-api/src/capabilities.ts"
      provides: "Mapa CapabilityName→*ForActor + executor singleton"
      contains: "createExecutor"
    - path: "tests/integration/pat-auth.test.ts"
      provides: "IDOR via Bearer + ciclo PAT + regressão de sessão"
      min_lines: 120
  key_links:
    - from: "apps/core-api/src/auth/requireAuth.ts"
      to: "apps/core-api/src/auth/pat.ts"
      via: "resolvePat + touchPat em background (como touchSession)"
      pattern: "resolvePat|touchPat"
    - from: "apps/core-api/src/routes/lab.ts"
      to: "apps/core-api/src/capabilities.ts"
      via: "toda handler chama execute(), nenhuma importa lib/*ForActor direto"
      pattern: "execute\("
    - from: "apps/core-api/src/routes/auth.ts"
      to: "apps/core-api/src/auth/pat.ts"
      via: "POST /auth/token emite; GET/DELETE /auth/tokens listam/revogam"
      pattern: "personal_access_tokens|issuePat"
---

<objective>
Servidor headless: emissão e validação de PATs + rotas lab/projects como adaptadores finos sobre execute(). Fecha D-60–D-63 e o núcleo de CORE-02/CORE-05 server-side.

Purpose: Sem este plano CLI/MCP não têm contra o que autenticar nem capability única para chamar. Rotas viram prova viva de "uma regra, uma implementação": REST passa a consumir o mesmo execute() que documenta o contrato §10.
Output: pat.ts, requireAuth Bearer, capabilities.ts com mapa total, rotas refatoradas sem mudança de semântica, PAT CRUD, integração PG verde.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prova-headless/05-CONTEXT.md
@.planning/phases/05-prova-headless/05-01-SUMMARY.md
@apps/core-api/src/auth/requireAuth.ts
@apps/core-api/src/auth/session.ts
@apps/core-api/src/auth/tokens.ts
@apps/core-api/src/routes/auth.ts
@apps/core-api/src/routes/lab.ts
@apps/core-api/src/routes/projects.ts
@apps/core-api/src/plugins/rateLimit.ts

<interfaces>
<!-- Contratos do 05-01 que o executor consome verbatim; nao reexplorar. -->
From packages/core/src/capabilities.ts (05-01):
```typescript
export type CapabilityHandler = (input: unknown, actor: ActorContext) => Promise<unknown>;
export function createExecutor(handlers: Record<CapabilityName, CapabilityHandler>): (name: CapabilityName | string, input: unknown, actor: ActorContext) => Promise<unknown>;
export class UnknownCapabilityError extends Error;
```
From packages/contracts/src/auth.ts (05-01): `patCreateSchema { email, password, deviceName }`, `PersonalAccessTokenInfo { id, deviceName, createdAt, lastSeenAt, expiresAt }` (sem hash/raw).
From apps/core-api/src/auth/requireAuth.ts (existente): cookie `uhhu_session` → resolveSession → actor session; 401 UNAUTHENTICATED genérico; `void touchSession(...).catch(warn)` em background. Manter TODAS essas semânticas para cookie.
From apps/core-api/src/auth/session.ts: ler `resolveSession`/`touchSession` e espelhar em pat.ts (janela sliding, update de lastSeenAt/expiresAt, erro nunca bloqueia).
Lockout existente (routes/auth.ts): MAX_FAILED_ATTEMPTS=5, LOCKOUT_MS=15min por e-mail via users.failedAttempts/lockedUntil; throttled retorna RATE_LIMITED sem tocar contadores. Emissão de PAT USA O MESMO caminho (D-63).
Rate-limit fino (plugins/rateLimit.ts): login 10/min por IP. POST /auth/token entra no MESMO bucket do login (mesma chave de throttle) — não criar bucket novo.
Escopo do refactor: SOMENTE apps/core-api/src/routes/lab.ts + projects.ts passam a chamar execute(). Rotas auth/sessão continuam diretas (bootstrap de identidade, análogo ao login — fora do registry §10). Mapeamento erro→HTTP existente (NOT_FOUND→404, VALIDATION_ERROR→400/422, IdempotencyConflictError→422, SOURCE_DISABLED→400, RATE_LIMITED→429) move para helper compartilhado sem mudar nenhum código de status.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: pat.ts + Bearer no requireAuth com sliding espelhado</name>
  <files>apps/core-api/src/auth/pat.ts, apps/core-api/src/auth/requireAuth.ts</files>
  <read_first>apps/core-api/src/auth/session.ts, apps/core-api/src/auth/tokens.ts, apps/core-api/src/auth/requireAuth.ts, packages/db/src/schema.ts</read_first>
  <action>1. Criar apps/core-api/src/auth/pat.ts (header PT-BR D-60/D-61/D-63; `import { hashToken, newOpaqueToken, patExpiry } from './tokens.js';`, nunca `Math.random`, nunca `any`):
- `export async function issuePat(db: Db, userId: string, deviceName: string): Promise<{ rawToken: string; info: PersonalAccessTokenInfo }>` — gera raw via newOpaqueToken, insere `{ userId, tokenHash: hashToken(raw), deviceName, expiresAt: patExpiry() }` em personalAccessTokens, retorna raw UMA vez + info camelCase ISO (toDTO snake→camel no molde projects.ts).
- `export async function resolvePat(db: Db, rawToken: string): Promise<{ user: User; pat: PersonalAccessToken } | null>` — lookup por hashToken; null se ausente, expirado (`expiresAt <= now`) ou revogado (`revokedAt !== null`). NUNCA distingue os três motivos (retorna null único).
- `export async function touchPat(db: Db, pat: PersonalAccessToken): Promise<void>` — espelha touchSession de session.ts (LER o arquivo e copiar a semântica sliding: atualiza lastSeenAt sempre; estende expiresAt para patExpiry() quando restante < 50% da janela de 30d). Erro nunca lança para o caller (void + catch no requireAuth).
- `export async function listPatsForUser(db, userId)` (só não-revogados, ordenados por createdAt), `revokePat(db, userId, patId)` (set revokedAt=now, escopado por userId — fora do escopo retorna null→404), `revokeAllPats(db, userId)` (revoga todos ativos; usado por logout-all).
- Logar só ids técnicos + requestId; raw NUNCA em log.
2. Estender requireAuth.ts (manter cookie 100% intacto): no início do handler, ler `request.headers.authorization`; se `Bearer <token>` presente (regex `/^Bearer ([a-f0-9]{64})$/` — raw é hex 32 bytes; formato inválido = mesmo 401 genérico, sem mensagem distinta): `resolvePat` → null = 401 UNAUTHENTICATED genérico idêntico ao de sessão; ok = `request.actor = { userId, role, requestId, authMethod: 'pat' }` + `void touchPat(...).catch(warn)` em background. Sem Bearer → caminho cookie existente. `requireAdmin` NÃO muda (lê actor, agnóstico ao método).
3. Typecheck do pacote.</action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck && pnpm vitest run --project smoke 2>&1 | tail -n 5</automated>
  </verify>
  <done>Bearer PAT válido gera actor pat; inválido/expirado/revogado dá o mesmo 401; sessão cookie inalterada</done>
  <acceptance_criteria>grep -c "resolvePat\|touchPat" apps/core-api/src/auth/requireAuth.ts >= 2; grep -c "export async function \(issuePat\|resolvePat\|touchPat\|listPatsForUser\|revokePat\|revokeAllPats\)" apps/core-api/src/auth/pat.ts == 6; grep -v '^ *//' apps/core-api/src/auth/pat.ts | grep -c "any\|Math.random" == 0; grep -c "authMethod: 'pat'" apps/core-api/src/auth/requireAuth.ts == 1</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: capabilities.ts + rotas lab/projects como adaptadores finos de execute()</name>
  <files>apps/core-api/src/capabilities.ts, apps/core-api/src/routes/lab.ts, apps/core-api/src/routes/projects.ts</files>
  <read_first>apps/core-api/src/lib/searches.ts, apps/core-api/src/lib/searchRuns.ts, apps/core-api/src/lib/projects.ts, apps/core-api/src/routes/lab.ts, apps/core-api/src/routes/projects.ts</read_first>
  <action>1. Criar apps/core-api/src/capabilities.ts: importa `createExecutor` de `@uhhu/core`, schemas de `@uhhu/contracts` e as funções `*ForActor` existentes (searches.ts, searchRuns.ts, projects.ts + — após Phase 4 — lib de corpus/decisões/export/compare/sources; LER o que existir e mapear TUDO que for capability §10; o que não existir ainda falha o generate do mapa em typecheck, o que é intencional: este plano exige Phase 4 executada). Cada entrada do mapa valida input com o schema contracts correspondente (safeParse → VALIDATION_ERROR PT-BR igual à rota fazia) e chama a função `*ForActor(db, actor, input)` — o `db` é capturado por closure via `buildExecutor(db)` (db = instância uhhu_app do boot, mesma das rotas). Exportar `buildExecutor(db)` + `execute` singleton lazy? NÃO singleton global com db: exportar `buildExecutor(db): Executor` e o boot (index.ts — ÚNICO toque permitido nele: registrar `app.decorate('executor', buildExecutor(db))` ou importar direto nas rotas via factory `buildLabRoutes(child, db)` existente passando executor; preferir: capabilities.ts exporta `buildExecutor(db)` e as rotas recebem executor como já recebem db — SEM mudar assinatura de boot além de 1 linha se preciso; se der para evitar tocar index.ts, evitar).
2. Refatorar routes/lab.ts + routes/projects.ts: cada handler troca a chamada direta `xxxForActor(db, actor, ...)` por `execute('nome.capability', input, actor)` com o MESMO input e o MESMO mapeamento erro→status (extrair helper `toHttpError(error, requestId)` no próprio capabilities.ts e reusar nas duas rotas — códigos e mensagens idênticos aos atuais, nenhum status muda). Nenhuma rota importa mais `lib/searches.js` etc. diretamente (grep de verificação abaixo). Query/body parsing Zod nas rotas permanece onde está (fronteira HTTP), o mapa revalida o núcleo.
3. Provar sem regressão: `pnpm vitest run --project integration -- lab-search-runs projects idor` (ou o subset existente que cubra lab/projects) verde contra PG DEV; typecheck verde.</action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck && grep -rn "lib/searches\|lib/searchRuns\|lib/projects\|lib/corpus" apps/core-api/src/routes/ || echo "NO_DIRECT_LIB_IMPORTS"</automated>
  </verify>
  <done>100% das rotas lab/projects via execute(); zero import direto de lib/* nas rotas; integração existente verde</done>
  <acceptance_criteria>grep -rn "lib/" apps/core-api/src/routes/*.ts | grep -c "import" == 0; grep -c "buildExecutor\|createExecutor" apps/core-api/src/capabilities.ts >= 2; grep -c "execute(" apps/core-api/src/routes/lab.ts >= 8; grep -c "execute(" apps/core-api/src/routes/projects.ts >= 3</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: rotas PAT (emissão/listagem/revogação) + lockout/rate-limit + integração</name>
  <files>apps/core-api/src/routes/auth.ts, apps/core-api/src/plugins/rateLimit.ts, tests/integration/pat-auth.test.ts</files>
  <read_first>apps/core-api/src/routes/auth.ts, apps/core-api/src/plugins/rateLimit.ts, apps/core-api/src/auth/pat.ts, tests/integration/auth.test.ts</read_first>
  <action>1. Em routes/auth.ts (seguir o molde do login D-19/D-26: lockout, envelope, X-Request-Id, sem vazar existência):
- `POST /api/v1/auth/token` — body `patCreateSchema`; lockout idêntico ao login (mesmos users.failedAttempts/lockedUntil, 5→15min, resposta 429 ACCOUNT_LOCKED); credencial inválida → 401 genérico (sem distinguir e-mail/senha); ok → `issuePat(db, user.id, deviceName)` → 201 `{ token, id, deviceName, createdAt, lastSeenAt, expiresAt }` (raw UMA vez) + zera failedAttempts como o login faz. Throttle: ENTRAR no mesmo bucket do login no rateLimit.ts (adicionar a URL à condição do login, não criar limite novo).
- `GET /api/v1/auth/tokens` (requireAuth: sessão OU Bearer) → 200 lista `PersonalAccessTokenInfo[]` do usuário.
- `DELETE /api/v1/auth/tokens/:id` (requireAuth) → fora do escopo → 404 idêntico (revokePat null → NOT_FOUND); ok → 204.
- `POST /api/v1/auth/logout` existente: além da sessão, se chamado com Bearer revoga o PAT atual (D-61 "logout revoga o PAT atual"); `logout-all`/reset-confirm existentes passam a chamar `revokeAllPats` junto (D-61 "sair de todas"). LER os handlers atuais e estender sem mudar seus status.
2. Criar tests/integration/pat-auth.test.ts (molde auth.test.ts + idor-matrix.test.ts; exige PG DEV, skip gracioso se offline como os demais): ciclo completo — (a) bootstrap convite+2 usuários; (b) POST /auth/token A → 201 com token hex64; (c) Bearer A acessa GET /api/v1/lab/searches?projectId=... e GET projects → mesmos códigos que cookie; (d) GET /auth/tokens lista 1; (e) DELETE revoga → Bearer seguinte 401; (f) IDOR via Bearer: usuário B com Bearer próprio recebe 404 em projeto/search/run de A + UUID fantasma 404 (dono/estranho/adulterado); (g) lockout: 5 POST /auth/token com senha errada → 6º dá 429 ACCOUNT_LOCKED (mesmo contador do login); (h) regressão: cookie de sessão continua funcionando após todas as mudanças. >=10 its, rodar contra PG DEV verde.
3. Aplicar migration 0004 no PG DEV (`pnpm --filter @uhhu/db db:migrate` com .env.dev) e provar `personal_access_tokens` existe (select 1 via teste).</action>
  <verify>
    <automated>pnpm vitest run --project integration -- pat-auth 2>&1 | tail -n 15</automated>
  </verify>
  <done>Ciclo PAT ponta a ponta + IDOR Bearer + lockout compartilhado verdes contra PG DEV real</done>
  <acceptance_criteria>grep -c "it(" tests/integration/pat-auth.test.ts >= 10; grep -c "/auth/token\|/auth/tokens" apps/core-api/src/routes/auth.ts >= 4; grep -c "revokeAllPats" apps/core-api/src/routes/auth.ts >= 1; grep -v '^ *//' tests/integration/pat-auth.test.ts | grep -c "any" == 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | body {email,password,deviceName} + Authorization Bearer cruzam aqui (não confiáveis) |
| API→PG | token_hash comparado server-side; raw nunca persistido nem logado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-ENUM | Information Disclosure | POST /auth/token + DELETE /auth/tokens/:id | mitigate | 401 genérico sem distinguir e-mail/senha/expirado; 404 idêntico fora do escopo; sem enumeração (D-63); teste IDOR dono/estranho/adulterado neste plano |
| T-05-02-BRUTE | Denial of Service | POST /auth/token | mitigate | Lockout 5→15min por e-mail (mesmo contador do login) + throttle 10/min IP no bucket do login; teste de 429 neste plano |
| T-05-02-TOKEN | Information Disclosure | raw PAT | mitigate | Raw retornado 1 vez, só hash sha256 no banco, nunca em log/resposta subsequente (redact já cobre authorization/cookie no boot); teste assert ausência de token em GET /auth/tokens |
| T-05-02-SLIDE | Elevation of Privilege | touchPat | mitigate | Espelha touchSession (janela 30d, sem extensão infinita além do sliding); erro em background nunca autentica |
| T-05-02-REGRESS | Tampering | refactor lab/projects → execute() | mitigate | Helper de erro compartilhado com códigos idênticos; suite de integração existente + nova rodam contra PG real neste plano |
</threat_model>

<verification>
- `pnpm --filter @uhhu/core-api run typecheck` verde
- `pnpm vitest run --project integration -- pat-auth` verde contra PG DEV (migration 0004 aplicada)
- Suite de integração lab/projects existente ainda verde (sem regressão do refactor)
- grep gates das acceptance_criteria passam; nenhum `any` nos arquivos novos
</verification>

<success_criteria>
- PAT emitido/listado/revogado por API com sliding 30d; Bearer acessa tudo que cookie acessa com mesmo 404/envelope
- Zero chamada direta lib/* nas rotas lab/projects — execute() é o único caminho (CORE-02 server-side provado)
- Lockout + rate-limit compartilhados com login; logout revoga PAT atual; sair-de-todas revoga tudo
</success_criteria>

<output>
After completion, create `.planning/phases/05-prova-headless/05-02-SUMMARY.md`
</output>
