---
phase: 05-prova-headless
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - apps/mcp/package.json
  - apps/mcp/src/index.ts
  - apps/mcp/src/tools.ts
  - apps/mcp/src/mcp-client.ts
  - tests/smoke/mcp-tools.test.ts
  - tests/integration/mcp-headless.test.ts
autonomous: true
requirements: [CORE-05, CORE-02]
must_haves:
  truths:
    - "Agente lista 11 tools semânticas e executa a cadeia Etapa 2 por tools com o mesmo PAT e o mesmo 404"
    - "Tools destrutivas exigem confirm:true explícito e declaram efeitos/proveniência no schema"
    - "MCP nunca importa @uhhu/db nem expõe tool SQL/genérica; DTOs e paginação idênticos ao REST"
    - "Erros voltam no envelope PT-BR com requestId, sem stack/SQL/tokens"
  artifacts:
    - path: "apps/mcp/src/tools.ts"
      provides: "11 tools da cadeia total com schemas Zod + confirm"
      contains: "lab_execute_search"
    - path: "apps/mcp/src/index.ts"
      provides: "Servidor MCP stdio sobre as tools"
      contains: "StdioServerTransport"
    - path: "apps/mcp/src/mcp-client.ts"
      provides: "HTTP client Bearer PAT + polling igual ao REST"
      exports: ["mcpFetch", "mcpWaitForJob"]
    - path: "tests/smoke/mcp-tools.test.ts"
      provides: "Contrato das tools (lista, confirm, sem SQL) + guard"
      min_lines: 80
  key_links:
    - from: "apps/mcp/src/tools.ts"
      to: "apps/mcp/src/mcp-client.ts"
      via: "toda tool chama REST via mcpFetch, nenhuma toca banco"
      pattern: "mcpFetch|mcpWaitForJob"
    - from: "apps/mcp/src/index.ts"
      to: "apps/mcp/src/tools.ts"
      via: "registra as 11 tools no servidor stdio"
      pattern: "ListTools|CallTool|tool"
    - from: "apps/mcp/src/mcp-client.ts"
      to: "UHHU_TOKEN"
      via: "PAT via env (mesmo Bearer do CLI/REST)"
      pattern: "UHHU_TOKEN"
---

<objective>
Servidor MCP headless: 11 tools semânticas sobre a mesma API REST com mesma authZ, confirm para destrutivas e DTOs idênticos. Fecha D-67, D-68, D-69, D-70 e D-59 (lado MCP), D-56/D-58 (lado MCP).

Purpose: Prova que agentes operam a cadeia Etapa 2 com a mesma autoridade do usuário — sem SQL genérico, sem acesso a tokens/tabelas (contrato §18, CORE-05).
Output: app @uhhu/mcp funcional via stdio, testes smoke+integração verdes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prova-headless/05-CONTEXT.md
@.planning/phases/05-prova-headless/05-02-SUMMARY.md
@apps/mcp/README.md
@apps/core-api/src/routes/lab.ts

<interfaces>
<!-- Tools exatas D-67 (+ health). Cada tool = 1 endpoint REST existente; schemas de input espelham os query/body Zod das rotas. -->
Tools (nomes VERBATIM, sem renomear): `lab_create_project`, `lab_list_projects`, `lab_create_search`, `lab_execute_search`, `lab_get_run`, `lab_list_results`, `lab_set_result_decision`, `lab_get_corpus`, `lab_export_project`, `lab_list_sources`, `lab_get_source_health`.
Mapeamento (LER routes/lab.ts + projects.ts pós-05-02/Phase 4 para paths exatos):
create_project→POST /api/v1/projects; list_projects→GET /api/v1/projects (limit+cursor, nextCursor/hasMore — D-70); create_search→POST /api/v1/lab/searches; execute_search→POST runs (201 direto | 202 + polling jobs D-56, mesmo 25s/polling do REST); get_run→GET run; list_results→GET results (cursor); set_result_decision→decisão Phase 4 (UMA por grupo D-46); get_corpus→corpus Phase 4; export_project→GET export (retorna referência `{filename, contentType, size, contentBase64|text}` — D-59 "referência/conteúdo", sem gravar PG); list_sources→GET /lab/sources; get_source_health→GET /lab/sources/:name/health.
Confirm D-68: `lab_create_project`, `lab_create_search`, `lab_execute_search`, `lab_set_result_decision`, `lab_export_project` exigem `confirm: z.literal(true)` no input ("Tool destrutiva/com efeito: passe confirm:true") e declaram `sideEffects`, `provenance` (cadeia Result→Run→Search→Project) e limites na `description` de cada tool. Reads (list/get/health) NÃO exigem confirm. Sem confirm válido → erro MCP `confirm_required` PT-BR ("Esta operação exige confirm:true explícito.") sem chamar a API.
Auth D-69: `UHHU_TOKEN` (env) + `UHHU_API_URL` (env, default http://127.0.0.1:3000); mesmo Bearer, mesmo ActorContext server-side, mesmo 404 IDOR; sampling do agente não muda autoridade. Sem tool SQL, sem leitura de tokens/tabelas.
DTOs D-70: mesmos DTOs camelCase, mesma paginação limit+cursor, mesmo envelope PT-BR com requestId; erros viram MCP error com `code/message/requestId` (sem stack/SQL/tokens).
SDK: `@modelcontextprotocol/sdk` (stdio). Instalar com `pnpm --filter @uhhu/mcp add @modelcontextprotocol/sdk` e PINAR a versão exata instalada (remover `^` — reproducibilidade; supply-chain MIT do SDK oficial). Se o SDK não instalar (offline), fallback documentado: servidor stdio JSON-RPC mínimo hand-rolled com `tools/list`+`tools/call` — decisão do executor registrada no SUMMARY (não é "simplificação", é contingência de rede).
Atualizar apps/mcp/README.md (substituir "fronteira reservada" por tools reais + regras sem SQL/tokens).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: scaffold @uhhu/mcp + client HTTP + servidor stdio</name>
  <files>apps/mcp/package.json, apps/mcp/src/mcp-client.ts, apps/mcp/src/index.ts</files>
  <read_first>apps/mcp/README.md, packages/contracts/src/errors.ts, apps/cli/src/client.ts</read_first>
  <action>1. Criar apps/mcp/package.json: `{ "name": "@uhhu/mcp", "version": "0.1.0", "type": "module", "scripts": { "typecheck": "tsc --noEmit -p tsconfig.json", "start": "node ./dist/index.js ou tsx src/index.ts (seguir padrão core-api: tsx em dev)" }, "dependencies": { "@uhhu/contracts": "workspace:*", "@modelcontextprotocol/sdk": "<EXATA pinada>", "zod": "^4.5.4" } }` + tsconfig strict (molde apps/core-api/tsconfig.json). Instalar SDK e pinar exato. NUNCA `@uhhu/db`/drizzle/pg (guard D-55).
2. apps/mcp/src/mcp-client.ts (espelho do client do CLI — LER apps/cli/src/client.ts e reusar o padrão, sem importar do CLI — apps não importam entre si): `export async function mcpFetch<T>(path, opts: { method?; body?: unknown; idempotencyKey?: string }): Promise<{ status; data: T; filename? }>` — baseUrl de `process.env.UHHU_API_URL ?? 'http://127.0.0.1:3000'`, token OBRIGATÓRIO de `process.env.UHHU_TOKEN` (ausente → lança `McpConfigError('UHHU_TOKEN não configurado.')` antes de qualquer rede), `Authorization: Bearer`, `X-Request-Id: randomUUID()`; envelope de erro → `{ code, message, requestId }` preservados. + `export async function mcpWaitForJob(jobId): Promise<JobDTO>` — mesmo polling D-56 (2s, timeout 600s). `import type` de contracts; nunca `any`.
3. apps/mcp/src/index.ts (nesta task = boot mínimo com 1 tool de fumaça `lab_list_sources` fiação real via tools.ts da task 2? NÃO — nesta task criar o servidor com registro dinâmico: `import { TOOL_DEFINITIONS } from './tools.js'` + loop de registro ListTools/CallTool genérico que delega para `callTool(name, args)` de tools.ts; assim a task 1 compila com stub de tools.ts? Arquivo tools.ts ainda não existe → typecheck quebraria. Decisão concreta: nesta task criar tools.ts STUB com `lab_list_sources` funcional completo + `TOOL_DEFINITIONS`/`callTool` com 1 entrada (a task 2 estende para 11 sem mudar o mecanismo). Servidor stdio via SDK (`StdioServerTransport`), nome `uhhu-mcp`, versão `0.1.0`. Typecheck verde + `echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | pnpm --filter @uhhu/mcp start` lista 1 tool.</action>
  <verify>
    <automated>pnpm --filter @uhhu/mcp run typecheck && echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"smoke","version":"0"}}}' | timeout 20 pnpm --filter @uhhu/mcp start 2>&1 | head -n 5</automated>
  </verify>
  <done>Scaffold MCP compila; servidor stdio sobe; guard D-55 intacto</done>
  <acceptance_criteria>grep -c "@modelcontextprotocol/sdk" apps/mcp/package.json == 1; grep '"@modelcontextprotocol/sdk"' apps/mcp/package.json | grep -c '\^' == 0; grep -c "UHHU_TOKEN" apps/mcp/src/mcp-client.ts >= 1; grep -c "StdioServerTransport" apps/mcp/src/index.ts == 1; grep -rn "@uhhu/db\|drizzle" apps/mcp/package.json apps/mcp/src/ | wc -l == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: as 11 tools com confirm e proveniência declarada</name>
  <files>apps/mcp/src/tools.ts</files>
  <read_first>apps/mcp/src/mcp-client.ts, apps/core-api/src/routes/lab.ts, packages/contracts/src/lab.ts</read_first>
  <action>Estender apps/mcp/src/tools.ts de 1 para 11 tools (manter `TOOL_DEFINITIONS` + `callTool(name, args)` — o index.ts NÃO muda). Para cada tool: `name` verbatim da lista, `description` PT-BR com 3 linhas fixas (`Efeitos: ...`, `Proveniência: ...`, `Limites: ...`), `inputSchema` Zod (espelho dos schemas das rotas: term max 500, arrays bounded, limit 1–100 default 20, cursor opaco), `confirm: z.literal(true)` OBRIGATÓRIO nas 5 com efeito (create_project, create_search, execute_search, set_result_decision, export_project) com mensagem PT-BR; handler chama `mcpFetch` no path exato + `mcpWaitForJob` após 202 em execute_search (repasse `--idempotency-key`? tools recebem `idempotencyKey?: string` opcional em execute/export — D-58 lado MCP); export retorna `{ filename, contentType, sizeBytes, contentJson|contentText }` conforme formato (JSON → objeto parseado; CSV/BibTeX → texto) — nunca binário cru sem rótulo. list_results/get_corpus repassam `nextCursor/hasMore` verbatim (D-70). Erro de API vira `McpToolError { code, message, requestId }` sem stack/SQL/tokens. Descriptions citam a capability §10 correspondente. Nenhum `any`; `import type` para DTOs.</action>
  <verify>
    <automated>pnpm --filter @uhhu/mcp run typecheck && pnpm vitest run --project smoke -- mcp-tools 2>&1 | tail -n 5</automated>
  </verify>
  <done>11 tools registradas, 5 com confirm obrigatório, typecheck verde</done>
  <acceptance_criteria>grep -c "lab_create_project\|lab_list_projects\|lab_create_search\|lab_execute_search\|lab_get_run\|lab_list_results\|lab_set_result_decision\|lab_get_corpus\|lab_export_project\|lab_list_sources\|lab_get_source_health" apps/mcp/src/tools.ts >= 11; grep -c "confirm" apps/mcp/src/tools.ts >= 5; grep -c "Proveniência\|Efeitos" apps/mcp/src/tools.ts >= 11; grep -v '^ *//' apps/mcp/src/tools.ts | grep -c "any" == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: smoke das tools + integração MCP contra PG DEV</name>
  <files>tests/smoke/mcp-tools.test.ts, tests/integration/mcp-headless.test.ts</files>
  <read_first>apps/mcp/src/tools.ts, tests/smoke/boot.test.ts, tests/integration/auth.test.ts</read_first>
  <action>1. tests/smoke/mcp-tools.test.ts (sem PG, sem rede — stub de mcp-client via `vi.mock`? O client é importado por tools.ts; usar `vi.spyOn` no módulo ou injetar fetch fake via `vi.stubGlobal('fetch', ...)`: (a) lista tem exatamente 11 nomes verbatim; (b) cada tool com efeito SEM `confirm:true` rejeita com `confirm_required` e NÃO chama fetch (assert fetch não chamado); (c) com confirm chama o path esperado (assert URL contém `/api/v1/...`); (d) descriptions contêm Efeitos+Proveniência+Limites; (e) nenhum arquivo em apps/mcp importa db (leitura de package.json + varredura src como no guard 05-01); (f) erro 404 da API vira erro com requestId sem stack. >=12 its.
2. tests/integration/mcp-headless.test.ts (molde de boot dos demais integration; PG DEV + servidor; UHHU_TOKEN de usuário real via POST /auth/token; UHHU_API_URL apontando ao servidor): via `callTool` direto — `lab_create_project` (confirm) → `lab_create_search` → `lab_execute_search` (aceita ok|partial) → `lab_list_results` (hasMore boolean presente) → `lab_get_corpus` → `lab_export_project` (filename começa com `corpus-`) → `lab_list_sources` + `lab_get_source_health`. IDOR: segundo usuário via callTool em projeto do primeiro → erro NOT_FOUND (404). Assert env do teste NÃO contém *DATABASE* no lado MCP (só UHHU_TOKEN+UHHU_API_URL). >=6 its verdes.</action>
  <verify>
    <automated>pnpm vitest run --project smoke -- mcp-tools 2>&1 | tail -n 5 && pnpm vitest run --project integration -- mcp-headless 2>&1 | tail -n 8</automated>
  </verify>
  <done>Smoke >=12 its + cadeia total via tools contra PG DEV com IDOR 404</done>
  <acceptance_criteria>grep -c "it(" tests/smoke/mcp-tools.test.ts >= 12; grep -c "it(" tests/integration/mcp-headless.test.ts >= 6; grep -c "UHHU_TOKEN" tests/integration/mcp-headless.test.ts >= 2; grep -v '^ *//' tests/smoke/mcp-tools.test.ts tests/integration/mcp-headless.test.ts | grep -c "any" == 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente→MCP | args JSON não confiáveis (validar TUDO com Zod antes de chamar a API) |
| MCP→API | PAT env; mesma autoridade do usuário — sem elevação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-CONFIRM | Elevation of Privilege | tools com efeito | mitigate | `confirm: z.literal(true)` obrigatório nas 5; sem confirm = erro sem E/S; teste sem-fetch neste plano (D-68, SUITE-04) |
| T-05-04-SQL | Elevation of Privilege | superfície MCP | mitigate | Zero tools genéricas; grep anti-`SELECT/SQL` + guard D-55; só paths REST allowlist (D-69) |
| T-05-04-PROMPT | Spoofing | descriptions | mitigate | Efeitos/proveniência/limites declarados para o agente decidir; sampling não muda autoridade (servidor decide) |
| T-05-04-LEAK | Information Disclosure | erros e export | mitigate | Envelope PT-BR com requestId, sem stack/SQL/tokens; PAT nunca em output de tool |
| T-05-04-SUPPLY | Tampering | @modelcontextprotocol/sdk | mitigate | Versão exata pinada; `pnpm audit` no gate 05-05 |
</threat_model>

<verification>
- `pnpm --filter @uhhu/mcp run typecheck` verde
- `pnpm vitest run --project smoke -- mcp-tools` verde
- `pnpm vitest run --project integration -- mcp-headless` verde contra PG DEV
- 11 nomes verbatim; guard D-55 verde; nenhum `any`
</verification>

<success_criteria>
- Cadeia total executável por tools com mesmo PAT, mesmos DTOs/paginação e mesmo 404
- Destrutivas exigem confirm:true com erro sem E/S quando ausente
- Superfície MCP sem SQL/tokens/tabelas, auditável pelo gate 05-05
</success_criteria>

<output>
After completion, create `.planning/phases/05-prova-headless/05-04-SUMMARY.md`
</output>
