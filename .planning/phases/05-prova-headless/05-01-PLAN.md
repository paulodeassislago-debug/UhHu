---
phase: 05-prova-headless
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/capabilities.ts
  - packages/contracts/src/auth.ts
  - packages/core/src/capabilities.ts
  - packages/core/src/actor.ts
  - packages/core/src/index.ts
  - packages/db/src/schema.ts
  - packages/db/drizzle/0004_personal_access_tokens.sql
  - apps/core-api/src/auth/tokens.ts
  - tests/smoke/capabilities-guard.test.ts
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - "execute() despacha capability registrada para o handler com ActorContext e rejeita nome desconhecido com erro tipado"
    - "ActorContext aceita authMethod session|pat sem quebrar nenhuma rota de sessão existente"
    - "Tabela personal_access_tokens existe via migration versionada com hash único, expiração e revogação"
    - "Guard estrutural quebra se apps/cli ou apps/mcp importarem @uhhu/db, drizzle ou SQL"
  artifacts:
    - path: "packages/contracts/src/capabilities.ts"
      provides: "Nomes de capability + versão + schemas PAT (definição única)"
      contains: "CapabilityName"
    - path: "packages/core/src/capabilities.ts"
      provides: "Registry + execute(name, input, actor) com injeção de handlers, sem importar db"
      exports: ["createExecutor", "executeCapability", "CAPABILITY_VERSION"]
    - path: "packages/db/src/schema.ts"
      provides: "Tabela personal_access_tokens espelhando sessions"
      contains: "personalAccessTokens"
    - path: "tests/smoke/capabilities-guard.test.ts"
      provides: "Guard anti-PG D-55 + unit do execute()"
      min_lines: 60
  key_links:
    - from: "packages/core/src/capabilities.ts"
      to: "@uhhu/contracts"
      via: "import type { CapabilityName } — definição única, nunca duplicar nomes"
      pattern: "CapabilityName"
    - from: "apps/core-api/src/auth/tokens.ts"
      to: "packages/db/src/schema.ts"
      via: "patExpiry() gera expiração consumida pela tabela personal_access_tokens"
      pattern: "patExpiry"
---

<objective>
Fundação headless: registry de capabilities + execute() no CORE, ActorContext com PAT, tabela de PATs e guard anti-PG. Sem rotas, sem CLI/MCP ainda.

Purpose: Destrava 05-02 (servidor vira adaptador fino sobre execute + emite PATs). Implementa D-60/D-61 no nível de contrato+schema e D-55 no nível estrutural (guard). Segue contrato §10 (execute) e §8.1 (Bearer própria, formato a fechar — fecha aqui).
Output: capabilities.ts em contracts+core, actor.ts estendido, migration 0004, patExpiry(), guard smoke verde.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prova-headless/05-CONTEXT.md
@packages/core/src/actor.ts
@packages/core/src/index.ts
@packages/contracts/src/auth.ts
@packages/contracts/src/index.ts
@packages/db/src/schema.ts
@apps/core-api/src/auth/tokens.ts
@apps/core-api/src/auth/requireAuth.ts

<interfaces>
<!-- Contratos existentes que o executor reutiliza verbatim; nao reexplorar. -->
From packages/core/src/actor.ts (existente):
```typescript
export interface ActorContext { userId: string; role: UserRole; requestId: string; authMethod: 'session'; }
export function requireUser(actor: ActorContext | null | undefined): ActorContext;
```
From apps/core-api/src/auth/tokens.ts (existente):
```typescript
export function newOpaqueToken(): string; // randomBytes(32).hex — nunca Math.random
export function hashToken(token: string): string; // sha256 hex, vai ao banco
export function sessionExpiry(rememberMe: boolean, from?: Date): Date; // 30d | 24h
```
From packages/db/src/schema.ts — tabela sessions (molde exato do PAT):
```typescript
export const sessions = pgTable('sessions', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
  tokenHash: text('token_hash').notNull().unique(),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  rememberMe: boolean('remember_me').notNull().default(true),
  userAgent: text('user_agent'), ip: text('ip'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  lastSeenAt: timestamp('last_seen_at', { withTimezone: true }).defaultNow().notNull(),
});
```
From dev-docs/07-core-contract.md §10 — nomes de capability (copiar verbatim, só as implementadas no v1 entram no registry):
platform.session.get, platform.project.create/list/get/update, lab.search.create/list/update/delete/execute/compare, lab.run.get/list, lab.run.results.list, lab.result.decision.update, lab.result.duplicate.diverge, lab.corpus.get, lab.project.export, lab.source.list, lab.source.health
Research: skipped per .planning/config.json workflow.research=false — domínio já validado em dev-docs/07+03+08 e decisões D-54–D-70 no 05-CONTEXT.md. Nyquist VALIDATION.md não aplicável sem RESEARCH.md (workflow §5.5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: registry de capabilities em contracts + execute() em core + actor pat</name>
  <files>packages/contracts/src/capabilities.ts, packages/core/src/capabilities.ts, packages/core/src/actor.ts, packages/core/src/index.ts, packages/contracts/src/auth.ts</files>
  <read_first>packages/core/src/actor.ts, packages/core/src/index.ts, packages/contracts/src/auth.ts, packages/contracts/src/index.ts, .planning/phases/05-prova-headless/05-CONTEXT.md</read_first>
  <action>1. Criar packages/contracts/src/capabilities.ts (definição ÚNICA dos nomes — D-60: formato e ciclo do PAT também nascem em contracts):
- `export const CAPABILITY_VERSION = 'v1' as const;`
- `export const capabilityNameSchema = z.enum(['platform.session.get','platform.project.create','platform.project.list','platform.project.get','platform.project.update','lab.search.create','lab.search.list','lab.search.update','lab.search.delete','lab.search.execute','lab.search.compare','lab.run.get','lab.run.list','lab.run.results.list','lab.result.decision.update','lab.result.duplicate.diverge','lab.corpus.get','lab.project.export','lab.source.list','lab.source.health']); export type CapabilityName = z.infer<typeof capabilityNameSchema>;`
- Header PT-BR citando contrato §10 + D-60/D-61. Re-exportar em packages/contracts/src/index.ts (`export * from './capabilities.js';` seguindo o padrão das linhas existentes).
2. Estender packages/contracts/src/auth.ts (NÃO criar arquivo novo — definição única de auth):
- `export const patDeviceNameSchema = z.string().trim().min(1,'Nome do dispositivo é obrigatório.').max(100,'Nome deve ter no máximo 100 caracteres.');`
- `export const patCreateSchema = z.object({ email: emailSchema, password: z.string().min(1).max(128), deviceName: patDeviceNameSchema }); export type PatCreateInput = z.infer<typeof patCreateSchema>;` (reusa o emailSchema privado do arquivo)
- `export interface PersonalAccessTokenInfo { id: string; deviceName: string; createdAt: string; lastSeenAt: string; expiresAt: string; }` (NUNCA inclui hash nem raw — D-62)
- `export interface PersonalAccessTokenCreated extends PersonalAccessTokenInfo { token: string; }` (raw circula UMA vez, como sessão/reset em tokens.ts)
3. Criar packages/core/src/capabilities.ts — registry SEM importar db (packages/core NÃO depende de @uhhu/db; handlers são injetados no boot pelo 05-02):
- `import type { CapabilityName } from '@uhhu/contracts'; import type { ActorContext } from './actor.js';`
- `export const CAPABILITY_VERSION = 'v1' as const;`
- `export type CapabilityHandler = (input: unknown, actor: ActorContext) => Promise<unknown>;`
- `export class UnknownCapabilityError extends Error { readonly name = 'UnknownCapabilityError'; constructor(readonly capability: string) { super(`Capability desconhecida: ${capability}.`); } }`
- `export function createExecutor(handlers: Record<CapabilityName, CapabilityHandler>): (name: CapabilityName | string, input: unknown, actor: ActorContext) => Promise<unknown>` — valida o nome com lista fechada (aceita string para erro tipado), lança UnknownCapabilityError se fora do registry, senão delega. `export const executeCapability` = executor default vazio? NÃO — exigir handlers (sem default): `createExecutor` é o único construtor; sem mapa injetado nada executa (fail-closed).
- Header PT-BR citando contrato §10 + D-54 (cadeia total, não capability isolada).
4. Estender packages/core/src/actor.ts: `authMethod: 'session' | 'pat';` (única mudança de tipo) + comentário `// D-60–D-63: PAT espelha sessão; requireAuth deriva, nunca body`. Verificar com grep que nenhum consumer faz `=== 'session'` de forma que quebraria com 'pat' — se houver, ajustar para checagem positiva de presença de actor em vez de método.
5. Re-exportar em packages/core/src/index.ts: `export { createExecutor, UnknownCapabilityError, CAPABILITY_VERSION, type CapabilityHandler } from './capabilities.js';` + `export type { CapabilityName } from '@uhhu/contracts';`
Mensagens PT-BR, `import type` para tipos, nenhum `any`, nenhum `Math.random`.</action>
  <verify>
    <automated>pnpm --filter @uhhu/contracts run typecheck && pnpm --filter @uhhu/core run typecheck</automated>
  </verify>
  <done>Typechecks verdes; registry com 20 nomes; ActorContext aceita pat; PAT schemas em auth.ts</done>
  <acceptance_criteria>grep -c "lab.search.execute" packages/contracts/src/capabilities.ts >= 1; grep -c "createExecutor\|UnknownCapabilityError" packages/core/src/capabilities.ts >= 3; grep -c "authMethod: 'session' | 'pat'" packages/core/src/actor.ts == 1; grep -c "patCreateSchema\|PersonalAccessTokenInfo" packages/contracts/src/auth.ts >= 3; grep -v '^ *//' packages/contracts/src/capabilities.ts packages/core/src/capabilities.ts | grep -c "any" == 0; grep -rn "from '@uhhu/db'\|from \"@uhhu/db\"" packages/core/src/ | wc -l == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: tabela personal_access_tokens + migration 0004 + patExpiry</name>
  <files>packages/db/src/schema.ts, packages/db/drizzle/0004_personal_access_tokens.sql, apps/core-api/src/auth/tokens.ts</files>
  <read_first>packages/db/src/schema.ts, apps/core-api/src/auth/tokens.ts, packages/db/package.json</read_first>
  <action>1. Em packages/db/src/schema.ts, após `sessions`, adicionar (espelho de sessions + deviceName + revokedAt, sem rememberMe/userAgent/ip — PAT é por device, D-60/D-61):
```typescript
export const personalAccessTokens = pgTable('personal_access_tokens', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
  tokenHash: text('token_hash').notNull().unique(),
  deviceName: text('device_name').notNull(),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  revokedAt: timestamp('revoked_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  lastSeenAt: timestamp('last_seen_at', { withTimezone: true }).defaultNow().notNull(),
});
export type PersonalAccessToken = typeof personalAccessTokens.$inferSelect;
export type NewPersonalAccessToken = typeof personalAccessTokens.$inferInsert;
```
2. Gerar migration via `pnpm --filter @uhhu/db db:generate` (drizzle-kit, mantém meta journal consistente). ANTES: `ls packages/db/drizzle/` — se a Phase 4 já criou `0003_*`, o generate produz `0004_*`; renomear o arquivo gerado para `0004_personal_access_tokens.sql` se o sufixo for aleatório (manter o conteúdo, ajustar o nome no journal `packages/db/drizzle/meta/_journal.json` de acordo). Se a Phase 4 NÃO executou ainda (sem 0003_*), gerar mesmo assim e usar o próximo índice livre — NUNCA reutilizar índice existente; registrar no SUMMARY qual índice foi usado. Verificar que o SQL contém `CREATE TABLE ... personal_access_tokens` + `token_hash UNIQUE`.
3. Em apps/core-api/src/auth/tokens.ts adicionar: `// PAT 30d sliding (D-61: espelha sessão rememberMe=true, sem PAT eterno no v1). export function patExpiry(from: Date = new Date()): Date { return new Date(from.getTime() + 30 * DAY_MS); }`
Nenhum segredo em log; nenhum `any`.</action>
  <verify>
    <automated>pnpm --filter @uhhu/db run typecheck && pnpm --filter @uhhu/core-api run typecheck && ls packages/db/drizzle/*.sql</automated>
  </verify>
  <done>Schema com personalAccessTokens; migration 0004 aplica sem erro; patExpiry exportado</done>
  <acceptance_criteria>grep -c "personalAccessTokens" packages/db/src/schema.ts >= 3; ls packages/db/drizzle/ | grep -c "0004.*\.sql" == 1; grep -c "CREATE TABLE" packages/db/drizzle/0004_personal_access_tokens.sql >= 1; grep -c "personal_access_tokens" packages/db/drizzle/0004_personal_access_tokens.sql >= 2; grep -c "patExpiry" apps/core-api/src/auth/tokens.ts >= 2</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: guard anti-PG D-55 + unit do execute() em smoke</name>
  <files>tests/smoke/capabilities-guard.test.ts</files>
  <read_first>packages/core/src/capabilities.ts, apps/cli/README.md, apps/mcp/README.md, tests/smoke/boot.test.ts, vitest.workspace.ts</read_first>
  <action>Criar tests/smoke/capabilities-guard.test.ts (projeto smoke, sem PG — padrão boot.test.ts: `import { describe, expect, it } from 'vitest';`, type-guards com unknown, nunca any). Duas suites:
A) `describe('D-55 guard anti-PG', ...)`: lê em runtime os arquivos de fronteira e falha se houver import proibido. Implementação: `import { readFileSync, existsSync, readdirSync } from 'node:fs'; import { join } from 'node:path';` raiz = `process.cwd()`; para cada dir `apps/cli`, `apps/mcp`: (1) se `package.json` existir, parse e assert que `dependencies+devDependencies` NÃO contêm `@uhhu/db`, `drizzle-orm`, `postgres`, `pg`; (2) varre recursivamente `src/` (se existir) e assert que nenhum `.ts` contém `@uhhu/db`, `drizzle-orm`, `CREATE TABLE`, `SELECT ... FROM` (regex `/\bSELECT\b.+?\bFROM\b/i`). Nesta fase src/ ainda não existe (só READMEs) — o teste deve PASSAR com "fronteira ainda reservada" (skip gracioso por dir sem src, mas o check de package.json vale quando criado). Documentar no header: quebra estrutural além da demonstração por execução (D-55), roda no CI fail-closed.
B) `describe('execute()', ...)`: importa `createExecutor, UnknownCapabilityError` de `@uhhu/core` (workspace link — verificar como boot.test.ts importa pacotes; se precisar de path relativo, usar relativo `../../packages/core/src/capabilities.js`? Preferir `@uhhu/core` se resolvido no workspace, fallback relativo documentado): handler fake `(input, actor) => ({ echo: input, user: actor.userId })` registrado para `platform.project.list`; assert resolve com actor `{ userId:'u1', role:'member', requestId:'r1', authMethod:'pat' }`; assert `createExecutor(map)('cap.inexistente', {}, actor)` rejeita com UnknownCapabilityError. >=6 its no total.
Rodar `pnpm vitest run --project smoke -- capabilities-guard` verde.</action>
  <verify>
    <automated>pnpm vitest run --project smoke -- capabilities-guard</automated>
  </verify>
  <done>Guard smoke verde com >=6 its cobrindo D-55 estrutural + dispatch/erro do execute()</done>
  <acceptance_criteria>grep -c "it(" tests/smoke/capabilities-guard.test.ts >= 6; pnpm vitest run --project smoke -- capabilities-guard retorna 0 failures; grep -v '^ *//' tests/smoke/capabilities-guard.test.ts | grep -c "any" == 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev→registry | drizzle-kit generate + nenhuma dep nova (zero supply-chain nesta fase) |
| contrato→código | nomes de capability duplicados entre contracts/core rotas divergiriam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SPOOF | Spoofing | packages/core/src/actor.ts (authMethod pat) | mitigate | Tipo fechado `'session'\|'pat'`; requireAuth deriva do hash server-side no 05-02; unit usa actor pat explícito neste plano |
| T-05-01-LEAK | Information Disclosure | PersonalAccessTokenInfo | mitigate | Interface sem hash/raw; raw só em PersonalAccessTokenCreated (uma vez); teste de ausência de `tokenHash` no tipo via grep neste plano |
| T-05-01-TAMPER | Tampering | migration 0004 | mitigate | Via drizzle-kit generate (journal consistente); token_hash UNIQUE + FK cascade igual sessions; push BLOCKING verificado no 05-02 contra PG real |
| T-05-01-BYPASS | Elevation of Privilege | CLI/MCP futuros importando db | mitigate | Guard D-55 criado neste plano e executado no CI; falha fail-closed |
</threat_model>

<verification>
- `pnpm --filter @uhhu/contracts run typecheck` + `pnpm --filter @uhhu/core run typecheck` + `pnpm --filter @uhhu/db run typecheck` verdes
- `pnpm vitest run --project smoke -- capabilities-guard` verde
- grep gates das acceptance_criteria passam; `packages/core/src` sem import de `@uhhu/db`
</verification>

<success_criteria>
- Registry com 20 capabilities + CAPABILITY_VERSION v1 em contracts (definição única) e executor com injeção em core
- ActorContext session|pat sem quebrar sessão; PAT contracts + tabela + migration 0004 + patExpiry 30d
- Guard D-55 verde em smoke (base para CI fail-closed)
</success_criteria>

<output>
After completion, create `.planning/phases/05-prova-headless/05-01-SUMMARY.md`
</output>
