---
phase: 05-prova-headless
plan: 05
type: execute
wave: 4
depends_on: ["05-03", "05-04"]
files_modified:
  - scripts/prova-headless.sh
  - scripts/mcp-call.mjs
  - tests/integration/headless-idor.test.ts
autonomous: false
requirements: [CORE-02, CORE-05]
must_haves:
  truths:
    - "Cadeia projeto→busca→run→resultados→dedup→decisão→corpus→exportação executa por REST e repete por CLI e por MCP contra o mesmo banco"
    - "Nenhum passo da prova toca o PostgreSQL direto pelo CLI/MCP (só a API)"
    - "IDOR dono/estranho/adulterado passa nas superfícies novas e auditoria adversarial + scanners passam sem crítico/alto"
    - "Humano aprova a prova ponta a ponta com a cadeia visível nos 3 canais"
  artifacts:
    - path: "scripts/prova-headless.sh"
      provides: "Cadeia total nos 3 canais com asserts ALL PASS"
      contains: "ALL PASS"
    - path: "scripts/mcp-call.mjs"
      provides: "Helper stdio para chamar tools MCP no script de prova"
      contains: "StdioClientTransport"
    - path: "tests/integration/headless-idor.test.ts"
      provides: "Matriz IDOR das superfícies novas (PAT/CLI/MCP/export)"
      min_lines: 100
  key_links:
    - from: "scripts/prova-headless.sh"
      to: "uhhu CLI + MCP"
      via: "mesma cadeia por curl (cookie), CLI (PAT) e MCP (PAT via mcp-call.mjs)"
      pattern: "uhhu |mcp-call"
    - from: "tests/integration/headless-idor.test.ts"
      to: "/auth/tokens + export + corpus"
      via: "dono/estranho/adulterado em cada superfície nova"
      pattern: "404|estranho|adulterado"
---

<objective>
Prova headless ponta a ponta + auditoria + scanners + checkpoint humano. Fecha D-54, D-55 (gate), D-57 e o gate Etapa 2 do roadmap geral.

Purpose: Transformar "funciona no meu terminal" em evidência auditável: mesma cadeia nos 3 canais, sem PG direto, com segurança provada e aceite humano — critério de done da fase.
Output: scripts de prova ALL PASS, matriz IDOR, relatório de auditoria no SUMMARY, checkpoint aprovado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prova-headless/05-CONTEXT.md
@.planning/phases/05-prova-headless/05-03-SUMMARY.md
@.planning/phases/05-prova-headless/05-04-SUMMARY.md
@scripts/curl-lab.sh
@scripts/curl-idor.sh

<interfaces>
<!-- Molde de prova herdado do 03-06/04-05; estender, não reinventar. -->
From scripts/curl-lab.sh (existente): `BASE_URL="${BASE_URL:-http://127.0.0.1:3000}"`, jars de cookie por usuário, `assert_code <esperado> <obtido> <nome>`, `trap cleanup EXIT` com mktemp, echo final `ALL PASS`. GHOST=`00000000-0000-4000-8000-000000000000` para adulterado.
Pre-req da prova (documentar no header do script): PG DEV migrado (`pnpm --filter @uhhu/db db:migrate` com .env.dev), servidor `pnpm --filter @uhhu/core-api start` com o mesmo env; NUNCA contra prod de terceiros.
Gate Etapa 2 (ROADMAP success 3): projeto→busca→run→revisão→dedup→decisão→exportação sem tocar o banco — o script prova "sem tocar" rodando CLI/MCP com env FILTRADO (sem *DATABASE*: `env -u APP_DATABASE_URL -u MIGRATION_DATABASE_URL`) + guard D-55 verde no CI.
Auditoria D-57: adversarial arquivo-a-arquivo dos arquivos criados em 05-01–05-04 (autorização decorativa, IDOR, confiança no browser, segredos, XSS/input/upload/SSRF, sessões/PATs, rate limit, SQL/command injection, isolamento) + curl IDOR + scanners (Gitleaks tree+histórico, SAST/OpenGrep JS/TS, `pnpm audit`); críticos/altos corrigidos ou com aceite formal registrado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: helper MCP stdio + script prova-headless.sh nos 3 canais</name>
  <files>scripts/mcp-call.mjs, scripts/prova-headless.sh</files>
  <read_first>scripts/curl-lab.sh, scripts/curl-idor.sh, apps/mcp/src/index.ts, apps/cli/src/uhhu.ts</read_first>
  <action>1. Criar scripts/mcp-call.mjs (JS puro, deps do workspace @uhhu/mcp já instalado): `import { Client } from '@modelcontextprotocol/sdk/client/index.js'; import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js';` uso: `UHHU_TOKEN=... UHHU_API_URL=... node scripts/mcp-call.mjs <tool> '<json-args>'` → spawna `pnpm --filter @uhhu/mcp start` via stdio, chama a tool, imprime JSON do resultado no stdout, exit por erro (confirm ausente, 4xx/5xx → exit != 0 com envelope no stderr). Modo de uso exclusivo da prova/operador — sem segredos em argv além do env.
2. Criar scripts/prova-headless.sh (molde curl-lab.sh: `set -u`, BASE_URL default, assert_code/assert_code_in, trap cleanup, ALL PASS final; header com pre-req + "NUNCA contra prod de terceiros"):
FASE A (REST, cookie — paridade com curl-lab.sh): bootstrap convite → usuário A → projeto → search (fontes ambas) → run sync/poll → results → decisão eligible + tag → corpus contém → compare com 2ª busca → export JSON salva `corpus-*.json` (assert filename + conteúdo com projectId).
FASE B (CLI, PAT, env SEM *DATABASE*): `uhhu auth login` (UHHU_API_URL=BASE_URL) → `project list --json` contém o projeto da Fase A (mesmo banco!) → `lab search run` da mesma search com `--idempotency-key prova-<data>` → repete e assert MESMO run (replay 24h D-58) → `lab export --format csv` salva `corpus-*.csv` com 1 linha por grupo (D-53) → `auth logout`.
FASE C (MCP, PAT via mcp-call.mjs): `lab_list_projects` vê o projeto → `lab_execute_search` com confirm:true → `lab_list_results` → `lab_set_result_decision` com confirm:true (undecided→eligible num pending, ou divergência/nota D-46) → `lab_get_corpus` → `lab_export_project` filename `corpus-*` → `lab_list_sources` + health.
FASE D (negativas): Bearer inválido → 401; estranho (usuário B) via CLI/MCP no projeto de A → 404; GHOST adulterado → 404; `lab_set_result_decision` sem confirm → erro confirm_required sem E/S.
Cada passo com assert; qualquer FAIL aborta (exit 1). Rodar as 4 fases contra PG DEV e obter ALL PASS.</action>
  <verify>
    <automated>BASE_URL=http://127.0.0.1:3000 bash scripts/prova-headless.sh 2>&1 | tail -n 8</automated>
  </verify>
  <done>Cadeia total passa nos 3 canais contra o mesmo banco + replay de idempotência + 4 negativas, ALL PASS</done>
  <acceptance_criteria>grep -c "assert_code" scripts/prova-headless.sh >= 20; grep -c "mcp-call" scripts/prova-headless.sh >= 4; grep -c "env -u .*DATABASE" scripts/prova-headless.sh >= 2; grep -c "ALL PASS" scripts/prova-headless.sh == 1; grep -c "StdioClientTransport" scripts/mcp-call.mjs == 1</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: matriz IDOR das superfícies novas + regressão da suite</name>
  <files>tests/integration/headless-idor.test.ts</files>
  <read_first>tests/integration/idor-matrix.test.ts, tests/integration/pat-auth.test.ts, scripts/curl-idor.sh</read_first>
  <action>Criar tests/integration/headless-idor.test.ts (molde idor-matrix.test.ts; PG DEV; 2 usuários A/B + GHOST adulterado; via REST+Bearer e via callTool/commands onde fizer sentido): para CADA superfície nova — `GET /auth/tokens` (B não vê PATs de A: lista só os próprios), `DELETE /auth/tokens/:id` (B deletando PAT de A → 404), export/corpus/compare/results/decisão de projeto de A por B (Bearer e cookie) → 404, GHOST em cada rota com ID → 404, sem credencial → 401 — matriz dono/estranho/adulterado/ausente × {tokens, export, corpus, compare, results, decision, run get, job get} (≥8 superfícies × 4 atores = ≥24 asserts, agrupados em >=8 its). Depois rodar a SUITE completa: `pnpm vitest run --project smoke` + `pnpm vitest run --project integration` + `pnpm typecheck` + `pnpm lint` verdes (regressão total das 5 fases). Registrar contagens no SUMMARY.</action>
  <verify>
    <automated>pnpm vitest run --project integration -- headless-idor 2>&1 | tail -n 6</automated>
  </verify>
  <done>Matriz IDOR das superfícies novas verde + suite completa sem regressão</done>
  <acceptance_criteria>grep -c "it(" tests/integration/headless-idor.test.ts >= 8; grep -c "404" tests/integration/headless-idor.test.ts >= 8; grep -c "00000000-0000-4000-8000" tests/integration/headless-idor.test.ts >= 1; grep -v '^ *//' tests/integration/headless-idor.test.ts | grep -c "any" == 0</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Auditoria adversarial (arquivo a arquivo, 05-01–05-04) + scanners + cadeia 3 canais ALL PASS + suite verde — executados pelo executor antes deste checkpoint</what-built>
  <how-to-verify>
    1. O executor DEVE antes deste checkpoint: (a) rodar `BASE_URL=http://127.0.0.1:3000 bash scripts/prova-headless.sh` e anexar o tail com ALL PASS; (b) rodar `gitleaks detect --source . --verbose` (tree) + `gitleaks detect --source . --verbose` com histórico (`--log-opts=--all` ou `git log --all -p | gitleaks detect --pipe` conforme instalado) + SAST/OpenGrep JS/TS + `pnpm audit` e anexar saídas; (c) escrever a auditoria adversarial arquivo a arquivo (cada arquivo novo de 05-01–05-04 com veredito por categoria: IDOR, segredos, XSS/input, sessões/PATs, rate limit, injection, isolamento) no 05-05-SUMMARY.md; críticos/altos ou corrigidos com teste ou com aceite formal do Paulo transcrito.
    2. Você (Paulo) verifica: rode `uhhu project list` (com UHHU_TOKEN de teste) e confira a tabela; rode uma tool MCP via `node scripts/mcp-call.mjs lab_list_sources '{}'` e confira o JSON; confira que `env | grep -i database` não é necessário para nenhum dos dois (só BASE_URL/TOKEN).
    3. Confira o gate Etapa 2: a cadeia REST→CLI→MCP operou o MESMO projeto/banco (ids coincidem nos logs anexados).
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva achados (ex.: "rejeitado: export CSV sem linha X")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prova→PG DEV | scripts usam só BASE_URL + PAT/cookie; DATABASE_URL nunca no env do CLI/MCP |
| auditor→código | auditoria adversarial precede qualquer patch (AGENTS.md); achados viram teste antes de fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-IDOR | Information Disclosure | superfícies novas | mitigate | Matriz 8×4 neste plano; 404 idêntico; sem enumeração |
| T-05-05-SECRET | Information Disclosure | PATs em logs/script | mitigate | Redact cobre authorization/cookie; script usa env, nunca echo do token; Gitleaks tree+histórico neste plano |
| T-05-05-SUPPLY | Tampering | SDK MCP + zero deps CLI | mitigate | `pnpm audit` + SAST neste plano; pin exato do SDK verificado |
| T-05-05-REPUD | Repudiation | cadeia sem rastro | mitigate | requestId em todo erro dos 3 canais; eksport com proveniência; logs sem credenciais |
| T-05-05-DOS | Denial of Service | polling/export pesado | mitigate | Timeout 600s + rate-limit herdado (10 runs/h, 200/min); export via attachment sem job próprio no v1 — aceitar e documentar limite |
</threat_model>

<verification>
- `bash scripts/prova-headless.sh` → ALL PASS contra PG DEV (4 fases: REST, CLI sem DATABASE_URL, MCP, negativas)
- `pnpm vitest run --project integration -- headless-idor` verde + suite total sem regressão
- Gitleaks (tree+histórico) + SAST + `pnpm audit` sem crítico/alto ou com aceite formal
- Checkpoint humano "approved"
</verification>

<success_criteria>
- Gate Etapa 2 atendido: mesma cadeia por REST, CLI e MCP contra o mesmo banco, sem PG direto (evidência nos logs)
- D-54–D-70 todos demonstrados; D-57 (auditoria+scanners) completa; checkpoint aprovado pelo Paulo
</success_criteria>

<output>
After completion, create `.planning/phases/05-prova-headless/05-05-SUMMARY.md`
</output>
