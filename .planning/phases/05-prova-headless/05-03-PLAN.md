---
phase: 05-prova-headless
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - apps/cli/package.json
  - apps/cli/src/uhhu.ts
  - apps/cli/src/client.ts
  - apps/cli/src/auth-store.ts
  - apps/cli/src/commands.ts
  - apps/cli/src/table.ts
  - tests/smoke/cli-unit.test.ts
  - tests/integration/cli-headless.test.ts
autonomous: true
requirements: [CORE-02, CORE-05]
must_haves:
  truths:
    - "Usuário faz login pelo CLI, lista projetos e executa a cadeia lab sem abrir o navegador"
    - "CLI nunca importa @uhhu/db nem abre conexão PG — só HTTPS contra a API com Bearer PAT"
    - "Saída legível em tabela por default e JSON puro com --json; -v mostra progresso do polling"
    - "Export baixa o attachment e salva corpus-<projeto>-<data>.ext; --idempotency-key repassa o header"
  artifacts:
    - path: "apps/cli/src/uhhu.ts"
      provides: "Bin uhhu com todos os comandos D-64"
      contains: "lab search run"
    - path: "apps/cli/src/client.ts"
      provides: "HTTP client com PAT (env>arquivo), --api, X-Request-Id, polling 25s+jobs"
      exports: ["apiFetch", "waitForJob"]
    - path: "apps/cli/src/auth-store.ts"
      provides: "Credencial file 600 + UHHU_TOKEN + --logout"
      exports: ["saveToken", "loadToken", "clearToken"]
    - path: "apps/cli/src/table.ts"
      provides: "Tabela humana + JSON puro"
      exports: ["printTable", "printJson"]
  key_links:
    - from: "apps/cli/src/client.ts"
      to: "https://api/api-v1"
      via: "fetch com Authorization Bearer, nunca import de @uhhu/db"
      pattern: "Authorization.*Bearer"
    - from: "apps/cli/src/commands.ts"
      to: "apps/cli/src/client.ts"
      via: "cada comando = 1 capability via REST (lista D-64)"
      pattern: "apiFetch|waitForJob"
    - from: "apps/cli/src/auth-store.ts"
      to: "0600 local + UHHU_TOKEN"
      via: "env precede arquivo; logout apaga"
      pattern: "0o600|UHHU_TOKEN"
---

<objective>
CLI headless sobre a API: auth PAT, comandos da cadeia Etapa 2, tabela/JSON, polling igual ao REST, export com arquivo. Fecha D-56 (lado CLI), D-58 (lado CLI), D-59 (lado CLI), D-62, D-64, D-65, D-66.

Purpose: Prova que a mesma capability executa fora do browser sem tocar o PG e sem duplicar regra — o CLI é adaptador fino de E/S (contrato §18, CORE-05).
Output: app @uhhu/cli funcional via pnpm, testes smoke+integração verdes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prova-headless/05-CONTEXT.md
@.planning/phases/05-prova-headless/05-02-SUMMARY.md
@apps/cli/README.md
@apps/core-api/src/routes/lab.ts
@packages/contracts/src/auth.ts

<interfaces>
<!-- Endpoints que o CLI consome (espelho exato do REST; o CLI NÃO inventa contrato). -->
Base `http://127.0.0.1:3000` (default; PORT default 3000 em packages/config/src/env.ts; override por env UHHU_API_URL ou flag global `--api <url>` — D-66).
Auth: `POST /api/v1/auth/token {email,password,deviceName}` → 201 `{token,...}`; `DELETE /api/v1/auth/tokens/:id`; logout local = apagar arquivo.
Recursos (todos com `Authorization: Bearer <PAT>` + `X-Request-Id` gerado por chamada + `Accept: application/json`):
`GET /api/v1/projects`, `POST /api/v1/projects`, `POST /api/v1/lab/searches`, `POST /api/v1/lab/searches/:id/runs` (201 sync | 202 + `Location: /api/v1/jobs/<id>`), `GET /api/v1/jobs/:id`, `GET /api/v1/lab/runs/:id/results`, `GET /api/v1/lab/results/:id`, decisão/divergência (rotas Phase 4 sob /api/v1/lab — LER routes/lab.ts no estado pós-05-02/Phase 4 e usar paths exatos), export (GET export com `Content-Disposition: attachment; filename=corpus-...` — D-52/D-59: salvar com o filename do servidor), compare.
Erros: envelope `{error:{code,message,details,requestId}}` PT-BR → stderr + exit code por status (2xx=0, 401/403=3, 404=4, 422/400=2, 429=5, 5xx=1 — documentar em --help e no README do app).
Polling D-56: após POST run, se 201 → run final; se 202 → `GET /jobs/:id` a cada 2s até `ok|partial|failed|cancelled`, timeout default 600s (`--timeout`), `-v` imprime cada transição; `failed|cancelled` = exit 1 com envelope no stderr.
Idempotência D-58: flag `--idempotency-key <k>` repassa header `Idempotency-Key` em run e export (janela 24h server-side já existe — D-39).
Comandos exatos D-64 (SEM renomear) + `project create` (mesmo padrão, exigido pela cadeia D-54; não é rename):
`uhhu auth login|logout`, `uhhu project list|create`, `uhhu lab search create|run`, `uhhu lab run results`, `uhhu lab result decide`, `uhhu lab export`, `uhhu job get` (+ `lab corpus get`, `lab search compare`, `lab source list` para a cadeia TOTAL D-54/D-67-paridade — mesmos padrões, sem inventar semântica).
Credencial D-62: arquivo `$HOME/.config/uhhu/credentials.json` `{ "baseUrl": string, "token": string }` criado com mode 0o600 (open com flag 'w' + chmod 0o600 + verificação via stat após escrita; se umask impedir, chmod explícito e re-stat); env `UHHU_TOKEN` PRECEDE o arquivo; `--logout` apaga o arquivo (missing = ok, exit 0).
Zero deps novas (fetch nativo Node 22, sem table lib — tabela hand-rolled com padEnd); deps: `@uhhu/contracts` (schemas/DTOs via `import type`), `tsx` (runner do bin). NUNCA `@uhhu/db`/drizzle/pg (guard D-55 do 05-01 quebra se importar).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: scaffold @uhhu/cli + client HTTP + auth-store 600</name>
  <files>apps/cli/package.json, apps/cli/src/client.ts, apps/cli/src/auth-store.ts, apps/cli/src/table.ts</files>
  <read_first>apps/cli/README.md, packages/config/src/env.ts, packages/contracts/src/errors.ts, tests/smoke/capabilities-guard.test.ts</read_first>
  <action>1. Criar apps/cli/package.json: `{ "name": "@uhhu/cli", "version": "0.1.0", "type": "module", "bin": { "uhhu": "./src/uhhu.ts" }, "scripts": { "typecheck": "tsc --noEmit -p tsconfig.json" }, "dependencies": { "@uhhu/contracts": "workspace:*", "tsx": "^4.23.13" } }` + tsconfig.json estendendo a base do monorepo (LER tsconfig.base/raiz e apps/core-api/tsconfig.json como molde; strict true, sem enfraquecer). src/uhhu.ts nesta task = stub que imprime help e exit 0 (comandos vêm na task 2). Shebang `#!/usr/bin/env -S tsx` na primeira linha? NÃO — bin via pnpm funciona com `tsx src/uhhu.ts`; usar shebang `#!/usr/bin/env node` + runner tsx quebra. Decisão concreta: bin = `./src/uhhu.ts` com shebang `#!/usr/bin/env -S pnpm dlx tsx` é frágil; em vez disso o bin é executado como `pnpm --filter @uhhu/cli exec uhhu` após `pnpm install` linkar .bin/uhhu → `tsx ./src/uhhu.ts`? O campo bin aponta para arquivo; pnpm cria shim node que executa o arquivo — .ts não roda sob node puro. Solução v1 documentada (D-66 "bin via pnpm, sem build"): package.json `"bin": { "uhhu": "./bin/uhhu.mjs" }` onde `bin/uhhu.mjs` é um wrapper JS puro de 5 linhas que faz `import { spawn } from 'node:child_process'` → executa `tsx <dir>/../src/uhhu.ts` com argv repassado (resolve tsx em node_modules/.bin). Atualizar apps/cli/README.md (substituir "fronteira reservada" por uso real + "opera por API, nunca PG").
2. apps/cli/src/client.ts: `export interface ApiOptions { baseUrl: string; token: string; idempotencyKey?: string; verbose?: boolean; timeoutMs?: number; }` + `export async function apiFetch<T>(path: string, opts: ApiOptions & { method?: string; body?: unknown }): Promise<{ status: number; data: T; filename?: string }>` — monta `Authorization: Bearer`, `X-Request-Id: randomUUID()`, `Idempotency-Key` quando dado, Content-Type JSON; parseia JSON sempre (envelope de erro vira `CliApiError { status, code, message, requestId }` com mensagens PT-BR repassadas verbatim); captura `content-disposition` para filename do export. + `export async function waitForJob(jobId, opts): Promise<JobDTO>` — GET /api/v1/jobs/:id a cada 2000ms até estado terminal `ok|partial|failed|cancelled`, timeout default 600000ms (erro `Timeout após 600s aguardando job.`), verbose imprime `job <id>: <status>` por transição. `import type` de contracts para DTOs; nunca `any` (unknown + narrowing no parse).
3. apps/cli/src/auth-store.ts: `const DIR = join(homedir(), '.config', 'uhhu'); const FILE = join(DIR, 'credentials.json');` `export async function saveToken(baseUrl, token)` (mkdir 0o700, writeFile + chmod 0o600 + stat confirma `(mode & 0o777) === 0o600` senão lança), `loadToken(): { baseUrl, token, from: 'env'|'file' }` (UHHU_TOKEN precede; baseUrl de UHHU_API_URL > --api resolvido no caller > default), `clearToken()` (unlink, missing=ok). PAT nunca em stdout/log (só `Salvo em <path>` sem o token).
4. apps/cli/src/table.ts: `printTable(headers: string[], rows: string[][])` (padEnd por coluna, sem cor) + `printJson(value: unknown)` (JSON.stringify 2 espaços, puro para scripts — D-65).
Typecheck verde.</action>
  <verify>
    <automated>pnpm --filter @uhhu/cli run typecheck && pnpm --filter @uhhu/cli exec uhhu --help</automated>
  </verify>
  <done>Bin uhhu responde --help; client/store/table com exports; typecheck verde; guard D-55 continua passando</done>
  <acceptance_criteria>grep -c '"uhhu"' apps/cli/package.json == 1; grep -c "export async function \(apiFetch\|waitForJob\)" apps/cli/src/client.ts == 2; grep -c "export async function \(saveToken\|loadToken\|clearToken\)" apps/cli/src/auth-store.ts == 3; grep -c "0o600\|UHHU_TOKEN" apps/cli/src/auth-store.ts >= 3; grep -c "printTable\|printJson" apps/cli/src/table.ts >= 2; grep -rn "@uhhu/db\|drizzle" apps/cli/package.json apps/cli/src/ | wc -l == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: comandos D-64 da cadeia total + export com arquivo + --json/-v</name>
  <files>apps/cli/src/uhhu.ts, apps/cli/src/commands.ts</files>
  <read_first>apps/cli/src/client.ts, apps/cli/src/auth-store.ts, apps/cli/src/table.ts, apps/core-api/src/routes/lab.ts</read_first>
  <action>Implementar apps/cli/src/commands.ts + fiação em uhhu.ts (parse manual de argv, sem commander — zero deps; flags globais `--api <url>`, `--json`, `-v`, `--idempotency-key <k>`, `--timeout <s>`; `--help` por comando). Cada comando = validação leve local + 1+ chamadas apiFetch; erros CliApiError → stderr `Erro <CODE>: <message> (requestId <id>)` + exit por status (401/403→3, 404→4, 400/422→2, 429→5, 5xx→1):
- `auth login --email --password [--device <nome, default hostname>]` → POST /auth/token → saveToken → tabela `{id, device, expira em}` (sem imprimir o token).
- `auth logout` → DELETE do PAT atual? O servidor não tem "atual por id" sem listar — implementar: GET /auth/tokens, achar por match? NÃO há match sem o raw. Decisão concreta: `logout` chama POST /api/v1/auth/logout com Bearer (servidor revoga PAT atual — 05-02) e depois clearToken local; se 404 (servidor sem a rota por algum motivo) segue e apaga local mesmo assim com warn. Documentar no help.
- `project list [--limit]`, `project create --title [--question]` → tabela id|título.
- `lab search create --project <id> --term "<termo>" [--sources bdtd,capes] [--year-from/--year-to/--type/--area/--institution/--program]` → tabela da search criada.
- `lab search run --search <id> [--idempotency-key]` → POST runs → 201 usa run direto; 202 waitForJob (D-56) → tabela métricas (totals, por fonte, status ok|partial).
- `lab run results --run <id> [--limit]` → tabela fonte|título|ano.
- `lab result decide --result <id> --decision eligible|ineligible|undecided [--reason]` (+ `--tag`, divergência/nota e corpus/compare/source conforme rotas Phase 4 exatas — LER lab.ts e espelhar paths/fields verbatim).
- `lab export --project <id> --format csv|bibtex|json [--scope corpus|selection] [--selection a,b,c] [--out <dir, default cwd>]` → baixa attachment, salva com filename do `content-disposition` do servidor (fallback local `corpus-<id>-<YYYYMMDD>.<ext>`), imprime `Salvo: <path> (<N> bytes)` (D-59 — NÃO é só stdout).
- `job get --job <id>` → tabela status|progresso.
Default tabela, `--json` JSON puro (sem prefixo humano), silencioso sem `-v` (só o essencial). uhhu.ts: roteia argv, `--help` geral lista todos os comandos com 1 linha cada. Nenhum acesso a PG; nenhum `any`.</action>
  <verify>
    <automated>pnpm --filter @uhhu/cli run typecheck && pnpm --filter @uhhu/cli exec uhhu lab export --help && pnpm vitest run --project smoke -- cli-unit 2>&1 | tail -n 5</automated>
  </verify>
  <done>Todos os comandos D-64 + cadeia total respondem --help; typecheck verde</done>
  <acceptance_criteria>grep -c "lab search run\|lab run results\|lab result decide\|lab export\|job get\|project list\|auth login" apps/cli/src/uhhu.ts apps/cli/src/commands.ts >= 7; grep -c "waitForJob" apps/cli/src/commands.ts >= 1; grep -c "content-disposition\|contentDisposition" apps/cli/src/commands.ts apps/cli/src/client.ts >= 1; grep -c "Idempotency-Key\|idempotencyKey" apps/cli/src/client.ts apps/cli/src/commands.ts >= 2; grep -v '^ *//' apps/cli/src/*.ts | grep -c "any" == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: smoke do CLI (parse/store/tabela/guard) + integração contra PG DEV</name>
  <files>tests/smoke/cli-unit.test.ts, tests/integration/cli-headless.test.ts</files>
  <read_first>apps/cli/src/commands.ts, apps/cli/src/auth-store.ts, tests/smoke/boot.test.ts, tests/integration/auth.test.ts</read_first>
  <action>1. tests/smoke/cli-unit.test.ts (sem PG, sem rede): importa table.ts (printTable alinha colunas com padEnd — assert contém header + linha), auth-store com HOME fake (`process.env.HOME = tmpdir` + UHHU_TOKEN set/unset: env precede arquivo; saveToken cria arquivo com mode 0o600 verificado via statSync; clearToken idempotente), client CliApiError mapping (mock global fetch? Vitest `vi.stubGlobal('fetch', ...)` retornando envelope 404 → apiFetch lança com code NOT_FOUND; 202→ sem body). Guard: re-assert package.json do cli sem @uhhu/db (defesa em profundidade além do guard 05-01). >=10 its.
2. tests/integration/cli-headless.test.ts (exige PG DEV + servidor local? NÃO — sobe via import do app? Padrão do repo: testes chamam HTTP contra servidor rodando (curl-lab.sh) ou via inject? LER tests/integration/lab-search-runs.test.ts e seguir o MESMO molde de boot do servidor). Fluxo via funções de commands (import direto, sem spawn — exceto 1 spawn `uhhu --help` exit 0): cria usuário+projeto via API, faz `auth login` via comando (PAT em HOME fake), `project list --json` contém o projeto, `lab search create/run` contra fonte real (aceita ok|partial — CAPES pode estar bloqueada, precedente 03-06), `lab export --format json` salva arquivo no tmpdir com nome `corpus-*.json` e conteúdo parseável com projectId. Assert NENHUMA conexão PG pelo CLI: o teste verifica que `apps/cli` não importa db (já coberto) + o fluxo inteiro passa só com BASE_URL (sem DATABASE_URL no env do processo filho — spawn com env filtrado sem *DATABASE*). >=6 its.
Rodar smoke verde; integração verde contra PG DEV.</action>
  <verify>
    <automated>pnpm vitest run --project smoke -- cli-unit 2>&1 | tail -n 5 && pnpm vitest run --project integration -- cli-headless 2>&1 | tail -n 8</automated>
  </verify>
  <done>Smoke >=10 its + integração ponta a ponta via CLI contra PG DEV, sem DATABASE_URL no cliente</done>
  <acceptance_criteria>grep -c "it(" tests/smoke/cli-unit.test.ts >= 10; grep -c "it(" tests/integration/cli-headless.test.ts >= 6; grep -c "DATABASE_URL" tests/integration/cli-headless.test.ts >= 1; grep -v '^ *//' tests/smoke/cli-unit.test.ts tests/integration/cli-headless.test.ts | grep -c "any" == 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→API | PAT em Authorization, baseUrl via flag/env — host hostil possível; validar TLS fora do escopo v1 (localhost/dev), nunca logar PAT |
| fs local | credentials.json com PAT — mode 600 obrigatório, UHHU_TOKEN em env de CI com cuidado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-LEAK | Information Disclosure | auth-store + logs | mitigate | File 0o600 verificado por stat; env precede; PAT nunca em stdout/log/resposta; teste de mode neste plano |
| T-05-03-SPOOF | Spoofing | baseUrl arbitrário | mitigate | Default localhost:3000; flag/env explícitos; Bearer só via HTTPS fora de localhost (documentado; v1 sem cert pinning — aceitar: dev local) |
| T-05-03-INJECT | Injection | argv → API | mitigate | Inputs repassados verbatim aos schemas Zod server-side; CLI não concatena SQL (não tem SQL); filename de export vem do servidor |
| T-05-03-BYPASS | Elevation of Privilege | CLI acessando PG | mitigate | Guard D-55 (05-01) + assert neste plano + integração sem DATABASE_URL no env |
| T-05-03-IDOR | Information Disclosure | comandos com ID | mitigate | Servidor decide (404 idêntico); CLI repassa requestId no erro; exit codes sem vazar existência além do que o servidor já diz |
</threat_model>

<verification>
- `pnpm --filter @uhhu/cli run typecheck` verde
- `pnpm vitest run --project smoke -- cli-unit` verde
- `pnpm vitest run --project integration -- cli-headless` verde contra PG DEV (servidor local, cliente sem DATABASE_URL)
- Guard D-55 (05-01) ainda verde; nenhum `any` no app
</verification>

<success_criteria>
- `uhhu auth login → project → search → run → results → decide → export` executa só com BASE_URL+PAT
- Export salva arquivo `corpus-*.{csv,bib,json}`; polling 25s+jobs igual ao REST; `--json` puro para scripts
- Zero imports de db no app; credencial 600 + env + logout
</success_criteria>

<output>
After completion, create `.planning/phases/05-prova-headless/05-03-SUMMARY.md`
</output>
