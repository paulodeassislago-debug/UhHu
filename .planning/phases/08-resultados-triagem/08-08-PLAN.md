---
phase: 08-resultados-triagem
plan: "08"
type: execute
wave: 8
depends_on:
  - 08-01
gap_closure: true
gap_source: Auditoria externa Hermes 12/09 — CAPES zera com Ano+Grau juntos (fonte) + year null no mapper (dataDefesa); busca do Paulo "Ensino de Quimica" 2024-2026 CAPES 0
files_modified:
  - packages/integrations/src/capes.ts
  - tests/integration/lab-sources-contract.test.ts
autonomous: true
requirements:
  - UI-14
must_haves:
  truths:
    - Payload CAPES nunca combina Ano + Grau Acadêmico na mesma chamada (qualquer filtro de período + tipo)
    - Item CAPES com dataDefesa ISO recebe year real; ano volta a filtrar no postFilter do Core
    - Busca período+tipo retorna CAPES > 0 onde a fonte tem dados (caso Paulo: 2024-2026 + tese/dissertação)
  artifacts:
    - path: packages/integrations/src/capes.ts
      provides: Payload sem combo Ano×Grau + year de dataDefesa
      contains: dataDefesa
    - path: tests/integration/lab-sources-contract.test.ts
      provides: Casos Ano+Grau e dataDefesa→year
      contains: dataDefesa
---

<objective>
CAPES funcional com período+tipo: payload sem combo que zera a fonte + year real do mapper.

Purpose: Sem isso, CAPES é inutilizável com filtro de período (caso real do Paulo).
Output: Adapter corrigido, casos de contrato, suítes verdes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/08-security-baseline.md
@packages/integrations/src/capes.ts
@tests/integration/lab-sources-contract.test.ts

<interfaces>
Evidência viva (12/09, API real): sem filtros 2936 | só Ano 3 | só Grau 1777 | Ano+Grau SEMPRE 0; "Ano"="2024/2026" dá HTTP 500 (nunca enviar range como valor único). Fonte zera ao combinar campos DIFERENTES — contorno client-side.
Regras:
1. buildCapesPayload: se houver filtro de Grau (docTypes não-vazio), NÃO enviar nenhum `Ano` (range, unilateral ou qualquer forma). Ano fica 100% com o postFilter do Core (D-32: fonte aproxima, Core garante). Sem Grau: manter comportamento atual (Ano expande normal — funciona: só-Ano retorna). RangeTooWideError (>30a) e ano-invertido continuam validados ANTES da rede mesmo quando Ano não é enviado (o período do usuário continua valendo para o postFilter).
2. Mapper: `year: yearFromValue(record['dataDefesa'] ?? record['ano'] ?? record['year'])` — dataDefesa primeiro (ISO "2024-01-26T00:00:00.000Z"; YEAR_PATTERN já extrai), fallback nos antigos. Checar yearFromValue aceita ISO (se não, estender com teste).
3. Casos de contrato (fixture CAPES): (a) período+tipo → payload SEM campo Ano e COM Grau; (b) só-período → COM Ano; (c) record com dataDefesa ISO → year=ano correto; (d) record sem dataDefesa/ano → year null (comportamento preservado). Atualizar descrições que afirmarem o envio combinado antigo.
4. Fora de escopo: combos Área/Programa × outros campos (sem evidência de zeramento — não mexer); HTTP 500 do "Ano" range (nunca enviamos range como valor único).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: payload sem combo + year de dataDefesa</name>
  <files>packages/integrations/src/capes.ts</files>
  <read_first>
    - packages/integrations/src/capes.ts buildCapesPayload (276-327), mapCapesRecord (346-378), yearFromValue + YEAR_PATTERN, canonicalDocTypes/degreeLabel
    - postFilter do Core para ano (onde itens fora do range caem — confirmar que null passa como "não-provado")
  </read_first>
  <action>
    1. Em buildCapesPayload: computar `hasDegree = canonicalDocTypes(def.docTypes).length > 0`; só montar filtros `Ano` quando `!hasDegree`. Manter validação de range/invertido sempre (lança antes da rede). Comentário citando a evidência (Ano×Grau→0 na fonte, 12/09/2026) e D-32.
    2. Em mapCapesRecord: year de `dataDefesa` primeiro com fallback `ano`/`year`; estender yearFromValue para ISO se preciso (com teste).
    3. Typecheck + eslint do pacote exit 0; zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "dataDefesa" packages/integrations/src/capes.ts` >= 2 (mapper + comentário ou teste não conta — só fonte)
    - `pnpm --filter @uhhu/integrations exec tsc --noEmit 2>/dev/null || pnpm --filter integrations exec tsc --noEmit` exit 0 (ajustar nome do pacote ao real)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && echo ROOT_TYPECHECK_OK</automated>
  </verify>
  <done>Payload nunca zera por combo; year real vindo da fonte</done>
</task>

<task type="auto">
  <name>task 2: casos de contrato + suítes + auditoria</name>
  <files>tests/integration/lab-sources-contract.test.ts</files>
  <read_first>
    - tests/integration/lab-sources-contract.test.ts bloco CAPES (padrão makeFakeFetch/testContext, fixture rest/busca)
  </read_first>
  <action>
    1. Casos: período+tipo → `filtros` sem `campo:'Ano'` e com `Grau Acadêmico`; só-período → com `Ano`; fixture com `dataDefesa` ISO → year correto; sem dataDefesa/ano → null. Reusar fixture existente estendida (não quebrar casos vivos BDTD/CAPES).
    2. Gates: `pnpm test` monorepo + vitest lab + typecheck raiz exit 0. Auditoria adversarial rápida do diff (payload injeta termo cru? filtros vêm de schemas Zod já validados — confirmar).
  </action>
  <acceptance_criteria>
    - `grep -c "dataDefesa" tests/integration/lab-sources-contract.test.ts` >= 2
    - `pnpm test` exit 0 e `pnpm exec tsc --noEmit -p tsconfig.json` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test 2>&1 | tail -n 5</automated>
  </verify>
  <done>Contrato CAPES trava o contorno; suítes verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload → fonte | filtros vêm de SearchDef validada por Zod; termo trimado; sem SQL/injection (POST JSON) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Tampering | período ignorado pela fonte e pelo Core | mitigate | RangeTooWideError mantido; postFilter de ano cobre o range (mapper agora entrega year real); teste período+tipo no contrato |
| T-08-08-02 | Information Disclosure | dataDefesa expõe algo novo | mitigate | campo já vinha no rawMetadata; só promovido a year canônico |
</threat_model>

<verification>
- Payload período+tipo sem Ano e com Grau; só-período com Ano; dataDefesa→year; sem→null
- `pnpm test` + typecheck raiz verdes; auditoria do diff
</verification>

<success_criteria>
- UI-14: busca do Paulo (período+tipo) retorna CAPES com anos reais
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-08-SUMMARY.md`
</output>
