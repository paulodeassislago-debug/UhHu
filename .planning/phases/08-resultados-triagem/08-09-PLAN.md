---
phase: 08-resultados-triagem
plan: "09"
type: execute
wave: 9
depends_on:
  - 08-01
  - 08-08
gap_closure: false
gap_source: null
product_decision: Paulo 12/09 definitivas — (1) Mestrado Profissional SEPARADO (terceiro valor, visível/selecionável na UI); (2) auto-aspas REMOVIDAS (termo cru, fidelidade ao site). Incorpora contorno Ano×Grau + sem Grande Área
files_modified:
  - packages/contracts/src/lab.ts
  - packages/integrations/src/capes.ts
  - apps/lab/src/search/searchTerm.ts
  - apps/lab/src/search/SearchForm.tsx
  - apps/lab/app/project/[id]/run.tsx
  - tests/integration/lab-sources-contract.test.ts
  - apps/lab/src/search/__tests__/searchTerm.test.ts
autonomous: true
requirements:
  - UI-13
  - UI-14
must_haves:
  truths:
    - Período+tipo na CAPES usa só Ano na fonte com tipo no pós-filtro; Grande Área nunca vai à fonte; MP é categoria própria selecionável
    - Termo multi-palavra vai cru às fontes (sem aspas automáticas); aspas digitadas pelo usuário preservadas
    - Busca do Paulo (2024-2026 + tese/dissertação) retorna CAPES com anos reais; MP excluído salvo se selecionado
  artifacts:
    - path: packages/contracts/src/lab.ts
      provides: docTypeSchema com terceiro valor
      contains: professionalMaster
    - path: apps/lab/src/search/searchTerm.ts
      provides: Composição sem auto-aspas
      contains: buildSearchTerm
  key_links:
    - from: apps/lab/src/search/SearchForm.tsx
      to: packages/contracts/src/lab.ts
      via: terceiro checkbox mapeia para o terceiro valor do contrato
      pattern: professionalMaster
---

<objective>
Decisões Paulo definitivas: MP separado + termo cru + contorno Ano×Grau revisado (Ano-fonte).

Purpose: CAPES período+tipo funciona com anos reais; MP explícito; busca fiel ao site.
Output: Contrato com 3º valor, payload Ano-only, sem Grande Área, sem auto-aspas, testes, hint 2026.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/08-security-baseline.md
@packages/contracts/src/lab.ts
@packages/integrations/src/capes.ts
@apps/lab/src/search/searchTerm.ts
@apps/lab/src/search/SearchForm.tsx

<interfaces>
Verificado no código: `docTypeSchema = z.enum(['masterThesis','doctoralThesis'])`, `docTypes max(2)`; coluna `doc_type` é `text` nullable (SEM migration para 3º valor); `canonicalDocType('Mestrado Profissional')` → null hoje; BDTD envia termo cru (`lookfor`, sem aspas próprias); form tem checkboxes tese/dissertação; postFilter de área é heurística instituição/programa (D-32).
Regras (decisões Paulo 12/09):
1. Contrato: `docTypeSchema` += `'professionalMaster'` (estilo dos existentes), `docTypes max(2)→max(3)`. `DocType`/`NormalizedItem` fluem pelo tipo (sem migration — coluna text). Aditivo §19.
2. `canonicalDocType`: 'Mestrado Profissional' (norm) → `'professionalMaster'`; `degreeLabel` mapeia os 3 (MP → 'Mestrado Profissional').
3. Payload CAPES: com ano E tipo → SÓ Ano (tipo 100% pós-filtro); só tipo → Grau (labels incl. MP); só ano → Ano; sem ambos → nenhum; NUNCA Ano+Grau juntos; NUNCA 'Grande Área Conhecimento' (def.area cai no pós-filtro heurístico D-32 — documentar a troca no comentário); validações RangeTooWideError/invertido sempre antes da rede.
4. postFilter tipo: MP casa SOMENTE `professionalMaster` (tese+dissertação exclui MP; sem filtro de tipo, MP incluído com rótulo). Verificar que o allowed-set é genérico (se houver allowlist hardcoded binária, estender).
5. searchTerm.ts: REMOVER `quoteIfNeeded` (aspas automáticas); texto vai trimado e cru; aspas digitadas explicitamente PRESERVADAS; `splitSearchTerm` (inverso p/ edição) acompanha; validação de tamanho/balanceamento mantida no schema chamador. Atualizar comentários D-09/D-31 que citarem aspas automáticas.
6. SearchForm: terceiro checkbox "Mestrado profissional" (mapeia `professionalMaster`); edição preserva os 3; sem inverter tese/dissertação.
7. Hint 2026 (pequeno): na tela do run, quando CAPES `total===0` E `search.yearTo>=2026`, sufixar o banner/status com "(a fonte tem poucos dados de 2026)" — dados já carregados na tela; sem nova rota. Se o wiring exigir prop-drilling desproporcional, fazer só com dados presentes e documentar.
8. Testes: ATUALIZAR casos 08-08 que afirmam Grau-only com ambos (viram Ano-only); contrato: 3º valor, max 3, MP→professionalMaster, degreeLabel MP, nunca-GrandeÁrea, período+tipo sem Ano; searchTerm: multi-palavra sem aspas + aspas explícitas preservadas + edição round-trip; BDTD: termo cru inalterado (regressão). Typecheck raiz cobre tudo (lição 08-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: contrato 3º valor + payload Ano-only + sem Grande Área</name>
  <files>packages/contracts/src/lab.ts, packages/integrations/src/capes.ts</files>
  <read_first>
    - packages/contracts/src/lab.ts docTypeSchema (50), docTypes max, DocType/NormalizedItem (201/442)
    - packages/integrations/src/capes.ts buildCapesPayload (pós-08-08), canonicalDocType, degreeLabel, yearFromValue (já dataDefesa-first)
    - postFilter keepItem tipo (allowed-set: genérico ou binário?)
  </read_first>
  <action>
    1. Contrato: enum += 'professionalMaster', max 3. Confirmar que nada exige migration (coluna text) e que UI/server fluem pelo tipo.
    2. canonicalDocType MP → professionalMaster; degreeLabel cobre os 3.
    3. Payload: ano+tipo → só Ano; só-tipo → Grau (3 labels); nunca combinar; remover Grande Área (área → pós-filtro; comentário com evidência 12/09 + D-32); validações mantidas.
    4. postFilter: MP só casa professionalMaster. Typecheck+lint exit 0, zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "professionalMaster" packages/contracts/src/lab.ts packages/integrations/src/capes.ts` >= 3
    - `grep -c "Grande Área Conhecimento" packages/integrations/src/capes.ts` == 0
    - `grep -c "docTypes.*max(3)\|max(3)" packages/contracts/src/lab.ts` >= 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && echo ROOT_TYPECHECK_OK</automated>
  </verify>
  <done>MP separado no contrato e no payload; Ano-only com ambos; sem Grande Área</done>
</task>

<task type="auto">
  <name>task 2: sem auto-aspas + checkbox MP + hint 2026</name>
  <files>apps/lab/src/search/searchTerm.ts, apps/lab/src/search/SearchForm.tsx, apps/lab/app/project/[id]/run.tsx</files>
  <read_first>
    - apps/lab/src/search/searchTerm.ts (quoteIfNeeded, buildSearchTerm, splitSearchTerm, comentários D-09/D-31)
    - apps/lab/src/search/SearchForm.tsx (checkboxes tese/dissertacao 115-194, edição)
    - apps/lab/app/project/[id]/run.tsx (banner/status por fonte onde cabe o hint 2026)
  </read_first>
  <action>
    1. Remover quoteIfNeeded; buildSearchTerm compõe cru (trim, sem expressão livre — D-09 intacto); aspas explícitas intactas; splitSearchTerm acompanha; comentários atualizados (fidelidade ao site, decisão 12/09).
    2. Checkbox "Mestrado profissional" ↔ professionalMaster (criar + editar preservam os 3; tese/dissertação sem inversão).
    3. Hint 2026 condicional mínimo conforme interfaces item 7.
    4. Typecheck+lint do app exit 0, zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "quoteIfNeeded" apps/lab/src/search/searchTerm.ts` == 0
    - `grep -c "professionalMaster" apps/lab/src/search/SearchForm.tsx` >= 2
    - `pnpm --filter @uhhu/lab run typecheck && pnpm --filter @uhhu/lab run lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit && pnpm --filter @uhhu/lab exec eslint apps/lab/src/search 2>&1 | tail -n 3</automated>
  </verify>
  <done>Termo cru, MP selecionável, hint 2026</done>
</task>

<task type="auto">
  <name>task 3: testes (contrato + searchTerm + regressão) + auditoria</name>
  <files>tests/integration/lab-sources-contract.test.ts, apps/lab/src/search/__tests__/searchTerm.test.ts</files>
  <read_first>
    - Casos 08-08 em lab-sources-contract.test.ts (afirmam Grau-only com ambos — ATUALIZAR para Ano-only)
    - apps/lab/src/search/__tests__/searchTerm.test.ts (casos de aspas automáticas — ATUALIZAR)
  </read_first>
  <action>
    1. Contrato: período+tipo → sem Ano + (sem Grau); só-tipo → Grau incl. MP; terceiro valor/max3; MP→professionalMaster; nunca GrandeÁrea; dataDefesa→year mantido (08-08).
    2. searchTerm: cru sem aspas, explícitas preservadas, round-trip edição, validação mantida.
    3. Gates: `pnpm test` + typecheck raiz + vitest lab exit 0. Auditoria adversarial do diff.
  </action>
  <acceptance_criteria>
    - `pnpm test` exit 0 (sem fails) e `pnpm exec tsc --noEmit -p tsconfig.json` exit 0
    - `grep -c "Grande Área" tests/integration/lab-sources-contract.test.ts` == 0 (nenhum caso exige o filtro removido)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test 2>&1 | tail -n 5</automated>
  </verify>
  <done>Contrato trava as decisões; suítes verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload → fonte | filtros de SearchDef validada; termo cru trimado (site-like); sem SQL/injection |
| 3º valor → banco | coluna text, sem migration; servidor revalida enum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Tampering | tipo ignorado volta tudo | mitigate | postFilter classifica MP separado; testes período+tipo por valor |
| T-08-09-02 | Tampering | termo cru injeta operador | mitigate | operadores só via linhas AND/OR/NOT da UI (D-09); texto é operando; validação mantida |
</threat_model>

<verification>
- Ano-only com ambos; MP terceiro valor fim-a-fim; termo cru; sem Grande Área; hint 2026
- `pnpm test` + typecheck raiz + lab verdes; auditoria do diff
</verification>

<success_criteria>
- UI-13/UI-14: decisões Paulo definitivas implementadas (MP separado, sem aspas, Ano-fonte)
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-09-SUMMARY.md`
</output>
