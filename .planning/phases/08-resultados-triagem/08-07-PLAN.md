---
phase: 08-resultados-triagem
plan: "07"
type: execute
wave: 7
depends_on:
  - 08-01
  - 08-02
gap_closure: false
gap_source: null
product_decision: Paulo 12/09/2026 REVISADA — SUBSTITUI 08-06 (eager sem teto). Lote incremental com BUSCAR MAIS: 100/fonte por vez, totalKnown da pág. 1, custo sob demanda
files_modified:
  - apps/core-api/src/lib/searchRuns.ts
  - apps/core-api/src/routes/lab.ts
  - apps/core-api/src/capabilities.ts
  - packages/contracts/src/lab.ts
  - packages/core/src/searchRuns.ts
  - apps/lab/src/api/lab.ts
  - apps/lab/app/project/[id]/results.tsx
  - apps/lab/src/search/useRunPolling.ts
  - tests/integration/lab-search-runs-full.test.ts
  - tests/integration/lab-run-fetch-more.test.ts
autonomous: true
requirements:
  - UI-14
  - UI-17
must_haves:
  truths:
    - Run inicial armazena só o 1º lote (BDTD 100 + CAPES 2×50) com totalKnown da fonte exposto; UI mostra "X de Y" com Y total da fonte
    - Botão BUSCAR MAIS puxa +100/fonte até 250/250 com hasMore correto; badge≡contador mantido a cada lote (newCount recomputado)
    - Sem teto de itens alcançável por puxadas sucessivas; rate limit e cancel preservados; sem run de 13 min
  artifacts:
    - path: apps/core-api/src/routes/lab.ts
      provides: Endpoint fetch-more por run
      contains: fetch-more|fetchMore
    - path: tests/integration/lab-run-fetch-more.test.ts
      provides: Prova incremental 250→100+100+50
      contains: hasMore
  key_links:
    - from: apps/lab/app/project/[id]/results.tsx
      to: apps/lab/src/api/lab.ts
      via: BUSCAR MAIS chama fetchMore e continua o scroll infinito
      pattern: fetchMore|BUSCAR MAIS
---

<objective>
Trocar o eager 08-06 por lote incremental: run traz 100/fonte + totalKnown; BUSCAR MAIS puxa o resto sob demanda.

Purpose: Decisão Paulo revisada — 100% alcançável sem run de 13 min; custo distribuído por puxada.
Output: Batch inicial + endpoint fetch-more + UI com botão + testes 250/250, com o eager revertido de forma controlada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/08-security-baseline.md
@apps/core-api/src/lib/searchRuns.ts
@packages/contracts/src/lab.ts
@apps/lab/src/api/lab.ts
@apps/lab/app/project/[id]/results.tsx

<interfaces>
Limites medidos: BDTD `limit=100` retorna 100 (1 chamada/lote); CAPES `registrosPorPagina` máx efetivo 50 (60+ retorna 20 — teto interno) → lote CAPES = 2×50. Total vem na pág. 1 (BDTD resultCount, CAPES total). Lote = 100 POR FONTE. Offset por fonte por run = count armazenado.
O QUE REVERTER DO 08-06 (forward rework, sem git revert): loop-até-total vira loop-de-1-lote (primeiras páginas); `RUN_QUEUE_TIMEOUT_MS` 30min → 60s de volta; `RUN_POLL_MAX_POLLS` 720 → 240 de volta; teste eager 120/120 SUBSTITUÍDO pelo incremental (comportamento mudou). MANTER do 08-06: rank global contínuo, pagesFetched/pagesTotal, checagem cancel por página, insert por página, trava anti-loop.
DESIGN NOVO:
1. Run inicial: BDTD pág.1 limit=100 + CAPES págs.1-2 ×50 (=100). `perSource.total` = totalKnown da fonte (já é page.total — confirmar e documentar); `returned` = armazenados; hasMore implícito: stored < total.
2. `POST /api/v1/lab/runs/:runId/fetch-more` (+ capability `lab.run.fetchMore`, contrato §10/§18): body opcional `{sources?: ('bdtd'|'capes')[]}` (default: fontes com hasMore). Por fonte: offset = count(runId, source); busca próximas páginas até +100 NOVOS itens ou fonte esgotada (mesmas paradas anti-loop do 08-06); insert com rank contínuo (rank = storedCount + i); atualiza metrics.returned + pagesFetched; RECOMPUTA `newCount` = count(itens do run ausentes em runs anteriores) — OBRIGATÓRIO para badge≡contador após cada lote (D-15: lote posterior pertence ao run corrente); retorna `{added: {bdtd, capes}, hasMore: {bdtd, capes}, newCount, returned, total}`. IDOR owner-scoped (404 fora do escopo), Zod, rate limit de execução valendo, timeout 60s por batch, sem auto-retry. Double-tap seguro: onConflictDoNothing + UI desabilita durante load.
3. UI results.tsx: fim da lista infinita mostra "BUSCAR MAIS — mais 100 de ~Y" quando hasMore (qualquer fonte); loading honesto ("buscando mais 100…"), erro com retry, nunca travada; após lote, scroll continua de onde parou. "Mostrando X de Y" com Y = totalKnown. Botão cancela-run da tela run mantido (runs agora são curtos; fetch-more é a longa duração sob comando).
4. Modo IA futuro: só anotar no SUMMARY como deferred (decisão: pensar depois).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: reverter eager + batch inicial + endpoint fetch-more</name>
  <files>apps/core-api/src/lib/searchRuns.ts, apps/core-api/src/routes/lab.ts, apps/core-api/src/capabilities.ts, packages/contracts/src/lab.ts, packages/core/src/searchRuns.ts</files>
  <read_first>
    - apps/core-api/src/lib/searchRuns.ts (loop 08-06, timeout 30min, métricas pagesFetched/pagesTotal, checagem cancel por página)
    - packages/contracts/src/lab.ts (PerSourceMetrics, DTOs de run; onde entra FetchMore request/response)
    - apps/core-api/src/routes/lab.ts + capabilities.ts (molde de rota owner-scoped + capability lab.*)
  </read_first>
  <action>
    1. Engine: loop vira 1 lote (BDTD: pág.1 limit=100; CAPES: págs.1-2 ×50) com as mesmas paradas/proteções; `RUN_QUEUE_TIMEOUT_MS` volta a 60s com comentário (lotes são curtos; decisão revisada 12/09 substitui 08-06); rank contínuo e insert por página mantidos.
    2. Extrair o miolo de fetch+insert de um lote em função reusável `fetchBatch(db, runId, source, offset, limit)` usada pelo run inicial E pelo fetch-more (sem duplicar regra).
    3. `POST /runs/:runId/fetch-more`: Zod `{sources?}`, owner via run (404 idêntico fora do escopo/inexistente), por fonte com hasMore busca até +100 novos (offset=stored), atualiza returned/pagesFetched, RECOMPUTA newCount do run (D-15), responde added/hasMore/newCount/returned/total. Sem auto-retry; erro de fonte no lote → 200 com added parcial + hasMore preservado? Decidir e documentar: parcial honesto (lote é interação, não run) — nunca 500 por fonte caída.
    4. Capability `lab.run.fetchMore` no registry (definição única em contracts). Typecheck+lint exit 0, zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "RUN_QUEUE_TIMEOUT_MS = 60" apps/core-api/src/lib/searchRuns.ts` >= 1 (teto revertido com comentário da revisão)
    - `grep -c "fetch-more\|fetchMore" apps/core-api/src/routes/lab.ts apps/core-api/src/capabilities.ts packages/contracts/src/lab.ts` >= 3
    - `grep -c "newCount" apps/core-api/src/lib/searchRuns.ts` >= 2 (inicial + recomputo no lote)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && echo ROOT_TYPECHECK_OK</automated>
  </verify>
  <done>Run inicial em lote + endpoint fetch-more com newCount recomputado por lote</done>
</task>

<task type="auto">
  <name>task 2: testes incrementais (250→100+100+50) + IDOR</name>
  <files>tests/integration/lab-search-runs-full.test.ts, tests/integration/lab-run-fetch-more.test.ts</files>
  <read_first>
    - tests/integration/lab-search-runs-full.test.ts (teste eager 120 a SUBSTITUIR — comportamento mudou)
    - tests/integration/lab-results-isnew.test.ts (padrão offline-skip, `const api = app`, bootstrapAdmin)
  </read_first>
  <action>
    1. SUBSTITUIR o conteúdo eager por lote: fixture total 250 (BDTD e/ou CAPES via fetchFn) → run inicial = 100 armazenados + totalKnown 250 + hasMore; fetch-more → 200; fetch-more → 250 + hasMore false; 4º fetch-more → nada novo, hasMore false (idempotente, sem dupes: count segue 250).
    2. isNew dos itens dos lotes 2-3 = true (run corrente, D-15 só-anteriores) e newCount do run == count(isNew) após CADA lote (recomputo provado).
    3. IDOR: estranho + fantasma → 404 idêntico no fetch-more; rota sem auth → 401.
    4. Manter/ajustar o teste de cancel mid-loop se ainda fizer sentido em lote (cancel de batch); remover o que era específico do eager (timeout 30min, 120/120) com nota no SUMMARY. Zero `any`; raiz cobre os arquivos (lição 08-05).
  </action>
  <acceptance_criteria>
    - `grep -c "hasMore" tests/integration/lab-run-fetch-more.test.ts` >= 3
    - `pnpm exec tsc --noEmit -p tsconfig.json` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && echo ROOT_TYPECHECK_OK</automated>
  </verify>
  <done>Incremental provado 250/250 com hasMore, isNew e newCount por lote + IDOR</done>
</task>

<task type="auto">
  <name>task 3: UI BUSCAR MAIS + polling de volta + X de Y</name>
  <files>apps/lab/src/api/lab.ts, apps/lab/app/project/[id]/results.tsx, apps/lab/src/search/useRunPolling.ts</files>
  <read_first>
    - apps/lab/app/project/[id]/results.tsx (lista infinita 08-02 + fim de lista atual)
    - apps/lab/src/api/lab.ts (onde entra fetchMore no client tipado)
    - apps/lab/src/search/useRunPolling.ts (RUN_POLL_MAX_POLLS=720 a reverter)
  </read_first>
  <action>
    1. Client: `fetchMore(runId, {sources?})` tipado retornando added/hasMore/newCount/returned/total.
    2. results.tsx: ao fim do scroll infinito, se hasMore (qualquer fonte): botão "BUSCAR MAIS — mais 100 de ~Y" (Y = totalKnown somado das fontes com hasMore); loading desabilita ("buscando mais 100…"); erro → retry local; sucesso → anexa e segue o scroll (sem pular ao topo); sem hasMore → fim silencioso atual. "Mostrando X de Y" com Y totalKnown (não armazenado).
    3. `RUN_POLL_MAX_POLLS` 720 → 240 de volta (runs voltam a ser curtos) com comentário; progresso de páginas do 08-06: manter se útil ao fetch-more loading, senão remover com nota.
    4. Testes app (sem rede): hasMore mostra botão; loading desabilita + double-tap não duplica chamada; erro mostra retry; typecheck+lint+test exit 0, zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "BUSCAR MAIS" "apps/lab/app/project/[id]/results.tsx"` >= 1
    - `grep -c "RUN_POLL_MAX_POLLS = 240" apps/lab/src/search/useRunPolling.ts` >= 1
    - `pnpm --filter @uhhu/lab run typecheck && pnpm --filter @uhhu/lab run lint && pnpm --filter @uhhu/lab run test` todos exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test 2>&1 | tail -n 6</automated>
  </verify>
  <done>BUSCAR MAIS funcional com loading/retry; polling e contadores coerentes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetch-more → fontes | rate limit SourceClient intocado; perPage dentro dos máx (100/50) |
| botão → servidor | owner do run decide; 404 fora do escopo; sem ID novo confiado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Denial of Service | puxadas infinitas seguram worker | mitigate | timeout 60s/lote; 10 runs/h; double-tap guard; cada lote é curto |
| T-08-07-02 | Tampering | offset adulterado pula itens | mitigate | offset é SEMPRE count armazenado no servidor, nunca vem do client |
| T-08-07-03 | Information Disclosure | hasMore/total de outro usuário | mitigate | run owner-scoped; 404 idêntico; contagens só do próprio run |
</threat_model>

<verification>
- 250/250 via 3 lotes + hasMore + isNew/newCount por lote + IDOR no fetch-more
- UI: botão, loading, retry, X de Y totalKnown; polling 240 de volta
- typecheck raiz + app exit 0; suítes verdes; auditoria do diff; 08-06 marcado superseded no SUMMARY
</verification>

<success_criteria>
- UI-14/UI-17: 100% alcançável por puxadas, sem run de 13 min (decisão revisada 12/09)
- 08-06 eager revertido de forma controlada (história preservada nos commits)
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-07-SUMMARY.md`
</output>
