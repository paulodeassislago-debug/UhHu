---
phase: 08-resultados-triagem
plan: "04"
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - apps/lab/src/results/TagInput.tsx
  - apps/lab/src/results/TagManagerModal.tsx
  - apps/lab/app/project/[id]/result.tsx
  - apps/lab/src/results/ResultCard.tsx
  - apps/lab/app/project/[id]/results.tsx
  - apps/lab/app/_layout.tsx
  - apps/lab/src/results/__tests__/tagInput.test.ts
autonomous: true
requirements:
  - UI-20
  - UI-22
must_haves:
  truths:
    - User associa tag com autocomplete, cria inexistente digitando, e gerencia tags sem sair da triagem
    - User abre a ficha completa sob demanda em tela dedicada, com decisão ali também, skeleton e retry
    - Renomear/excluir tag reflete nos cards e nos filtros sem refetch manual
  artifacts:
    - path: apps/lab/src/results/TagInput.tsx
      provides: Autocomplete + criar-e-associar no card
      contains: autocomplete|createProjectTag|attachTag
    - path: apps/lab/src/results/TagManagerModal.tsx
      provides: Modal de gestão sem sair da triagem
      contains: renameProjectTag|deleteProjectTag
    - path: apps/lab/app/project/[id]/result.tsx
      provides: Ficha dedicada sob demanda
      contains: getResult|skeleton|Skeleton
  key_links:
    - from: apps/lab/src/results/TagInput.tsx
      to: labApi.attachTag
      via: associa tag existente ou recém-criada ao grupo
      pattern: attachTag
    - from: apps/lab/src/results/TagManagerModal.tsx
      to: labApi.renameProjectTag
      via: PATCH por tagId com 400 em colisão
      pattern: renameProjectTag
    - from: apps/lab/app/project/[id]/result.tsx
      to: labApi.getResult
      via: busca fresca ao abrir, nunca cache da lista
      pattern: getResult
---

<objective>
Tags com autocomplete + modal de gestão + ficha completa sob demanda (D-20, D-21, D-18, D-19, UI-20, UI-22).

Purpose: Fecha a fase: triagem sem tag rápida e sem ficha on-demand obriga o pesquisador a decidir no escuro. Este plano completa o card e entrega a tela dedicada, consumindo o client da 08-02 e o mapa de overrides da 08-03.
Output: TagInput + TagManagerModal no card/tela, tela result (ficha), testes, navegação registrada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/08-security-baseline.md
@apps/lab/src/results/ResultCard.tsx
@apps/lab/src/results/DecisionBar.tsx
@apps/lab/app/project/[id]/results.tsx

<interfaces>
Tags D-20/D-21 (defaults canônicos do servidor pós-08-01: `incluir, excluir, duplicado, indisponível, revisar` — sempre sugeridos primeiro, nesta ordem):
- `TagInput({group, projectTags, getToken, onGroupUpdated, onTagsChanged})`: input filtra `projectTags` por prefixo case-insensitive (helper puro `suggestTags(tags, prefix): ProjectTag[]` — defaults primeiro, depois alfabética); escolher existente → `attachTag(group.id, tag.id)`; texto sem match + submit → `createProjectTag(projectId, {name: texto})` e em seguida `attachTag` com a criada (duas chamadas, erro da 2ª com retry mantendo a tag criada); chips das `group.tags` com × → `detachTag`. Erro → verbatim inline; group null → desabilitado. Debounce/estilo à discrição, respeitando FlatList/ScrollView e `any` proibido.
- `TagManagerModal({visible, projectId, getToken, onClose, onTagsChanged})` (molde ProjectModal: Modal transparent, validação local, erro verbatim sem fechar): FlatList das tags (keyExtractor id — tripwire proíbe `.map` de lista); cada linha: TextInput do nome + [Salvar] (nada muda → sem request; vazio/overlong → erro local sem request, molde 07-06) → `renameProjectTag` (400 colisão → mensagem verbatim na linha); [Excluir] two-tap ([Excluir]→[Confirmar?]) → `deleteProjectTag` + fecha linha; rodapé: input nome + input cor opcional (texto ≤20, ex. `vermelho`; sem color-picker — fora do v1) + [Criar] → `createProjectTag`. Fechar (X ou backdrop) → `onTagsChanged()` (tela refaz `listProjectTags` + limpa overrides de tags? tags vêm no group DTO: após qualquer mutação, refetch dos grupos afetados via `listGroups` + re-merge? Simples e correto: `onTagsChanged` dispara `refreshGroups()` na tela — recarrega listGroups e reaplica overrides de decisão? Decisão também vive no DTO... Para não perder overrides de decisão pendentes: reaplicar é desnecessário — GET groups retorna estado fresco do servidor (decisões já persistidas via PUT). Logo `refreshGroups` = listGroups fresco substituindo overrides (descartar mapa, estado canônico). Documentar no SUMMARY.
- Ficha D-18/D-19 (tela dedicada, NUNCA modal): rota `/project/${projectId}/result?resultId=<uuid>` (só resultId; grupo achado via `listGroups` + memberIds para decisão/tags — reaproveita padrão da 08-02). Tela `result.tsx`: busca FRESCA `getResult(resultId)` ao montar (mesmo que o card tenha os dados — prova do sob-demanda) + skeleton (CardSkeleton) + ErrorBanner com [repetir] (§11 verbatim); nunca lista, nunca prefetch. Corpo: todos os campos do ResultDTO (título, autores, ano ou 'ano desconhecido', tipo label, instituição/programa, resumo/abstract verbatim ou "sem resumo", originUrl/sourceUrl tocáveis https, proveniência formatProvenance, badge NOVO) + `<DecisionBar group/>` + `<TagInput/>` (decisão também ali, D-18) + grupo expansível se originCount>1 (reusar DedupGroupSection com cache local de 1). resultId inválido → ErrorBanner sem request; 401 → expired+next da ficha. Registrar `project/[id]/result` title 'Ficha' no Stack após results.
- Integração results.tsx: montar `<TagManagerModal/>` + botão `[Tags]` no header da lista (ao lado dos filtros) abrindo o modal; ResultCard ganha `<TagInput/>` (props group/projectTags/onGroupUpdated/onTagsChanged repassadas; projectTags descem da tela que já carrega para o filtro). Reusar `groupOverrides` da 08-03 para updates de tags/decisão/divergência (mesmo mapa, mesma regra).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: TagInput no card + testes</name>
  <files>apps/lab/src/results/TagInput.tsx, apps/lab/src/results/ResultCard.tsx, apps/lab/src/results/__tests__/tagInput.test.ts</files>
  <read_first>
    - apps/lab/src/api/lab.ts (attachTag/detachTag/createProjectTag/listProjectTags exatos da 08-02)
    - apps/lab/src/results/ResultCard.tsx (onde o TagInput será embutido, junto a DecisionBar da 08-03)
    - apps/lab/src/ui/ProjectModal.tsx (molde de validação local + erro verbatim sem fechar)
    - packages/contracts/src/lab.ts (tagNameSchema 1..100, DEFAULT_TAGS do servidor)
  </read_first>
  <action>Implementar D-20 + UI-20 (lado card):
  1. Criar apps/lab/src/results/TagInput.tsx: `TagInput({group, projectId, projectTags, getToken, onGroupUpdated})` — chips das tags atuais (`group.tags`, × destaca detach com busy por chip); input com sugestões filtradas por `suggestTags` (helper puro exportado: defaults do servidor primeiro na ordem canônica, depois alfabética, prefixo case-insensitive, limite 8); submit: match exato (case-insensitive) → `attachTag`; senão validar local (vazio/101+ chars → erro sem request, molde 07-06) → `createProjectTag` + `attachTag` da criada; tudo → `onGroupUpdated`. Erro ApiError → verbatim inline; group null → input disabled + "grupo indisponível". Zero `any`.
  2. Embutir no ResultCard abaixo da DecisionBar, repassando props. Preservar 08-02/08-03 intactos.
  3. Teste tagInput.test.ts: `suggestTags` (defaults primeiro, prefixo filtra, limite, case-insensitive) + helper `resolveTagAction(input, tags): {type:'attach',tagId}|{type:'create',name}|{type:'invalid'}` cobrindo match/exato/criar/vazio. Sem rede.
  </action>
  <acceptance_criteria>
    - `grep -c "attachTag\|detachTag\|createProjectTag" apps/lab/src/results/TagInput.tsx` >= 4 (associar + criar-na-hora + remover)
    - `grep -c "suggestTags\|resolveTagAction" apps/lab/src/results/TagInput.tsx apps/lab/src/results/__tests__/tagInput.test.ts` >= 5 (helpers puros + testes)
    - `grep -c "TagInput" apps/lab/src/results/ResultCard.tsx` >= 2 (import + uso)
    - `grep -rn ": any\|as any" apps/lab/src/results/TagInput.tsx apps/lab/src/results/ResultCard.tsx apps/lab/src/results/__tests__/tagInput.test.ts | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test -- src/results/__tests__/tagInput.test.ts 2>&1 | tail -n 8</automated>
  </verify>
  <done>Autocomplete com defaults + criar-inexistente + chips com × funcionam por grupo; helpers testados sem rede</done>
</task>

<task type="auto">
  <name>task 2: modal de gestão de tags + montagem na tela</name>
  <files>apps/lab/src/results/TagManagerModal.tsx, apps/lab/app/project/[id]/results.tsx</files>
  <read_first>
    - apps/lab/src/api/lab.ts (renameProjectTag/deleteProjectTag/createProjectTag exatos)
    - apps/lab/src/ui/ProjectModal.tsx (molde Modal transparent + validação + erro verbatim)
    - apps/lab/app/project/[id]/results.tsx (header dos filtros + groupOverrides + projectTags da 08-02/08-03)
    - apps/lab/src/ui/__tests__/scroll-containers.test.ts (tripwire a manter verde)
  </read_first>
  <action>Implementar D-21 + UI-20 (lado gestão):
  1. Criar apps/lab/src/results/TagManagerModal.tsx: `TagManagerModal({visible, projectId, getToken, onClose, onTagsChanged})` — FlatList (keyExtractor id) de linhas editáveis (TextInput nome + [Salvar] com no-op sem mudança e validação local sem request + [Excluir] two-tap → [Confirmar?]); rodapé criar (nome + cor opcional + [Criar]); rename 400 → verbatim na linha; qualquer mutação com sucesso → `onTagsChanged`. Sem seção fixa na tela (só modal). Zero `any`.
  2. Em results.tsx: botão `[Tags]` no header abrindo o modal; `refreshGroups()` (listGroups fresco até esgotar cursor, rebuild do índice, DESCARTAR groupOverrides — servidor é canônico pós-PUT) chamado em `onTagsChanged` do modal + refaz `listProjectTags` do filtro. Documentar a regra canônica em comentário no arquivo.
  </action>
  <acceptance_criteria>
    - `grep -c "renameProjectTag\|deleteProjectTag\|createProjectTag" apps/lab/src/results/TagManagerModal.tsx` >= 3 (criar/renomear/excluir D-21)
    - `grep -c "FlatList" apps/lab/src/results/TagManagerModal.tsx` >= 1 (tripwire: sem .map de lista)
    - `grep -c "TagManagerModal\|refreshGroups" "apps/lab/app/project/[id]/results.tsx"` >= 4 (botão + montagem + refresh canônico)
    - `grep -rn ": any\|as any" apps/lab/src/results/TagManagerModal.tsx "apps/lab/app/project/[id]/results.tsx" | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit -p tsconfig.json && pnpm --filter @uhhu/lab exec eslint apps/lab/src/results/TagInput.tsx apps/lab/src/results/TagManagerModal.tsx apps/lab/src/results/ResultCard.tsx "apps/lab/app/project/[id]/results.tsx" && pnpm --filter @uhhu/lab run test -- src/results src/ui 2>&1 | tail -n 6</automated>
  </verify>
  <done>Modal cria/renomeia/exclui sem sair da triagem; tela recarrega estado canônico; tripwire verde</done>
</task>

<task type="auto">
  <name>task 3: ficha dedicada sob demanda</name>
  <files>apps/lab/app/project/[id]/result.tsx, apps/lab/app/_layout.tsx</files>
  <read_first>
    - apps/lab/src/api/lab.ts (getResult + listGroups para achar o grupo por memberId)
    - apps/lab/src/results/DecisionBar.tsx (reuso da decisão na ficha, D-18)
    - apps/lab/src/results/TagInput.tsx (reuso das tags na ficha)
    - apps/lab/src/results/DedupGroupSection.tsx (reuso do expansível quando originCount>1)
    - apps/lab/app/project/[id]/run.tsx (molde uuid-inválido-sem-request + 401 expired/next)
    - apps/lab/app/_layout.tsx (registro Stack)
  </read_first>
  <action>Implementar D-18/D-19 + UI-22:
  1. Criar apps/lab/app/project/[id]/result.tsx: params `resultId` (+projectId do `[id]`); uuid inválido → ErrorBanner "Ficha inválida" + [Voltar], sem request; válido → busca fresca `getResult(resultId)` (sempre, mesmo vindo do card) + `listGroups` para o grupo (memberIds) com overrides locais (decisão/tags/divergência atualizam sem refetch, mesmo mapa da 08-03); skeleton até resolver; erro → ErrorBanner verbatim + [repetir]; corpo completo (todos os campos + proveniência + NOVO + DecisionBar + TagInput + DedupGroupSection quando grupo multi-origem); links só https; 401 → expired+next da ficha. ScrollView raiz. Zero `any`.
  2. Registrar `project/[id]/result` title 'Ficha' no Stack após results. Confirmar o link "ver ficha →" da 08-02 aponta para esta rota (ajustar ResultCard se divergir de `/project/${projectId}/result?resultId=${id}`).
  </action>
  <acceptance_criteria>
    - `grep -c "getResult" "apps/lab/app/project/[id]/result.tsx"` >= 1 (busca fresca obrigatória, nunca props da lista)
    - `grep -c "Skeleton\|skeleton" "apps/lab/app/project/[id]/result.tsx"` >= 1 (D-19: nunca abre vazia sem estado)
    - `grep -c "DecisionBar\|TagInput" "apps/lab/app/project/[id]/result.tsx"` >= 4 (decisão também na ficha, D-18)
    - `grep -c "project/\[id\]/result" apps/lab/app/_layout.tsx apps/lab/src/results/ResultCard.tsx` >= 2 (registro + link)
    - `grep -rn ": any\|as any" "apps/lab/app/project/[id]/result.tsx" | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit -p tsconfig.json && pnpm --filter @uhhu/lab exec eslint "apps/lab/app/project/[id]/result.tsx" apps/lab/app/_layout.tsx && pnpm --filter @uhhu/lab run test 2>&1 | tail -n 6</automated>
  </verify>
  <done>Ficha abre sob demanda com skeleton/retry, decisão+tags ali, sem pré-carregar a lista; suite toda verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resultId na URL → getResult | Hostil: inválido → banner sem request; alheio → 404 genérico |
| nome/cor de tag → servidor | Zod 1..100 / ≤20; colisão → 400; cor é texto livre opaco, nunca estilo executável |
| nota de divergência → render | Mesma regra 08-03 (bloqueio client + strip server + Text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | ficha de item alheio | mitigate | getResult owner-scoped; 404 idêntico; 401 → expired+next com destino interno validado |
| T-08-04-02 | Tampering | rename colide / exclui default | mitigate | 400 verbatim na linha; delete two-tap; default excluído não ressuscita (08-01 seed-if-empty) |
| T-08-04-03 | Tampering | override local diverge do servidor | mitigate | refreshGroups canônico após gestão; PUTs atualizam o mapa só no sucesso |
| T-08-04-04 | Cross-site Scripting | nome/tag/nota com HTML | mitigate | Text escapa; sem WebView; teste manual `<img>` vira texto no chip e na ficha |
| T-08-04-05 | Denial of Service | autocomplete sem teto | mitigate | sugestões cap 8, filtro local sobre lista já carregada; criar exige submit explícito |
</threat_model>

<verification>
- typecheck + lint + suite completa do app verdes, zero `any`, tripwire verde
- Fluxo manual tablet: digitar prefixo sugere defaults; inexistente cria e associa; modal cria/renomeia/exclui e reflete nos cards+filtros; ficha abre do card com skeleton, decide e tagueia ali, retry funciona com rede cortada
- Auditoria adversarial do app da fase (autorização decorativa, IDOR, XSS, segredos no bundle via dist grep 0) antes do done
</verification>

<success_criteria>
- UI-20: autocomplete com defaults + criar-na-hora + gestão em modal sem sair da triagem
- UI-22: ficha completa sob demanda em tela dedicada com skeleton + erro com repetir, decisão ali também
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-04-SUMMARY.md`
</output>
