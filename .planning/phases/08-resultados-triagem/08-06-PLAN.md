---
phase: 08-resultados-triagem
plan: "06"
type: execute
wave: 6
depends_on:
  - 08-01
  - 08-03
gap_closure: false
gap_source: null
product_decision: Paulo 12/09/2026 — run retorna a busca COMPLETA, 100%, sem teto; rate limit é proteção, não truncamento
files_modified:
  - apps/core-api/src/lib/searchRuns.ts
  - packages/contracts/src/lab.ts
  - apps/lab/src/search/useRunPolling.ts
  - apps/lab/app/project/[id]/run.tsx
  - tests/integration/lab-search-runs-full.test.ts
  - apps/lab/src/search/__tests__/runPolling.test.ts
autonomous: true
requirements:
  - UI-14
must_haves:
  truths:
    - Run com fonte de 120 itens armazena os 120 (3 páginas de 50) com rank global contínuo, sem cap fixo
    - Run longo não é abortado pelo timeout antigo de 60s; cancelamento no meio continua funcionando e preserva o coletado (parcial)
    - UI acompanha runs longos sem falso "falha/timeout", mostra progresso de páginas e permite cancelar
  artifacts:
    - path: apps/core-api/src/lib/searchRuns.ts
      provides: Loop de páginas até o total da fonte
      contains: pagesFetched
    - path: tests/integration/lab-search-runs-full.test.ts
      provides: Prova de busca completa multi-página
      contains: 120
  key_links:
    - from: apps/lab/src/search/useRunPolling.ts
      to: apps/core-api/src/lib/searchRuns.ts
      via: teto de polling cobre o teto do run; progresso de páginas flui via métricas
      pattern: RUN_POLL_MAX_POLLS
---

<objective>
Busca completa por run: loop de páginas até o total da fonte, timeout generoso, UI acompanha sem falso timeout.

Purpose: Hoje o run trunca em 20/fonte; revisão sistemática precisa de tudo (decisão Paulo 12/09).
Output: Engine pagina tudo, UI mostra progresso e cancela, testes provam 120/120.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/08-security-baseline.md
@apps/core-api/src/lib/searchRuns.ts
@packages/contracts/src/lab.ts
@apps/lab/src/search/useRunPolling.ts
@apps/lab/app/project/[id]/run.tsx

<interfaces>
Estado atual: `RUN_PER_PAGE=20` + `page:1` fixo (searchRuns.ts:380); `RUN_QUEUE_TIMEOUT_MS=60000` com abort global; rank=index por página; polling app 240×2500ms (≈10min) → pollState 'timeout'. Rate limit SourceClient: 10 chamadas/fonte + 2s/batch (proteção anti-ban, intocado). perPage máx das duas fontes = 50 (PER_PAGE_MAX). BDTD sem cap de profundidade; CAPES batch no payload.
Regras do gap:
1. Loop por fonte: `perPage: 50`, páginas até `coletados >= page.total` OU página vazia (qualquer condição de parada primeiro). Rank global contínuo: `rank = (page-1)*50 + i`. Insert por página (memória limitada) com o mesmo `onConflictDoNothing`.
2. Falha de página no meio do loop (após página 1 ok): preserva o coletado, marca fonte failed → run parcial (D-37), nunca apaga. Retry de challenge (D-38) mantido na página 1; demais páginas tentativa única.
3. Checagem de cancelamento (`readRunStatus`) e `controller.signal.aborted` A CADA página (não só por fonte) — run longo cancelável de verdade.
4. Timeout: `RUN_QUEUE_TIMEOUT_MS` sobe para 30min (`30*60_000`, constante nomeada + comentário do trade-off: run longo segura worker/DB; Paulo aceitou o custo; sync race 25s/202 inalterado). Sem timeout dinâmico nesta versão (simplicidade auditável).
5. Progresso: campos ADITIVOS opcionais em PerSourceMetrics (`pagesFetched?: number`, `pagesTotal?: number | null`, contrato §19 permite aditivo) preenchidos por página; UI usa se presentes.
6. UI: `RUN_POLL_MAX_POLLS` sobe para cobrir 30min (720 polls × 2.5s) sem falso timeout; tela de execução mostra "buscando página X de ~Y" quando métricas trazem; Cancelar sempre visível; D-08 (sair/voltar sem cancelar no servidor) preservado.
7. Rate limit do SourceClient intocado. Idempotência 24h e 10 runs/h intocados.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: loop de páginas no engine + timeout 30min + progresso</name>
  <files>apps/core-api/src/lib/searchRuns.ts, packages/contracts/src/lab.ts</files>
  <read_first>
    - apps/core-api/src/lib/searchRuns.ts linhas 340-451 (loop por fonte, attemptOnce, insert, métricas) + 76 (timeout) + 88-89 (RUN_PER_PAGE)
    - packages/contracts/src/lab.ts PerSourceMetrics (shape a estender com opcionais)
    - packages/integrations SourcePage (o que `total` significa por adapter; página vazia como parada)
  </read_first>
  <action>
    1. Trocar `RUN_PER_PAGE=20` por `RUN_FETCH_PER_PAGE=50` + loop `page=1,2,…`: para quando `coletados >= (page.total ?? coletados)` OU `page.items.length === 0` (página vazia = parada, protege contra total mentiroso/infinito) OU abort/cancel. Trava de segurança adicional: parar se uma página retornar itens já vistos (sourceIds repetidos) — anti-loop infinito.
    2. Rank global: `rank = (page-1)*50 + indexNaPagina` no insert por página (mesmo onConflictDoNothing).
    3. `RUN_QUEUE_TIMEOUT_MS = 30*60_000` com comentário do trade-off (worker/DB segurados; decisão Paulo 12/09; sync 25s inalterado).
    4. `pagesFetched`/`pagesTotal` opcionais em PerSourceMetrics (aditivo §19, sem migration — métricas são JSON), atualizados por página; `total`/`returned` passam a refletir a soma de TODAS as páginas (returned=kept total, não só página 1).
    5. Falha mid-loop → fonte failed + coletado preservado (parcial). Challenge retry só página 1. Cancel/abort checados por página.
    6. Typecheck + eslint dos pacotes tocados exit 0; zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "page: 1, perPage: RUN_PER_PAGE" apps/core-api/src/lib/searchRuns.ts` == 0 (página única eliminada)
    - `grep -c "RUN_FETCH_PER_PAGE = 50\|perPage: 50" apps/core-api/src/lib/searchRuns.ts` >= 1
    - `grep -c "RUN_QUEUE_TIMEOUT_MS = 30" apps/core-api/src/lib/searchRuns.ts` >= 1
    - `grep -c "pagesFetched" packages/contracts/src/lab.ts apps/core-api/src/lib/searchRuns.ts` >= 2
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/contracts exec tsc --noEmit && pnpm --filter @uhhu/core-api exec tsc --noEmit 2>/dev/null || pnpm --filter core-api exec tsc --noEmit</automated>
  </verify>
  <done>Engine busca todas as páginas até o total, com rank contínuo, timeout 30min e progresso por página</done>
</task>

<task type="auto">
  <name>task 2: teste multi-página (120/120) + rerun + timeout</name>
  <files>tests/integration/lab-search-runs-full.test.ts</files>
  <read_first>
    - tests/integration/lab-results-isnew.test.ts (padrão: bootstrapAdmin, apiRequest, seed, offline-skip, `const api = app` gap 08-05)
    - adapters com fetchFn injetável (como simular páginas: fetchFn que retorna page.total=120 e fatias de 50/50/20)
  </read_first>
  <action>
    1. Criar tests/integration/lab-search-runs-full.test.ts (offline-skip como os demais): fonte fake via `fetchFn` (ou stub de adapter pelo padrão do repo) com total 120 em 3 páginas (50+50+20) → executar run → assert 120 resultados armazenados, rank 0..119 contínuo e único, `returned===120`, `pagesFetched===3`.
    2. Rerun na mesma search → `newCount===0` (nada novo; D-35/D-15 com busca completa).
    3. Timeout não dispara: run com páginas rápidas completa bem abaixo do teto (prova que o loop não é abortado no meio); + caso abort simulado mid-loop (se viável via fetchFn que trava) → parcial com coletado preservado — se inviável sem flakiness, documentar o skip com motivo no SUMMARY em vez de teste flaky.
    4. Zero `any`; typecheck raiz cobre o arquivo novo (lição 08-05: sem closures com `let` não-estreitado — usar `const api = app`).
  </action>
  <acceptance_criteria>
    - `test -f tests/integration/lab-search-runs-full.test.ts && grep -c "120" tests/integration/lab-search-runs-full.test.ts` >= 2
    - `pnpm exec tsc --noEmit -p tsconfig.json` exit 0 (raiz cobre o teste novo)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && echo ROOT_TYPECHECK_OK</automated>
  </verify>
  <done>Busca completa provada: 120/120 armazenados, rank contínuo, rerun newCount 0</done>
</task>

<task type="auto">
  <name>task 3: UI acompanha runs longos (polling + progresso + cancela)</name>
  <files>apps/lab/src/search/useRunPolling.ts, apps/lab/app/project/[id]/run.tsx, apps/lab/src/search/__tests__/runPolling.test.ts</files>
  <read_first>
    - apps/lab/src/search/useRunPolling.ts (RUN_POLL_MAX_POLLS=240, pollState 'timeout', cleanup D-08)
    - apps/lab/app/project/[id]/run.tsx (banners + botão cancelar atuais)
    - SearchRunDTO/métricas (onde pagesFetched/pagesTotal chegam)
  </read_first>
  <action>
    1. `RUN_POLL_MAX_POLLS`: 240 → 720 (30min ÷ 2.5s) com comentário (cobre o teto do servidor; timeout de acompanhamento continua existindo, só maior). Sem backoff (fora do escopo).
    2. Tela run: quando métricas trazem `pagesFetched/pagesTotal`, mostrar "Buscando página X de ~Y (fonte)" junto ao progresso por fonte; sem métricas, comportamento atual. Cancelar sempre visível e funcional; D-08 preservado (sair não cancela no servidor; cleanup só timer).
    3. Testes: teto novo (polling sobrevive além de 240 polls simulados sem 'timeout' falso); progresso renderiza com métricas e some sem elas; zero `any`.
    4. Typecheck + lint + test do app exit 0.
  </action>
  <acceptance_criteria>
    - `grep -c "RUN_POLL_MAX_POLLS = 720" apps/lab/src/search/useRunPolling.ts` >= 1
    - `grep -c "pagesFetched" apps/lab/app/project/\[id\]/run.tsx apps/lab/src/search/useRunPolling.ts` >= 1
    - `pnpm --filter @uhhu/lab run typecheck && pnpm --filter @uhhu/lab run lint && pnpm --filter @uhhu/lab run test` todos exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test 2>&1 | tail -n 6</automated>
  </verify>
  <done>UI acompanha 30min sem falso timeout, mostra página X de ~Y, cancela sempre</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loop → fontes | rate limit SourceClient intocado (anti-ban); perPage 50 = máx honrado pelos adapters |
| run 30min → worker/DB | trade-off aceito por Paulo; cancelamento preserva parcial; sem retry automático |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Denial of Service | run gigante segura worker | mitigate | teto 30min aborta; cancel manual; 10 runs/h por usuário; trava anti-loop (página vazia/repetida para) |
| T-08-06-02 | Tampering | total mentiroso da fonte (loop infinito) | mitigate | parada em página vazia + sourceIds repetidos + teto global; nunca laço só por total |
| T-08-06-03 | Information Disclosure | progresso expõe algo novo | mitigate | só contagens (página/total), já públicas no run do dono |
</threat_model>

<verification>
- 120/120 armazenados com rank 0..119 + rerun newCount 0 + timeout 30min
- UI: 720 polls sem falso timeout, progresso de páginas, cancela funcional
- typecheck raiz + pacotes + app exit 0; zero `any`; auditoria do diff
</verification>

<success_criteria>
- UI-14: run retorna busca completa (decisão Paulo 12/09)
- Sem regressão: parcial/cancel/idempotência/rate-limit intactos
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-06-SUMMARY.md`
</output>
