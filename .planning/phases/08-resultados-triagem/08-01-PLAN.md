---
phase: 08-resultados-triagem
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/lab.ts
  - apps/core-api/src/lib/searches.ts
  - apps/core-api/src/lib/corpus.ts
  - apps/core-api/src/capabilities.ts
  - apps/core-api/src/routes/lab.ts
  - tests/integration/lab-results-isnew.test.ts
  - tests/integration/lab-groups-triage.test.ts
autonomous: true
requirements:
  - UI-18
  - UI-19
  - UI-20
  - UI-21
must_haves:
  truths:
    - badge NOVO de um run antigo nunca apaga por causa de run posterior; newCount congelado sempre igual à contagem de isNew true
    - GET groups do projeto expõe por grupo as tags, as divergências e quando foi decidido
    - Dono renomeia e exclui tags do projeto; tag padrão excluída não ressuscita na próxima listagem
  artifacts:
    - path: apps/core-api/src/lib/searches.ts
      provides: isNew só-anteriores (D-15)
      contains: seenKeysForSearch
    - path: packages/contracts/src/lab.ts
      provides: DedupGroupDTO com tags/divergences/decidedAt + updateTagSchema
      contains: decidedAt
    - path: apps/core-api/src/routes/lab.ts
      provides: PATCH/DELETE de tag por projeto
      contains: /tags/:tagId
  key_links:
    - from: apps/core-api/src/lib/searches.ts
      to: labSearchRuns.executedAt
      via: anti-join filtra só runs anteriores ao run corrente
      pattern: executedAt
    - from: apps/core-api/src/lib/corpus.ts
      to: DedupGroupDTO.tags
      via: join labGroupTags→labTags agregado por grupo
      pattern: labGroupTags
    - from: apps/core-api/src/routes/lab.ts
      to: lab.tag.rename
      via: PATCH com 404 idêntico fora do escopo
      pattern: lab\.tag\.rename
---

<objective>
Fix CORE H-01 (D-15: isNew só-anteriores) + expor triagem no contrato (tags/divergências/decidedAt por grupo, renomear/excluir tags) — base de servidor da fase 8 (D-15, UI-18/19/20/21).

Purpose: Sem isto a UI mente (badge NOVO apaga com o tempo, diverge do newCount) e não tem de onde ler tags/divergências por grupo nem gerenciar tags (renomear/excluir não existem no servidor). Este plano destrava 08-02/08-03/08-04.
Output: Contrato estendido (definição única), libs corrigidas, 2 endpoints novos, testes PG reais de 3 runs + triagem.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/03-lab-spec-v1.md
@dev-docs/07-core-contract.md
@dev-docs/08-security-baseline.md
@packages/contracts/src/lab.ts
@packages/contracts/src/errors.ts
@apps/core-api/src/lib/searches.ts
@apps/core-api/src/lib/corpus.ts

<interfaces>
Correção D-15 (H-01, decisão Paulo 12/09 — "só anteriores", texto D-35):
- Hoje `seenKeysForSearch(db, searchId, excludeRunId)` em apps/core-api/src/lib/searches.ts:61 faz anti-join contra TODOS os outros runs (`ne(labSearchRuns.id, excludeRunId)`) — run posterior apaga NOVO de itens antigos enquanto `metrics.newCount` (congelado na execução, só via runs anteriores) os conta. Fix: excluir também runs com `executedAt >= executedAt do run corrente` (coluna `labSearchRuns.executedAt`, mesma usada no `orderBy(desc(executedAt))` de corpus.ts:375). Empate exato de ms → trata como posterior (não-visto = isNew true; aceitável e documentado no teste).
- `searchRuns.ts:456-477` (newCount congelado) NÃO muda: no momento da execução runs futuros não existem, logo já é só-anteriores por construção.
- Divergência CONTEXT vs realidade (honrar intenção D-46, usar paths reais): 08-CONTEXT.md cita `PATCH /results/:id/decision` e `POST .../duplicate-divergence`, mas o servidor real expõe `PUT /api/v1/lab/groups/:groupId/decision` (body `decisionInputSchema`: decision eligible|ineligible|undecided, reason ≤500) e `PUT /api/v1/lab/groups/:groupId/divergence` (body `divergenceInputSchema`: source bdtd|capes, note 1..1000 sem HTML). Decisão é UMA por grupo (D-46). Usar os paths reais; a UI (08-03) endereça groupId, nunca resultId.

Contrato a estender (definição única, aditivo — sem version bump, §19):
- `DedupGroupDTO` += `tags: string[]` (nomes, ordenados) + `divergences: Array<{ source: ExecutableSource; note: string }>` + `decidedAt: string | null` (ISO de `labGroupDecisions.updatedAt`, null = nunca decidido = "não triado" do filtro D-17; distingue indeciso-explícito de não-triado). UI-19 exige "decidido_em atualizado" — é este campo.
- Novo `updateTagSchema = z.object({ name: tagNameSchema.optional(), color: z.string().trim().max(20).nullable().optional() }).refine(o => o.name !== undefined || o.color !== undefined, { message: 'Nada para atualizar.' })` + `UpdateTagInput`. Reuso de `tagNameSchema` (1..100) — nunca schema novo de nome.
- Catálogo de erros NÃO muda (sem TAG_EXISTS): colisão de nome no rename → `400 VALIDATION_ERROR` com details `{ name: 'Já existe uma tag com este nome.' }` via `buildEnvelope` (mensagem do catálogo, detalhe livre como `parsed.error.flatten()`).

Endpoints novos (molde idêntico às rotas de tag existentes: requireAuth + resolveRequestId + header x-request-id + 404 idêntico):
- `PATCH /api/v1/lab/projects/:projectId/tags/:tagId` → 200 ProjectTag; `DELETE` mesma path → 204. Capabilities `lab.tag.rename` (input {projectId, tagId, input: UpdateTagInput}) e `lab.tag.delete` (input {projectId, tagId}) em EXTENDED_CAPABILITY_NAMES + handlers com parseOrThrow.
- `ensureDefaultTags` vira seed-if-empty (conta tags do projeto; só insere DEFAULT_TAGS `['incluir','excluir','duplicado','indisponível','revisar']` quando zero): sem isto a tag padrão excluída ressuscita a cada `GET .../tags` (a rota dá ensure antes de list). Primeira listagem continua semeando.
- FKs já dão cascade em `labGroupTags` (onDelete cascade nos dois lados — verificado no schema); delete de tag não precisa limpar joins manualmente.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: isNew só-anteriores + teste de 3 runs</name>
  <files>apps/core-api/src/lib/searches.ts, tests/integration/lab-results-isnew.test.ts</files>
  <read_first>
    - apps/core-api/src/lib/searches.ts (seenKeysForSearch + listResultsForActor + getResultForActor atuais)
    - apps/core-api/src/lib/searchRuns.ts (linhas 453-477: semântica D-35 do newCount congelado — NÃO alterar)
    - tests/integration/lab-results-isnew.test.ts (seedTwoRuns + asserts badge≡contador + IDOR a preservar)
    - packages/db/src/schema.ts (nome exato da coluna temporal de labSearchRuns: executedAt)
  </read_first>
  <action>Implementar D-15 (H-01):
  1. Em `seenKeysForSearch`: buscar o run corrente (já disponível nos callers: `run.searchId` em listResultsForActor, `row.searchId/row.runId` em getResultForActor) e restringir o anti-join a runs da mesma search com `executedAt < executedAt do run corrente` (WHERE = searchId AND id != excludeRunId AND executedAt < currentExecutedAt). Sem coluna nova, sem migration.
  2. Estender o teste para 3 runs: `seedThreeRuns` (run1: A; run2: A+B; run3: A+C, inserts diretos como hoje, metrics.newCount 1/1/1): asserts run1 A.isNew true; run2 A false + B true + count(isNew)===newCount===1 MESMO DEPOIS do run3 existir (prova do histórico congelado — o caso que quebrava); run3 A false + C true; GET unitário espelha; IDOR estranho/adulterado → 404 preservado. Manter skipIf offline + nunca imprimir connection string + `unknown`+narrowing, zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "executedAt" apps/core-api/src/lib/searches.ts` >= 2 (filtro no anti-join + comentário D-15)
    - `grep -c "seedThreeRuns\|run3" tests/integration/lab-results-isnew.test.ts` >= 4 (seed + asserts do run posterior ignorado)
    - `grep -c "count(isNew" tests/integration/lab-results-isnew.test.ts` >= 1 (badge ≡ contador asserido no run2 pós-run3)
    - `grep -rn ": any\|as any" apps/core-api/src/lib/searches.ts tests/integration/lab-results-isnew.test.ts | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/integration/lab-results-isnew.test.ts 2>&1 | tail -n 10</automated>
  </verify>
  <done>Run antigo mantém badge após runs posteriores; newCount === count(isNew) nos 3 runs; IDOR 404 intacto</done>
</task>

<task type="auto">
  <name>task 2: grupo expõe tags/divergências/decidedAt + rename/delete de tags</name>
  <files>packages/contracts/src/lab.ts, apps/core-api/src/lib/corpus.ts, apps/core-api/src/capabilities.ts, apps/core-api/src/routes/lab.ts, tests/integration/lab-groups-triage.test.ts</files>
  <read_first>
    - packages/contracts/src/lab.ts (DedupGroupDTO + decisionInputSchema/divergenceInputSchema/tagNameSchema/createTagSchema a reusar)
    - apps/core-api/src/lib/corpus.ts (loadGroupBundles + toGroupDTO + ensureDefaultTags + attachTagForActor como molde owner-first)
    - apps/core-api/src/capabilities.ts (EXTENDED_CAPABILITY_NAMES + extendedHandlers + parseOrThrow)
    - apps/core-api/src/routes/lab.ts (rotas GET/POST .../tags e PUT .../decision como molde de params/body/404)
    - packages/contracts/src/errors.ts (buildEnvelope só com códigos do catálogo — sem mensagem inline)
  </read_first>
  <action>Expor triagem no servidor (UI-18/19/20):
  1. contracts/lab.ts: `DedupGroupDTO` += `tags: string[]; divergences: Array<{ source: ExecutableSource; note: string }>; decidedAt: string | null;` + `updateTagSchema`/`UpdateTagInput` (conforme interfaces). `decidedAt` = ISO de `labGroupDecisions.updatedAt`, null sem linha.
  2. corpus.ts: `GroupBundle` += `{ tagNames: string[]; divergences: Array<{source,note}>; decidedAt: string | null }`; `loadGroupBundles` carrega os 3 (join labGroupTags→labTags ordenado, labDivergences, updatedAt da decisão); `toGroupDTO` inclui; `ensureDefaultTags` só semeia com tabela vazia; novas `renameTagForActor` (JOIN tag→project owner; colisão de nome com outra tag → erro tipado `TagNameConflictError` mapeado na rota para 400 VALIDATION_ERROR + details `{ name: 'Já existe uma tag com este nome.' }`; cor null limpa) e `deleteTagForActor` (owner check; delete simples — joins caem por cascade). `ownerId` sempre de `actor.userId`, nunca do body.
  3. capabilities.ts: registrar `lab.tag.rename` + `lab.tag.delete` (schemas zod inline no estilo tagCreateInputSchema/groupTagInputSchema).
  4. routes/lab.ts: `PATCH` + `DELETE /api/v1/lab/projects/:projectId/tags/:tagId` no molde (params projectId+tagId uuid, body updateTagSchema no PATCH; 404 inválido/estrangeiro idêntico; PATCH colisão → 400 VALIDATION_ERROR envelope; DELETE → 204).
  5. Criar tests/integration/lab-groups-triage.test.ts (molde lab-results-isnew: bootstrapAdmin, skipIf offline, sem segredos em log): seed mínimo via HTTP (project→search→2 runs diretos + attach via D-43? usar `lab.group.list` via GET groups após inserts de results nos últimos runs) assertando: GET groups traz `tags: []`, `divergences: []`, `decidedAt: null`; PUT decision eligible → decidedAt ISO não-nulo + tags/divergences intactos; attach tag → aparece em `tags`; PUT divergence → aparece em `divergences`; rename ok + colisão 400 + delete 204 + default excluído NÃO volta no GET seguinte; estranho/adulterado → 404 em todos (groups, decision, tags, rename, delete, divergence).
  </action>
  <acceptance_criteria>
    - `grep -c "decidedAt" packages/contracts/src/lab.ts apps/core-api/src/lib/corpus.ts` >= 4 (tipo + bundle + mapper + loader)
    - `grep -c "lab.tag.rename\|lab.tag.delete" apps/core-api/src/capabilities.ts apps/core-api/src/routes/lab.ts` >= 4 (registro + handlers + rotas)
    - `grep -c "onConflictDoNothing\|count\|length === 0" apps/core-api/src/lib/corpus.ts` >= 2 (seed-if-empty do ensure)
    - `grep -c "TAG_EXISTS\|'Tag já existe'\|Já existe uma tag" apps/core-api/src/routes/lab.ts packages/contracts/src/errors.ts` == 1 (só no details da rota; catálogo intocado)
    - `grep -rn ": any\|as any" packages/contracts/src/lab.ts apps/core-api/src/lib/corpus.ts apps/core-api/src/routes/lab.ts apps/core-api/src/capabilities.ts tests/integration/lab-groups-triage.test.ts | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/integration/lab-groups-triage.test.ts tests/integration/lab-corpus.test.ts 2>&1 | tail -n 10</automated>
  </verify>
  <done>Groups expõem tags/divergências/decidedAt; rename/delete funcionam owner-scoped com 404/400 corretos; corpus existente sem regressão</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runId/resultId/groupId/tagId → lib | IDs são hostis: uuid inválido ou de outro dono → null → 404 idêntico, sem oráculo |
| body de rename/divergence/decision → Zod | Validação estrita (tamanhos, enum, sem HTML em note); colisão vira 400 catalogado |
| executedAt → anti-join | Coluna interna do servidor; cliente nunca influencia o cálculo do isNew |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | IDOR em groups/decision/tags/divergence/rename/delete | mitigate | scopedGroup + JOIN owner em rename/delete; testes estranho+adulterado → 404 em todas as rotas novas e alteradas |
| T-08-01-02 | Tampering | rename colide / note com HTML / reason gigante | mitigate | unique (projectId,name) + 400 catalogado; divergence stripa `<[^>]*>` no lib + Zod 1..1000; reason ≤500 no schema |
| T-08-01-03 | Tampering | cliente forja isNew/decidedAt | mitigate | ambos derivados server-side (anti-join por executedAt; updatedAt da decisão); nunca vêm do body; sem coluna cliente-escritível |
| T-08-01-04 | Denial of Service | list groups sem limite / cursor malformado | mitigate | reaproveitar paginação existente (limit clamp 1..100, cursor tolerante); sem rota nova de listagem |
</threat_model>

<verification>
- `pnpm --filter @uhhu/contracts exec tsc --noEmit -p tsconfig.json && pnpm --filter @uhhu/core-api exec tsc --noEmit -p tsconfig.json` verdes
- eslint nos 5 arquivos-fonte exit 0; zero `any` (grep gate acima)
- vitest isnew (3 runs) + groups-triage + lab-corpus verdes com PG DEV (APP_DATABASE_URL/MIGRATION_DATABASE_URL; skip gracioso offline reportado, nunca falha silenciosa)
- curl manual: dono vê tags/decidedAt; estranho recebe 404 idêntico em groups/decision/tags/divergence; rename colidido dá 400 VALIDATION_ERROR
- Auditoria adversarial do diff (IDOR, input hostil, segredos) antes de declarar done; achados corrigidos com teste
</verification>

<success_criteria>
- UI-21: badge NOVO estável no tempo; newCount === count(isNew) provado com 3 runs
- UI-18/19/20 (lado servidor): grupo expõe tags, divergências e decidedAt; tags renomeiam/excluem owner-scoped
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-01-SUMMARY.md`
</output>
