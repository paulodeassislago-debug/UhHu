---
phase: 08-resultados-triagem
plan: "05"
type: execute
wave: 5
depends_on:
  - 08-01
gap_closure: true
gap_source: Auditoria externa Hermes 12/09/2026 — typecheck RAIZ vermelho (TS2345 ×2 em lab-results-isnew.test.ts, closures fetchPage/fetchSingle); bloqueia CI (gates.yml:40)
files_modified:
  - tests/integration/lab-results-isnew.test.ts
  - .planning/phases/08-resultados-triagem/08-VERIFICATION.md
autonomous: true
requirements:
  - UI-18
  - UI-21
must_haves:
  truths:
    - `pnpm typecheck` na RAIZ retorna 0 erros (não só --filter)
    - Suíte monorepo 200/200 + vitest lab 82/82 continuam verdes
    - 12/12 must-haves da fase 8 revalidados com typecheck raiz no loop
  artifacts:
    - path: tests/integration/lab-results-isnew.test.ts
      provides: Teste D-15 sem erro de tipo em closures
      contains: const api = app
---

<objective>
Fechar o único achado da auditoria externa: TS2345 nas closures do teste isNew, que quebra o CI.

Purpose: Fase 8 não fecha com typecheck raiz vermelho.
Output: Fix mínimo (Opção A), gates verdes, VERIFICATION.md revalidado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/08-security-baseline.md
@tests/integration/lab-results-isnew.test.ts

<interfaces>
Diagnóstico: `let app: FastifyInstance | undefined` (linha 460) + guard no corpo do `it` (532-535) estreita só o corpo síncrono; nas closures `fetchPage` (558) e `fetchSingle` (603) o TS reavalia `| undefined` → TS2345 em 559 e 604. Introduzido em f13a482 (08-01). Demais `apiRequest(app, ...)` (537/544/625-657) estão no corpo síncrono e passam.
Fix: `const api = app;` logo após o guard + `api` nas 2 closures. Opções B (`app!`) e C (guard interno) rejeitadas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: fix const api nas 2 closures</name>
  <files>tests/integration/lab-results-isnew.test.ts</files>
  <read_first>
    - tests/integration/lab-results-isnew.test.ts linhas 531-536 (guard), 556-565 (fetchPage), 602-609 (fetchSingle)
  </read_first>
  <action>
    1. Após o guard do teste D-15 (linha 535), inserir `const api = app;` com comentário curto (narrowing preservado em const para closures).
    2. Trocar `app` por `api` em `apiRequest(...)` dentro de `fetchPage` (559) e `fetchSingle` (604). Não tocar nas demais ocorrências (corpo síncrono, já estreitadas).
  </action>
  <acceptance_criteria>
    - `grep -c "const api = app" tests/integration/lab-results-isnew.test.ts` == 1
    - `pnpm exec tsc --noEmit -p tsconfig.json` exit 0, output vazio
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && echo ROOT_TYPECHECK_OK</automated>
  </verify>
  <done>Typecheck raiz 0 erros</done>
</task>

<task type="auto">
  <name>task 2: suítes verdes + revalidação 12/12</name>
  <files>.planning/phases/08-resultados-triagem/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-resultados-triagem/08-VERIFICATION.md (12 must-haves + status atual)
  </read_first>
  <action>
    1. Rodar `pnpm test` (monorepo) e vitest do lab; registrar contagens. Integração isnew roda se PG disponível, senão offline-skip documentado (CI cobre via job integration-pg).
    2. Revalidar os 12/12 must-haves: mudança é test-only (2 linhas em closures de teste, sem runtime) — confirmar que nenhum must-have depende do código alterado além do próprio teste D-15, que continua idêntico em comportamento.
    3. Atualizar 08-VERIFICATION.md: registrar o gap, o fix, o typecheck raiz verde e a revalidação (manter status human_needed — UAT humano de tablet segue pendente).
  </action>
  <acceptance_criteria>
    - `pnpm test` exit 0 (200/200 ou documentado) e vitest lab exit 0 (82/82)
    - 08-VERIFICATION.md menciona gap 08-05 + typecheck raiz verde
  </acceptance_criteria>
  <verify>
    <automated>pnpm test 2>&1 | tail -n 5</automated>
  </verify>
  <done>Suítes verdes e 12/12 revalidados com typecheck raiz no loop</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste → servidor local | mudança test-only; nenhum runtime, rota ou auth alterados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | fix mascarar regressão | mitigate | comportamento do teste idêntico (só narrowing); suíte completa re-rodada |
</threat_model>

<verification>
- `pnpm typecheck` raiz 0 erros
- `pnpm test` + vitest lab verdes
- 08-VERIFICATION.md atualizado (gap + revalidação); status segue human_needed (UAT tablet pendente)
</verification>

<success_criteria>
- CI desbloqueado (gates.yml:40 passa)
- 12/12 must-haves revalidados
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-05-SUMMARY.md`
</output>
