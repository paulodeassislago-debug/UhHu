---
phase: 08-resultados-triagem
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/lab/src/api/lab.ts
  - apps/lab/src/results/triage.ts
  - apps/lab/src/results/useResultsList.ts
  - apps/lab/src/results/ResultCard.tsx
  - apps/lab/app/project/[id]/results.tsx
  - apps/lab/app/project/[id]/run.tsx
  - apps/lab/app/_layout.tsx
  - apps/lab/src/results/__tests__/triageFilter.test.ts
autonomous: true
requirements:
  - UI-17
  - UI-21
must_haves:
  truths:
    - User abre os resultados de um run concluído e lê cards com proveniência completa e badge NOVO
    - User rola 200+ itens sem travar e sem botão "carregar mais"; filtros combináveis refinam a lista
    - O run concluído desemboca na tela real (acaba o placeholder "lista de itens na fase 8")
  artifacts:
    - path: apps/lab/app/project/[id]/results.tsx
      provides: Tela de resultados com scroll infinito + filtros
      contains: FlatList|onEndReached
    - path: apps/lab/src/results/ResultCard.tsx
      provides: Card com proveniência + badge NOVO
      contains: NOVO|proveniência|isNew
    - path: apps/lab/src/api/lab.ts
      provides: Client de grupos/decisão/tags/divergência
      contains: listGroups|setGroupDecision
  key_links:
    - from: apps/lab/app/project/[id]/results.tsx
      to: labApi.listResults
      via: cursor limit+nextCursor com append
      pattern: nextCursor
    - from: apps/lab/src/results/useResultsList.ts
      to: labApi.listGroups
      via: join resultId→grupo por memberIds
      pattern: memberIds
    - from: apps/lab/app/project/[id]/run.tsx
      to: results route
      via: botão Ver resultados com runId+searchId
      pattern: /results\?runId=
---

<objective>
Tela de resultados com scroll infinito + filtros + cards com proveniência e NOVO, acessível do run concluído (D-16, D-17, UI-17, UI-21).

Purpose: É o coração visível da fase: sem lista real com proveniência e badge, não há triagem. Este plano cria o client de triagem (contrato das telas 08-03/08-04) e a lista base; decisão/grupo-expansível/tags/ficha vêm depois sobre este join.
Output: labApi estendido, hook useResultsList com join results↔groups, ResultCard base, tela results, run linkado, testes de filtro.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-resultados-triagem/08-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/08-security-baseline.md
@apps/lab/src/api/lab.ts
@apps/lab/src/search/SearchCard.tsx
@apps/lab/app/project/[id]/run.tsx

<interfaces>
Client a estender em apps/lab/src/api/lab.ts (paths/métodos REAIS do servidor — ver 08-01; o CONTEXT cita `PATCH results/:id/decision`, que NÃO existe: usar PUT em groups):
- `listGroups(projectId, {limit?, cursor?}, opts)` → GET `/api/v1/lab/projects/${projectId}/groups?limit=&cursor=` → `{ items: DedupGroupDTO[]; page: PageInfo }` (buscar com limit 100 e seguir `page.hasMore`+`nextCursor` até esgotar; volume v1 pequeno).
- `setGroupDecision(groupId, {decision, reason?}, opts)` → PUT `/api/v1/lab/groups/:id/decision` → DedupGroupDTO. decision: 'eligible'|'ineligible'|'undecided'.
- `listProjectTags(projectId, opts)` → GET `/api/v1/lab/projects/:id/tags` → `ProjectTag[] {id,name,color}` (servidor já garante defaults).
- `createProjectTag(projectId, {name, color?}, opts)` → POST mesma base → 201 ProjectTag. `renameProjectTag(projectId, tagId, {name?, color?}, opts)` → PATCH `.../tags/:tagId` → ProjectTag. `deleteProjectTag(projectId, tagId, opts)` → DELETE → void. `attachTag(groupId, tagId, opts)` → POST `/api/v1/lab/groups/:id/tags` body `{tagId}` → DedupGroupDTO. `detachTag(groupId, tagId, opts)` → DELETE `.../tags/:tagId` → void.
- `setDivergence(groupId, {source, note}, opts)` → PUT `/api/v1/lab/groups/:id/divergence` → DedupGroupDTO. source: 'bdtd'|'capes'.
- Tipos em definição única via `import type` de @uhhu/contracts (DedupGroupDTO, ResultDTO, ProjectTag→ verificar export; se ProjectTag não é exportado de contracts, definir em lab.ts a partir do shape `{id,name,color}` observado na rota — checar `apps/core-api/src/capabilities.ts` ProjectTag e espelhar sem `any`).
- Ficha (D-19) = `getResult(resultId)` fresco sob demanda — NÃO existe endpoint de enrich por adapter; adapters `enrichBdtd/enrichCapes` não têm rota. Nunca pré-carregar a lista.

Join results↔groups (contrato para 08-03/08-04 — implementar em apps/lab/src/results/triage.ts, puro e testado):
- `TriagedItem = { result: ResultDTO; group: DedupGroupDTO | null }`; `buildResultGroupIndex(groups): Map<string, DedupGroupDTO>` (para cada group, para cada memberId → group); `mergeResultsWithGroups(results, index): TriagedItem[]` (sem grupo → group null, card single degradado com decisão desabilitada e texto "grupo indisponível").
- `applyResultFilters(items, { decision: 'all'|'eligible'|'ineligible'|'undecided'|'untriaged', tag: string | null (nome), source: 'all'|'bdtd'|'capes', year: number | null }): TriagedItem[]` — pós-filtro client-side (D-17, sem nova rota): decision usa `group.decision` (null→'undecided'); `untriaged` = group null OU `group.decidedAt === null`; tag usa `group.tags.includes(tag)`; year usa `result.year === year` (year null nunca casa quando filtro setado); tudo combinável por AND.
- `formatProvenance(result): string` = `origem: ${source==='bdtd'?'BDTD':'CAPES'} · run ${result.runId.slice(0,8)} · ${DD/MM HH:MM de retrievedAt}` (helper de data no estilo formatRunWhen da 07-04). Labels docType: doctoralThesis→'tese', masterThesis→'dissertação' (molde SearchCard.summarizeFilters — importar se exportado, senão função local idêntica, sem duplicar mapa em 2 lugares: colocar em triage.ts e reusar).

Rota e navegação (expo-router, molde run.tsx/strategies):
- Nova tela `apps/lab/app/project/[id]/results.tsx`, rota `/project/${projectId}/results?runId=<uuid>&searchId=<uuid>`; params inválidos (runId fora de uuid) → ErrorBanner "Resultados inválidos" + [Voltar], SEM request (molde run.tsx T-07-03-01).
- Registrar em apps/lab/app/_layout.tsx após a linha do run: `<Stack.Screen name="project/[id]/results" options={{ title: 'Resultados' }} />`.
- run.tsx: trocar o bloco placeholder (linhas ~419-426) por: mantém `N NOVOS desde a última execução` + `<Button title="Ver resultados" onPress={() => router.push(`/project/${projectId}/results?runId=${run.id}&searchId=${searchId}`)} />` onde projectId vem de `useLocalSearchParams` id (rota /project/[id]/run). Só em terminal succeeded/partial (com resultados); failed/cancelled mantêm fluxos atuais.
- 401 em qualquer fetch → markExpired + replace `/login` com `expired=1&next=/project/${projectId}/results?runId=...` (molde SearchCard.handleUnauthorized).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: client de triagem + helpers puros de join/filtro</name>
  <files>apps/lab/src/api/lab.ts, apps/lab/src/results/triage.ts, apps/lab/src/results/__tests__/triageFilter.test.ts</files>
  <read_first>
    - apps/lab/src/api/lab.ts (toRequestOptions/withPaging/labApi a estender; bodies via parse dos schemas como createSearch faz)
    - packages/contracts/src/lab.ts (DedupGroupDTO pós-08-01: tags/divergences/decidedAt + decisionInputSchema/divergenceInputSchema/createTagSchema/updateTagSchema)
    - apps/core-api/src/routes/lab.ts (paths/métodos/status exatos das rotas de groups/decision/tags/divergence)
    - apps/lab/src/search/SearchCard.tsx (labels de fonte/docType e handleUnauthorized a reusar como molde)
  </read_first>
  <action>Implementar o contrato client da triagem (D-16/D-17/UI-17/UI-21 — base de 08-03/08-04):
  1. Em apps/lab/src/api/lab.ts: adicionar `listGroups`, `setGroupDecision`, `listProjectTags`, `createProjectTag` (body via `createTagSchema.parse`), `renameProjectTag` (body via `updateTagSchema.parse`), `deleteProjectTag` (→void), `attachTag` (body `{tagId}`), `detachTag` (→void), `setDivergence` (body via `divergenceInputSchema.parse`) exatamente como nas interfaces (paths, métodos, query limit/cursor). Zero `any`; erros via ApiError existente.
  2. Criar apps/lab/src/results/triage.ts: `TriagedItem`, `GroupFilter` (`{ decision, tag, source, year }` com os literais das interfaces), `buildResultGroupIndex`, `mergeResultsWithGroups`, `applyResultFilters` (AND combinável; untriaged = group null ou decidedAt null), `formatProvenance`, `docTypeLabel` (único mapa tese/dissertação), `formatResultWhen` (hoje HH:MM senão DD/MM HH:MM, fallback ISO). Sem fetch aqui (puro).
  3. Criar apps/lab/src/results/__tests__/triageFilter.test.ts: join por memberIds (2 grupos + órfão→null), filtro por cada dimensão isolada e combinada, untriaged distingue decidedAt null de undecided explícito, year setado exclui year null, formatProvenance contém BDTD/CAPES + run curto. Sem rede/timers/`any`.
  </action>
  <acceptance_criteria>
    - `grep -c "listGroups\|setGroupDecision\|listProjectTags\|createProjectTag\|renameProjectTag\|deleteProjectTag\|attachTag\|detachTag\|setDivergence" apps/lab/src/api/lab.ts` >= 18 (defs + usos)
    - `grep -c "PUT\|POST\|DELETE" apps/lab/src/api/lab.ts` >= 6 (métodos reais de escrita, nunca PATCH em results)
    - `grep -c "applyResultFilters\|mergeResultsWithGroups\|buildResultGroupIndex" apps/lab/src/results/triage.ts` >= 6
    - `grep -rn ": any\|as any" apps/lab/src/api/lab.ts apps/lab/src/results/triage.ts apps/lab/src/results/__tests__/triageFilter.test.ts | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test -- src/results/__tests__/triageFilter.test.ts 2>&1 | tail -n 8</automated>
  </verify>
  <done>Client de triagem completo contra rotas reais; join + filtros combináveis testados; contrato pronto para 08-03/08-04</done>
</task>

<task type="auto">
  <name>task 2: hook de lista infinita + tela results + card base + wiring do run</name>
  <files>apps/lab/src/results/useResultsList.ts, apps/lab/src/results/ResultCard.tsx, apps/lab/app/project/[id]/results.tsx, apps/lab/app/project/[id]/run.tsx, apps/lab/app/_layout.tsx</files>
  <read_first>
    - apps/lab/src/results/triage.ts (tipos/helpers da task 1 — usar, sem reimplementar)
    - apps/lab/src/api/lab.ts (listResults com isNew/newCount + listGroups da task 1)
    - apps/lab/app/project/[id]/run.tsx (bloco placeholder ~419-426 a substituir; params runId/searchId; projectId via [id])
    - apps/lab/app/_layout.tsx (registro do Stack a estender)
    - apps/lab/src/ui/Empty.tsx, apps/lab/src/ui/Skeleton.tsx (CardSkeleton), apps/lab/src/ui/ErrorBanner.tsx (estados §11 verbatim)
    - apps/lab/src/ui/__tests__/scroll-containers.test.ts (tripwire: o que conta como lista rolável)
  </read_first>
  <action>Implementar lista infinita + filtros + cards (D-16/D-17/UI-17/UI-21):
  1. Criar apps/lab/src/results/useResultsList.ts: `useResultsList({runId, projectId, getToken})` — fetch inicial `listResults(runId,{limit:30},{getToken})` + `listGroups(projectId,{limit:100})` seguindo cursor até `hasMore` false; estado items TriagedItem[] (merge via triage.ts), filtros GroupFilter (default tudo), itemsFiltrados via applyResultFilters; `onEndReached` → próxima página por `page.nextCursor` append (threshold 0.5, guarda `loadingMore`, sem botão carregar-mais, sem páginas numeradas); `refresh()` refaz tudo (pós-decisão futura); erro inicial → ErrorBanner + [tentar de novo]; vazio → Empty verbatim ("Nenhum resultado — ..."); 401 → markExpired+login/next. Barra de filtros: 4 controles (estado: todos/elegível/não/indeciso/não triado; tag: dropdown das tags do projeto via listProjectTags + todas; fonte: todas/BDTD/CAPES; ano: input numérico vazio=todos). Contador "N NOVOS desde a última execução" (newCount da 1ª página) + "mostrando X de Y".
  2. Criar apps/lab/src/results/ResultCard.tsx (BASE — 08-03/08-04 acoplam decisão/tags/divergência): `ResultCard({item, getToken, projectId})` — título (2 linhas), autores join + ano (ou 'ano desconhecido') + tipo label + instituição/programa quando presentes, badges de fonte ([BDTD]/[CAPES] ou `[BDTD+CAPES — N origens ▸]` quando group.originCount>1), badge `NOVO` quando `result.isNew`, linha de proveniência via formatProvenance, link "ver ficha →" (push `/project/${projectId}/result?resultId=${id}` — tela criada na 08-04; link pode existir antes, destino chega na 08-04). Sem decisão/tags aqui (slots vêm nas próximas plans).
  3. Criar apps/lab/app/project/[id]/results.tsx: ScrollView raiz + FlatList virtualizada (keyExtractor result.id, ListHeader filtros+contadores, ListFooter carregando-mais/fim, ListEmpty verbatim), skeleton inicial (CardSkeleton ×3), ErrorBanner com retry. Registrar Stack.Screen results + run.tsx com botão "Ver resultados" (interfaces). Zero `any`; Text escapa por padrão.
  </action>
  <acceptance_criteria>
    - `grep -c "onEndReached\|nextCursor\|hasMore" apps/lab/app/project/[id]/results.tsx apps/lab/src/results/useResultsList.ts` >= 6 (infinito por cursor, sem botão)
    - `grep -c "carregar mais\|Carregar mais\|loadMore.*Button" apps/lab/app/project/[id]/results.tsx apps/lab/src/results/useResultsList.ts` == 0 (D-16: sem botão)
    - `grep -c "NOVO" apps/lab/src/results/ResultCard.tsx` >= 1 (badge D-35/UI-21)
    - `grep -c "project/\[id\]/results" apps/lab/app/_layout.tsx apps/lab/app/project/\[id\]/run.tsx` >= 2 (registro + navegação)
    - `grep -c "lista de itens na fase 8" apps/lab/app/project/\[id\]/run.tsx` == 0 (placeholder morto)
    - `grep -rn ": any\|as any" apps/lab/src/results/useResultsList.ts apps/lab/src/results/ResultCard.tsx "apps/lab/app/project/[id]/results.tsx" "apps/lab/app/project/[id]/run.tsx" | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit -p tsconfig.json && pnpm --filter @uhhu/lab exec eslint apps/lab/src/results/ apps/lab/app/project/\[id\]/results.tsx apps/lab/app/project/\[id\]/run.tsx apps/lab/app/_layout.tsx apps/lab/src/api/lab.ts && pnpm --filter @uhhu/lab run test 2>&1 | tail -n 6</automated>
  </verify>
  <done>Run concluído abre lista real infinita com filtros combináveis, cards com proveniência+NOVO e estados §11; suite verde incluindo tripwire</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runId/searchId na URL → fetch | Query é hostil: uuid inválido → banner sem request; 404 vira "não encontrado", sem distinguir alheio |
| filtros → render | Filtros são client-side pós-filtro (D-17); nunca substituem autorização; decisão/tag exibidas vêm do servidor |
| links de origem → browser | originUrl/sourceUrl abrem via Linking só se https; sem WebView/eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | IDOR em results/groups | mitigate | owner-scoped no servidor; 401 → expired+next; teste manual dono/estranho/adulterado via curl antes do done |
| T-08-02-02 | Information Disclosure | segredo no bundle | mitigate | nenhum token/log em componente; dist grep 0 no done |
| T-08-02-03 | Tampering | group null (sem grupo) executa ação | mitigate | card degradado: ações de grupo desabilitadas + "grupo indisponível"; nunca decisão por resultId |
| T-08-02-04 | Denial of Service | scroll infinito sem teto | mitigate | páginas limit 30 por cursor do servidor; prefetch só no fim; sem N+1 (groups 1 fetch + páginas) |
| T-08-02-05 | Cross-site Scripting | título/autores/notas da fonte | mitigate | Text escapa por padrão; sem WebView/dangerouslySetInnerHTML; divergências só na 08-03 com mesma regra |
</threat_model>

<verification>
- typecheck + lint + suite completa do app verdes, zero `any`, tripwire scroll-containers verde
- Fluxo manual tablet: run ok → [Ver resultados] → lista com NOVO + proveniência; rolar até o fim pagina sozinho; filtros combinam (estado+tag+fonte+ano); 401 → login com aviso preservando destino
- `grep -c "\.map" apps/lab/src/results/ResultCard.tsx apps/lab/app/project/[id]/results.tsx` == 0 fora de FlatList/renderItem (tripwire)
</verification>

<success_criteria>
- UI-17: cards com título/autores/ano/tipo/instituição/programa/fontes + proveniência busca→run→data→fonte
- UI-21: badge NOVO (regra D-35) + contador consistente; D-16 infinito por cursor; D-17 filtros combináveis client-side
</success_criteria>

<output>
After completion, create `.planning/phases/08-resultados-triagem/08-02-SUMMARY.md`
</output>
