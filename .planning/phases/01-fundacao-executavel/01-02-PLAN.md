---
phase: 01-fundacao-executavel
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - compose.dev.yml
  - .env.example
  - .env.dev
  - packages/config/src/env.ts
  - packages/config/src/index.ts
  - packages/db/src/schema.ts
  - packages/db/src/client.ts
  - packages/db/src/index.ts
  - packages/db/drizzle.config.ts
  - packages/db/drizzle/0000_infra_proof.sql
  - packages/db/package.json
  - apps/core-api/src/index.ts
  - apps/core-api/src/health.ts
  - apps/core-api/package.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04
user_setup:
  - service: vps-tailnet-pg-dev
    why: "PostgreSQL DEV roda na VPS via tailnet (D-06); este container opera so como cliente"
    env_vars: []
    dashboard_config: []
    manual_steps:
      - "Na VPS com tailnet: `docker compose -f compose.dev.yml up -d postgres-dev`"
      - "Criar roles `uhhu_migrate` (DDL) e `uhhu_app` (DML) e os bancos `uhhu_dev` conforme .env.example"
      - "Preencher `.env.dev` local (nunca commitado) com as duas DATABASE_URLs apontando para o PG da VPS"

must_haves:
  truths:
    - "Dev aplica migrations versionadas do monorepo contra o PostgreSQL DEV e a contagem aparece no /health"
    - "`GET /health` retorna status do banco, versao e migrations aplicadas sem revelar segredos"
    - "Role de runtime nao tem privilegio DDL; role de migrations e separada (menor privilegio ja no DEV)"
    - "Nenhum segredo em codigo, Git, logs ou respostas; dev e prod usam bancos/credenciais/.env distintos"
  artifacts:
    - path: "compose.dev.yml"
      provides: "PostgreSQL DEV dedicado (volume e credenciais proprios)"
      contains: "postgres-dev"
    - path: "packages/db/drizzle/0000_infra_proof.sql"
      provides: "Migration inicial de prova de infra (sem tabelas de dominio)"
      contains: "infra_proof"
    - path: "apps/core-api/src/health.ts"
      provides: "GET /health com status do banco e contagem de migrations"
      exports:
        - "healthRoute"
    - path: "packages/config/src/env.ts"
      provides: "Validacao Zod do env por NODE_ENV (dev/prod separados)"
      exports:
        - "env"
  key_links:
    - from: "apps/core-api/src/health.ts"
      to: "packages/db/src/client.ts"
      via: "import do client Drizzle (core-api nao cria conexao propria)"
      pattern: "@uhhu/db"
    - from: "packages/db/src/client.ts"
      to: "packages/config/src/env.ts"
      via: "connection string validada, nunca hardcoded"
      pattern: "env\\.DATABASE_URL|env\\.APP_DATABASE_URL"
    - from: "apps/core-api/src/health.ts"
      to: "HTTP GET /health"
      via: "rota Fastify registrada no boot"
      pattern: "/health"
---

<objective>
Subir o CORE contra PostgreSQL DEV com migrations versionadas, envs dev/prod separados, roles distintas e `GET /health` completo (D-05 a D-10): a entrega observavel FOUND-01 + FOUND-04.

Purpose: Prova que persistencia canonica, isolamento de ambientes e menor privilegio funcionam antes de qualquer tabela de dominio existir.
Output: `pnpm db:migrate` aplica a migration de prova no PG DEV; `/health` verde com contagem de migrations; nenhum segredo no repo.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-executavel/01-CONTEXT.md
@.planning/phases/01-fundacao-executavel/01-01-SUMMARY.md
@dev-docs/07-core-contract.md
@dev-docs/05-infra.md
@dev-docs/08-security-baseline.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>task 1: PG DEV dedicado + separacao dev/prod com Zod</name>
  <files>compose.dev.yml, .env.example, .env.dev, packages/config/src/env.ts, packages/config/src/index.ts, packages/config/package.json</files>
  <read_first>
    - .planning/phases/01-fundacao-executavel/01-CONTEXT.md (D-05, D-06, D-07 e criterio "bancos, credenciais e .env distintos")
    - dev-docs/05-infra.md §2-§5 (topologia VPS, dev so localhost/tailnet, segredos 600, credenciais separadas)
    - packages/config/src/index.ts (placeholder do plano 01-01 a substituir)
    - .gitignore (confirmar `.env*` ignorado com excecao de `.env.example`)
  </read_first>
  <action> Criar a camada de ambientes (per D-05/D-06/D-07, detalhes de formato sao discricao OpenCode dentro do criterio travado):
  - `compose.dev.yml`: servico `postgres-dev`, imagem `postgres:16-alpine`, volume nomeado `uhhu-pgdata-dev`, bind de porta SOMENTE local `127.0.0.1:55432:5432`, `POSTGRES_DB: uhhu_dev`, senha via `${PG_DEV_PASSWORD:?required}` (sem default, sem segredo no arquivo), `restart: unless-stopped`. Nada de prod neste compose.
  - `packages/config/src/env.ts`: schema Zod com `NODE_ENV` (`development|production`), `APP_DATABASE_URL` (role de runtime), `MIGRATION_DATABASE_URL` (role DDL), `PORT` default 3000, `LOG_LEVEL` default `info`; exportar `env` ja parseado (`envSchema.parse(process.env)`); DEPURAÇÃO: nunca logar valores parseados.
  - `packages/config` depende de `zod` (adicionar dependencia real no package.json).
  - `.env.example` (COMMITADO): chaves vazias/documentadas `NODE_ENV=development`, `APP_DATABASE_URL=postgresql://uhhu_app:CHANGE_ME@HOST:55432/uhhu_dev`, `MIGRATION_DATABASE_URL=postgresql://uhhu_migrate:CHANGE_ME@HOST:55432/uhhu_dev`, `PG_DEV_PASSWORD=`, `PORT=3000`.
  - `.env.dev` (criado a partir do example, com placeholders `CHANGE_ME`; se o executor tiver acesso a VPS preenche, senao mantem placeholder e registra no SUMMARY como pendencia de `user_setup`).
  - Documentar no README de `packages/config` (criar `packages/config/README.md`): roles `uhhu_migrate` (DDL: CREATE/ALTER/DROP + DML no schema public) vs `uhhu_app` (DML apenas: SELECT/INSERT/UPDATE/DELETE, sem DDL) e o principio "servidor deriva tudo da sessao; connection string nunca vem do cliente".
  - NUNCA commitar `.env.dev` ou qualquer senha real; verificar com `git status --porcelain` que so `.env.example` aparece.</action>
  <acceptance_criteria>
    - `compose.dev.yml` contem `postgres-dev`, `uhhu-pgdata-dev` e `127.0.0.1:55432:5432` (bind local, sem 0.0.0.0, sem 80/443)
    - `compose.dev.yml` NAO contem `password` literal nem segredo (grep -i `password:` so mostra `${PG_DEV_PASSWORD`)
    - `packages/config/src/env.ts` contem `APP_DATABASE_URL` e `MIGRATION_DATABASE_URL` e `zod`
    - `.env.example` existe e `.env.dev` NAO aparece em `git status --porcelain` como novo arquivo rastreado apos `git add -A -n` (segue ignorado)
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && git status --porcelain | grep -v "^??" | grep -c "env.dev" | grep -x "0"</automated>
  </verify>
  <done>Compose DEV local-only, env validado por Zod com duas URLs (app vs migrate), example commitado, nenhum segredo rastreado</done>
</task>

<task type="auto">
  <name>task 2: Drizzle dono total + migration de prova de infra</name>
  <files>packages/db/src/schema.ts, packages/db/src/client.ts, packages/db/src/index.ts, packages/db/drizzle.config.ts, packages/db/drizzle/0000_infra_proof.sql, packages/db/package.json</files>
  <read_first>
    - packages/db/src/index.ts (placeholder do plano 01-01 a substituir)
    - packages/config/src/env.ts (URLs validadas a consumir — nunca recriar validacao aqui)
    - .planning/phases/01-fundacao-executavel/01-CONTEXT.md (D-08: NENHUMA tabela de dominio; D-10: db dono de schema/client/config/migrations)
    - dev-docs/07-core-contract.md §17 (Drizzle dialeto PG, migrations no monorepo)
  </read_first>
  <action>Implementar `packages/db` como dono total do Drizzle (per D-08/D-10):
  - Dependencias reais em `packages/db/package.json`: `drizzle-orm`, `postgres` (driver), `drizzle-kit` (dev), scripts `db:generate: drizzle-kit generate`, `db:migrate: tsx ./src/migrate.ts` (criar `packages/db/src/migrate.ts` que conecta com `MIGRATION_DATABASE_URL` e roda as migrations; falha com exit != 0 e mensagem sem credenciais em caso de erro — redact de password na mensagem).
  - `packages/db/src/schema.ts`: UMA tabela de prova `infra_proof (id serial pk, checked_at timestamptz default now(), note text)` + comentario explícito "prova de infra da Fase 1 — tabelas platform/lab (users, projects...) PROIBIDAS aqui; schema nao congela sem validacao do contrato por secoes com Paulo (D-08)".
  - `packages/db/drizzle.config.ts`: dialeto `postgresql`, `schema: ./src/schema.ts`, `out: ./drizzle`, dbCredentials via `MIGRATION_DATABASE_URL`.
  - `packages/db/drizzle/0000_infra_proof.sql`: SQL versionado correspondente (`CREATE TABLE infra_proof...`), com header `-- Fase 1: prova de infra, sem dominio`.
  - `packages/db/src/client.ts`: factory `createDb(url)` com driver postgres + drizzle, `max: 10`; `packages/db/src/index.ts` re-exporta schema + client + migrate. `core-api` importara daqui — NENHUMA conexao SQL fora deste pacote.
  - Queries parametrizadas via Drizzle em todo acesso; nenhum SQL concatenado.</action>
  <acceptance_criteria>
    - `packages/db/drizzle/0000_infra_proof.sql` contem `CREATE TABLE` de `infra_proof`
    - `packages/db/src/schema.ts` NAO contem `users`, `projects`, `search`, `result` (grep retorna 0)
    - `packages/db/src/migrate.ts` usa `MIGRATION_DATABASE_URL` e nunca `APP_DATABASE_URL`
    - `apps/core-api/src` NAO contem `postgres`, `drizzle` nem `DATABASE_URL` (db e dono total)
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm --filter @uhhu/db db:migrate && pnpm --filter @uhhu/db exec psql "$MIGRATION_DATABASE_URL" -c "SELECT count(*) FROM infra_proof;"</automated>
  </verify>
  <done>Migration de prova aplicada no PG DEV via role DDL; runtime sem acesso DDL; zero tabelas de dominio</done>
</task>

<task type="auto">
  <name>task 3: boot Fastify + GET /health sem segredos</name>
  <files>apps/core-api/src/index.ts, apps/core-api/src/health.ts, apps/core-api/package.json</files>
  <read_first>
    - apps/core-api/src/index.ts (placeholder do plano 01-01 a substituir)
    - packages/db/src/client.ts (factory a importar — nao abrir conexao propria)
    - packages/config/src/env.ts (PORT e APP_DATABASE_URL validados)
    - packages/db/src/schema.ts (tabela infra_proof a contar como prova de migrations)
    - .planning/phases/01-fundacao-executavel/01-CONTEXT.md (D-09: status ok/degraded, versao, contagem de migrations, sem segredos)
  </read_first>
  <action>Substituir o placeholder pelo boot real minimo (per D-09/D-10):
  - Dependencias reais em `apps/core-api/package.json`: `fastify`, `@uhhu/db workspace:*`, `@uhhu/config workspace:*`, `@uhhu/contracts workspace:*`.
  - `apps/core-api/src/health.ts`: exportar `healthRoute` (plugin Fastify) com `GET /health` que retorna EXATAMENTE `{ status: "ok"|"degraded", db: "ok"|"degraded", version: <APP_VERSION const "0.1.0-fase1">, migrationsApplied: <number> }`; checagem: `select count(*) from drizzle.__drizzle_migrations` (ou da tabela de migrations do Drizzle configurada) + `select 1` de ping; qualquer excecao => `db: degraded`, `status: degraded`, HTTP 200 (health nunca 500 por design, nunca stack trace); NENHUM campo de env, config, URL ou senha na resposta.
  - `apps/core-api/src/index.ts`: cria Fastify com `logger` sem redacao custom? usar `logger: { level: env.LOG_LEVEL, redact: ["req.headers.authorization", "req.headers.cookie"] }`, registra `healthRoute`, conecta `createDb(env.APP_DATABASE_URL)` (role de runtime — prova viva do menor privilegio), listen em `env.PORT` bind `127.0.0.1` no DEV. Rota `/health` SEM prefixo `/api/v1` per D-09 literal.
  - `X-Request-Id`: aceitar o recebido ou gerar `randomUUID` (crypto, nunca Math.random) e retornar no header da resposta do /health.</action>
  <acceptance_criteria>
    - `apps/core-api/src/health.ts` exporta `healthRoute` e contem `migrationsApplied`
    - Resposta de `/health` NAO contem `password`, `DATABASE_URL`, `secret`, `token` (teste de corpo)
    - `apps/core-api/src/index.ts` contem `redact` e `randomUUID` (ou `crypto.randomUUID`), e NAO contem `Math.random`
    - `curl localhost:PORT/health` retorna `db: ok` com PG DEV no ar
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && (pnpm --filter @uhhu/core-api start &>/tmp/uhhu-api.log & echo $! > /tmp/uhhu-api.pid; sleep 4; curl -sf http://127.0.0.1:3000/health; kill $(cat /tmp/uhhu-api.pid))</automated>
  </verify>
  <done>API sobe contra PG DEV com role de runtime, /health verde com contagem de migrations, zero vazamento de segredo, request-id propagado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose.dev.yml → VPS | Credenciais do PG DEV; arquivo versionado nao pode carregar segredo |
| core-api → PostgreSQL | Conexao com menor privilegio; resposta /health cruza para qualquer chamador (ate nao autenticado) |
| .env.dev → processo | Segredos vivem so em arquivo 600 ignorado + memoria; nunca em log/resposta/Git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | compose.dev.yml + .env.dev | mitigate | Senha so via `${PG_DEV_PASSWORD:?required}`; `.env*` ignorado exceto example; verify conta que `.env.dev` nao e rastreado |
| T-02-02 | E | postgres-dev port bind | mitigate | Bind `127.0.0.1:55432` (nunca 0.0.0.0/80/443); dev restrito a localhost/tailnet per 05-infra §2 |
| T-02-03 | E | roles uhhu_app vs uhhu_migrate | mitigate | migrate.ts usa SOMENTE MIGRATION_DATABASE_URL; boot usa SOMENTE APP_DATABASE_URL; README documenta grants; teste de regressao: app sem DDL (plano 01-03 cobre com teste negativo) |
| T-02-04 | I | GET /health publica | mitigate | Corpo fixo em 4 campos (status/db/version/migrationsApplied); logger com redact de authorization/cookie; teste de corpo proibe password/DATABASE_URL/secret/token |
| T-02-05 | S | session/random em request-id | mitigate | `crypto.randomUUID`; grep proibe `Math.random` em apps/packages |
| T-02-06 | T | SQL injection futura | mitigate | Todo acesso via Drizzle parametrizado; SQL concatenado proibido (SATE do plano 01-03 barra `query(` com template) |
</threat_model>

<verification>
- `pnpm --filter @uhhu/db db:migrate` aplica 0000 contra PG DEV (exit 0)
- `curl http://127.0.0.1:3000/health` => `{"status":"ok","db":"ok","version":"0.1.0-fase1","migrationsApplied":1}`
- `git status --porcelain` nao lista `.env.dev`, `.env.prod`, nem senha
- `grep -rin "users\|projects" packages/db/src/schema.ts` vazio (sem dominio)
- Logs do boot nao contem a senha (grep no /tmp/uhhu-api.log por password/CHANGE_ME real)
</verification>

<success_criteria>
- FOUND-01 observavel: install -> migrate -> /health verde contra PG DEV
- FOUND-04 observavel: dev/prod separados, roles separadas, zero segredo no repo/logs/respostas
- D-05, D-06, D-07, D-08, D-09, D-10 implementadas sem reducao
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-executavel/01-02-SUMMARY.md`
</output>
