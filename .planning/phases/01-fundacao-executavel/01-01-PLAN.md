---
phase: 01-fundacao-executavel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - eslint.config.mjs
  - .prettierrc.json
  - .gitignore
  - .node-version
  - apps/core-api/package.json
  - apps/core-api/tsconfig.json
  - apps/core-api/src/index.ts
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/src/index.ts
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/db/src/index.ts
  - packages/config/package.json
  - packages/config/tsconfig.json
  - packages/config/src/index.ts
autonomous: true
requirements:
  - FOUND-02
user_setup: []

must_haves:
  truths:
    - "Dev executa `pnpm install` na raiz e todas as workspaces instalam sem erro"
    - "`pnpm typecheck` e `pnpm lint` passam no esqueleto antes de qualquer codigo de produto"
    - "Fronteiras do monorepo estao visiveis: apps futuros e packages futuros existem como pastas com README de fronteira, sem codigo"
  artifacts:
    - path: "package.json"
      provides: "Workspace root pnpm com engines pinados"
      contains: "packageManager"
    - path: "tsconfig.base.json"
      provides: "TS strict + extras herdado por todos os pacotes"
      contains: "exactOptionalPropertyTypes"
    - path: "pnpm-workspace.yaml"
      provides: "Definicao das workspaces apps/* e packages/*"
      contains: "apps/*"
    - path: "apps/core-api/src/index.ts"
      provides: "Entry point minimo do CORE API (placeholder de boot, sem produto)"
      min_lines: 5
    - path: "packages/contracts/src/index.ts"
      provides: "Esqueleto do pacote unico de tipos/DTOs/erros"
      min_lines: 3
  key_links:
    - from: "apps/core-api/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends da base"
      pattern: "extends.*tsconfig.base"
    - from: "apps/core-api/package.json"
      to: "packages/contracts"
      via: "dependencia workspace"
      pattern: "@uhhu/contracts.*workspace"
---

<objective>
Materializar o esqueleto minimo do monorepo pnpm + TypeScript strict (D-01 a D-04): toolchain fixada, configs de raiz, `apps/core-api` e `packages/{contracts,db,config}` como esqueletos compilaveis, demais apps/packages do contrato como pastas com README de fronteira sem codigo.

Purpose: Sem este esqueleto nenhum plano posterior compila, linta ou testa. E a ancora fisica de FOUND-02 e das decisoes D-01--D-04.
Output: Raiz do monorepo instalavel (`pnpm install` verde) com `pnpm typecheck` e `pnpm lint` verdes sobre placeholders.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-executavel/01-CONTEXT.md
@dev-docs/07-core-contract.md
@dev-docs/01-arquitetura.md
@dev-docs/02-decisoes.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>task 1: raiz do monorepo com toolchain fixada</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json, eslint.config.mjs, .prettierrc.json, .gitignore, .node-version</files>
  <read_first>
    - AGENTS.md (regras normativas: strict, any proibido, sufixos de convencao)
    - .planning/phases/01-fundacao-executavel/01-CONTEXT.md (D-01 a D-04, valores travados)
    - dev-docs/01-arquitetura.md §5-§6 (layout apps/ + packages/ e stack)
  </read_first>
  <action>Criar a raiz do monorepo com estes valores exatos (per D-02/D-03/D-04):
  - `package.json`: `"private": true`, `"packageManager": "pnpm@9.15.0"`, `"engines": {"node": "22.x", "pnpm": "9.x"}`, scripts `typecheck` (tsc --build em todas as workspaces), `lint` (eslint .), `format` (prettier --check .), `test` (vitest run nas workspaces que tiverem testes).
  - `pnpm-workspace.yaml`: packages `apps/*` e `packages/*`.
  - `tsconfig.base.json`: `"strict": true`, `"noUncheckedIndexedAccess": true`, `"exactOptionalPropertyTypes": true`, `"module": "NodeNext"`, `"moduleResolution": "NodeNext"`, `"target": "ES2022"`, `"declaration": true`, `"sourceMap": true`. Nao afrouxar nada para compilar (AGENTS.md proibe).
  - `eslint.config.mjs`: flat config com `typescript-eslint` recomendado + regra que bane `any` (`@typescript-eslint/no-explicit-any: error`); Prettier via `.prettierrc.json` (singleQuote, trailingComma all, printWidth 100).
  - `.gitignore`: `node_modules/`, `dist/`, `.env*` (EXCETO `.env.example` — usar negacao `!.env.example`), `coverage/`, `*.tsbuildinfo`.
  - `.node-version`: `22`.
  Nao criar nenhum codigo de produto, nenhuma tabela, nenhum endpoint.</action>
  <acceptance_criteria>
    - `package.json` contem `pnpm@9` em packageManager (grep `packageManager.*pnpm@9`)
    - `tsconfig.base.json` contem `noUncheckedIndexedAccess` e `exactOptionalPropertyTypes`
    - `eslint.config.mjs` contem `no-explicit-any`
    - `.gitignore` contem `.env*` e a negacao `!.env.example`
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>Install, typecheck e lint verdes na raiz ainda sem pacotes de produto; toolchain pinada Node 22 + pnpm 9</done>
</task>

<task type="auto">
  <name>task 2: esqueletos compilaveis + READMEs de fronteira</name>
  <files>apps/core-api/package.json, apps/core-api/tsconfig.json, apps/core-api/src/index.ts, packages/contracts/package.json, packages/contracts/tsconfig.json, packages/contracts/src/index.ts, packages/db/package.json, packages/db/tsconfig.json, packages/db/src/index.ts, packages/config/package.json, packages/config/tsconfig.json, packages/config/src/index.ts</files>
  <read_first>
    - package.json (raiz criada na task 1: scripts e convenções de workspace)
    - tsconfig.base.json (base a ser herdada via extends)
    - dev-docs/07-core-contract.md §5 (organizacao-alvo: fronteira, nao lista de codigo imediato)
    - .planning/phases/01-fundacao-executavel/01-CONTEXT.md (D-01: so core-api + contracts/db/config ganham codigo; resto e README)
  </read_first>
  <action>Materializar SÓ `apps/core-api` + `packages/{contracts,db,config}` como esqueletos compilaveis (per D-01 e D-10):
  - Cada `package.json`: nome escopado `@uhhu/core-api`, `@uhhu/contracts`, `@uhhu/db`, `@uhhu/config`; `type: module`; script `typecheck: tsc --noEmit -p tsconfig.json`; `main: ./dist/index.js`, `types: ./dist/index.d.ts`.
  - Cada `tsconfig.json`: `{ "extends": "../../tsconfig.base.json", "include": ["src"] }` (apps usam `../../../` conforme profundidade).
  - `apps/core-api/src/index.ts`: placeholder que apenas registra o futuro boot Fastify (comentario + `export const CORE_API_PLACEHOLDER = "fase-1-esqueleto"` + `console.log` de boot). Nenhuma rota, nenhum endpoint.
  - `packages/contracts/src/index.ts`: `export const CONTRACTS_PLACEHOLDER = "fase-1-esqueleto"` + comentario de que este pacote sera a unica definicao de tipos/DTOs/erros (FOUND-02).
  - `packages/db/src/index.ts` e `packages/config/src/index.ts`: placeholders equivalentes (D-10: db sera dono do Drizzle no plano 01-02; config sera dono do env Zod no plano 01-02).
  - `apps/core-api` depende de `@uhhu/contracts`, `@uhhu/db`, `@uhhu/config` via `workspace:*`.
  - Pastas de fronteira SEM codigo, cada uma com `README.md` de 3-10 linhas declarando dono/fronteira e "nao implementar antes da spec propria": `apps/lab`, `apps/core-worker`, `apps/cli`, `apps/mcp`, `packages/core`, `packages/modules`, `packages/integrations`. README do `packages/modules` lista `platform|lab|lib|note|plan|prof` como fronteiras futuras.
  - `import type` onde houver import de tipo, mesmo nos placeholders (convencao AGENTS.md).</action>
  <acceptance_criteria>
    - `apps/core-api/src/index.ts` contem `CORE_API_PLACEHOLDER`
    - `apps/core-api/package.json` referencia `@uhhu/contracts` com `workspace`
    - Todo `packages/*/tsconfig.json` e `apps/*/tsconfig.json` contem `extends` para `tsconfig.base.json`
    - `apps/lab/README.md`, `apps/cli/README.md`, `apps/mcp/README.md`, `packages/modules/README.md` existem e NAO ha `.ts` fora de core-api/contracts/db/config (grep `*.ts` so nesses 4 src/)
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>4 pacotes compilam e lintam; 7 pastas de fronteira existem so com README; zero codigo de produto</done>
</task>

<task type="auto">
  <name>task 3: prova de gates locais no esqueleto</name>
  <files>package.json</files>
  <read_first>
    - package.json (scripts typecheck/lint/test a provar)
    - eslint.config.mjs (regras que precisam passar)
  </read_first>
  <action>Provar que os gates locais passam no esqueleto e quebrar de proposito NAO passa (fail-closed per D-13 aplicado ao lint/type):
  - Rodar `pnpm typecheck`, `pnpm lint`, `pnpm format`, `pnpm test` (test pode reportar "no tests yet" com exit 0 via `vitest run --passWithNoTests`).
  - Inserir temporariamente `const x: any = 1` num arquivo scratch fora do src, rodar lint, confirmar erro `no-explicit-any`, remover o scratch.
  - Registrar o resultado (comandos + exits) no corpo do SUMMARY do plano, nao em arquivo de docs.</action>
  <acceptance_criteria>
    - `pnpm typecheck` exit 0, `pnpm lint` exit 0 documentados no SUMMARY
    - Nenhum arquivo scratch com `any` permanece no repo (grep `:\s*any` retorna 0 ocorrencias em `apps/` e `packages/`)
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm format && pnpm test</automated>
  </verify>
  <done>Gates locais verdes comprovados; ban de `any` verificado na pratica; evidencias no SUMMARY</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-machine → repo | Codigo e config entram no Git; segredo commitado aqui vaza no historico |
| raiz → workspaces | Configs de raiz (tsconfig/eslint) definem o piso de seguranca de todo codigo futuro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | tsconfig.base.json / eslint.config.mjs | mitigate | strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes + no-explicit-any:error travados; task 3 prova que afrouxar quebra o gate; CI do plano 01-03 re-executa |
| T-01-02 | T | package.json engines | mitigate | engines + packageManager pinados (Node 22, pnpm 9); builds irreprodutiveis falham no install em vez de silenciosamente derivar |
| T-01-03 | I | .gitignore / futuros .env | mitigate | `.env*` ignorado com excecao so para `.env.example`; plano 01-02 cria os .env e o plano 01-03 barra segredo com Gitleaks tree+historico |
| T-01-04 | E | pastas de fronteira (apps/lab, cli, mcp...) | accept | Sem codigo nesta fase nao ha superficie; READMEs declaram a fronteira e AGENTS.md + CI impedem backend por app (revisitar no Phase 5 headless) |
</threat_model>

<verification>
- `pnpm install && pnpm typecheck && pnpm lint && pnpm format && pnpm test` exit 0 na raiz
- `grep -rn "extends.*tsconfig.base" apps packages` cobre os 4 pacotes
- `grep -rn ": any\|as any" apps packages --include='*.ts'` retorna vazio
- Nenhum `.ts` fora de `apps/core-api/src`, `packages/{contracts,db,config}/src`
</verification>

<success_criteria>
- FOUND-02 ancorada: monorepo pnpm/TS strict com fronteiras visiveis e contracts como definicao unica (esqueleto)
- D-01, D-02, D-03, D-04 implementadas sem reducao
- Zero tabelas, zero endpoints, zero segredos
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-executavel/01-01-SUMMARY.md`
</output>
