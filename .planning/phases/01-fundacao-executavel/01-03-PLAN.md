---
phase: 01-fundacao-executavel
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .github/workflows/gates.yml
  - .gitleaks.toml
  - vitest.workspace.ts
  - tests/smoke/boot.test.ts
  - tests/integration/health-pg.test.ts
  - tests/integration/least-privilege.test.ts
  - packages/db/package.json
  - apps/core-api/package.json
autonomous: false
requirements:
  - FOUND-03
user_setup: []

must_haves:
  truths:
    - "CI falha em segredo confirmado (tree+historico), achado SAST alto/critico e `pnpm audit` alto/critico sem aceite formal"
    - "Suite minima verde: typecheck, lint, testes + integracao que aplica migrations e bate no /health contra PG de servico"
    - "Contrato inicial validado por secoes com Paulo antes de qualquer schema de dominio existir (criterio 4 da fase)"
  artifacts:
    - path: ".github/workflows/gates.yml"
      provides: "Pipeline de gates + job de integracao PG"
      contains: "gitleaks"
    - path: "tests/integration/health-pg.test.ts"
      provides: "Teste que migra e bate no /health contra PG de servico"
      contains: "/health"
    - path: ".gitleaks.toml"
      provides: "Config do secret scan (tree + historico no CI)"
      contains: "title"
  key_links:
    - from: ".github/workflows/gates.yml"
      to: "postgres service container"
      via: "job de integracao com service postgres:16-alpine + MIGRATION/APP URLs"
      pattern: "services:\\s*postgres"
    - from: "tests/integration/health-pg.test.ts"
      to: "apps/core-api/src/health.ts"
      via: "boot real da API em porta efemera + GET /health"
      pattern: "/health"
    - from: ".github/workflows/gates.yml"
      to: "fail-closed"
      via: "nenhum gate com continue-on-error ou allow-failure"
      pattern: "continue-on-error"
---

<objective>
Fechar a fase com CI de seguranca verde e falha dura (D-11 a D-14): Gitleaks tree+historico, SAST OpenGrep, `pnpm audit`, typecheck, lint, suite smoke + integracao PG, e o checkpoint humano de validacao do contrato por secoes com Paulo.

Purpose: FOUND-03 e o criterio 4 da fase (contrato validado antes de congelar schema). Sem este plano, a fundacao "funciona na maquina" mas nao barra regressao nem segredo.
Output: Push em branch com CI verde; evidencias de falha dura; aceite de Paulo registrado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-executavel/01-CONTEXT.md
@.planning/phases/01-fundacao-executavel/01-01-SUMMARY.md
@.planning/phases/01-fundacao-executavel/01-02-SUMMARY.md
@dev-docs/08-security-baseline.md
@dev-docs/07-core-contract.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>task 1: pipeline de gates com falha dura</name>
  <files>.github/workflows/gates.yml, .gitleaks.toml</files>
  <read_first>
    - dev-docs/08-security-baseline.md §5 (gates no CI: Gitleaks tree+historico, SAST JS/TS, pnpm audit, integracao PG, falha em critico/alto sem aceite)
    - .planning/phases/01-fundacao-executavel/01-CONTEXT.md (D-11: Actions + job integracao PG via service; D-12: OpenGrep; D-13: falha dura, aceite formal com prazo como excecao registrada)
    - package.json (scripts typecheck/lint/test/db:migrate a invocar no CI)
  </read_first>
  <action>Criar `.github/workflows/gates.yml` (per D-11/D-12/D-13) com jobs, TODOS fail-closed (proibido `continue-on-error: true` e `allow-failure` em qualquer gate):
  - `gates` (runs-on ubuntu-24.04, node 22, pnpm 9 via corepack): `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm lint`, `pnpm format --check` (ou `pnpm format` se for check), `pnpm test`.
  - `secrets-tree`: `gitleaks/gitleaks-action` com `gitleaks detect --source . --no-git -v` (working tree; `.gitleaks.toml` na raiz com `[extend] useDefault = true` + allowlist minima documentada).
  - `secrets-history`: `gitleaks detect --source . -v` (historico completo do clone full-history: `fetch-depth: 0` no checkout).
  - `sast`: OpenGrep (`opengrep/opengrep-action` ou `returntocorp/opengrep-action`; se a action indisponivel, rodar via `docker run returntocorp/opengrep:latest opengrep scan --config auto`) sobre `apps/ packages/`; falha em `high`/`critical`.
  - `audit`: `pnpm audit --audit-level high` (falha em high/critical).
  - `integration-pg`: `services: postgres: { image: postgres:16-alpine, env POSTGRES_DB/USER/PASSWORD de teste, ports 5432, healthcheck pg_isready }`; steps: aplicar `pnpm --filter @uhhu/db db:migrate` com `MIGRATION_DATABASE_URL` apontando ao service, subir API, `curl --fail /health`, rodar `pnpm test -- integration`.
  - Aceite formal (excecao D-13): arquivo `.planning/aceites-seguranca.md` (criar vazio com cabecalho "Nenhum aceite vigente") — qualquer supressao futura EXIGE entrada datada com prazo + responsavel, nunca edicao silenciosa do workflow.
  - `.gitleaks.toml`: `[extend] useDefault = true`, `[stage]`, sem allowlists de segredo real.</action>
  <acceptance_criteria>
    - `.github/workflows/gates.yml` contem `gitleaks`, `opengrep`, `pnpm audit --audit-level high` e `services:` com `postgres`
    - `.github/workflows/gates.yml` NAO contem `continue-on-error: true` nem `allow-failure` (grep retorna 0)
    - `.gitleaks.toml` contem `useDefault`
    - Workflow e valido YAML com `on: [push, pull_request]`
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/gates.yml'))" && grep -c "continue-on-error: true" .github/workflows/gates.yml | grep -x "0" && (command -v gitleaks >/dev/null && gitleaks detect --source . --no-git --config .gitleaks.toml -v || echo "GITLEAKS_BIN_MISSING — CI executara")</automated>
  </verify>
  <done>Pipeline com 6 jobs fail-closed; secret scan tree+historico; SAST; audit high; integracao PG via service; aceite formal como unico caminho de excecao</done>
</task>

<task type="auto">
  <name>task 2: suite minima smoke + integracao PG + menor privilegio</name>
  <files>vitest.workspace.ts, tests/smoke/boot.test.ts, tests/integration/health-pg.test.ts, tests/integration/least-privilege.test.ts, packages/db/package.json, apps/core-api/package.json</files>
  <read_first>
    - apps/core-api/src/health.ts (contrato do /health a testar: 4 campos, sem segredos)
    - apps/core-api/src/index.ts (como a API sobe: env, boot, porta)
    - packages/db/src/migrate.ts (como aplicar migrations no teste)
    - .planning/phases/01-fundacao-executavel/01-CONTEXT.md (D-14: suite minima + teste que migra e bate no /health)
    - dev-docs/08-security-baseline.md §2.1 (roles menor privilegio: provar que runtime nao tem DDL)
  </read_first>
  <action>Implementar a suite minima com `vitest` (escolha discricionaria OpenCode: runner rapido, TS nativo; adicionar `vitest` + `tsx` como devDeps na raiz):
  - `vitest.workspace.ts`: workspaces `tests/smoke` e `tests/integration`.
  - `tests/smoke/boot.test.ts`: importa o `healthRoute` (ou o builder do Fastify) e, sem banco, asserta que `GET /health` responde `degraded` (db fora => degraded, nunca throw, nunca 500, corpo com as 4 chaves `status|db|version|migrationsApplied`).
  - `tests/integration/health-pg.test.ts` (roda com `APP/MIGRATION_DATABASE_URL` do ambiente — CI service ou PG DEV): aplica `db:migrate`, sobe a API em porta efemera (`PORT=0` ou porta livre fixa `3210`), `GET /health` => `db: ok`, `migrationsApplied >= 1`; asserta que o corpo NAO contem `password|DATABASE_URL|secret|token` (case-insensitive).
  - `tests/integration/least-privilege.test.ts`: conecta com `APP_DATABASE_URL` e tenta `CREATE TABLE least_privilege_probe (id int)` => DEVE falhar com `permission denied`/`42501`; conecta com `MIGRATION_DATABASE_URL` e roda `SELECT 1` => ok. Prova viva de D-07.
  - `import type` para tipos; `any` proibido tambem nos testes (AGENTS.md sem excecao); timeouts explicitos (health < 5s); sem `Math.random` (usar `crypto.randomUUID` se precisar de sufixo).
  - Script raiz `test`: `vitest run`; `test:integration`: `vitest run --project integration` (ou filtro por path).</action>
  <acceptance_criteria>
    - `tests/integration/health-pg.test.ts` contem `/health` e `migrationsApplied`
    - `tests/integration/least-privilege.test.ts` contem `42501` ou `permission denied` e `MIGRATION_DATABASE_URL`
    - `grep -rn ": any\|as any" tests/` retorna vazio
    - `pnpm test` verde local (smoke) e `pnpm test:integration` verde contra PG DEV/service
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm test && pnpm test:integration</automated>
  </verify>
  <done>Smoke prova degraded-sem-banco; integracao prova migrate+/health-ok contra PG real; menor privilegio provado por teste negativo; any banido ate nos testes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fundacao executavel completa e automatizada: monorepo (01-01), PG DEV + migrations + /health (01-02), CI fail-closed + suite de integracao (tasks 1-2 deste plano). Tudo que OpenCode podia automatizar esta feito e verde.</what-built>
  <how-to-verify>
    1. Na maquina com acesso a VPS/tailnet: `docker compose -f compose.dev.yml up -d postgres-dev`, preencher `.env.dev`, rodar `pnpm install && pnpm --filter @uhhu/db db:migrate && pnpm --filter @uhhu/core-api start` e abrir `curl http://127.0.0.1:3000/health` — esperado `{"status":"ok","db":"ok","version":"0.1.0-fase1","migrationsApplied":1}`.
    2. Rodar `pnpm typecheck && pnpm lint && pnpm test && pnpm test:integration` — esperado exit 0 em tudo.
    3. Validacao do contrato por secoes com Paulo (criterio 4 da fase, D-08): percorrer `dev-docs/07-core-contract.md` por secoes e CONFIRMAR que nenhuma tabela de dominio (`users`, `projects`, `searches`...) entra no schema antes do congelamento. Registrar as secoes validadas e pendentes na resposta.
    4. Confirmar que nenhum segredo foi commitado: `git log --all --oneline | head` + Gitleaks local se disponivel.
  </how-to-verify>
  <resume-signal>Type "approved: <secoes do contrato validadas>" ou descreva ajustes (ex. "approved com ressalva: secao X pendente" / "bloqueado: ...")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → main | CI e o unico porteiro; autobypass (admin merge sem checks) anula todos os gates |
| teste → banco real | Integration usa credenciais de teste; credencial de DEV com senha real nunca entra em log de teste |
| aceite formal → pipeline | Excecao documentada com prazo; supressao silenciosa e indistinguivel de sabotagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | segredos (tree+historico) | mitigate | Gitleaks nos dois modos com fetch-depth 0; `.env*` ignorado; teste de corpo do /health; aceite so via `.planning/aceites-seguranca.md` datado |
| T-03-02 | T | codigo JS/TS (SAST) | mitigate | OpenGrep auto sobre apps/+packages/; falha em high/critical; sem supressao inline sem aceite |
| T-03-03 | T | dependencias (supply chain) | mitigate | `pnpm audit --audit-level high` + `--frozen-lockfile` (lockfile versionado, sem resolucao flutuante no CI) |
| T-03-04 | D/E | bypass do CI (merge sem checks) | mitigate | Branch protection em `main` exigindo o workflow `gates` verde (passo manual do humano no checkpoint: ativar em Settings > Branches); workflow sem `continue-on-error` |
| T-03-05 | I | vazamento em logs de teste | mitigate | migrate.ts com redact de password; teste de menor privilegio asserta falha sem imprimir a URL (regex na mensagem, nunca a string) |
| T-03-06 | R | negacao de aceite (quem liberou o quê) | mitigate | Aceites datados com responsavel+prazo em arquivo versionado; auditoria adversarial pre-patch per AGENTS.md antes de qualquer correcao automatica |
</threat_model>

<verification>
- Push em branch executa `gates.yml` com os 6 jobs verdes (ou falha explicita fail-closed)
- `pnpm test && pnpm test:integration` exit 0 contra PG de service/DEV
- Checkpoint humano respondido com `approved: <secoes>` (contrato validado por secoes, schema segue sem dominio)
- Branch protection de `main` exige `gates` (confirmado pelo humano no checkpoint)
</verification>

<success_criteria>
- FOUND-03 observavel: CI barra segredo/SAST/audit sem aceite e mantem typecheck+lint+testes verdes
- D-11, D-12, D-13, D-14 implementadas sem reducao
- Criterio 4 da fase cumprido: contrato validado por secoes com Paulo, schema de dominio segue proibido
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-executavel/01-03-SUMMARY.md`
</output>
