---
phase: 09-corpus-exportacao-comparacao
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [apps/lab/app/project/[id]/compare.tsx, apps/lab/src/compare/compareHelpers.ts, apps/lab/src/compare/__tests__/compareHelpers.test.ts, apps/lab/app/_layout.tsx, apps/lab/app/project/[id].tsx]
autonomous: true
requirements: [UI-27, UI-28]
must_haves:
  truths:
    - "User compara 2 a 4 buscas lado a lado com total, por ano, por fonte e sobreposicao"
    - "User ve o destaque automatico da estrategia mais inclusiva e nenhum destaque de menos ruido"
    - "Aba Comparacao abre a tabela e o vazio orienta a marcar estrategias"
  artifacts:
    - path: "apps/lab/src/compare/compareHelpers.ts"
      provides: "mais-inclusiva + overlap par-a-par + ordenacao de anos"
      exports: ["mostInclusive", "pairOverlap", "sortedYearRows"]
    - path: "apps/lab/app/project/[id]/compare.tsx"
      provides: "Tabela 2-4 lado a lado com vencedora"
      contains: "mais inclusiva"
    - path: "apps/lab/app/_layout.tsx"
      provides: "Rota compare registrada"
      contains: "project/[id]/compare"
  key_links:
    - from: "apps/lab/app/project/[id]/compare.tsx"
      to: "/api/v1/lab/searches/:searchId/compare"
      via: "labApi.compareSearches base + with"
      pattern: "labApi\\.compareSearches"
    - from: "apps/lab/app/[id].tsx"
      to: "apps/lab/app/project/[id]/compare"
      via: "TabBar Comparacao como Link"
      pattern: "project/\\[id\\]/compare"
---

<objective>
Comparacao 2–4 lado a lado com destaque so-"mais inclusiva" (UI-27, UI-28, esqueleto §9, §14-1).

Purpose: Dar ao pesquisador a tabela estrategia-atual vs estrategia-atual para escolher a base do corpus — sem formula magica de "menos ruido".
Output: `compareHelpers.ts` + testes + tela `compare.tsx` + rota registrada + TabBar ligada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-corpus-exportacao-comparacao/09-CONTEXT.md
@apps/lab/src/api/lab.ts
@apps/lab/app/_layout.tsx
@apps/lab/app/project/[id].tsx
@apps/lab/app/project/[id]/strategies.tsx

<interfaces>
<!-- Contratos que o executor usa direto. -->
From apps/lab/src/api/lab.ts:
```typescript
export const labApi = {
  listSearches(projectId: string, query?: PagedQuery, opts?: LabRequestOptions)
    : Promise<{ items: SearchDTO[]; page: PageInfo }>;
  compareSearches(searchId: string, withIds: string[], opts?: LabRequestOptions): Promise<CompareDTO>;
};
```
From packages/contracts/src/lab.ts (D-48/D-49, exatamente 4 blocos, sem item lists):
```typescript
export interface CompareDTO {
  searches: string[];                       // [base, ...with] na ordem pedida
  totals: Record<string, number>;            // por searchId
  yearHistogram: Record<string, number>;     // GLOBAL (todas as buscas), 'desconhecido' p/ ano null
  bySource: Record<string, Record<'bdtd'|'capes', number>>; // por searchId
  pairwiseOverlap: Record<string, number>;   // chave `${a}|${b}` (ordem de pedido)
}
```
Restricoes honestas (nao inventar): `yearHistogram` e GLOBAL — renderizar como bloco unico "Por ano (todas)"; NAO existe split por-ano-por-busca nem `so-BDTD/so-CAPES/ambas` por busca no DTO — a tabela mostra Resultados (totals), BDTD, CAPES (bySource), Sobreposicao par-a-par (pairwiseOverlap) e Por ano global. Cobertura BDTD/CAPES por busca = as linhas BDTD/CAPES; "Em ambas" entre A e B = overlap(A,B).
Padrao de tela/rota: copiar strategies.tsx (load/401/Empty/Skeleton/FlatList) + registrar `Stack.Screen name="project/[id]/compare"` em `_layout.tsx` + trocar em `[id].tsx` o botao disabled Comparacao por `Link href={{ pathname: '/project/[id]/compare', params: { id: projectId } }}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: helpers da tabela com testes</name>
  <files>apps/lab/src/compare/compareHelpers.ts, apps/lab/src/compare/__tests__/compareHelpers.test.ts</files>
  <read_first>packages/contracts/src/lab.ts (ver CompareDTO + comentarios D-47/D-48/D-49); apps/lab/src/search/pageProgress.ts (ver molde de helper puro com teste existente)</read_first>
  <action>Criar `apps/lab/src/compare/compareHelpers.ts` (per §14-1) com 3 funcoes puras tipadas sem `any`: `export function mostInclusive(totals: Record<string, number>, order: string[]): string | null` (maior total; empate devolve o primeiro em `order`; vazio/ausentes devolve null); `export function pairOverlap(pairwiseOverlap: Record<string, number>, a: string, b: string): number` (tenta `${a}|${b}` depois `${b}|${a}`, ausente = 0); `export function sortedYearRows(yearHistogram: Record<string, number>): Array<{ bucket: string; count: number }>` (anos numericos crescentes, `desconhecido` sempre por ultimo). Teste com 8 its: vencedora simples; empate pelo primeiro; vazio null; overlap nas 2 ordens; overlap ausente 0; anos ordenados com desconhecido no fim; histograma vazio; totals com id fora de order ignorado. Nunca mencionar "menos ruido".</action>
  <acceptance_criteria>`compareHelpers.ts` contem `mostInclusive`, `pairOverlap`, `sortedYearRows` e zero `menos`/`ruído`/`ruido`; teste contem 8 `it(` e zero `any`.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab vitest run src/compare/__tests__/compareHelpers.test.ts 2>&1 | tail -5</automated></verify>
  <done>Helpers puros verdes: vencedora deterministica, overlap bidirecional, anos ordenados</done>
</task>

<task type="auto">
  <name>task 2: tela compare 2-4 com vencedora</name>
  <files>apps/lab/app/project/[id]/compare.tsx</files>
  <read_first>apps/lab/app/project/[id]/strategies.tsx (molde completo load/401/FlatList/Empty/Skeleton); apps/lab/src/compare/compareHelpers.ts (assinaturas da task 1)</read_first>
  <action>Criar `apps/lab/app/project/[id]/compare.tsx` (per §9 + UI-27/UI-28 + §11): secao 1 selecao (FlatList de `listSearches(projectId, { limit: 100 })` com toggle por searchId, contador `N selecionadas (2 a 4)`, botao `Comparar` disabled fora de 2..4); secao 2 tabela (apos `compareSearches(base, rest)` com base = primeira selecionada): `ScrollView horizontal` com colunas por searchId (termo truncado 40 chars no header) e linhas `Resultados` (totals), `BDTD`, `CAPES` (bySource), `Sobreposição` (pairOverlap contra a base), bloco `Por ano (todas)` (sortedYearRows global); destaque `★ mais inclusiva` (via mostInclusive) no header da coluna vencedora — e NENHUM texto "menos ruido"/"menos ruído" em qualquer variacao; vazio inicial `Marque 2+ estratégias para comparar`; loading `CardSkeleton`, erro `ErrorBanner` verbatim + `Repetir`, 401 expired+next `/project/<id>/compare`. Textos de estrategia legiveis via `search.term`. Sem WebView/eval, sem `.map` para listas (FlatList).</action>
  <acceptance_criteria>`compare.tsx` contem `labApi.compareSearches`, `mostInclusive`, `mais inclusiva`, `Marque 2+ estratégias` e `ScrollView`; NAO contem `menos` seguido de `ru` (qualquer acento) nem `exportProject` nem `referenceSearchId` (referencia vem no 09-05).</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab run typecheck 2>&1 | tail -3 && pnpm --filter @uhhu/lab vitest run src/compare 2>&1 | tail -3</automated></verify>
  <done>Tabela 2-4 com 4 blocos do DTO, vencedora destacada, sem menos-ruido, §11 completo</done>
</task>

<task type="auto">
  <name>task 3: registrar rota e ligar a TabBar</name>
  <files>apps/lab/app/_layout.tsx, apps/lab/app/project/[id].tsx</files>
  <read_first>apps/lab/app/_layout.tsx (ver Stack.Screens existentes ate corpus); apps/lab/app/project/[id].tsx (ver TabBar com Comparacao disabled + contador Corpus)</read_first>
  <action>Em `_layout.tsx` adicionar `<Stack.Screen name="project/[id]/compare" options={{ title: 'Comparação' }} />` apos a linha do corpus (mesmo molde, sem mudar as demais). Em `[id].tsx` trocar o botao disabled da TabBar por `Link href={{ pathname: '/project/[id]/compare', params: { id: projectId } }}` com label `Comparação` (manter `Estratégias` e `Corpus: N` intocados, manter `formatCorpusCount` e o fetch de contador). Nao alterar logica de titulo/pergunta/arquivar (07-06 intacto).</action>
  <acceptance_criteria>`_layout.tsx` contem `project/[id]/compare`; `[id].tsx` contem `project/[id]/compare` e NAO contem mais `disabled` na linha da Comparacao; `grep -c "any" apps/lab/app/_layout.tsx apps/lab/app/project/[id].tsx` = 0.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab run lint 2>&1 | tail -3</automated></verify>
  <done>Rota compare navegavel pela TabBar; lint verde; estrategias/corpus intocados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> GET compare | base + with sao UUIDs; servidor valida escopo e devolve so agregados |
| tabela | agregados numericos; termos sao texto do proprio usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | I (IDOR/enumeracao) | compareSearches with | mitigate | with so com IDs listados do proprio projeto; base+with validados como UUID; busca alheia -> 404 generico no servidor; 401 vira expired+next |
| T-09-04-02 | X (XSS) | term/header | mitigate | Text escapa; sem WebView/innerHTML; sem links externos nesta tela |
| T-09-04-03 | I (Fabrico de metrica) | vencedora | accept | mostInclusive e funcao pura do DTO (max de totals); sem "menos ruido" em codigo ou texto (grep gate); manual final fica no 09-05 |
</threat_model>

<verification>
- `pnpm --filter @uhhu/lab vitest run src/compare` verde
- `grep -rin "menos.*ru" apps/lab/app/project/[id]/compare.tsx apps/lab/src/compare/` = 0
- `grep -c "\.map(" apps/lab/app/project/[id]/compare.tsx` = 0
</verification>

<success_criteria>
- 2-4 buscas lado a lado com Resultados/BDTD/CAPES/Sobreposicao/Por-ano-global, ★ mais inclusiva, rota na TabBar, zero menos-ruido
</success_criteria>

<output>
After completion, create `.planning/phases/09-corpus-exportacao-comparacao/09-04-SUMMARY.md`
</output>
