---
phase: 09-corpus-exportacao-comparacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/core-api/src/lib/exports.ts, tests/smoke/dedup-exports.test.ts]
autonomous: true
requirements: [UI-26]
must_haves:
  truths:
    - "BibTeX de trabalho de mestrado profissional exporta @mastersthesis com indicacao de profissional"
    - "Tese continua @phdthesis e dissertacao academica continua @mastersthesis sem marcacao extra"
    - "CSV e JSON de exportacao nao mudam"
  artifacts:
    - path: "apps/core-api/src/lib/exports.ts"
      provides: "toBibTeX com type Mestrado profissional para professionalMaster"
      contains: "Mestrado profissional"
    - path: "tests/smoke/dedup-exports.test.ts"
      provides: "Casos de contrato do note/type do MP"
      contains: "professionalMaster"
  key_links:
    - from: "apps/core-api/src/lib/exports.ts"
      to: "tests/smoke/dedup-exports.test.ts"
      via: "toBibTeX importado com .js suffix"
      pattern: "toBibTeX"
---

<objective>
CORE minimo da fase 9: `professionalMaster` exporta `@mastersthesis` com campo indicando profissional (D-26, UI-26).

Purpose: Fechar o unico cambio de semantica do CORE na fase 9 sem tocar em entry types, CSV ou JSON.
Output: `exports.ts` alterado + 3 casos de teste verdes no smoke existente.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-corpus-exportacao-comparacao/09-CONTEXT.md
@apps/core-api/src/lib/exports.ts
@tests/smoke/dedup-exports.test.ts
@packages/contracts/src/lab.ts

<interfaces>
<!-- Contratos que o executor usa direto, sem explorar a codebase. -->
From packages/contracts/src/lab.ts (`docTypeSchema`, `CorpusEntryDTO`):
```typescript
export const docTypeSchema = z.enum(['masterThesis', 'doctoralThesis', 'professionalMaster']);
export interface CorpusEntryDTO {
  groupId: string; canonicalKey: string; title: string; authors: string[];
  year: number | null; docType: DocType | null; institution: string | null;
  decision: 'eligible' | 'ineligible' | 'undecided'; tags: string[];
  originCount: number; origins: Array<'bdtd' | 'capes'>; memberIds: string[];
  originUrl: string | null; sourceUrl: string | null;
}
```
From apps/core-api/src/lib/exports.ts (assinaturas atuais, linha 117 = ponto D-26):
```typescript
export function toBibTeX(entries: CorpusEntryDTO[]): string;
export function toCSV(entries: CorpusEntryDTO[]): string;
export function toJSON(projectId: string, entries: CorpusEntryDTO[], provenance: unknown): string;
const entryType = e.docType === 'doctoralThesis' ? '@phdthesis' : '@mastersthesis';
```
From tests/smoke/dedup-exports.test.ts (molde `makeEntry`, imports com `.js`):
```typescript
import { toBibTeX, toCSV, toExportJSON } from '../../apps/core-api/src/lib/exports.js';
function makeEntry(overrides: Partial<CorpusEntryDTO> = {}): CorpusEntryDTO { /* ...docType: 'masterThesis' default */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: toBibTeX marca professionalMaster sem novo entry type</name>
  <files>apps/core-api/src/lib/exports.ts</files>
  <read_first>apps/core-api/src/lib/exports.ts (ver linhas 114-133 atuais + escapeBibtex); tests/smoke/dedup-exports.test.ts (ver bloco 'toBibTeX entry types')</read_first>
  <action>Em `toBibTeX` (per D-26), manter exatamente `const entryType = e.docType === 'doctoralThesis' ? '@phdthesis' : '@mastersthesis';` (SEM novo entry type, SEM tocar toCSV/toJSON). No array `fields`, apos a linha `note={decision:...}`, inserir condicional: `...(e.docType === 'professionalMaster' ? [`  type={${escapeBibtex('Mestrado profissional')}}`] : [])`. Nao renomear nada, nao mudar key/escape/protect, nao adicionar dependencia. `docType null` continua `@mastersthesis` sem `type=` (fallback atual preservado).</action>
  <acceptance_criteria>`apps/core-api/src/lib/exports.ts` contem a string literal `Mestrado profissional` exatamente 1 vez; contem `e.docType === 'professionalMaster'`; NAO contem `@mphil`, `@techreport` ou qualquer outro `@` novo; `toCSV`/`toJSON` intocados (grep `toCSV|toJSON` mostra so as funcoes antigas).</acceptance_criteria>
  <verify><automated>pnpm vitest run tests/smoke/dedup-exports.test.ts 2>&1 | tail -5</automated></verify>
  <done>toBibTeX emite `type={Mestrado profissional}` so para professionalMaster; suite smoke existente ainda verde antes dos casos novos</done>
</task>

<task type="auto">
  <name>task 2: casos de contrato MP no smoke existente</name>
  <files>tests/smoke/dedup-exports.test.ts</files>
  <read_first>tests/smoke/dedup-exports.test.ts (ver bloco 'toBibTeX entry types + toExportJSON' + makeEntry); apps/core-api/src/lib/exports.ts (confirmar linha do type= adicionada na task 1)</read_first>
  <action>Acrescentar `describe('toBibTeX professionalMaster (D-26)', ...)` com 3 its literais, usando `makeEntry`: (1) `docType: 'professionalMaster'` gera `@mastersthesis{` E contem `Mestrado profissional`; (2) `docType: 'masterThesis'` gera `@mastersthesis{` e NAO contem `Mestrado profissional`; (3) `docType: 'doctoralThesis'` gera `@phdthesis{` e NAO contem `Mestrado profissional`. Usar `expect(out).toContain(...)` / `expect(out).not.toContain('Mestrado profissional')`. Nao criar arquivo novo de teste, nao tocar nos describes existentes.</action>
  <acceptance_criteria>`tests/smoke/dedup-exports.test.ts` contem `professionalMaster` em ao menos 3 linhas novas; contem o describe literal `toBibTeX professionalMaster (D-26)`; nenhum `any`, `as any` ou `@ts-ignore` adicionado.</acceptance_criteria>
  <verify><automated>pnpm vitest run tests/smoke/dedup-exports.test.ts 2>&1 | tail -5</automated></verify>
  <done>Smoke dedup-exports verde incluindo os 3 casos MP (doctoral/master sem marcacao, professional com marcacao)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export serializer -> HTTP attachment | strings formatadas viram arquivo baixado; injection aqui vira formula/BibTeX malformado no cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | I (Tampering) | toBibTeX `type=` | mitigate | valor literal fixo via `escapeBibtex`, sem interpolar input do usuario; teste prova que so professionalMaster emite |
| T-09-01-02 | I (Injection) | BibTeX/CSV | accept | guards existentes (csvCell anti `=+@`, escapeBibtex, brace-protection) intocados; nenhum campo novo com input livre |
</threat_model>

<verification>
- `pnpm vitest run tests/smoke/dedup-exports.test.ts` verde
- `grep -n "Mestrado profissional" apps/core-api/src/lib/exports.ts` = 1 ocorrencia
- `grep -c "@mphil\|@techreport" apps/core-api/src/lib/exports.ts` = 0
</verification>

<success_criteria>
- professionalMaster -> `@mastersthesis` + `type={Mestrado profissional}`; demais tipos inalterados; CSV/JSON byte-identicos
</success_criteria>

<output>
After completion, create `.planning/phases/09-corpus-exportacao-comparacao/09-01-SUMMARY.md`
</output>
