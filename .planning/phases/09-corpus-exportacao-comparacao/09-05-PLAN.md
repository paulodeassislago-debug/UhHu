---
phase: 09-corpus-exportacao-comparacao
plan: 05
type: execute
wave: 2
depends_on: [09-04]
files_modified: [apps/lab/src/search/SearchCard.tsx, apps/lab/app/project/[id]/strategies.tsx, apps/lab/app/project/[id]/compare.tsx]
autonomous: true
requirements: [UI-29]
must_haves:
  truths:
    - "User ve o selo Referencia no card da estrategia de referencia"
    - "User troca a referencia com um toque sem confirmacao e a coluna do compare destaca"
    - "Referencia nao altera corpus, filtros ou resultados"
  artifacts:
    - path: "apps/lab/src/search/SearchCard.tsx"
      provides: "Selo Referencia + acao de um toque"
      contains: "Referência"
    - path: "apps/lab/app/project/[id]/strategies.tsx"
      provides: "Leitura e troca de referenceSearchId do projeto"
      contains: "referenceSearchId"
  key_links:
    - from: "apps/lab/app/project/[id]/strategies.tsx"
      to: "/api/v1/projects/:id"
      via: "projectsApi.update referenceSearchId"
      pattern: "referenceSearchId"
    - from: "apps/lab/app/project/[id]/compare.tsx"
      to: "/api/v1/projects/:id"
      via: "listById para coluna destacada"
      pattern: "referenceSearchId"
---

<objective>
Estrategia de referencia manual: selo no card + coluna destacada, troca em um toque (UI-29, D-24, D-25).

Purpose: Memoria visivel do projeto sobre qual estrategia baseia o corpus — sem efeito funcional em corpus/filtros/resultados.
Output: `SearchCard` com selo + `strategies.tsx` com leitura/troca + coluna destacada no `compare.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-corpus-exportacao-comparacao/09-CONTEXT.md
@.planning/phases/09-corpus-exportacao-comparacao/09-04-PLAN.md
@apps/lab/src/search/SearchCard.tsx
@apps/lab/app/project/[id]/strategies.tsx
@apps/lab/app/project/[id]/compare.tsx
@apps/lab/src/api/projects.ts

<interfaces>
<!-- Contratos que o executor usa direto. -->
From apps/lab/src/api/projects.ts:
```typescript
export const projectsApi = {
  listById(id: string, opts?: ProjectsRequestOptions): Promise<ProjectDTO>;
  update(id: string, patch: UpdateProjectInput, opts?: ProjectsRequestOptions): Promise<ProjectDTO>;
};
```
From packages/contracts/src/projects.ts: `updateProjectSchema` aceita `{ referenceSearchId: string(uuid).nullable().optional() }`; `ProjectDTO.referenceSearchId: string | null`; null limpa. Pertencimento mesmo projectId+owner e application-level no servidor (404 IDOR).
From apps/lab/src/search/SearchCard.tsx (props atuais): `{ search: SearchDTO; getToken: TokenProvider; onChanged: () => void; onDeleted: (searchId: string) => void }` — estender com `projectId: string; isReference: boolean; onSetReference: (searchId: string) => void` (sem quebrar chamadores: atualizar strategies.tsx junto).
Padrao PATCH parcial (copiar 07-06 titulo): PATCH so o campo, estados independentes, erro verbatim sem fechar, 401 expired+next.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: selo Referencia no SearchCard com um toque</name>
  <files>apps/lab/src/search/SearchCard.tsx</files>
  <read_first>apps/lab/src/search/SearchCard.tsx (ver props, bloco Comparar disabled, historico e 401); apps/lab/src/api/projects.ts (ver update com referenceSearchId)</read_first>
  <action>Estender `SearchCardProps` (per D-24) com `projectId: string; isReference: boolean; onSetReference: (searchId: string) => void`. No card, apos a linha de runs: quando `isReference` renderizar `Text` literal `Referência` (com acento, para o grep do gate); quando falso renderizar `Button title="Usar como referência" onPress={() => onSetReference(search.id)}` (mutavel, SEM confirmacao, SEM Alert). Manter `[Comparar]` disabled com hint fase 9 intocado nesta task (o compare abre pela TabBar do 09-04). Nao chamar API dentro do card (o PATCH mora em strategies.tsx); nao filtrar nada por referencia (D-25).</action>
  <acceptance_criteria>`SearchCard.tsx` contem `Referência`, `Usar como referência`, `isReference` e `onSetReference`; NAO contem `Alert`, `confirm`, `referenceSearchId` em filtro (`filter.*reference`) ou query de corpus/resultados.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab run typecheck 2>&1 | tail -3</automated></verify>
  <done>Card exibe selo ou acao de um toque via props, sem API e sem confirmacao</done>
</task>

<task type="auto">
  <name>task 2: strategies le e troca a referencia + compare destaca a coluna</name>
  <files>apps/lab/app/project/[id]/strategies.tsx, apps/lab/app/project/[id]/compare.tsx</files>
  <read_first>apps/lab/app/project/[id]/strategies.tsx (ver load de searches + handleDeleted para o molde); apps/lab/app/project/[id]/compare.tsx (ver tabela e headers criados no 09-04)</read_first>
  <action>Em `strategies.tsx` (per D-24/D-25 + UI-29): no `load`, `Promise.all` de `listSearches` + `projectsApi.listById(projectId, { getToken })` guardando `referenceSearchId: string | null`; passar a cada `SearchCard` `projectId`, `isReference={item.id === referenceSearchId}`, `onSetReference` que faz `projectsApi.update(projectId, { referenceSearchId: searchId }, { getToken })` e atualiza o estado local com o DTO (otimista revertido em erro verbatim no topo, 401 expired+next da lista). Vazio de referencia (null) = nenhum selo. Em `compare.tsx`: fetch `projectsApi.listById` no mount e no header da coluna cujo id === referenceSearchId acrescentar `Referência` + destaque visual (ex. `fontWeight: '700'`/borda — sem paleta nova, so peso/borda); nada mais muda na tabela (vencedora ★ do 09-04 intacta). Em nenhum arquivo usar referenceSearchId para filtrar corpus/resultados/export (grep gate D-25).</action>
  <acceptance_criteria>`strategies.tsx` contem `referenceSearchId` e `projectsApi.update`; `compare.tsx` contem `Referência` na coluna; nenhum dos 3 arquivos contem `filter` + `referenceSearchId` na mesma linha; zero `any`.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab run typecheck 2>&1 | tail -3 && pnpm --filter @uhhu/lab run lint 2>&1 | tail -3 && pnpm --filter @uhhu/lab vitest run src/projects src/search 2>&1 | tail -3</automated></verify>
  <done>Referencia persiste via PATCH, selo visivel na lista e coluna destacada no compare, sem efeito funcional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PATCH projects | referenceSearchId e UUID de search; servidor valida pertencimento mesmo projeto+owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | I (IDOR) | referenceSearchId cross-projeto | mitigate | servidor 404 quando search.projectId != projectId ou owner difere; UI so oferece IDs da lista do proprio projeto; 401 expired+next |
| T-09-05-02 | T (Tampering memoria) | troca sem confirmacao | accept | decisao D-24 explicita (mutavel, sem confirmacao); re-PATCH reverte em 1 toque; sem efeito em corpus/filtros (D-25 provado por grep) |
</threat_model>

<verification>
- `grep -n "Referência" apps/lab/src/search/SearchCard.tsx apps/lab/app/project/[id]/compare.tsx` >= 2 ocorrencias
- `grep -rn "referenceSearchId" apps/lab/app apps/lab/src --include="*.tsx" --include="*.ts" | grep -i "filter\|where\|query.*corpus\|corpus.*filter"` = 0 (D-25)
- typecheck+lint verdes; `vitest run src/projects src/search` verde
</verification>

<success_criteria>
- Selo Referencia no card, troca em 1 toque persistida, coluna destacada no compare, referencia sem efeito funcional
</success_criteria>

<output>
After completion, create `.planning/phases/09-corpus-exportacao-comparacao/09-05-SUMMARY.md`
</output>
