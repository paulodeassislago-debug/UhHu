---
phase: 09-corpus-exportacao-comparacao
plan: 06
type: execute
wave: 3
depends_on: [09-01, 09-03, 09-05]
files_modified: []
autonomous: false
requirements: [UI-23, UI-24, UI-25, UI-26, UI-27, UI-28, UI-29]
must_haves:
  truths:
    - "Slice vertical login -> corpus/export funciona na web beta com dados reais"
    - "Auditoria adversarial do app nao encontra critico nem alto aberto"
    - "Humano aprova corpus, export e comparacao no tablet"
  artifacts:
    - path: "apps/lab/app/project/[id]/corpus.tsx"
      provides: "Corpus + export provados na beta"
    - path: "apps/lab/app/project/[id]/compare.tsx"
      provides: "Comparacao + referencia provadas na beta"
  key_links:
    - from: "apps/lab/app/project/[id]/corpus.tsx"
      to: "/api/v1/lab/projects/:projectId/export"
      via: "attachment real na beta via curl + app"
      pattern: "attachment"
---

<objective>
Gate do milestone: slice vertical provado na beta + auditoria adversarial + revisao humana (success criteria 3 da fase 9).

Purpose: Provar que o pesquisador fecha corpus/export/comparacao de ponta a ponta no canal beta antes de encerrar o v1.1.
Output: Evidencia de beta + relatorio de auditoria + aceite humano registrados no SUMMARY.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-corpus-exportacao-comparacao/09-CONTEXT.md
@dev-docs/08-security-baseline.md
@apps/lab/src/api/client.ts
@apps/core-api/src/routes/lab.ts

<interfaces>
<!-- Alvos e comandos que o executor usa direto. -->
Beta (molde 06-04): app web exportado servido em porta beta (ex. 3009) com `EXPO_PUBLIC_API_BASE_URL` apontando a API; API com `CORS_ALLOWED_ORIGINS` incluindo a origem beta exata (allowlist, sem espelho). Prova via `curl`: login cookie -> `GET /projects` -> `GET /lab/projects/:id/corpus` -> `GET /lab/projects/:id/export?format=csv|bibtex|json&scope=corpus` (espera `Content-Disposition: attachment; filename="corpus-*.csv|bib|json"`) -> `GET /lab/searches/:id/compare?with=<id>` (4 blocos). IDOR: dono 200, estranho 404, adulterado 404, sem auth 401 — nas 4 superficies (corpus/export/compare/PATCH reference).
Gates do app: `pnpm --filter @uhhu/lab run typecheck`, `run lint`, `vitest run` (82+ novos do 09-02/03/04), `grep -c any` = 0, `pnpm audit`, Gitleaks/SAST via CI quando sem binario local (documentar).
Auditoria adversarial (AGENTS.md): arquivo por arquivo cobrindo autorizacao decorativa, IDOR, confianca no navegador, segredos, XSS/input/upload/SSRF (links so-https), sessoes, rate limit, SQL/command injection, isolamento entre usuarios. Achados criticos/altos bloqueiam o checkpoint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: gates + prova beta do slice vertical</name>
  <files></files>
  <read_first>dev-docs/08-security-baseline.md (gates minimos); apps/core-api/src/routes/lab.ts (rotas corpus/export/compare + codigos 401/404); apps/lab/src/api/client.ts (cookie web vs Bearer nativo)</read_first>
  <action>Rodar e registrar saidas literais: `pnpm --filter @uhhu/lab run typecheck`, `pnpm --filter @uhhu/lab run lint`, `pnpm --filter @uhhu/lab vitest run` (inclui corpusFilters/exportDelivery/compareHelpers), `pnpm vitest run tests/smoke/dedup-exports.test.ts`, `grep -rn "as any\|: any\|@ts-ignore" apps/lab/src/corpus apps/lab/src/export apps/lab/src/compare apps/lab/app/project/ | wc -l` (= 0), `grep -rn "expo-sharing\|expo-file-system" apps/lab/` (= 0), `grep -rin "menos.*ru" apps/lab/app/project/[id]/compare.tsx apps/lab/src/compare/` (= 0). Depois prova beta com dados reais: subir API+web beta, `curl -i` login -> corpus (contador vivo = elegiveis) -> export csv+bibtex+json conferindo `Content-Disposition: attachment` + BibTeX do MP com `Mestrado profissional` -> compare 2 buscas conferindo 4 blocos + ★ mais inclusiva na UI -> PATCH referenceSearchId + selo visivel. Registrar matriz IDOR 4x4 (dono/estranho/adulterado/sem-auth x corpus/export/compare/reference) com codigos.</action>
  <acceptance_criteria>SUMMARY contera: typecheck exit 0, lint exit 0, vitest lab verde com contagem, smoke MP verde, 3 greps = 0, 3 attachments `corpus-*` com content-types, compare com 4 blocos, matriz IDOR 16 celulas com 200/404/404/401.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab run typecheck 2>&1 | tail -3 && pnpm --filter @uhhu/lab vitest run src/corpus src/export src/compare 2>&1 | tail -5</automated></verify>
  <done>Gates verdes e slice login->corpus/export/compare provado na beta com matriz IDOR completa</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Corpus com filtros + export 3 formatos por plataforma + compare 2-4 com mais-inclusiva + referencia manual (09-01..09-05 automatizaram tudo acima)</what-built>
  <how-to-verify>
    1. Abrir a web beta e logar; abrir um projeto com elegiveis.
    2. Aba Corpus: conferir contador vivo; filtrar por tag, fonte e ano; vazio `Nenhum item elegível ainda` quando zerar elegiveis.
    3. Selecionar 2 grupos inteiros e Exportar seleção CSV; depois Exportar corpus completo BibTeX e conferir `@mastersthesis` do MP com `Mestrado profissional`; BibTeX vazio mostra o aviso sem baixar.
    4. No tablet/nativo: repetir um export e conferir o Share nativo.
    5. Aba Comparação: selecionar 2-4 buscas, conferir ★ mais inclusiva e nenhuma menção a menos-ruído; marcar referência e conferir selo `Referência` no card + coluna destacada; trocar 1 toque sem confirmação.
    6. Auditoria adversarial do executor anexada ao SUMMARY sem critico/alto aberto (ou com aceite formal).
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva os ajustes (tela + comportamento esperado)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| beta -> API | origem aprovada via CORS exato; auth cookie/PAT inalterados |
| humano -> aceite | ultimo gate antes de encerrar o milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | S/E (AuthZ) | corpus/export/compare/reference | mitigate | matriz IDOR 4x4 executada na beta; owner do ator no servidor; RLS (se houver) so defesa em profundidade |
| T-09-06-02 | I (Secrets/logs) | beta + SUMMARY | mitigate | Gitleaks tree+historico, bundle sem segredos, logs sem cookie/Authorization/signed-URL; `pnpm audit` sem alto sem tratamento |
</threat_model>

<verification>
- Gates + beta + IDOR registrados no SUMMARY com saidas literais
- Auditoria adversarial anexada sem critico/alto aberto
- Checkpoint humano `approved`
</verification>

<success_criteria>
- Slice vertical provado na beta, auditoria sem critico/alto, UAT humana aprovada — milestone v1.1 pronto para encerrar
</success_criteria>

<output>
After completion, create `.planning/phases/09-corpus-exportacao-comparacao/09-06-SUMMARY.md`
</output>
