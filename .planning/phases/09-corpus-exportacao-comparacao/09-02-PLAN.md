---
phase: 09-corpus-exportacao-comparacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [apps/lab/app/project/[id]/corpus.tsx, apps/lab/src/corpus/corpusFilters.ts, apps/lab/src/corpus/__tests__/corpusFilters.test.ts]
autonomous: true
requirements: [UI-23, UI-24]
must_haves:
  truths:
    - "User ve o corpus como lista derivada dos elegiveis com contador vivo que muda apos cada decisao"
    - "User filtra o corpus por tag, fonte e ano e ve o vazio orientador quando nada casa"
    - "Lista usa FlatList virtualizada com skeleton, erro com repetir e 401 que preserva o projeto"
  artifacts:
    - path: "apps/lab/src/corpus/corpusFilters.ts"
      provides: "Filtro puro tag/fonte/ano com semantica AND"
      exports: ["filterCorpus"]
    - path: "apps/lab/app/project/[id]/corpus.tsx"
      provides: "Aba Corpus real (contador vivo + filtros + FlatList)"
      contains: "Nenhum item elegível ainda"
  key_links:
    - from: "apps/lab/app/project/[id]/corpus.tsx"
      to: "/api/v1/lab/projects/:projectId/corpus"
      via: "labApi.getCorpus com getToken"
      pattern: "labApi\\.getCorpus"
    - from: "apps/lab/app/project/[id]/corpus.tsx"
      to: "apps/lab/src/corpus/corpusFilters.ts"
      via: "import do filtro puro"
      pattern: "filterCorpus"
---

<objective>
Aba Corpus real: view derivada dos elegiveis com contador vivo + filtros tag/fonte/ano (UI-23, UI-24, esqueleto §10).

Purpose: Materializar "corpus = view derivada" na UI — sem tela de criar corpus, so a contagem que reflete decisoes — com filtros leves client-side espelhando D-17.
Output: `corpusFilters.ts` puro + testes + `corpus.tsx` reescrito sobre `labApi.getCorpus`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-corpus-exportacao-comparacao/09-CONTEXT.md
@apps/lab/app/project/[id]/corpus.tsx
@apps/lab/src/api/lab.ts
@apps/lab/src/projects/counts.ts
@apps/lab/src/results/triage.ts
@apps/lab/app/project/[id]/strategies.tsx

<interfaces>
<!-- Contratos que o executor usa direto. -->
From apps/lab/src/api/lab.ts:
```typescript
export const labApi = {
  getCorpus(projectId: string, query?: PagedQuery, opts?: LabRequestOptions)
    : Promise<{ items: CorpusEntryDTO[]; page: PageInfo }>;
  exportProject(projectId, format, scope, selection, opts): Promise<string>; // raw, usado no 09-03, NAO aqui
};
export interface PagedQuery { limit?: number; cursor?: string; }
```
From packages/contracts/src/lab.ts:
```typescript
export interface CorpusEntryDTO {
  groupId: string; title: string; authors: string[]; year: number | null;
  docType: DocType | null; institution: string | null; decision: 'eligible'|'ineligible'|'undecided';
  tags: string[]; originCount: number; origins: Array<'bdtd'|'capes'>; memberIds: string[];
}
```
From apps/lab/src/projects/counts.ts:
```typescript
export function formatCorpusCount(itemCount: number, hasMore: boolean): string;
```
Padrao de tela (copiar de strategies.tsx): `useAuth()` + `labApi.*(projectId, { limit: 100 }, { getToken })` + estados `loading|ready|error` + `Empty`/`ErrorBanner`/`CardSkeleton` + `ApiError status 401 -> markExpired + router.replace({ pathname: '/login', params: { expired: '1', next: '/project/<id>/corpus' } })` + `FlatList keyExtractor={groupId}`. Proibido: `.map` para renderizar listas (tripwire scroll-containers), `any`/`as any`, tipos locais de dominio (usar `import type` de `@uhhu/contracts`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: filtro puro do corpus com testes</name>
  <files>apps/lab/src/corpus/corpusFilters.ts, apps/lab/src/corpus/__tests__/corpusFilters.test.ts</files>
  <read_first>apps/lab/src/results/triage.ts (ver semantica AND dos filtros D-17 + rotulos); apps/lab/src/projects/counts.ts (ver formatCorpusCount para nao duplicar)</read_first>
  <action>Criar `apps/lab/src/corpus/corpusFilters.ts` (per CONTEXT Discretion + UI-24) exportando `export interface CorpusFilter { tag?: string; source?: 'bdtd' | 'capes'; year?: number; }` e `export function filterCorpus(entries: CorpusEntryDTO[], filter: CorpusFilter): CorpusEntryDTO[]` com semantica AND: tag presente exige `e.tags.includes(tag)`; source presente exige `e.origins.includes(source)`; year presente exige `e.year === year` (year null nunca casa com numero). Filtro vazio `{}` devolve copia na mesma ordem. Importar `import type { CorpusEntryDTO } from '@uhhu/contracts'` (nunca redefinir). Criar `apps/lab/src/corpus/__tests__/corpusFilters.test.ts` com 7 its: vazio devolve tudo; so-tag; so-fonte; so-ano; combinacao AND que zera; year null nao casa; array vazio devolve vazio. Sem `any`.</action>
  <acceptance_criteria>`apps/lab/src/corpus/corpusFilters.ts` contem `export function filterCorpus`; `__tests__/corpusFilters.test.ts` contem 7 `it(`; grep `\.map\(` no teste so aparece para montar fixtures, nunca para render; zero `any` nos 2 arquivos.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab vitest run src/corpus/__tests__/corpusFilters.test.ts 2>&1 | tail -5</automated></verify>
  <done>Filtro puro com AND tag/fonte/ano verde em 7 casos; sem logica de rede ou UI neste arquivo</done>
</task>

<task type="auto">
  <name>task 2: corpus.tsx real com contador vivo e filtros</name>
  <files>apps/lab/app/project/[id]/corpus.tsx</files>
  <read_first>apps/lab/app/project/[id]/corpus.tsx (placeholder atual a substituir); apps/lab/app/project/[id]/strategies.tsx (molde load/401/FlatList/Empty/Skeleton); apps/lab/src/corpus/corpusFilters.ts (assinatura criada na task 1)</read_first>
  <action>Reescrever `corpus.tsx` (per D-25 referencia NAO filtra nada + UI-23/UI-24 + §10/§11): header `Corpus: {formatCorpusCount(items.length, page.hasMore)}` (contagem do GET, nunca de filtro local); busca `labApi.getCorpus(projectId, { limit: 100 }, { getToken })`; filtros client-side via `filterCorpus` com 3 controles simples (TextInput tag, 2 botoes BDTD/CAPES toggle, TextInput ano numerico com parse int e ignora NaN); `FlatList` com `keyExtractor={(g) => g.groupId}`, linha mostrando titulo + ano ou `s/ano` + instituicao + origens `BDTD+CAPES` + tags + checkbox de selecao por GRUPO INTEIRO (`selected: Set<string>` de groupIds, toggle por groupId, texto `Selecionar`/`Selecionado` — D-14-2, um trabalho uma referencia; a BARRA de exportar vem no 09-03, aqui so o estado + `selectedIds` derivado); vazio do corpus `Nenhum item elegível ainda — trie os resultados primeiro`, vazio de filtro `Nenhum item com estes filtros`; loading `CardSkeleton count={3}`, erro `ErrorBanner` verbatim + `Repetir`, 401 com `markExpired` + redirect `/login?expired=1&next=/project/<id>/corpus`. Exportar `selectedIds: string[]` via estado local (o 09-03 le e adiciona a barra). Sem `Share`/download aqui.</action>
  <acceptance_criteria>`corpus.tsx` contem `labApi.getCorpus`, `filterCorpus`, `formatCorpusCount`, `Nenhum item elegível ainda` e `keyExtractor`; contem `selected` por groupId e NAO contem `exportProject`, `Share`, `expo-sharing`, `expo-file-system` ou `referenceSearchId`.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab run typecheck 2>&1 | tail -3 && pnpm --filter @uhhu/lab run lint 2>&1 | tail -3</automated></verify>
  <done>Corpus lista elegiveis com contador vivo, 3 filtros AND, selecao por grupo inteiro, §11 completo, typecheck+lint verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> GET corpus | projectId da rota; dados do servidor sao hostis ate render |
| filtro local | nunca decide elegibilidade; so oculta visualmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | I (IDOR) | corpus.tsx projectId | mitigate | projectId so da rota, auth via getToken/cookie; 401 vira expired+next; 404 alheio vira erro generico sem distinguir; server deriva owner do ator |
| T-09-02-02 | X (XSS) | titulo/tags/instituicao | mitigate | `Text` escapa por padrao; sem WebView/`dangerouslySetInnerHTML`/`eval`; divergences/links nao renderizados aqui |
| T-09-02-03 | S (Spoofing filtro) | filterCorpus | accept | filtro e UX puro; corpus real vem do servidor (eligible); D-25 honrado: referencia nunca entra no filtro (grep gate no 09-05) |
</threat_model>

<verification>
- `pnpm --filter @uhhu/lab vitest run src/corpus` verde
- `grep -c "\.map(" apps/lab/app/project/[id]/corpus.tsx` = 0 (listas so via FlatList)
- `grep -c "any" apps/lab/src/corpus/corpusFilters.ts apps/lab/app/project/[id]/corpus.tsx` = 0
</verification>

<success_criteria>
- Corpus exibe elegiveis com contador vivo, filtra tag/fonte/ano com vazios orientadores, selecao por grupo inteiro pronta para o 09-03
</success_criteria>

<output>
After completion, create `.planning/phases/09-corpus-exportacao-comparacao/09-02-SUMMARY.md`
</output>
