---
phase: 09-corpus-exportacao-comparacao
plan: 03
type: execute
wave: 2
depends_on: [09-02]
files_modified: [apps/lab/src/export/exportDelivery.ts, apps/lab/src/export/__tests__/exportDelivery.test.ts, apps/lab/app/project/[id]/corpus.tsx]
autonomous: true
requirements: [UI-25, UI-26]
must_haves:
  truths:
    - "User seleciona itens por grupo deduplicado inteiro e exporta a selecao em CSV, BibTeX ou JSON"
    - "User exporta o corpus completo nos 3 formatos com entrega certa por plataforma"
    - "BibTeX vazio avisa em vez de exportar vazio"
  artifacts:
    - path: "apps/lab/src/export/exportDelivery.ts"
      provides: "Entrega blob+anchor na web e Share nativo, sem dep nova"
      exports: ["downloadExportFile", "mimeForFormat"]
    - path: "apps/lab/app/project/[id]/corpus.tsx"
      provides: "Barra exportar selecao + corpus completo com 3 formatos"
      contains: "Exportar corpus completo"
  key_links:
    - from: "apps/lab/app/project/[id]/corpus.tsx"
      to: "/api/v1/lab/projects/:projectId/export"
      via: "labApi.exportProject raw verbatim"
      pattern: "labApi\\.exportProject"
    - from: "apps/lab/app/project/[id]/corpus.tsx"
      to: "apps/lab/src/export/exportDelivery.ts"
      via: "downloadExportFile do conteudo raw"
      pattern: "downloadExportFile"
---

<objective>
Exportacao na UI: selecao por grupo inteiro + 3 formatos sempre + entrega por plataforma (UI-25, UI-26, D-22, D-23).

Purpose: Fechar o slice corpus/export — o pesquisador sai com o arquivo na web beta e no tablet sem dependencia nova.
Output: `exportDelivery.ts` + testes + barra de exportar dentro de `corpus.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-corpus-exportacao-comparacao/09-CONTEXT.md
@.planning/phases/09-corpus-exportacao-comparacao/09-02-PLAN.md
@apps/lab/app/project/[id]/corpus.tsx
@apps/lab/src/api/lab.ts
@apps/lab/src/api/client.ts

<interfaces>
<!-- Contratos que o executor usa direto. -->
From apps/lab/src/api/lab.ts:
```typescript
export type ExportFormat = 'csv' | 'bibtex' | 'json';
export type ExportScope = 'corpus' | 'selection';
export const labApi = {
  exportProject(projectId: string, format: ExportFormat, scope: ExportScope, selection: string | undefined, opts?: LabRequestOptions): Promise<string>; // raw verbatim
};
```
From packages/contracts/src/lab.ts (`exportQuerySchema`): `format: csv|bibtex|json`, `scope: corpus|selection`, `selection: string (CSV de UUIDs, max 40000) opcional`; scope=selection exige selection nao-vazia no servidor (400 PT-BR). UI monta `selection = selectedIds.join(',')` (groupIds UUID) e so chama com `selectedIds.length > 0`.
Entrega (D-22, zero dep nova): web = `Blob` + `URL.createObjectURL` + anchor `download`; nativo = `Share` de `react-native` (built-in). PROIBIDO: `expo-sharing`, `expo-file-system`, qualquer `+expo-*` novo em package.json.
Padrao 401/erro: mesmo molde strategies.tsx (`ApiError`, verbatim + requestId, `markExpired` + `/login?expired=1&next=/project/<id>/corpus`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: entrega por plataforma sem dependencia nova</name>
  <files>apps/lab/src/export/exportDelivery.ts, apps/lab/src/export/__tests__/exportDelivery.test.ts</files>
  <read_first>apps/lab/src/api/lab.ts (ver exportProject raw + ExportFormat/ExportScope); apps/lab/src/auth/secureToken.ts (ver branch Platform.OS web-vs-nativo existente)</read_first>
  <action>Criar `apps/lab/src/export/exportDelivery.ts` (per D-22) com: `export function mimeForFormat(format: 'csv'|'bibtex'|'json'): string` retornando `text/csv; charset=utf-8`, `application/x-bibtex; charset=utf-8`, `application/json; charset=utf-8`; `export function buildExportFilename(projectTitle: string, dateYYYYMMDD: string, ext: 'csv'|'bib'|'json'): string` com slug ASCII proprio (`normalize NFKD, remove diacriticos, lower, [^a-z0-9]+ -> '-', trim '-', slice 60, fallback 'projeto'`, formato `corpus-<slug>-<data>.<ext>`); `export function isExportEmpty(format, content: string, count: number): boolean` retornando true quando `count === 0` ou `content.trim().length === 0` (D-23, BibTeX vazio avisa); `export async function downloadExportFile(content: string, filename: string, mime: string): Promise<void>` com branch `Platform.OS === 'web'` usando `Blob`+anchor+revoke, senao `Share.share({ message: content, title: filename })` de `react-native` (import estatico `import { Platform, Share } from 'react-native'`). Nenhum import de `expo-sharing`/`expo-file-system`. Teste com 6 its: 3 mimes, slug com `São Paulo "../../etc"` vira `corpus-sao-paulo-etc-<data>.csv` sem `..`/`/`/`"`, empty true com count 0, empty false com conteudo.</action>
  <acceptance_criteria>`exportDelivery.ts` contem `mimeForFormat`, `buildExportFilename`, `isExportEmpty`, `downloadExportFile` e `from 'react-native'`; NAO contem `expo-sharing` nem `expo-file-system`; teste contem 6 `it(` e zero `any`.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab vitest run src/export/__tests__/exportDelivery.test.ts 2>&1 | tail -5</automated></verify>
  <done>Delivery puro testado por plataforma sem dep nova; vazio detectavel antes de chamar a API</done>
</task>

<task type="auto">
  <name>task 2: barra exportar selecao + completo no corpus</name>
  <files>apps/lab/app/project/[id]/corpus.tsx</files>
  <read_first>apps/lab/app/project/[id]/corpus.tsx (estado selectedIds + FlatList criados no 09-02); apps/lab/src/export/exportDelivery.ts (assinaturas da task 1)</read_first>
  <action>Editar `corpus.tsx` (per D-23 + UI-25/UI-26 + §10): acima da lista, barra com formato atual (`format: ExportFormat`, default `'csv'`, 3 botoes CSV/BibTeX/JSON) + texto `Seleção: {selectedIds.length} de {items.length}` + 2 botoes `[Exportar seleção]` (disabled quando `selectedIds.length === 0`) e `[Exportar corpus completo]`. Handler selecao: se `format === 'bibtex' && selectedIds.length === 0` mostrar aviso `Nada para exportar em BibTeX — selecione ao menos um item.` sem request (D-23); senao `labApi.exportProject(projectId, format, 'selection', selectedIds.join(','), { getToken })`. Handler completo: `labApi.exportProject(projectId, format, 'corpus', undefined, { getToken })`. Ambos: `busy` desabilita os 2 botoes, sucesso chama `downloadExportFile(content, buildExportFilename(projectTitle ?? 'projeto', hojeYYYYMMDD, extParaArquivo), mimeForFormat(format))` onde extParaArquivo mapeia bibtex->bib; erro vira `ErrorBanner` verbatim + requestId; 401 vira expired+next do corpus. `projectTitle` vem de `projectsApi.listById` (fetch unico no mount, erro nao quebra a lista). Nunca re-serializar o raw; nunca filtrar por referencia.</action>
  <acceptance_criteria>`corpus.tsx` contem `Exportar corpus completo`, `Exportar seleção`, `Nada para exportar em BibTeX`, `downloadExportFile` e `labApi.exportProject`; NAO contem `expo-sharing`, `expo-file-system` ou `referenceSearchId`; `grep "expo-sharing\|expo-file-system" apps/lab/package.json` vazio.</acceptance_criteria>
  <verify><automated>pnpm --filter @uhhu/lab run typecheck 2>&1 | tail -3 && pnpm --filter @uhhu/lab vitest run src/export 2>&1 | tail -3</automated></verify>
  <done>Selecao (grupo inteiro) e corpus completo exportam nos 3 formatos com entrega certa por plataforma; BibTeX vazio avisa</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> GET export | selection = CSV de UUIDs; servidor valida e resolve escopo |
| raw -> arquivo local | conteudo verbatim vira Blob/Share; nunca parseado como codigo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | I (IDOR) | exportProject selection | mitigate | selection so com groupIds visiveis do proprio corpus; owner derivado no servidor; 401 expired+next; teste manual dono/estranho/adulterado no 09-06 |
| T-09-03-02 | I (Injection) | filename/mime | mitigate | slug ASCII proprio + ext de allowlist (`csv|bib|json`); mime de mapa fixo; anchor `download` sem injetar HTML |
| T-09-03-03 | D (DoS) | export gigante | accept | limite 100 do corpus na UI + limite 1000 IDs no servidor; sem retry automatico; busy trava duplo-toque |
</threat_model>

<verification>
- `grep -rn "expo-sharing\|expo-file-system" apps/lab/` = 0
- `grep -c "any" apps/lab/src/export/exportDelivery.ts apps/lab/app/project/[id]/corpus.tsx` = 0
- `pnpm --filter @uhhu/lab vitest run src/export` verde
</verification>

<success_criteria>
- Exportar selecao (grupo inteiro) e corpus completo em CSV/BibTeX/JSON com blob+anchor na web e Share no nativo; BibTeX vazio avisa sem request
</success_criteria>

<output>
After completion, create `.planning/phases/09-corpus-exportacao-comparacao/09-03-SUMMARY.md`
</output>
