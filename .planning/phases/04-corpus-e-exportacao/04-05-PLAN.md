---
phase: 04-corpus-e-exportacao
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - scripts/curl-corpus.sh
autonomous: false
requirements: [LAB-06, LAB-07, LAB-08, LAB-09, LAB-10, LAB-11]
must_haves:
  truths:
    - "Pesquisador revisa, decide, compara e exporta o corpus com proveniencia sem tocar o banco"
    - "Lista de resultados por run + ficha individual continuam funcionando (regressao LAB-06)"
    - "Humano aprova busca viva BDTD com corpus real antes de fechar a fase"
  artifacts:
    - path: "scripts/curl-corpus.sh"
      provides: "Prova curl dedup→decisao→corpus→compare→export + IDOR 404/401"
  key_links:
    - from: "scripts/curl-corpus.sh"
      to: "/api/v1/lab/projects/:projectId/corpus"
      via: "curl GET com cookie do dono"
      pattern: "corpus"
    - from: "scripts/curl-corpus.sh"
      to: "/api/v1/lab/projects/:projectId/export"
      via: "curl GET com Content-Disposition grep"
      pattern: "Content-Disposition"
---

<objective>
Prova headless da Phase 4: curl end-to-end + regressao + auditoria + checkpoint humano.

Purpose: Fechar o goal "revisa, decide, compara e exporta com proveniencia" com evidencia observavel (padrao 03-06: integracao + curl ALL PASS + auditoria 0 crit/0 high + approved).
Output: curl-corpus.sh ALL PASS, suite verde, auditoria sem crit/high, aceite humano.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-e-exportacao/04-CONTEXT.md
@.planning/phases/04-corpus-e-exportacao/04-RESEARCH.md
@.planning/phases/04-corpus-e-exportacao/04-PATTERNS.md
@scripts/curl-lab.sh
@apps/core-api/src/routes/lab.ts

<interfaces>
From scripts/curl-lab.sh (moldes verbatim):
```bash
set -u; BASE_URL="${BASE_URL:-http://127.0.0.1:3000}"; JAR_A="/tmp/uhhu-lab-a.jar"; JAR_B="/tmp/uhhu-lab-b.jar"; GHOST="00000000-0000-4000-8000-000000000000"; TMPDIR_CURL="$(mktemp -d)"; cleanup() { rm -rf "$TMPDIR_CURL"; }; trap cleanup EXIT
assert_code() / assert_code_in() / jget() via node -e (sem jq) — copiar verbatim, renomear jars para uhhu-corpus-*.jar.
Fluxo: GET /health com x-request-id grep → bootstrap invite → register/login A+B (409-tolerante) → project → search → POST .../runs (201,202 via assert_code_in) → poll GET /jobs/:id ate terminal → GET run mirror → GET results.
```
Estender: groups pending → confirm/reject → decision → corpus reflete na hora → compare 4 blocos (grep totals/yearHistogram/bySource/pairwiseOverlap, assert sem `items`) → export csv|bib|json com `Content-Disposition: attachment` grep + `filename="corpus-` grep → LAB-06 regressao (GET runs/:runId/results + GET results/:resultId 200) → estranho 404s + ghost 404 + 401 sem cookie → `echo "ALL PASS corpus ..."`.
Pre-req no header: `pnpm --filter @uhhu/db db:migrate` DEV + server local com mesmo .env (.env.dev.cs); NUNCA prod terceiros.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: curl-corpus.sh prova end-to-end + IDOR + regressao LAB-06</name>
  <files>scripts/curl-corpus.sh</files>
  <read_first>scripts/curl-lab.sh, apps/core-api/src/routes/lab.ts, .planning/phases/04-corpus-e-exportacao/04-PATTERNS.md</read_first>
  <action>Criar scripts/curl-corpus.sh executavel (`chmod +x`) copiando preamble+asserts+jget de curl-lab.sh verbatim (jars renomeados `/tmp/uhhu-corpus-a.jar|b.jar`). Fluxo exato com asserts:
1. `GET /health` 200 + grep `x-request-id` (header presente).
2. Bootstrap invite admin → register/login usuario A (dono) e B (estranho), 409-tolerante em re-run; guarda cookies em JAR_A/B.
3. A: `POST /api/v1/lab/projects` {title:'Corpus Prova', researchQuestion:'...'} → 201, jget projectId.
4. A: `POST /api/v1/lab/searches` {projectId, term:'educacao inclusiva', filters:{}, sources:['bdtd','capes']} → 201, jget searchId (cria segunda search para compare: term:'letramento digital' → searchId2).
5. A: `POST /api/v1/lab/searches/:searchId/runs` → assert_code_in `201,202`; se 202 poll `GET /api/v1/jobs/:runId` loop max 30x2s ate status succeeded|partial|failed; jget runId.
6. LAB-06 regressao: `GET /api/v1/lab/runs/:runId/results?limit=5` → 200 com `items` + `page`; jget primeiro resultId → `GET /api/v1/lab/results/:resultId` → 200 com `source/sourceId/rawMetadata`.
7. `GET /api/v1/lab/projects/:projectId/groups` → 200; jget primeiro groupId pending (se nenhum pending, usa confirmed e documenta).
8. `POST /api/v1/lab/groups/:groupId/confirm` → 200 (D-41); `PUT .../decision` {decision:'eligible'} → 200 (D-46).
9. `GET .../corpus` → 200 e grep `eligible`; documenta reflexo na hora (D-46).
10. `GET /api/v1/lab/searches/:searchId/compare?with=:searchId2` → 200 com os 4 literais `totals|yearHistogram|bySource|pairwiseOverlap` e sem `"items"` (D-49).
11. `GET .../export?format=csv&scope=corpus` → 200 + grep `Content-Disposition: attachment` + grep `filename="corpus-` + Content-Type `text/csv`; repetir `format=bibtex` (grep `@phdthesis\|@mastersthesis`) e `format=json` (grep `provenance\|groups`); `scope=selection&selection=<2 resultIds>` → 200 (D-50).
12. IDOR: B em `GET /lab/projects/:projectId/groups` → 404; B em `GET .../corpus` → 404; B em `GET .../export?format=json&scope=corpus` → 404; ghost UUID com JAR_A → 404; sem cookie em corpus → 401. Final `echo "ALL PASS corpus (dedup→decisao→corpus→compare→export + IDOR + LAB-06)"`.
Rodar `BASE_URL=http://127.0.0.1:3000 bash scripts/curl-corpus.sh` contra servidor DEV + PG DEV; exit 0 = ALL PASS.
  </action>
  <verify>
    <automated>bash -n scripts/curl-corpus.sh && echo SYNTAX_OK</automated>
  </verify>
  <done>Script executavel cobre dedup→decisao→corpus→compare→3 exports + 5 provas IDOR + regressao LAB-06; sintaxe ok</done>
  <acceptance_criteria>test -x scripts/curl-corpus.sh; grep -c "assert_code\|jget\|ALL PASS corpus" scripts/curl-corpus.sh >= 10; grep -c "corpus\|compare\|export\|groups/:groupId" scripts/curl-corpus.sh >= 8; bash -n scripts/curl-corpus.sh retorna 0</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: checkpoint humano busca viva + auditoria</name>
  <read_first>scripts/curl-corpus.sh, tests/integration/lab-corpus.test.ts</read_first>
  <what-built>Fluxo corpus completo automatizado (04-01–04-04) + curl-corpus.sh com suite verde e IDOR</what-built>
  <acceptance_criteria>Humano digita "approved" apos curl ALL PASS + suite verde + CSV sem execucao de formula; nenhum crit/high aberto sem aceite formal</acceptance_criteria>
  <how-to-verify>
    1. Servidor DEV no ar: `pnpm --filter @uhhu/db db:migrate` (DEV) + `pnpm --filter @uhhu/core-api start` com .env.dev.cs.
    2. Suite: `pnpm test` (smoke+integration) verde; `pnpm vitest run --project smoke -- dedup` verde; `pnpm test:integration -- lab-corpus` verde.
    3. Curl: `BASE_URL=http://127.0.0.1:3000 bash scripts/curl-corpus.sh` → termina `ALL PASS corpus (...)`.
    4. Busca viva (julgamento humano, padrao 03-06): 1 busca real BDTD pelo REST, confirmar 1 grupo com 2 origens + decisão eligible refletindo no corpus + 1 export CSV aberto na planilha (sem fórmula executando).
    5. Auditoria adversarial arquivo-por-arquivo (dedup.ts/exports.ts/corpus.ts/lab.ts/rateLimit.ts/schema.ts) cobrindo IDOR, XSS/nota, CSV-injection, filename, SQLi, rate-limit, segredos em rawMetadata; `pnpm audit`, SAST/OpenGrep, Gitleaks tree+historico sem crit/high (ou aceite formal).
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva issues (ex. fuzzy reagrupou par rejeitado, CSV sem guard, pending no corpus)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl→API DEV | Prova com 2 usuarios reais exercita 401/404 sem vazar existencia |
| humano→busca viva | Rede real BDTD/CAPES somente leitura local, nunca auditoria ativa contra prod terceiros |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-IDOR | Elevation of privilege | curl-corpus.sh matriz | mitigate | Estranho/adulterado/sem-cookie provados via curl + integracao; fora→404 identico |
| T-04-05-CSV | Tampering | Export vivo aberto na planilha | mitigate | Verificacao humana que `=CMD` nao executa (guard `'`); teste automatizado em 04-04 |
| T-04-05-SECRET | Information disclosure | Suite + export JSON | mitigate | expectClean em todo corpo + Gitleaks tree+historico + `pnpm audit` antes do approve |
| T-04-05-DOS | Denial of service | Export/compare vivos | mitigate | Bucket lab-export 30/min + with/selection guards observados no curl (429 esperado em rajada manual opcional) |
</threat_model>

<verification>
- `bash scripts/curl-corpus.sh` → ALL PASS; `pnpm test` verde; auditoria 0 crit/0 high; checkpoint `approved`
</verification>

<success_criteria>
- Goal Phase 4 observavel via REST sem tocar o banco; LAB-06 regressao intacta; humano aprovou busca viva com corpus+export
</success_criteria>

<output>
After completion, create `.planning/phases/04-corpus-e-exportacao/04-05-SUMMARY.md`
</output>
