---
phase: 04-corpus-e-exportacao
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - apps/core-api/src/routes/lab.ts
  - apps/core-api/src/plugins/rateLimit.ts
  - tests/integration/lab-corpus.test.ts
autonomous: true
requirements: [LAB-07, LAB-08, LAB-09, LAB-10, LAB-11]
must_haves:
  truths:
    - "Dono confirma/rejeita grupo, decide elegibilidade e ve corpus refletir na hora via REST"
    - "Tags default existem e divergencia por fonte nao altera decisao do grupo"
    - "Compare retorna 4 blocos sem listas; export CSV/BibTeX/JSON baixa como attachment com filename seguro"
    - "Estranho/adulterado/sem-cookie recebem 404/404/401 identicos em toda rota nova"
  artifacts:
    - path: "apps/core-api/src/routes/lab.ts"
      provides: "11 endpoints novos no molde Zod+requireAuth+404"
      contains: "/corpus"
    - path: "apps/core-api/src/plugins/rateLimit.ts"
      provides: "Bucket fino de export"
      contains: "labExportRateLimit"
    - path: "tests/integration/lab-corpus.test.ts"
      provides: "Cobertura LAB-07–LAB-11 + IDOR + guards"
  key_links:
    - from: "apps/core-api/src/routes/lab.ts"
      to: "apps/core-api/src/lib/corpus.ts"
      via: "chamadas *ForActor com null→404"
      pattern: "ForActor"
    - from: "apps/core-api/src/routes/lab.ts"
      to: "apps/core-api/src/lib/exports.ts"
      via: "toCSV/toBibTeX/toJSON + exportFilename + Content-Disposition"
      pattern: "Content-Disposition"
---

<objective>
Superficie REST da revisao + rate-limit fino + integracao PG completa.

Purpose: Expor a lib 04-03 via REST no molde lab.ts (Zod+requireAuth+404 identico) e provar com PG real; D-41/D-46/D-48/D-49/D-50/D-51/D-52/D-53 no fio.
Output: rotas novas, bucket export, lab-corpus.test.ts expandido verde.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-e-exportacao/04-CONTEXT.md
@.planning/phases/04-corpus-e-exportacao/04-RESEARCH.md
@.planning/phases/04-corpus-e-exportacao/04-PATTERNS.md
@apps/core-api/src/routes/lab.ts
@apps/core-api/src/lib/corpus.ts
@apps/core-api/src/lib/exports.ts
@apps/core-api/src/plugins/rateLimit.ts
@tests/integration/lab-corpus.test.ts

<interfaces>
Molde de rota lab.ts (copiar verbatim por handler):
```typescript
{ preHandler: requireAuth(db) } → const requestId = resolveRequestId(request); reply.header('x-request-id', requestId);
const actor = request.actor; if (actor===undefined) { await reply.code(401).send(buildEnvelope('UNAUTHENTICATED', requestId, {})); return; }
const parsed = schema.safeParse(body|query|params); if (!parsed.success) { body/query→400 VALIDATION_ERROR com parsed.error.flatten(); params-UUID→404 NOT_FOUND }
const dto = await libForActor(db, actor, ...); if (dto===null) { await reply.code(404).send(buildEnvelope('NOT_FOUND', requestId, {})); return; }
await reply.code(200|201).send(dto); // 201 create, 200 update/read
```
Duplicar preamble resolveRequestId + withClampedLimit (30 linhas) no arquivo — nao extrair helper. Export: `reply.header('Content-Disposition', `attachment; filename="${name}"`)` + `reply.type(...)`; name ASCII de exportFilename. Nenhum ErrorCode novo.
RateLimit mold: `export const labRunsRateLimit = { max: 30, timeWindow: '1 minute' } as const;` + bucketFor + maxFor + hook sliding Map.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: rotas groups/decision/tags/divergence/pin/corpus/compare</name>
  <files>apps/core-api/src/routes/lab.ts</files>
  <read_first>apps/core-api/src/routes/lab.ts, apps/core-api/src/lib/corpus.ts, packages/contracts/src/lab.ts, .planning/phases/04-corpus-e-exportacao/04-PATTERNS.md</read_first>
  <action>Estender `export async function buildLabRoutes` em apps/core-api/src/routes/lab.ts (imports: acrescentar ao bloco @uhhu/contracts `decisionInputSchema, divergenceInputSchema, pinInputSchema, corpusQuerySchema, compareQuerySchema, exportQuerySchema, createTagSchema`-like + ao bloco lib `computeDedupGroupsForActor, confirmGroupForActor, rejectGroupForActor, setGroupDecisionForActor, ensureDefaultTags, attachTagForActor, detachTagForActor, listTagsForActor, setDivergenceForActor, setPinForActor, clearPinForActor, getCorpusForActor, compareSearchesForActor` de '../lib/corpus.js'; manter `.js` suffix e ordem). Novos schemas de params locais: `const groupIdParamsSchema=z.object({groupId:z.string().uuid()}); const projectIdParamsSchema=z.object({projectId:z.string().uuid()});` (malformado→404). Handlers exatos no molde (requestId+x-request-id+401+Zod+ForActor null→404+buildEnvelope, nunca mensagem inline):
- `GET /api/v1/lab/projects/:projectId/groups` query corpusQuerySchema via withClampedLimit → pre-check getProjectForActor 404 → computeDedupGroupsForActor + filtro status opcional → 200 `{items,page}` (D-40 pending visivel aqui, fora do corpus).
- `POST /api/v1/lab/groups/:groupId/confirm` → confirmGroupForActor → 200 grupo (D-41 dono via JOIN).
- `POST /api/v1/lab/groups/:groupId/reject` → rejectGroupForActor → 200 `{singles}` (D-42 divide + veto).
- `PUT /api/v1/lab/groups/:groupId/decision` body decisionInputSchema → setGroupDecisionForActor → 200 (D-46 UMA por grupo).
- `GET /api/v1/lab/projects/:projectId/tags` + `POST /api/v1/lab/projects/:projectId/tags` body createTagSchema → ensureDefaultTags primeiro depois create → 200/201 (LAB-09 defaults incluir/excluir/duplicado/indisponível/revisar).
- `POST /api/v1/lab/groups/:groupId/tags` body `{tagId:z.string().uuid()}` → attach → 200; `DELETE /api/v1/lab/groups/:groupId/tags/:tagId` → detach → 204.
- `PUT /api/v1/lab/groups/:groupId/divergence` body divergenceInputSchema → setDivergenceForActor → 200 (D-46 exemplo 'metadados da CAPES diferem' sem mudar decisao).
- `PUT /api/v1/lab/groups/:groupId/pin` body pinInputSchema {source,sourceId} → setPin → 200; `DELETE .../pin` → clear → 204 (D-45).
- `GET /api/v1/lab/projects/:projectId/corpus` query corpusQuerySchema via withClampedLimit → pre-check projeto 404 → getCorpusForActor → 200 `{items,page}` (LAB-08 view na hora).
- `GET /api/v1/lab/searches/:searchId/compare` query compareQuerySchema → split `with` por ',' trim filter Boolean → valida cada UUID + max 10 (invalido/excesso→400 VALIDATION_ERROR) → compareSearchesForActor → null→404 → 200 CompareDTO 4 blocos sem listas (D-47/D-48/D-49).
Status: 201 so em POST tags create; demais 200; DELETE pin/tag →204 vazio. `withClampedLimit` antes de todo parse de query. Nunca `any`.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <done>10 endpoints de revisao no molde com 401/400/404 corretos; typecheck verde</done>
  <acceptance_criteria>grep -c "groups/:groupId/confirm\|groups/:groupId/reject\|groups/:groupId/decision\|projects/:projectId/corpus\|searches/:searchId/compare\|groups/:groupId/divergence\|groups/:groupId/pin" apps/core-api/src/routes/lab.ts >= 7; grep -c "requireAuth(db)" apps/core-api/src/routes/lab.ts >= 10; grep -v '^ *//' apps/core-api/src/routes/lab.ts | grep -c "as any\|: any" == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: rota export + bucket fino rate-limit</name>
  <files>apps/core-api/src/routes/lab.ts, apps/core-api/src/plugins/rateLimit.ts</files>
  <read_first>apps/core-api/src/routes/lab.ts, apps/core-api/src/plugins/rateLimit.ts, apps/core-api/src/lib/exports.ts, apps/core-api/src/lib/corpus.ts</read_first>
  <action>1. Em apps/core-api/src/routes/lab.ts acrescentar `GET /api/v1/lab/projects/:projectId/export` query exportQuerySchema: parse via withClampedLimit (400 em invalido); pre-check getProjectForActor 404 (anti-enumeracao antes de agregar); resolve escopo D-50: se scope==='corpus' → getCorpusForActor com limit 1000 interno? Implementar `getCorpusForActor` paginado em loop ate esgotar (max 1000 grupos; DoS guard selection≤1000 espelhado) ou nova funcao `getCorpusEntriesForExportForActor` se preciso — server-side, nunca monta no cliente; OVERFLOW: se scope==='corpus' resolver mais de 1000 grupos eligible → responder 400 `VALIDATION_ERROR` PT-BR com detalhe 'Seleção excede o limite de 1000 grupos para exportação.' via `buildEnvelope('VALIDATION_ERROR', requestId, ...)` (catálogo/envelope, nunca `reply.send` com mensagem inline; sem ErrorCode novo — reuso VALIDATION_ERROR); consistente com D-50 ('todos os eligible' vale dentro do DoS guard documentado); se scope==='selection' → `selection` obrigatoria (ausente→400), split ',' → max 1000 UUIDs validos (invalido/excesso→400 `VALIDATION_ERROR` via envelope com mesma mensagem de limite), resolve grupos/membros escopados (fora escopo→404). Serializa via toCSV/toBibTeX/toExportJSON puros de 04-01 (D-53 uma linha por grupo canonico; BibTeX @phdthesis/@mastersthesis school=instituicao key slug+ano+fonte com -a/-b D-51). Filename via `exportFilename(project.title, new Date().toISOString().slice(0,10), ext)` onde ext csv|bib|json (D-52 `corpus-<slug>-<data>.ext` ASCII). Headers: `reply.header('Content-Disposition', `attachment; filename="${name}"`); reply.type(format==='csv'?'text/csv; charset=utf-8':format==='bibtex'?'application/x-bibtex; charset=utf-8':'application/json; charset=utf-8'); await reply.send(body);` Testa injection/filename via 04-01 guards (rota nao reimplementa escaping).
2. Em apps/core-api/src/plugins/rateLimit.ts (PLAT-05 export precisa entrada analoga a lab-runs): acrescentar `export const labExportRateLimit = { max: 30, timeWindow: '1 minute' } as const;` + union `type AuthBucket = 'login'|'register'|'invites'|'reset'|'lab-runs'|'lab-export';` + em bucketFor: `if ((method==='GET'||method==='POST') && path.includes('/api/v1/lab/projects/') && path.endsWith('/export')) return 'lab-export';` + em maxFor `if(bucket==='lab-export') return labExportRateLimit.max;` + atualizar comentario de topo documentando distincao global 200/min/IP vs fino export 30/min/IP. Hook body inalterado (Map sliding, 429 buildEnvelope RATE_LIMITED + x-request-id).
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <done>Export attachment com filename ASCII + 3 content-types + bucket lab-export 30/min; typecheck verde</done>
  <acceptance_criteria>grep -c "projects/:projectId/export" apps/core-api/src/routes/lab.ts >= 1; grep -c "Content-Disposition" apps/core-api/src/routes/lab.ts >= 1; grep -c "labExportRateLimit" apps/core-api/src/plugins/rateLimit.ts >= 2; grep -c "'lab-export'" apps/core-api/src/plugins/rateLimit.ts >= 2; grep -c "VALIDATION_ERROR" apps/core-api/src/routes/lab.ts >= 1; grep -v '^ *//' apps/core-api/src/routes/lab.ts | grep -c "Seleção excede o limite de 1000 grupos" >= 1</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: expandir lab-corpus.test.ts — LAB-07–LAB-11 + IDOR + guards</name>
  <files>tests/integration/lab-corpus.test.ts</files>
  <read_first>tests/integration/lab-corpus.test.ts, tests/integration/lab-search-runs.test.ts, apps/core-api/src/lib/corpus.ts, apps/core-api/src/lib/exports.ts</read_first>
  <action>Expandir tests/integration/lab-corpus.test.ts (harness 04-02 intacto; acrescentar describes, nunca recriar harness; FK-wipe ja inclui 8 tabelas) com casos de integracao PG real (fakeFetch para searches se preciso; senao seed direto de lab_results via db.insert para determinismo). Cobertura obrigatoria por REQ/ameaca:
- LAB-07: exact agrupa BDTD+CAPES (mesma canonicalKey → 1 grupo confidence exact originCount 2); fuzzy ≥0.9 mesmo ano → grupo pending fora do corpus; confirm → confirmed entra no corpus apos eligible; reject → 2 singles + veto (re-run simulado via novo lab_results mesma chave nao reagrupa); cross-ano nao fuzzy (Pitfall 3); pin sobrevive a re-run (setPin source/sourceId + novo run mesmo sourceId → canonical permanece).
- LAB-08: decisao por grupo persiste (eligible→corpus contem; ineligible→some na hora sem re-run); pending nunca no corpus mesmo se decision eligible (Pitfall 5 isCorpusEligible nos 3 caminhos).
- LAB-09: ensureDefaultTags cria incluir/excluir/duplicado/indisponível/revisar (color null); divergencia CAPES 'metadados da CAPES diferem' nao altera decision do grupo (assert decision inalterada apos PUT divergence).
- LAB-10: compare 2 buscas com ultimo run fixo: totals/yearHistogram/bySource/pairwiseOverlap exatos; overlap por chaves exatas; failed contribui zeros; sem item lists (assert !('items' in body)).
- LAB-11: export scope corpus vs selection explícita (≤1000); CSV header exato + 1 linha/grupo D-53; BibTeX -a/-b D-51 + escapes & e chaves balanceadas; JSON com bruto+proveniencia+grupos; Content-Type + Content-Disposition `attachment; filename="corpus-...` ASCII; overflow DoS guard: selection com 1001 UUIDs → 400 VALIDATION_ERROR com 'Seleção excede o limite de 1000 grupos para exportação.' (assert status + envelope code, sem ErrorCode novo); guards: titulo `=CMD(...)` exportado com prefixo `'`; filename com titulo malicioso sem header break; negative regex `set-cookie|token` no JSON export (higiene rawMetadata T-03-01-02).
- IDOR matriz por rota nova (group/corpus/compare/export): estranho→404 NOT_FOUND, ghost UUID→404, sem cookie→401 UNAUTHENTICATED, ownerId injetado no body ignorado; x-request-id em toda resposta + expectClean/expectIsolatedError.
Usar unknown+narrowing, nunca any. Comandos: `pnpm test:integration -- lab-corpus` verde; por commit `pnpm typecheck`.
  </action>
  <verify>
    <automated>pnpm test:integration -- lab-corpus 2>&1 | tail -n 30</automated>
  </verify>
  <done>Integracao cobre LAB-07–LAB-11 + IDOR + guards com PG real (ou skip gracioso offline documentado), verde</done>
  <acceptance_criteria>grep -c "describe(\|it(" tests/integration/lab-corpus.test.ts >= 20; grep -c "pairwiseOverlap\|Content-Disposition\|labExportRateLimit\|rejected\|pin" tests/integration/lab-corpus.test.ts >= 5; grep -c "Seleção excede o limite de 1000 grupos" tests/integration/lab-corpus.test.ts >= 1; pnpm test:integration -- lab-corpus passa ou skips sem falha; grep -v '^ *//' tests/integration/lab-corpus.test.ts | grep -c ": any\|as any" == 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Toda rota nova: requireAuth + Zod fronteira + owner-first lib (fora→404) |
| export→client | CSV/BibTeX/filename entregues como attachment (injection/header) |
| compare→client | Agregacao escopada antes de contar (anti-enumeracao) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-IDOR | Elevation of privilege | Todas as rotas novas com ID | mitigate | requireAuth + params-UUID→404 + lib JOIN→404 identico; matriz dono/estranho/adulterado neste plano |
| T-04-04-CSV | Tampering | GET export csv | mitigate | Reuso csvCell com guard `'` (04-01) + teste adversarial `=CMD` neste plano |
| T-04-04-FILENAME | Tampering | Content-Disposition filename | mitigate | exportFilename ASCII slug max60 + teste titulo malicioso neste plano |
| T-04-04-XSS | Spoofing | PUT divergence note / decision reason | mitigate | Zod max1000 + refine sem HTML + strip na lib 04-03; teste que divergencia nao muda decisao |
| T-04-04-ENUM | Information disclosure | GET compare overlap | mitigate | Pre-check projeto/buscas do ator antes de agregar; 404 em busca alheia; sem item lists |
| T-04-04-DOS | Denial of service | compare/export | mitigate | with≤10, selection≤1000, limit clamp 1..100, bucket lab-export 30/min/IP + global 200/min (PLAT-05) |
| T-04-04-SECRET | Information disclosure | GET export json rawMetadata | mitigate | Reuso higienizacao adapters + teste regex negativa set-cookie/token no corpo |
| T-04-04-SQLI | Tampering | filtros/ordenacao | mitigate | Drizzle parametrizado + allowlists; sem SQL concatenado |
</threat_model>

<verification>
- `pnpm --filter @uhhu/core-api run typecheck` verde
- `pnpm test:integration -- lab-corpus` verde (PG DEV) ou skip gracioso documentado
- `pnpm test` (smoke+integration) sem regressao LAB-06 (results list/ficha intactos)
</verification>

<success_criteria>
- 11 endpoints respondem 200/201/204/400/401/404 conforme molde; bucket export ativo; integracao LAB-07–LAB-11 + IDOR verde
</success_criteria>

<output>
After completion, create `.planning/phases/04-corpus-e-exportacao/04-04-SUMMARY.md`
</output>
