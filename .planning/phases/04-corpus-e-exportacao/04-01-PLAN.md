---
phase: 04-corpus-e-exportacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/core-api/package.json
  - packages/contracts/src/lab.ts
  - apps/core-api/src/lib/dedup.ts
  - apps/core-api/src/lib/exports.ts
  - tests/smoke/dedup-exports.test.ts
autonomous: true
requirements: [LAB-07, LAB-11]
must_haves:
  truths:
    - "Chave canonica SHA-256 identica para mesma obra BDTD+CAPES com variacao de acento/pontuacao"
    - "Similaridade fuzzy >=0.9 sobre titulos normalizados com trava de ano e exibida como pending"
    - "Serializadores CSV/BibTeX/filename geram saida deterministica com guards anti-injection"
  artifacts:
    - path: "packages/contracts/src/lab.ts"
      provides: "DTOs DedupGroup/CorpusEntry/Compare + schemas decisao/tag/divergencia/pin/compare/export"
      contains: "DedupGroupDTO"
    - path: "apps/core-api/src/lib/dedup.ts"
      provides: "Funcoes puras normalizeTitle/canonicalKey/titleSimilarity/completenessScore"
      exports: ["normalizeTitle", "canonicalKey", "titleSimilarity", "completenessScore", "FUZZY_THRESHOLD"]
    - path: "apps/core-api/src/lib/exports.ts"
      provides: "Serializadores puros toCSV/toBibTeX/toJSON + csvCell/escapeBibtex/exportFilename/bibtexKey"
      exports: ["toCSV", "toBibTeX", "toJSON", "csvCell", "escapeBibtex", "exportFilename", "bibtexKey"]
    - path: "tests/smoke/dedup-exports.test.ts"
      provides: "Testes unitarios puros sem PG incl. adversariais"
      min_lines: 80
  key_links:
    - from: "apps/core-api/src/lib/dedup.ts"
      to: "fastest-levenshtein"
      via: "import { distance } from 'fastest-levenshtein'"
      pattern: "from 'fastest-levenshtein'"
    - from: "packages/contracts/src/lab.ts"
      to: "apps/core-api/src/lib/dedup.ts"
      via: "tipos compartilhados definicao unica, import type"
      pattern: "DedupGroupDTO|CorpusEntryDTO|CompareDTO"
---

<objective>
Fundacoes puras da Phase 4: contratos compartilhados + motor fuzzy + serializadores, sem banco.

Purpose: Destrava 04-02/04-03 (libs consomem estes tipos/funcoes); isola risco de threshold e escaping em unit sem PG. Implementa D-40/D-44/D-49/D-51/D-52/D-53 no nivel puro.
Output: dep instalada, lab.ts estendido, dedup.ts + exports.ts puros, unit verde.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-e-exportacao/04-CONTEXT.md
@.planning/phases/04-corpus-e-exportacao/04-RESEARCH.md
@.planning/phases/04-corpus-e-exportacao/04-PATTERNS.md
@packages/contracts/src/lab.ts
@packages/contracts/src/errors.ts
@apps/core-api/package.json

<interfaces>
<!-- Contratos existentes que o executor reutiliza verbatim; nao reexplorar. -->
From packages/contracts/src/lab.ts (existente):
```typescript
export const executableSourceSchema = z.enum(['bdtd', 'capes']);
export interface ResultDTO { id: string; runId: string; source: ExecutableSource; sourceId: string; title: string; authors: string[]; year: number | null; docType: DocType | null; institution: string | null; program: string | null; abstract: string | null; originUrl: string | null; sourceUrl: string | null; rawMetadata: Record<string, unknown>; retrievedAt: string; }
export function encodeResultsCursor(source: string, rank: number, id: string): string;
```
From packages/contracts/src/errors.ts: usar buildEnvelope(code, requestId, details); NENHUM codigo novo nesta fase (reusar NOT_FOUND/VALIDATION_ERROR/RATE_LIMITED). Nao adicionar ErrorCode.
Spec deviation registrada (RESEARCH State of Art): contrato §12 parcialmente superseded — Decision.resultId → decisao UMA por grupo via canonicalKey (D-46); DedupGroup.runId → escopo por projectId (D-41–D-45). Documentar no header de lab.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: instalar fastest-levenshtein + estender contracts/lab.ts</name>
  <files>apps/core-api/package.json, packages/contracts/src/lab.ts</files>
  <read_first>apps/core-api/package.json, packages/contracts/src/lab.ts, .planning/phases/04-corpus-e-exportacao/04-PATTERNS.md, .planning/phases/04-corpus-e-exportacao/04-RESEARCH.md</read_first>
  <action>1. Instalar unica dep nova via `pnpm --filter @uhhu/core-api add fastest-levenshtein@1.0.16` (MIT; NAO usar string-similarity deprecated nem jaro-winkler). Conferir que apps/core-api/package.json contem `"fastest-levenshtein": "1.0.16"` (ou ^1.0.16).
2. Estender packages/contracts/src/lab.ts (definicao UNICA; header existente `D-28..D-39` → acrescentar `D-40..D-53`; comentario de spec deviation §12 superseded por D-41–D-46):
   - `export const groupConfidenceSchema = z.enum(['exact','fuzzy','single']);`
   - `export const groupStatusSchema = z.enum(['confirmed','pending']);` // D-40 fuzzy nao confirmado = pending fora do corpus
   - `export const groupDecisionSchema = z.enum(['eligible','ineligible','undecided']);`
   - `export const decisionInputSchema = z.object({ decision: groupDecisionSchema, reason: z.string().trim().max(500,'Motivo deve ter no máximo 500 caracteres.').optional() }); export type DecisionInput = z.infer<typeof decisionInputSchema>;`
   - `export const tagNameSchema = z.string().trim().min(1,'Nome da tag é obrigatório.').max(100,'Nome deve ter no máximo 100 caracteres.'); export const createTagSchema = z.object({ name: tagNameSchema, color: z.string().trim().max(20).nullable().optional() });`
   - `export const divergenceInputSchema = z.object({ source: executableSourceSchema, note: z.string().trim().min(1,'Nota é obrigatória.').max(1000,'Nota deve ter no máximo 1000 caracteres.').refine((v)=>!/<[^>]*>/.test(v),{message:'Nota não pode conter HTML.'}) });` // D-46 anotacao por origem, sanitizacao baseline §2.5
   - `export const pinInputSchema = z.object({ source: executableSourceSchema, sourceId: z.string().trim().min(1,'sourceId é obrigatório.').max(300,'sourceId deve ter no máximo 300 caracteres.') });` // D-45 pin estavel por (source,sourceId), NUNCA resultId (sobrevive a re-runs)
   - `export const corpusQuerySchema = z.object({ limit: z.coerce.number().int().min(1).max(100).default(20), cursor: z.string().max(512).optional(), status: groupStatusSchema.optional() });`
   - `export const compareQuerySchema = z.object({ with: z.string().trim().min(1,'Parâmetro with é obrigatório.').max(2000) });` // rota faz split(',') → valida cada UUID → max 10 (D-48/D-49, DoS guard); erro → 400 VALIDATION_ERROR
   - `export const exportQuerySchema = z.object({ format: z.enum(['csv','bibtex','json']), scope: z.enum(['corpus','selection']), selection: z.string().max(20000).optional() });` // selection = CSV de UUIDs, max 1000 IDs (D-50, DoS guard); scope=corpus resolve server-side
   - DTOs (camelCase, datas ISO string, nunca Date): `DedupGroupDTO { id:string; projectId:string; canonicalKey:string; confidence:'exact'|'fuzzy'|'single'; status:'confirmed'|'pending'; canonicalResultId:string; memberIds:string[]; decision:'eligible'|'ineligible'|'undecided'; originCount:number; origins:Array<'bdtd'|'capes'> }`, `CorpusEntryDTO { groupId:string; canonicalKey:string; title:string; authors:string[]; year:number|null; docType:DocType|null; institution:string|null; program:string|null; abstract:string|null; originUrl:string|null; sourceUrl:string|null; decision:'eligible'|'ineligible'|'undecided'; tags:string[]; originCount:number; origins:Array<'bdtd'|'capes'>; memberIds:string[] }`, `CompareDTO { searches:string[]; totals:Record<string,number>; yearHistogram:Record<string,number>; bySource:Record<string,Record<'bdtd'|'capes',number>>; pairwiseOverlap:Record<string,number> }` // exatamente 4 blocos D-49, sem item lists.
   Todos com `export type X = z.infer` onde schema; mensagens PT-BR; arrays sempre bounded. Usar `import type` nos consumers. Nao duplicar tipos em outro pacote (AGENTS.md). Nao usar `any` em nenhum ponto.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/contracts run typecheck && pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <done>package.json tem fastest-levenshtein 1.0.16; lab.ts exporta DedupGroupDTO/CorpusEntryDTO/CompareDTO + 7 schemas novos; typecheck verde</done>
  <acceptance_criteria>grep -c "DedupGroupDTO" packages/contracts/src/lab.ts >= 2; grep -c "fastest-levenshtein" apps/core-api/package.json == 1; grep -v '^#' packages/contracts/src/lab.ts | grep -c "decisionInputSchema\|divergenceInputSchema\|pinInputSchema\|compareQuerySchema\|exportQuerySchema" == 5; nenhum `any` em lab.ts (grep -v '^ *//' packages/contracts/src/lab.ts | grep -c "any" == 0)</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: criar libs puras dedup.ts + exports.ts</name>
  <files>apps/core-api/src/lib/dedup.ts, apps/core-api/src/lib/exports.ts</files>
  <read_first>packages/contracts/src/lab.ts, apps/core-api/src/lib/searchRuns.ts, packages/contracts/src/pagination.ts, .planning/phases/04-corpus-e-exportacao/04-RESEARCH.md</read_first>
  <action>Criar dois arquivos PUROS (sem db/actor/HTTP/fetch; apenas node:crypto + fastest-levenshtein + tipos contracts; header PT-BR citando D-XX). Import order molde searchRuns.ts: `node:crypto` primeiro, depois `fastest-levenshtein`, depois `type` de @uhhu/contracts.
apps/core-api/src/lib/dedup.ts:
- `import { createHash } from 'node:crypto'; import { distance } from 'fastest-levenshtein'; import type { DocType } from '@uhhu/contracts';`
- `export const FUZZY_THRESHOLD = 0.9;` com doc `/** Threshold D-40: similaridade normalizada >= 0.9 sobre titulos NORMALIZADOS, mesmo ano. */`
- `export function normalizeTitle(raw: string): string { return raw.normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9\s]/g,' ').replace(/\s+/g,' ').trim(); }`
- `export function normalizeAuthor(raw: string): string` identica ao title.
- `export function canonicalKey(title: string, year: number|null, authors: string[]): string { const parts=[normalizeTitle(title), year===null?'':String(year), ...authors.map(normalizeAuthor).filter(Boolean).sort()]; return createHash('sha256').update(parts.join('|'),'utf8').digest('hex'); }` // D-41–D-45 identidade por conteudo, nunca resultId
- `export function titleSimilarity(a:string,b:string): number { const na=normalizeTitle(a); const nb=normalizeTitle(b); if(na===nb) return 1; const maxLen=Math.max(na.length,nb.length); if(maxLen===0) return 0; return 1-distance(na,nb)/maxLen; }`
- `export interface CompletenessInput { abstract: string|null; authors: string[]; year: number|null; originUrl: string|null; sourceUrl: string|null; } export function completenessScore(r:CompletenessInput): number { return (r.abstract?1:0)+(r.authors.length>0?1:0)+(r.year!==null?1:0)+(r.originUrl||r.sourceUrl?1:0); }` // D-44 score; desempate deterministico documentado no comentario: score DESC, source ASC ('bdtd'<'capes'), sourceId ASC.
- Nenhum Math.random, nenhum any (unknown+narrowing se preciso).
apps/core-api/src/lib/exports.ts:
- `import type { CorpusEntryDTO } from '@uhhu/contracts';` + doc header D-50–D-53.
- `const BIBTEX_ESCAPES: Record<string,string> = { '&':'\\&','%':'\\%','#':'\\#','_':'\\_','$':'\\$','~':'\\textasciitilde{}','^':'\\textasciicircum{}','{':'\\{','}':'\\}' }; export function escapeBibtex(raw:string): string { return [...raw.normalize('NFC')].map((ch)=>BIBTEX_ESCAPES[ch]??ch).join(''); }`
- `export function protectBibtexTitle(title:string): string { return title.split(' ').map((tok)=>/^[A-ZÀ-Þ0-9-]{2,}$/.test(tok.replace(/[^A-Za-zÀ-Þà-þ0-9-]/g,''))?`{${tok}}`:tok).join(' '); }` // siglas ALL-CAPS contra case-fold.
- `export function csvCell(value:string): string { const guarded=/^[=+\-@]/.test(value)?`'${value}`:value; return /[",\n\r]/.test(guarded)?`"${guarded.replace(/"/g,'""')}"`:guarded; }` // Pitfall1 guard ' prefix.
- `export function slugifyAscii(raw:string): string { return raw.normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,60)||'projeto'; } export function exportFilename(projectTitle:string,dateYYYYMMDD:string,ext:'csv'|'bib'|'json'): string { return `corpus-${slugifyAscii(projectTitle)}-${dateYYYYMMDD}.${ext}`; }` // D-52 ASCII puro, sem filename*.
- `export function bibtexKey(firstAuthorLastName:string,year:number|null,source:string,order:number): string { const base=`${slugifyAscii(firstAuthorLastName).replace(/-/g,'')}${year===null?'sano':String(year)}${source}`; if(order===0) return base; const suffix=String.fromCharCode(96+order); return `${base}-${suffix}`; }` // D-51 primeira fica base, colisao -a/-b por ordem de grupo (order 1→-a, 2→-b).
- `export function toCSV(entries: CorpusEntryDTO[]): string` header exato `groupId,canonicalKey,title,authors,year,docType,institution,decision,tags,originCount,origins` + uma linha por grupo D-53 com csvCell em cada celula, authors join(';'), tags join(';'), origins join('|').
- `export function toBibTeX(entries: CorpusEntryDTO[]): string` por entry: tipo `@phdthesis` se docType==='doctoralThesis' senao `@mastersthesis`; campos `title={...}, author={a and b}, year={...}, school={instituicao}, note={decision:...}, url={originUrl||sourceUrl}` com escapeBibtex; key via bibtexKey por ordem.
- `export function toExportJSON(projectId:string, entries: CorpusEntryDTO[], provenance: unknown): string` retorna JSON.stringify({projectId, exportedAt:new Date().toISOString(), groups:entries, provenance},null,2).
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck && pnpm vitest run --project smoke 2>&1 | tail -n 20</automated>
  </verify>
  <done>dedup.ts e exports.ts existem, puros, exportam as 12 funcoes/consts; typecheck verde</done>
  <acceptance_criteria>grep -c "from 'fastest-levenshtein'" apps/core-api/src/lib/dedup.ts == 1; grep -c "FUZZY_THRESHOLD = 0.9" apps/core-api/src/lib/dedup.ts == 1; grep -c "BIBTEX_ESCAPES\|csvCell\|exportFilename\|bibtexKey\|toCSV\|toBibTeX" apps/core-api/src/lib/exports.ts >= 6; grep -v '^ *//' apps/core-api/src/lib/dedup.ts apps/core-api/src/lib/exports.ts | grep -c "any" == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: unit smoke dedup-exports sem PG</name>
  <files>tests/smoke/dedup-exports.test.ts</files>
  <read_first>apps/core-api/src/lib/dedup.ts, apps/core-api/src/lib/exports.ts, tests/smoke/boot.test.ts, vitest.workspace.ts</read_first>
  <action>Criar tests/smoke/dedup-exports.test.ts (NOTA: VALIDATION citava tests/unit/ mas vitest.workspace.ts so tem projetos smoke=tests/smoke/** e integration=tests/integration/**; unit fora do workspace NAO executa — por isso o arquivo vive em smoke, comando `pnpm vitest run --project smoke -- dedup`). Padrao boot.test.ts: `import { describe, expect, it } from 'vitest';` + type-guards com unknown, nunca any; `expectNoSecrets`-style onde aplicavel. Casos obrigatorios:
- normalizeTitle: 'Educação & Saúde: Guia!' → 'educacao saude guia'; acentos BDTD×CAPES equivalentes.
- canonicalKey: estabilidade (ordem de autores embaralhada → mesma key; ano null vs ano → keys distintas; hex 64 chars).
- titleSimilarity: identicos →1; typo 1 char em titulo longo →>=0.9; titulos distintos mesmo autor →<0.9; vazio→0.
- completenessScore D-44: completo (abstract+autores+ano+url)=4; vazio=0.
- csvCell adversarial: '=CMD(...)' → prefixo `'`; '-2+3' → `'`; '@sum' → `'`; valor com `"` e `,` → quoting RFC4180 com `""`.
- escapeBibtex: '& % # _ $ ~ ^ { }' escapados; balanceamento de chaves na saida toBibTeX.
- exportFilename adversarial: titulo 'Snapshots "../../etc" São Paulo' + '2026-09-11' → apenas [a-z0-9-.] (assert regex /^corpus-[a-z0-9-]+-2026-09-11\.csv$/).
- bibtexKey D-51: order 0 → base; order 1 → base-a; order 2 → base-b.
- toCSV D-53: 2 entries → header + 2 linhas, sem duplicar decisao por origem.
Todos os expects com valores literais (nao snapshots opacos). Rodar `pnpm vitest run --project smoke -- dedup` verde.
  </action>
  <verify>
    <automated>pnpm vitest run --project smoke -- dedup</automated>
  </verify>
  <done>Unit smoke com >=12 its cobrindo D-40/D-44/D-51/D-52/D-53 + adversariais CSV/filename/BibTeX, verde em <120s</done>
  <acceptance_criteria>grep -c "describe\|it(" tests/smoke/dedup-exports.test.ts >= 12; pnpm vitest run --project smoke -- dedup retorna 0 failures; grep -v '^ *//' tests/smoke/dedup-exports.test.ts | grep -c "any" == 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev→registry | pnpm install de fastest-levenshtein (supply-chain) |
| test→pure libs | inputs adversariais de titulo/nota/filename exercitados em unit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SUPPLY | Tampering | apps/core-api/package.json (nova dep) | mitigate | Pinar fastest-levenshtein@1.0.16 MIT verificada no registry; nao usar string-similarity deprecated; `pnpm audit` no gate final |
| T-04-01-CSV | Tampering | exports.ts csvCell | mitigate | Guard `'` prefix em /^[=+\-@]/ + teste adversarial `=CMD` neste plano |
| T-04-01-FILENAME | Tampering | exports.ts exportFilename | mitigate | Slug ASCII max60 fallback `projeto`, teste com `"../../etc"` neste plano |
| T-04-01-XSS | Spoofing | contracts divergence note | mitigate | Zod max1000 + refine sem HTML + sanitizacao na escrita (04-03/04-04); contrato ja bloqueia markup aqui |
</threat_model>

<verification>
- `pnpm --filter @uhhu/contracts run typecheck` + `pnpm --filter @uhhu/core-api run typecheck` verdes
- `pnpm vitest run --project smoke -- dedup` verde (Nyquist <120s)
- grep gates da acceptance_criteria passam; nenhum `any` nos 3 arquivos novos/alterados
</verification>

<success_criteria>
- fastest-levenshtein 1.0.16 instalada; lab.ts com 5 schemas + 3 DTOs (D-40/D-46/D-49/D-50/D-51/D-52/D-53 referenciados)
- dedup.ts/exports.ts puros com 12 exports; unit >=12 its verde
</success_criteria>

<output>
After completion, create `.planning/phases/04-corpus-e-exportacao/04-01-SUMMARY.md`
</output>
