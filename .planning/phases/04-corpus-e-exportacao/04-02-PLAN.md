---
phase: 04-corpus-e-exportacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema.ts
  - packages/db/drizzle/0003_corpus.sql
  - tests/integration/fixtures/dedup-overlap.json
  - tests/integration/lab-corpus.test.ts
autonomous: true
requirements: [LAB-07, LAB-08, LAB-09]
must_haves:
  truths:
    - "Revisão persiste por chave de conteúdo — decisão/pin/divergência sobrevivem a re-run"
    - "Base de revisão persiste sem drift — grupos/decisões criados permanecem disponíveis após migrate das 8 tabelas no PG DEV"
    - "Threshold 0.9 calibrado por pares exato/fuzzy/distinto/cross-ano e revisão testável com 2 usuários isolados"
  artifacts:
    - path: "packages/db/src/schema.ts"
      provides: "8 tabelas lab de revisao com CHECKs/FKs/UNIQUEs"
      contains: "labDedupGroups"
    - path: "packages/db/drizzle/0003_corpus.sql"
      provides: "Migration gerada via drizzle-kit com 8 CREATE TABLE"
      contains: "lab_dedup_groups"
    - path: "tests/integration/fixtures/dedup-overlap.json"
      provides: "Pares BDTD+CAPES para ancorar threshold 0.9"
    - path: "tests/integration/lab-corpus.test.ts"
      provides: "Harness 2 usuarios + migrate + wipe FK-safe (skeleton)"
  key_links:
    - from: "packages/db/src/schema.ts"
      to: "packages/db/drizzle/0003_corpus.sql"
      via: "drizzle-kit generate"
      pattern: "CREATE TABLE.*lab_dedup_groups"
    - from: "tests/integration/lab-corpus.test.ts"
      to: "packages/db/src/schema.ts"
      via: "imports das 8 tabelas novas no wipe"
      pattern: "labDedupGroups|labGroupDecisions"
---

<objective>
Persistencia da revisao: schema + migration 0003 + push BLOCKING + fixtures + scaffold de integracao.

Purpose: Sem as 8 tabelas enderecadas por (projectId, canonicalKey) D-42/D-43/D-45 sao impossiveis; este plano entrega o chao para 04-03/04-04. Inclui o push BLOCKING exigido pelo orquestrador.
Output: schema estendido, 0003 gerada e aplicada no DEV, fixtures e skeleton de teste.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-e-exportacao/04-CONTEXT.md
@.planning/phases/04-corpus-e-exportacao/04-RESEARCH.md
@.planning/phases/04-corpus-e-exportacao/04-PATTERNS.md
@packages/db/src/schema.ts
@packages/db/drizzle/0002_fancy_hiroim.sql
@tests/integration/lab-search-runs.test.ts

<interfaces>
From packages/db/src/schema.ts (moldes a copiar):
```typescript
// prettier-ignore
export const labSearches = pgTable('lab_searches', { id: uuid('id').primaryKey().defaultRandom(), projectId: uuid('project_id').notNull().references(() => projects.id, { onDelete: 'cascade' }), ... },
  (t) => [ index('lab_searches_project_created_idx').on(t.projectId, t.createdAt.desc()), check('lab_searches_term_length_check', sql`char_length(${t.term}) >= 1 AND char_length(${t.term}) <= 500`), ]);
export type LabSearch = typeof labSearches.$inferSelect; export type NewLabSearch = typeof labSearches.$inferInsert;
```
Migration mold 0002: `gen_random_uuid()` defaults, CONSTRAINT UNIQUE/CHECK inline, `--> statement-breakpoint` separators, FKs via ALTER TABLE ADD CONSTRAINT ON DELETE cascade, CREATE INDEX USING btree. Gerar via drizzle-kit, nunca DDL manual.
Identidade Pattern 1 (HIGH): review por (projectId, canonicalKey); lab_results.id so em members; rejected_pairs guarda par de chaves, nao result IDs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: estender schema.ts com 8 tabelas de revisao</name>
  <files>packages/db/src/schema.ts</files>
  <read_first>packages/db/src/schema.ts, .planning/phases/04-corpus-e-exportacao/04-PATTERNS.md, .planning/phases/04-corpus-e-exportacao/04-RESEARCH.md</read_first>
  <action>Acrescentar ao final de packages/db/src/schema.ts (header existente `Fase 3...` → acrescentar linha `// Fase 4 (D-40..D-53): revisao por chave de conteudo (projectId, canonicalKey), NUNCA resultId.`) as 8 tabelas com `// prettier-ignore`, colunas snake_case, `uuid(...).primaryKey().defaultRandom()`, FKs onDelete cascade, indexes, CHECKs espelhando Zod de 04-01, e pares `export type X/NewX`. Definicoes exatas:
- `labDedupGroups = pgTable('lab_dedup_groups', { id: uuid('id').primaryKey().defaultRandom(), projectId: uuid('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}), canonicalKey: text('canonical_key').notNull(), confidence: text('confidence').notNull(), status: text('status').notNull().default('confirmed'), createdAt: timestamp('created_at',{withTimezone:true}).defaultNow().notNull(), updatedAt: timestamp('updated_at',{withTimezone:true}).defaultNow().notNull().$onUpdate(()=>new Date()) }, (t)=>[ unique('lab_dedup_groups_project_key_unique').on(t.projectId,t.canonicalKey), index('lab_dedup_groups_project_status_idx').on(t.projectId,t.status), check('lab_dedup_groups_confidence_check', sql`${t.confidence} IN ('exact','fuzzy','single')`), check('lab_dedup_groups_status_check', sql`${t.status} IN ('confirmed','pending')`) ]);` // D-40 pending fora do corpus; D-41 persistente por projeto
- `labDedupMembers = pgTable('lab_dedup_members', { id: uuid('id').primaryKey().defaultRandom(), groupId: uuid('group_id').notNull().references(()=>labDedupGroups.id,{onDelete:'cascade'}), resultId: uuid('result_id').notNull().references(()=>labResults.id,{onDelete:'cascade'}) }, (t)=>[ unique('lab_dedup_members_group_result_unique').on(t.groupId,t.resultId), unique('lab_dedup_members_result_unique').on(t.resultId), index('lab_dedup_members_group_idx').on(t.groupId) ]);` // proveniencia intacta, 1 resultado em 1 grupo
- `labGroupDecisions = pgTable('lab_group_decisions', { id: uuid('id').primaryKey().defaultRandom(), groupId: uuid('group_id').notNull().references(()=>labDedupGroups.id,{onDelete:'cascade'}).unique(), decision: text('decision').notNull().default('undecided'), reason: text('reason'), updatedAt: timestamp('updated_at',{withTimezone:true}).defaultNow().notNull().$onUpdate(()=>new Date()) }, (t)=>[ check('lab_group_decisions_decision_check', sql`${t.decision} IN ('eligible','ineligible','undecided')`), check('lab_group_decisions_reason_check', sql`char_length(${t.reason}) <= 500`) ]);` // D-46 UMA por grupo (UNIQUE groupId)
- `labDivergences = pgTable('lab_divergences', { id: uuid('id').primaryKey().defaultRandom(), groupId: uuid('group_id').notNull().references(()=>labDedupGroups.id,{onDelete:'cascade'}), source: text('source').notNull(), note: text('note').notNull(), createdAt: timestamp('created_at',{withTimezone:true}).defaultNow().notNull() }, (t)=>[ unique('lab_divergences_group_source_unique').on(t.groupId,t.source), check('lab_divergences_source_check', sql`${t.source} IN ('bdtd','capes')`), check('lab_divergences_note_check', sql`char_length(${t.note}) >= 1 AND char_length(${t.note}) <= 1000`) ]);` // D-46 anotacao por origem, sem mudar decisao
- `labTags = pgTable('lab_tags', { id: uuid('id').primaryKey().defaultRandom(), projectId: uuid('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}), name: text('name').notNull(), color: text('color'), createdAt: timestamp('created_at',{withTimezone:true}).defaultNow().notNull() }, (t)=>[ unique('lab_tags_project_name_unique').on(t.projectId,t.name), index('lab_tags_project_idx').on(t.projectId), check('lab_tags_name_check', sql`char_length(${t.name}) >= 1 AND char_length(${t.name}) <= 100`) ]);` // LAB-09 defaults via seed preguicoso (sem data migration, RESEARCH A6)
- `labGroupTags = pgTable('lab_group_tags', { groupId: uuid('group_id').notNull().references(()=>labDedupGroups.id,{onDelete:'cascade'}), tagId: uuid('tag_id').notNull().references(()=>labTags.id,{onDelete:'cascade'}) }, (t)=>[ unique('lab_group_tags_group_tag_unique').on(t.groupId,t.tagId), index('lab_group_tags_group_idx').on(t.groupId) ]);` // sem id proprio; PK logica (groupId,tagId)
- `labRejectedPairs = pgTable('lab_rejected_pairs', { id: uuid('id').primaryKey().defaultRandom(), projectId: uuid('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}), keyA: text('key_a').notNull(), keyB: text('key_b').notNull(), createdAt: timestamp('created_at',{withTimezone:true}).defaultNow().notNull() }, (t)=>[ unique('lab_rejected_pairs_project_keys_unique').on(t.projectId,t.keyA,t.keyB), index('lab_rejected_pairs_project_idx').on(t.projectId) ]);` // D-42 par de canonicalKeys ordenado (keyA<keyB na escrita), nunca resultIds
- `labCanonicalPins = pgTable('lab_canonical_pins', { id: uuid('id').primaryKey().defaultRandom(), groupId: uuid('group_id').notNull().references(()=>labDedupGroups.id,{onDelete:'cascade'}).unique(), source: text('source').notNull(), sourceId: text('source_id').notNull(), createdAt: timestamp('created_at',{withTimezone:true}).defaultNow().notNull() }, (t)=>[ check('lab_canonical_pins_source_check', sql`${t.source} IN ('bdtd','capes')`) ]);` // D-45 override estavel (source,sourceId) sobrevive a re-runs; UNIQUE groupId = 1 pin por grupo
Imports: acrescentar `labDedupGroups` etc. ao bloco drizzle-orm/pg-core se faltar (check/index/unique/uuid/text/timestamp ja importados). Nenhum `any`; tipos inferidos via $inferSelect/$inferInsert.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/db run typecheck</automated>
  </verify>
  <done>schema.ts com 8 tabelas, UNIQUE(project,canonicalKey) + UNIQUE(group,source) + UNIQUE(project,name) + CHECKs, typecheck verde</done>
  <acceptance_criteria>grep -c "labDedupGroups\|labDedupMembers\|labGroupDecisions\|labDivergences\|labTags\|labGroupTags\|labRejectedPairs\|labCanonicalPins" packages/db/src/schema.ts >= 16; grep -c "lab_dedup_groups_project_key_unique\|lab_divergences_group_source_unique\|lab_tags_project_name_unique" packages/db/src/schema.ts == 3; grep -v '^ *//' packages/db/src/schema.ts | grep -c "any" == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2 [BLOCKING]: gerar migration 0003 e aplicar no PG DEV</name>
  <files>packages/db/drizzle/0003_corpus.sql</files>
  <read_first>packages/db/src/schema.ts, packages/db/src/migrate.ts, packages/db/drizzle/0002_fancy_hiroim.sql</read_first>
  <action>1. Gerar SQL via `pnpm --filter @uhhu/db db:generate` (drizzle-kit, mold 0002: gen_random_uuid, CONSTRAINTs inline, `--> statement-breakpoint`, FKs ALTER TABLE ON DELETE cascade, CREATE INDEX USING btree). Renomear/confirmar arquivo como `packages/db/drizzle/0003_corpus.sql` (se o generate criar nome com hash, renomear para 0003_corpus.sql e ajustar `packages/db/drizzle/meta/_journal.json` SOMENTE via generate — nunca editar snapshot manualmente; se rename quebrar journal, regerar).
2. [BLOCKING] Aplicar com DEV env (convecao 03-01, nunca prod): `pnpm --filter @uhhu/db db:migrate` com `.env.dev.cs` (ou APP/MIGRATION_DATABASE_URL do DEV na code-server rede uhhu-dev_default). Migration deve conter os 8 CREATE TABLE lab_dedup_groups/lab_dedup_members/lab_group_decisions/lab_divergences/lab_tags/lab_group_tags/lab_rejected_pairs/lab_canonical_pins. Se PG DEV inalcançavel (ECONNREFUSED/timeout), DOCUMENTAR o skip com motivo de conexao no SUMMARY e nao silenciar; a verificacao da fase NAO passa sem este push (requisito do orquestrador). Nunca rodar contra prod; nunca logar connection string (usar redactConnectionString).
3. Provar: `grep -c 'CREATE TABLE "lab_' packages/db/drizzle/0003_corpus.sql` == 8.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/db run typecheck && grep -c 'CREATE TABLE "lab_' packages/db/drizzle/0003_corpus.sql</automated>
  </verify>
  <done>0003 gerada com 8 tabelas e aplicada no PG DEV (migrate exit 0) ou skip documentado com motivo de conexao</done>
  <acceptance_criteria> Arquivo packages/db/drizzle/0003_corpus.sql existe; grep -c 'CREATE TABLE "lab_dedup_groups"\|CREATE TABLE "lab_group_decisions"\|CREATE TABLE "lab_canonical_pins"' packages/db/drizzle/0003_corpus.sql >= 3; migrate log sem segredo (grep -ci "postgresql://[^ ]*:[^ @]*@" packages/db/drizzle/0003_corpus.sql == 0)</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: fixtures dedup-overlap + skeleton lab-corpus.test.ts</name>
  <files>tests/integration/fixtures/dedup-overlap.json, tests/integration/lab-corpus.test.ts</files>
  <read_first>tests/integration/lab-search-runs.test.ts, tests/integration/fixtures/bdtd-search.json, apps/core-api/src/auth/session.ts</read_first>
  <action>1. Criar tests/integration/fixtures/dedup-overlap.json (sem segredos) com 8 resultados realistas cobrindo Open Question 1 / Pitfall 3: (a) par EXATO BDTD+CAPES 'Educação inclusiva e tecnologia assistiva' 2021 Silva (variacao acento/pontuacao NFKD-equivalente); (b) par FUZZY mesmo ano 2022 'Letramento digital nas escolas públicas' vs 'Letramento digital na escola publica' (typo, similaridade >=0.9 esperada); (c) par DISTINCT mesmo autor 2021 vs tese expandida (titulos proximos mas anos distintos); (d) par CROSS-ANO 'Alfabetização científica no ensino médio' 2019 vs 2023 (NAO deve fuzzy — trava de ano, Pitfall 3); cada item com {source, sourceId, title, authors, year, docType, institution}. Titulos com acentos para ancorar NFKD.
2. Criar tests/integration/lab-corpus.test.ts SKELETON (expansao completa em 04-04; aqui apenas harness verde): copiar verbatim de lab-search-runs.test.ts linhas 17-50 (imports + `void execFileSync`), 53-62 (fixture load via readFileSync+JSON.parse para unknown), 302-327 (apiRequest com GET/POST/PATCH/DELETE + cookie COOKIE_NAME), 329-404 (bootstrapAdmin/createMember/createProject/createSearch — 2 usuarios owner A + stranger B obrigatorios), 408-421 (expectClean + expectIsolatedError com regex /stack|passwordhash|password_hash|token|secret|set-cookie|cookie/), 427-500 (describe.skipIf(APP_URL===undefined||MIGRATION_URL===undefined) + beforeAll execFileSync migrate 60s + createDb + Fastify+cookie+requestIdPlugin+errorHandler+auth/project/lab routes + beforeEach wipe em ordem FK-safe children-first incluindo as 8 novas tabelas primeiro: lab_group_tags, lab_dedup_members, lab_canonical_pins, lab_divergences, lab_group_decisions, lab_rejected_pairs, lab_dedup_groups, lab_tags antes de lab_results/runs/searches + afterEach fetch restore). Incluir 1 teste fumaca `it('skeleton sobe com 2 usuarios e projeto', ...)` criando projeto via API e esperando 201 + x-request-id. Imports das 8 tabelas novas no bloco @uhhu/db. Nunca any; fixtures via unknown+narrowing. `void execFileSync;` para tree-shaking.
  </action>
  <verify>
    <automated>pnpm vitest run --project integration -- lab-corpus 2>&1 | tail -n 20</automated>
  </verify>
  <done>Fixture com 8 itens + skeleton com harness 2 usuarios verde (ou skip gracioso offline), pronto para expansao em 04-04</done>
  <acceptance_criteria>test -f tests/integration/fixtures/dedup-overlap.json; grep -c "labDedupGroups\|labRejectedPairs\|labCanonicalPins" tests/integration/lab-corpus.test.ts >= 3; grep -c "bootstrapAdmin\|createMember\|describe.skipIf" tests/integration/lab-corpus.test.ts >= 3; pnpm vitest run --project integration -- lab-corpus passa ou skips sem falha</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema→PG DEV | DDL 0003 aplicada via migrate com credencial DEV (nunca prod) |
| test→PG | Harness com 2 usuarios exercita isolamento futuro; fixtures sem PII/segredos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-SQLI | Tampering | packages/db/src/schema.ts | mitigate | Drizzle parametrizado; CHECKs allowlist; sem SQL concatenado; validado em 04-04 com filtros/ordenacao |
| T-04-02-IDOR-SCHEMA | Elevation of privilege | 8 tabelas (FK→projects cascade) | mitigate | Cadeia de ownership via projects.id ON DELETE cascade; queries owner-first JOIN em 04-03; teste dono/estranho em 04-04/04-05 |
| T-04-02-MIGRATE-LEAK | Information disclosure | db:migrate logs | mitigate | redactConnectionString; nunca printar connection string; Gitleaks no gate final |
| T-04-02-FIXTURE | Information disclosure | dedup-overlap.json | accept | Dados ficticios sem PII/segredos; rationale: fixture sintetica para threshold |
</threat_model>

<verification>
- `pnpm --filter @uhhu/db run typecheck` verde
- `grep -c CREATE TABLE` == 8 em 0003; `db:migrate` exit 0 no DEV ou skip documentado
- `pnpm vitest run --project integration -- lab-corpus` verde/skip; skeleton com 2 usuarios
</verification>

<success_criteria>
- 8 tabelas com UNIQUEs/CHECKs/FKs (D-42/D-45/D-46); 0003 aplicada; fixtures + skeleton prontos para 04-03
</success_criteria>

<output>
After completion, create `.planning/phases/04-corpus-e-exportacao/04-02-SUMMARY.md`
</output>
