---
phase: 04-corpus-e-exportacao
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/core-api/src/lib/corpus.ts
autonomous: true
requirements: [LAB-07, LAB-08, LAB-09, LAB-10]
must_haves:
  truths:
    - "Mesma obra BDTD+CAPES aparece em um grupo expansivel com N origens sem apagar proveniencia"
    - "Fuzzy nao confirmado fica pending fora do corpus; aceite/rejeicao por grupo persiste por projeto"
    - "Decisao UMA por grupo dirige corpus que reflete na hora; divergencia por fonte nao muda decisao"
    - "Comparacao 2+ buscas retorna exatamente 4 blocos sobre ultimo run concluido"
  artifacts:
    - path: "apps/core-api/src/lib/corpus.ts"
      provides: "Casos de uso *ForActor: groups/confirm/reject/decision/tags/divergence/pin/corpus/compare"
      exports: ["computeDedupGroupsForActor", "confirmGroupForActor", "rejectGroupForActor", "setGroupDecisionForActor", "getCorpusForActor", "compareSearchesForActor"]
  key_links:
    - from: "apps/core-api/src/lib/corpus.ts"
      to: "projects.owner_id"
      via: "INNER JOIN projects ON groups.projectId=projects.id WHERE ownerId=actor.userId"
      pattern: "innerJoin\\(projects"
    - from: "apps/core-api/src/lib/corpus.ts"
      to: "apps/core-api/src/lib/dedup.ts"
      via: "canonicalKey/titleSimilarity/completenessScore"
      pattern: "canonicalKey|titleSimilarity|completenessScore"
---

<objective>
Lib de revisao owner-first: dedup lifecycle + decisao/corpus + comparacao, tudo reutilizavel por REST (04-04) e CLI/MCP (Phase 5).

Purpose: Regra de negocio central D-40–D-49; sem rotas ainda (rotas em 04-04 consomem esta lib sem duplicar regra — AGENTS.md headless).
Output: apps/core-api/src/lib/corpus.ts completo com predicado compartilhado isCorpusEligible.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-e-exportacao/04-CONTEXT.md
@.planning/phases/04-corpus-e-exportacao/04-RESEARCH.md
@.planning/phases/04-corpus-e-exportacao/04-PATTERNS.md
@packages/contracts/src/lab.ts
@packages/db/src/schema.ts
@apps/core-api/src/lib/searches.ts
@apps/core-api/src/lib/dedup.ts

<interfaces>
From apps/core-api/src/lib/searches.ts (moldes verbatim):
```typescript
const uuidSchema = z.string().uuid();
const DEFAULT_LIMIT = 20; const MAX_LIMIT = 100;
function clampLimit(raw: number|undefined): number { if (raw===undefined||!Number.isSafeInteger(raw)) return DEFAULT_LIMIT; return Math.min(MAX_LIMIT, Math.max(1, raw)); }
// owner-first: .innerJoin(projects, eq(table.projectId, projects.id)).where(and(eq(table.id,id), eq(projects.ownerId, actor.userId))).limit(1) → undefined→null
// list: scope-check-then-list, limit+1 probe → hasMore → slice, cursor tolerante nunca lanca
// mappers: parse* narrowers para jsonb, isExecutableSource allowlist, .toISOString() para datas, nunca any
import { getProjectForActor } from './projects.js'; // reutilizar, nunca reimplementar scope
```
From dedup.ts (04-01): canonicalKey(title,year,authors), titleSimilarity(a,b), completenessScore(r), FUZZY_THRESHOLD=0.9.
Predicado compartilhado obrigatorio (Pitfall 5): `export function isCorpusEligible(g:{status:string;decision:string}): boolean { return (g.status==='confirmed'||g.status==='single-confirmed'||g.status==='confirmed') && g.decision==='eligible'; }` — na pratica: status==='confirmed' (exact/single criados confirmed; fuzzy pending ate aceite) && decision==='eligible'. Usado por corpus + compare-overlap + export-scope-corpus (04-04 importa, nao duplica WHERE).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: dedup engine — compute/confirm/reject/auto-attach/canonico/pin</name>
  <files>apps/core-api/src/lib/corpus.ts</files>
  <read_first>apps/core-api/src/lib/searches.ts, apps/core-api/src/lib/dedup.ts, packages/db/src/schema.ts, packages/contracts/src/lab.ts, .planning/phases/04-corpus-e-exportacao/04-PATTERNS.md</read_first>
  <action>Criar apps/core-api/src/lib/corpus.ts parte 1 (imports molde searches.ts linhas 15-45: drizzle-orm and/eq/inArray/desc, zod, type imports de @uhhu/contracts incluindo DedupGroupDTO/CorpusEntryDTO/CompareDTO + decision/tag/divergence/pin types, `import type { ActorContext } from '@uhhu/core'`, tabelas @uhhu/db incluindo labDedupGroups/labDedupMembers/labRejectedPairs/labCanonicalPins/labResults/labSearches/labSearchRuns/projects + type Db, `import { getProjectForActor } from './projects.js'`, `import { canonicalKey, titleSimilarity, completenessScore, FUZZY_THRESHOLD, normalizeTitle } from './dedup.js'`; nota `.js` suffix). Incluir `const uuidSchema=z.string().uuid(); const DEFAULT_LIMIT=20; const MAX_LIMIT=100; function clampLimit` verbatim + `function parseAuthors/parseNullableString` narrowers + `isExecutableSource`.
Implementar (todas owner-first via JOIN projects.ownerId=actor.userId; uuid invalido→null; fora escopo→null):
- `export async function computeDedupGroupsForActor(db:Db, actor:ActorContext, projectId:string): Promise<DedupGroupDTO[]>` — precedencia D-43: (1) carrega rejected_pairs do projeto como Set `min(kA,kB)|max(kA,kB)` para veto D-42; (2) carrega resultados do ULTIMO run concluido (succeeded|partial latest executedAt) de cada search do projeto (mesma base do compare D-48; evita ressuscitar runs antigos — RESEARCH A3); (3) passo exato: Map canonicalKey→membros via `canonicalKey(r.title,r.year,parseAuthors(r.authors))`; grupos existentes por (projectId,canonicalKey) mantem status+pin+decisao (auto-attach D-43 sem reconfirmar); novos exact → status confirmed confidence exact; (4) passo fuzzy: pares de chaves distintas com year igual non-null (blocking Pitfall 3; year null→sem fuzzy→single), `titleSimilarity>=FUZZY_THRESHOLD (0.9)` sobre titulos normalizados, veto se par em rejected → cria grupo confidence fuzzy status pending D-40 (nunca auto-confirma); restantes → singles confidence single status confirmed. Persiste grupos/membros (upsert por UNIQUE(project,canonicalKey); members UNIQUE(resultId)). Retorna DTOs com memberIds/origins/originCount/decision (default undecided se sem row)/canonicalResultId via resolveCanonical.
- `export function resolveCanonicalResult(members:Array<{id:string;source:string;sourceId:string;abstract:string|null;authors:string[];year:number|null;originUrl:string|null;sourceUrl:string|null}>, pin:{source:string;sourceId:string}|null): string` — se pin e membro com mesmo (source,sourceId) existe → seu id (D-45 override antes do score); senao maior completenessScore D-44, desempate source ASC ('bdtd'<'capes') depois sourceId ASC. Pura, exportada para teste.
- `export async function confirmGroupForActor(db,actor,groupId): Promise<DedupGroupDTO|null>` — JOIN scope D-41 (so dono); UPDATE status pending→confirmed; null se fora escopo/uuid invalido.
- `export async function rejectGroupForActor(db,actor,groupId): Promise<DedupGroupDTO[]|null>` — D-42: so grupos fuzzy pending; insere par rejeitado em lab_rejected_pairs com keyA<keyB ordenado (par de canonicalKeys dos 2 membros representativos — ou de todos os pares do grupo) UNIQUE(project,keyA,keyB) onConflictDoNothing; divide em singles confirmed independentes (novos grupos confidence single, 1 membro cada); retorna os singles. Lembra veto para re-runs.
- `export async function setPinForActor(db,actor,groupId,input:{source:'bdtd'|'capes';sourceId:string}): Promise<DedupGroupDTO|null>` + `clearPinForActor` — upsert/delete lab_canonical_pins UNIQUE(groupId) D-45; persiste por projeto, sobrevive a auto-attach (compute le pin antes do score).
Mappers `toGroupDTO` com snake→camel, dates toISOString, unknown narrowing, nunca any. Sem HTTP/envelope na lib.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <done>compute/confirm/reject/pin com veto ordenado, trava de ano, threshold 0.9 e desempate deterministico; typecheck verde</done>
  <acceptance_criteria>grep -c "computeDedupGroupsForActor\|confirmGroupForActor\|rejectGroupForActor\|setPinForActor\|resolveCanonicalResult" apps/core-api/src/lib/corpus.ts >= 5; grep -c "FUZZY_THRESHOLD\|titleSimilarity\|completenessScore" apps/core-api/src/lib/corpus.ts >= 3; grep -c "innerJoin(projects" apps/core-api/src/lib/corpus.ts >= 3; grep -v '^ *//' apps/core-api/src/lib/corpus.ts | grep -c "any" == 0</acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: decisao/tags/divergencia/corpus view</name>
  <files>apps/core-api/src/lib/corpus.ts</files>
  <read_first>apps/core-api/src/lib/corpus.ts, packages/db/src/schema.ts, packages/contracts/src/lab.ts</read_first>
  <action>Acrescentar a corpus.ts (mesmo arquivo, append apos task 1; nao recriar):
- `export const DEFAULT_TAGS = ['incluir','excluir','duplicado','indisponível','revisar'] as const;` + `export async function ensureDefaultTags(db,actor,projectId): Promise<{id:string;name:string}[]>` — seed preguicoso idempotente D/LAB-09: getProjectForActor scope primeiro (null→[] ou throw? retornar null e rota vira 404); insert onConflictDoNothing por UNIQUE(project,name) com color null (RESEARCH A6/Q3); cobre projetos pre-existentes Phase 2 sem data migration.
- `export async function setGroupDecisionForActor(db,actor,groupId,input:{decision:'eligible'|'ineligible'|'undecided';reason?:string}): Promise<DedupGroupDTO|null>` — D-46 UMA por grupo: upsert lab_group_decisions por UNIQUE(groupId) (insert ou update decision+reason trim, reason max500 ja validado no Zod + CHECK); JOIN scope; reflete na hora (sem snapshot — corpus-as-view contrato §12).
- `export async function setDivergenceForActor(db,actor,groupId,input:{source:'bdtd'|'capes';note:string}): Promise<DedupGroupDTO|null>` — D-46 anotacao: sanitiza HTML na escrita `note.replace(/<[^>]*>/g,'').trim()` (baseline §2.5; contrato ja refina sem markup, defesa em profundidade); upsert UNIQUE(group,source); NUNCA altera lab_group_decisions (assert via teste 04-04).
- `export async function attachTagForActor/detachTagForActor/listTagsForActor` — tags escopadas por projeto (JOIN projects), group_tags por groupId com scope do projeto; fora escopo→null.
- `export function isCorpusEligible(g:{status:string;decision:string}): boolean { return g.status==='confirmed' && g.decision==='eligible'; }` // Pitfall 5 predicado UNICO.
- `export async function getCorpusForActor(db,actor,projectId,opts:{limit?:number;cursor?:string}): Promise<{items:CorpusEntryDTO[];page:PageInfo}>` — LAB-08 view na hora server-side: scope projeto primeiro (null→ {items:[],page:{limit,nextCursor:null,hasMore:false},total?} e rota vira 404); resolve grupos do projeto filtrados por isCorpusEligible via predicado compartilhado (pending NUNCA vaza); projeta registro canonico vigente via resolveCanonicalResult (pin antes do score D-45); membros vindos do ultimo run concluido de cada busca (consistencia A3); paginacao limit clamp 20/100 + cursor opaco base64url tolerante (malformado→primeira pagina, nunca lanca) + order estavel + limit+1 probe. Mapper toCorpusEntryDTO com tags (nomes), decision, originCount, origins.
Tudo owner-first, null→404 na rota, nunca envelope na lib, nunca any.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <done>Decisao unica por grupo + divergencia anotacao + seed lazy 5 tags + corpus view com predicado compartilhado; typecheck verde</done>
  <acceptance_criteria>grep -c "ensureDefaultTags\|setGroupDecisionForActor\|setDivergenceForActor\|getCorpusForActor\|isCorpusEligible" apps/core-api/src/lib/corpus.ts >= 5; grep -c "incluir.*excluir.*duplicado.*indisponível.*revisar" apps/core-api/src/lib/corpus.ts >= 1; grep -c "replace(/<\[^>\]" apps/core-api/src/lib/corpus.ts >= 1 || grep -c "replace(/<" apps/core-api/src/lib/corpus.ts >= 1</acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: comparacao 4 blocos sobre runs fixos</name>
  <files>apps/core-api/src/lib/corpus.ts</files>
  <read_first>apps/core-api/src/lib/corpus.ts, packages/integrations/src/health.ts, packages/contracts/src/lab.ts</read_first>
  <action>Acrescentar `export async function compareSearchesForActor(db,actor,baseSearchId:string,withSearchIds:string[]): Promise<CompareDTO|null>` — LAB-10/D-47–D-49: (1) valida base+with UUIDs (invalido→null); limita with a 10 buscas (DoS guard; >10 → throw com code VALIDATION_ERROR? Simples: slice? NAO — retornar null e rota da 400; documentar); total buscas 2..11 (base+with>=1); (2) para cada searchId: getSearchForActor-equivalente via JOIN projects (fora escopo ou de outro dono→null→rota 404 antes de agregar, anti-enumeracao); (3) ultimo run CONCLUIDO fixo D-48: `succeeded|partial` latest executedAt DESC limit 1 (nunca queued/running/cancelled; `failed` contribui zeros + sinalizacao — incluir com totals 0); (4) agrega do run: totals[searchId]=count results; yearHistogram[year]=count (year null→'desconhecido'); bySource[searchId]={bdtd:n,capes:n}; pairwiseOverlap[`a|b`]=intersecao de Sets de canonicalKeys exatas (+ grupos fuzzy confirmados compartilhados via members) D-47 dedup-aware cross-fonte; (5) retorna CompareDTO exatamente {searches, totals, yearHistogram, bySource, pairwiseOverlap} — SEM item lists D-49. Contagem em JS (molde health.ts windowed-aggregate), sem SQL concatenado, allowlist de status, nunca any. Exporta `toCompareDTO` mapper puro.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <done>compare com 4 blocos exatos sobre runs succeeded|partial fixos, overlap por chaves, escopo antes de agregar; typecheck verde</done>
  <acceptance_criteria>grep -c "compareSearchesForActor" apps/core-api/src/lib/corpus.ts >= 2; grep -c "pairwiseOverlap\|yearHistogram\|bySource" apps/core-api/src/lib/corpus.ts >= 3; grep -c "succeeded.*partial\|partial.*succeeded" apps/core-api/src/lib/corpus.ts >= 1</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib→PG | Todas as queries com actor.userId via JOIN projects (fora→null→404) |
| fuzzy→corpus | pending nunca vaza (predicado unico); overlap escopado antes de agregar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-IDOR | Elevation of privilege | corpus.ts todas as funcoes *ForActor | mitigate | JOIN projects.ownerId=actor.userId em groups/decisions/tags/compare; uuid invalido→null; testes dono/estranho em 04-04/04-05 |
| T-04-03-ENUM | Information disclosure | compareSearchesForActor overlap | mitigate | Pre-check de escopo por busca antes de agregar; buscas de outro dono→null→404; sem item lists (so counts) |
| T-04-03-XSS | Tampering | setDivergenceForActor note + reason | mitigate | Zod max-length + strip HTML `<[^>]*>` na escrita; nunca persistir markup |
| T-04-03-DOS | Denial of service | compute/compare Listas | mitigate | clampLimit 1..100, with≤10, selection≤1000 (rota reforca); fuzzy com blocking por ano (evita O(n²) global) |
| T-04-03-SQLI | Tampering | filtros/ordenacao | mitigate | Drizzle parametrizado + allowlists de enums; sem SQL concatenado |
</threat_model>

<verification>
- `pnpm --filter @uhhu/core-api run typecheck` verde apos cada task
- grep gates passam; `isCorpusEligible` unico (sem WHEREs divergentes)
- Nenhum `any`; nenhuma importacao de fetch/HTTP na lib
</verification>

<success_criteria>
- corpus.ts exporta 10+ funcoes *ForActor + predicado + resolveCanonical; D-40–D-49 implementados na lib prontos para rotas
</success_criteria>

<output>
After completion, create `.planning/phases/04-corpus-e-exportacao/04-03-SUMMARY.md`
</output>
