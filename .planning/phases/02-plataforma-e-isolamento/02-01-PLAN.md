---
phase: 02-plataforma-e-isolamento
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/index.ts
  - packages/contracts/src/errors.ts
  - packages/contracts/src/pagination.ts
  - packages/contracts/src/auth.ts
  - packages/contracts/src/projects.ts
  - packages/db/src/schema.ts
  - packages/db/src/index.ts
  - packages/config/src/env.ts
  - apps/core-api/src/plugins/requestId.ts
  - apps/core-api/src/plugins/errorHandler.ts
  - packages/core/src/actor.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - CORE-01
  - PLAT-05
must_haves:
  truths:
    - "Toda resposta de erro segue o envelope {error:{code,message,details,requestId}} com mensagem PT-BR e sem stack/SQL/tokens"
    - "Toda resposta carrega X-Request-Id (aceita o enviado quando seguro, gera quando ausente/inseguro)"
    - "Tipos de auth/projetos/paginacao existem em packages/contracts e nenhum outro pacote os duplica"
    - "Tabelas users/sessions/invites/password_resets/projects existem no schema Drizzle com ownerId server-side e constraints"
  artifacts:
    - path: "packages/contracts/src/errors.ts"
      provides: "Catalogo PT-BR de codigos de erro estaveis + envelope"
      contains: "ERROR_CATALOG"
    - path: "packages/contracts/src/pagination.ts"
      provides: "Helpers cursor limit+cursor nextCursor/hasMore"
      exports: ["encodeCursor", "decodeCursor", "paginationQuerySchema"]
    - path: "packages/db/src/schema.ts"
      provides: "Tabelas platform + projects"
      contains: "pgTable"
    - path: "apps/core-api/src/plugins/errorHandler.ts"
      provides: "Error handler global com envelope + requestId"
      contains: "errorHandler"
    - path: "packages/core/src/actor.ts"
      provides: "ActorContext derivado da sessao"
      exports: ["ActorContext", "requireUser"]
  key_links:
    - from: "apps/core-api/src/plugins/errorHandler.ts"
      to: "packages/contracts/src/errors.ts"
      via: "importa ERROR_CATALOG, nunca monta envelope inline"
      pattern: "ERROR_CATALOG"
    - from: "packages/db/src/schema.ts"
      to: "packages/contracts/src/auth.ts"
      via: "nomes de colunas snake_case mapeiam para camelCase dos DTOs"
      pattern: "owner_id|token_hash"
---

<objective>
Fundacao da Fase 2: contratos compartilhados, schema Drizzle + migrations, env estendido e convencoes transversais (envelope de erro PT-BR, X-Request-Id, ActorContext, paginacao). Nenhuma rota de produto aqui — este plano destrava 02-02 e 02-03 em paralelo.

Purpose: Sem contratos + schema congelados primeiro, auth e projetos divergem (duplicacao de tipos, ownerId no body, envelope inconsistente). Este plano e o contrato que os executores paralelos implementam contra.
Output: packages/contracts populado, schema + migration 0001, plugins requestId/errorHandler, ActorContext, deps instaladas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plataforma-e-isolamento/02-CONTEXT.md
@dev-docs/07-core-contract.md
@dev-docs/08-security-baseline.md
@AGENTS.md

<interfaces>
From apps/core-api/src/health.ts (padrao a estender, nao duplicar):
```typescript
export interface HealthResponse { status: 'ok'|'degraded'; db: 'ok'|'degraded'; version: string; migrationsApplied: number; }
const REQUEST_ID_PATTERN = /^[A-Za-z0-9_.:~-]{1,128}$/;
function resolveRequestId(header: string | string[] | undefined): string
// health registra via app.get('/health') com reply.header('x-request-id', requestId)
```
From packages/db/src/client.ts (unico lugar que abre conexao):
```typescript
export type Db = PostgresJsDatabase<typeof schema>;
export function createDb(url: string): Db
// NUNCA ler process.env aqui; NUNCA SQL concatenado; usar sql`` parametrizado
```
From packages/config/src/env.ts (estender, nao quebrar):
```typescript
// envSchema Zod com APP_DATABASE_URL, MIGRATION_DATABASE_URL, PORT, LOG_LEVEL, NODE_ENV
// DEPURAÇÃO: nunca logar valores parseados
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: contratos compartilhados + catalogo de erros PT-BR</name>
  <files>packages/contracts/src/errors.ts, packages/contracts/src/pagination.ts, packages/contracts/src/auth.ts, packages/contracts/src/projects.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/index.ts (estado atual: placeholder)
    - dev-docs/07-core-contract.md §9 (convenoes, envelope, paginacao)
    - .planning/phases/02-plataforma-e-isolamento/02-CONTEXT.md (D-25, D-27)
  </read_first>
  <action>Substituir o placeholder por 5 modulos reais, todos com `import type` compatível e ZERO `any` (nem em testes). Valores concretos, nao referencias:
  1. `errors.ts`: exportar `ERROR_CATALOG: Record<ErrorCode, string>` com EXATAS mensagens PT-BR: INVALID_CREDENTIALS="E-mail ou senha inválidos.", ACCOUNT_LOCKED="Conta temporariamente bloqueada. Tente novamente em 15 minutos ou redefina a senha.", INVITE_INVALID="Convite inválido ou expirado.", INVITE_REVOKED="Convite revogado.", SESSION_EXPIRED="Sessão expirada. Entre novamente.", UNAUTHENTICATED="Autenticação necessária.", NOT_FOUND="Recurso não encontrado.", VALIDATION_ERROR="Dados inválidos.", CONFIRMATION_REQUIRED="Confirme a exclusão passando ?confirm=true.", RATE_LIMITED="Muitas tentativas. Tente novamente em instantes.", PASSWORD_RESET_SENT="Se o e-mail estiver cadastrado, enviamos o link.", INTERNAL_ERROR="Erro interno. Tente novamente."; exportar `ErrorCode = keyof typeof ERROR_CATALOG`, `ErrorEnvelope = { error: { code: ErrorCode; message: string; details: unknown; requestId: string } }`, e `buildEnvelope(code, requestId, details?)` que monta o envelope a partir do catalogo (nunca mensagem inline nas rotas).
  2. `pagination.ts`: exportar `paginationQuerySchema = z.object({ limit: z.coerce.number().int().min(1).max(100).default(20), cursor: z.string().max(512).optional() })`, `encodeCursor(createdAt: string, id: string): string` (base64url de `${createdAt}|${id}`), `decodeCursor(cursor: string): { createdAt: string; id: string } | null` (retorna null em cursor malformado, nunca lanca), `PageInfo = { limit: number; nextCursor: string | null; hasMore: boolean }`.
  3. `auth.ts`: schemas Zod `registerSchema = z.object({ name: z.string().trim().min(1).max(100), email: z.string().trim().toLowerCase().email().max(254), password: z.string().min(12).max(128), inviteToken: z.string().min(32).max(256) })`, `loginSchema = z.object({ email, password: z.string().min(1).max(128), rememberMe: z.boolean().optional().default(true) })`, `resetRequestSchema`, `resetConfirmSchema = z.object({ token: z.string().min(32).max(256), newPassword: z.string().min(12).max(128) })`; DTOs `PublicUser = { id, name, email, role, createdAt }` (NUNCA passwordHash); `SessionInfo = { id, createdAt, lastSeenAt, userAgent, ip, current }`.
  4. `projects.ts`: `createProjectSchema = z.object({ title: z.string().trim().min(1).max(200), researchQuestion: z.string().trim().max(2000).optional(), description: z.string().trim().max(5000).optional() })`, `updateProjectSchema = createProjectSchema.partial().extend({ status: z.enum(['active','archived']).optional() })`, `ProjectDTO` camelCase `{ id, title, researchQuestion: string | null, description: string | null, status, createdAt, updatedAt }`.
  5. `index.ts`: re-exportar os 4 modulos (`export * from './errors.js'` etc.) e REMOVER `CONTRACTS_PLACEHOLDER`. Nenhum tipo de buscas/runs/results aqui (Phase 3+).</action>
  <acceptance_criteria>
    - `packages/contracts/src/errors.ts contains ERROR_CATALOG` e `contains E-mail ou senha inválidos.`
    - `packages/contracts/src/pagination.ts contains encodeCursor` e `contains nextCursor`
    - `grep -rn "any" packages/contracts/src/ | wc -l` == 0 (proibido ate em testes)
    - `pnpm --filter @uhhu/contracts typecheck` exit 0 (ou `pnpm typecheck` parcial sem erro no pacote)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/contracts typecheck 2>&1 | tail -5; grep -c "ERROR_CATALOG" packages/contracts/src/errors.ts; grep -rn "as any\|: any" packages/contracts/src/ | wc -l</automated>
  </verify>
  <done>Contratos importaveis por qualquer pacote; catalogo PT-BR unico; zero `any`.</done>
</task>

<task type="auto">
  <name>task 2: schema Drizzle + migration 0001 + env + ActorContext + plugins base</name>
  <files>packages/db/src/schema.ts, packages/db/src/index.ts, packages/config/src/env.ts, packages/core/src/actor.ts, packages/core/src/index.ts, apps/core-api/src/plugins/requestId.ts, apps/core-api/src/plugins/errorHandler.ts</files>
  <read_first>
    - packages/db/src/schema.ts (atual: so infra_proof — PRESERVAR a tabela, nao apagar)
    - packages/db/src/client.ts (factory createDb, checkDatabase)
    - packages/config/src/env.ts (schema atual)
    - apps/core-api/src/health.ts (padrao REQUEST_ID_PATTERN + resolveRequestId)
    - apps/core-api/src/index.ts (boot atual)
  </read_first>
  <action>Implementar a base de persistencia e convencoes (valores concretos):
  1. Deps (neste plano, para nao haver conflito de package.json nos planos paralelos): `pnpm add argon2@^0.41.1 @fastify/cookie@^11.0.2 @fastify/rate-limit@^10.3.0 -w` ou nos pacotes corretos (`argon2` em core-api; `@fastify/cookie`, `@fastify/rate-limit` em core-api). Travar versoes resolvidas no lockfile.
  2. `packages/db/src/schema.ts`: PRESERVAR `infraProof`; ADICIONAR com `pgTable` snake_case: `users(id uuid PK defaultRandom, email text UNIQUE NOT NULL (sempre minusculo), name text NOT NULL, password_hash text NOT NULL, role text NOT NULL DEFAULT 'member' CHECK role IN ('admin','member'), failed_attempts int NOT NULL DEFAULT 0, locked_until timestamptz, created_at defaultNow, updated_at)`, `invites(id uuid PK, token_hash text UNIQUE NOT NULL, invited_email text nullable, expires_at NOT NULL, used_at nullable, revoked_at nullable, created_by uuid FK users.id nullable, created_at)`, `sessions(id uuid PK, user_id uuid FK users.id ON DELETE CASCADE NOT NULL, token_hash text UNIQUE NOT NULL, expires_at NOT NULL, remember_me boolean NOT NULL DEFAULT true, user_agent text, ip text, created_at, last_seen_at)`, `password_resets(id uuid PK, user_id FK CASCADE NOT NULL, token_hash UNIQUE NOT NULL, expires_at NOT NULL, used_at nullable, created_at)`, `projects(id uuid PK, owner_id uuid FK users.id ON DELETE CASCADE NOT NULL, title text NOT NULL CHECK char_length 1..200, research_question text nullable, description text nullable, status text DEFAULT 'active' CHECK IN ('active','archived'), created_at, updated_at)` + index `(owner_id, created_at DESC)` via `index()`. Tipos inferidos exportados. NENHUM `workspaceId` (D-23: ownerId so).
  3. Gerar migration: `pnpm --filter @uhhu/db db:generate --name 0001_platform_projects` (drizzle-kit) e conferir que `packages/db/drizzle/0001_*.sql` cria as 5 tabelas com FK/UNIQUE/CHECK; nao editar SQL a mao salvo para `gen_random_uuid()` (exige `pgcrypto`? usar `gen_random_uuid()` nativo PG13+ sem extensao).
  4. `packages/config/src/env.ts`: ADICIONAR opcionais `COOKIE_SECRET: z.string().min(32).optional()`, `SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASS/SMTP_FROM` todos `.optional()` (reset cai para log-only em DEV quando ausentes — sem quebrar boot). Manter fail-closed para as URLs existentes.
  5. `packages/core/src/actor.ts`: `ActorContext = { userId: string; role: 'admin'|'member'; requestId: string; authMethod: 'session' }`, `requireUser(actor: ActorContext | null): ActorContext` (lanca erro tipado `UNAUTHENTICATED` quando null), `isAdmin(actor)`. `packages/core/src/index.ts` re-exporta. `packages/core/package.json` se inexistente: criar pacote `@uhhu/core` minimo (type module, exports ./src/index.ts) com deps `zod`, `@uhhu/contracts`.
  6. `apps/core-api/src/plugins/requestId.ts`: hook `onRequest` que reutiliza o MESMO `REQUEST_ID_PATTERN` do health (copiar a regex literalmente): header valido -> propaga; ausente/invalido -> `randomUUID()`; sempre `reply.header('x-request-id', id)` + `request.requestId = id` (declarar via `declare module 'fastify'`). `errorHandler.ts`: `setErrorHandler` que (a) ZodError -> 400 VALIDATION_ERROR com `details: err.flatten()`, (b) erros com `statusCode` -> preserva status com envelope do catalogo, (c) resto -> 500 com code `INTERNAL_ERROR` (definido no catalogo da task 1). Logar erro com requestId redigindo `authorization/cookie` (logger ja redige). NUNCA incluir stack/SQL/token na resposta.
  NAO tocar em `apps/core-api/src/index.ts` (wiring e do plano 02-04) e NAO criar rotas aqui.</action>
  <acceptance_criteria>
    - `packages/db/src/schema.ts contains pgTable('users'` e `contains pgTable('projects'` e `contains owner_id`
    - `ls packages/db/drizzle/0001_*.sql` existe e `grep -c "CREATE TABLE" packages/db/drizzle/0001_*.sql` >= 5
    - `apps/core-api/src/plugins/errorHandler.ts contains INTERNAL_ERROR` e `contains requestId`
    - `packages/core/src/actor.ts contains ActorContext` e `contains requireUser`
    - `pnpm typecheck` parcial (contracts+db+config+core) exit 0
  </acceptance_criteria>
  <verify>
    <automated>ls packages/db/drizzle/0001_*.sql && grep -c "CREATE TABLE" packages/db/drizzle/0001_*.sql; grep -c "INTERNAL_ERROR" apps/core-api/src/plugins/errorHandler.ts packages/contracts/src/errors.ts; pnpm --filter @uhhu/contracts --filter @uhhu/db --filter @uhhu/config typecheck 2>&1 | tail -8</automated>
  </verify>
  <done>Migration gera as 5 tabelas; envelope+requestId+ActorContext prontos para os planos paralelos usarem sem se tocar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (plugins) | Envelope/requestId manipulaveis; header X-Request-Id hostil |
| schema→PG (migrations) | DDL com role uhhu_migrate; runtime nunca DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | S (Spoofing) | requestId.ts | mitigate | Allowlist regex `^[A-Za-z0-9_.:~-]{1,128}$` copiada do health; valor fora → `randomUUID()`; nunca refletir CRLF |
| T-02-01-02 | I (Info Disclosure) | errorHandler.ts | mitigate | 500 usa `INTERNAL_ERROR` generico PT-BR; stack/SQL/cookie nunca na resposta; logger com redact herdado do boot |
| T-02-01-03 | T (Tampering) | schema users/projects | mitigate | `owner_id` FK CASCADE + UNIQUE em email/token_hash; CHECKs de role/status/tamanho no banco, nao so no Zod |
| T-02-01-04 | E (Elevation) | actor.ts | mitigate | `ActorContext.role` derivado do banco na fase de auth (planos 02-02/03); `requireUser` falha fechada; sem `workspaceId` para nao inventar escopo (D-23) |
</threat_model>

<verification>
- `pnpm typecheck` nos pacotes tocados passa; `pnpm lint` sem erro nos novos arquivos; `any` ausente em contracts/core.
- Migration aplica em PG efemero do CI (service) sem erro — prova na integracao dos planos seguintes.
</verification>

<success_criteria>
- Contratos unicos importaveis; schema com 5 tabelas + migration; plugins de convencao prontos; rotas ainda nao existem (escopo respeitado).
</success_criteria>

<output>
After completion, create `.planning/phases/02-plataforma-e-isolamento/02-01-SUMMARY.md`
</output>
