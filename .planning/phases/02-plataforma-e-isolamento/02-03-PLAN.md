---
phase: 02-plataforma-e-isolamento
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/core-api/src/lib/projects.ts
  - apps/core-api/src/routes/projects.ts
  - tests/integration/projects.test.ts
autonomous: true
requirements:
  - LAB-01
  - PLAT-03
  - CORE-01
must_haves:
  truths:
    - "Usuario cria, lista (paginado), le e atualiza projetos (titulo obrigatorio, pergunta/descricao opcionais e editaveis)"
    - "Projeto fora do owner logado retorna 404 sem revelar existencia em leitura, alteracao e exclusao"
    - "Excluir exige ?confirm=true e apaga; arquivar oculta da lista e permite reativar"
    - "Lista pagina com limit+cursor (default 20, max 100) e retorna nextCursor/hasMore com ordenacao estavel"
  artifacts:
    - path: "apps/core-api/src/routes/projects.ts"
      provides: "CRUD /api/v1/projects com ownerId da sessao"
      contains: "/api/v1/projects"
    - path: "apps/core-api/src/lib/projects.ts"
      provides: "Queries escopadas por owner + serializacao camelCase"
      contains: "owner_id"
    - path: "tests/integration/projects.test.ts"
      provides: "Isolamento dono/estranho + paginacao"
      contains: "404"
  key_links:
    - from: "apps/core-api/src/routes/projects.ts"
      to: "packages/contracts/src/projects.ts"
      via: "createProjectSchema/updateProjectSchema validam todo body"
      pattern: "createProjectSchema|updateProjectSchema"
    - from: "apps/core-api/src/lib/projects.ts"
      to: "packages/db/src/schema.ts"
      via: "toda query filtra owner_id = actor.userId"
      pattern: "owner_id.*actor|eq\(projects.ownerId"
    - from: "apps/core-api/src/routes/projects.ts"
      to: "packages/core/src/actor.ts"
      via: "actor vem de requireAuth do 02-01/02-02, nunca do body"
      pattern: "request.actor|requireAuth"
---

<objective>
Primeiro recurso isolado: projetos do Lab (LAB-01) com isolamento ownerId server-side (PLAT-03) e convencoes REST/paginacao (CORE-01). Decide D-22, D-23, D-24 e D-27-aplicado (per D-22--D-24, D-27).

Purpose: Prova que o isolamento funciona num recurso real antes das buscas (Phase 3). O padrao ownerId-first criado aqui e copiado por todos os recursos futuros.
Output: Rotas /api/v1/projects/* + testes PG (sem wiring no index.ts — wiring e do 02-04).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plataforma-e-isolamento/02-CONTEXT.md
@dev-docs/03-lab-spec-v1.md
@dev-docs/07-core-contract.md
@dev-docs/08-security-baseline.md
@AGENTS.md

<interfaces>
From 02-01 (consumir, nao recriar):
```typescript
// packages/contracts/src/projects.ts
createProjectSchema, updateProjectSchema, ProjectDTO
// packages/contracts/src/pagination.ts
paginationQuerySchema, encodeCursor, decodeCursor, PageInfo
// packages/contracts/src/errors.ts
buildEnvelope, ERROR_CATALOG (NOT_FOUND, VALIDATION_ERROR, CONFIRMATION_REQUIRED)
// packages/db/src/schema.ts
projects { id, ownerId(owner_id), title, researchQuestion(research_question), description, status, createdAt, updatedAt }
// packages/core/src/actor.ts
ActorContext { userId, role, requestId, authMethod }
```
Constraint: NAO modificar contracts/schema/env/index.ts/routes/auth neste plano. Para autenticar nos testes sem depender do codigo do 02-02 (paralelo), importar `requireAuth` de `apps/core-api/src/auth/requireAuth.ts` SE existir, senao usar stub de teste que injeta `request.actor` com o MESMO formato ActorContext (o wiring real com sessao e provado no 02-04). Nunca aceitar `ownerId` do body/query.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: lib de projetos escopada + rotas CRUD com paginacao</name>
  <files>apps/core-api/src/lib/projects.ts, apps/core-api/src/routes/projects.ts</files>
  <read_first>
    - packages/contracts/src/projects.ts (schemas e limites exatos)
    - packages/contracts/src/pagination.ts (encode/decodeCursor)
    - packages/db/src/schema.ts (colunas snake_case + tipos inferidos)
    - apps/core-api/src/auth/requireAuth.ts (se o 02-02 ja escreveu; senao seguir a assinatura do 02-01: preHandler que seta request.actor)
  </read_first>
  <action>Implementar com valores concretos (zero `any`, `import type` para DTOs):
  1. `lib/projects.ts`: `toProjectDTO(row): ProjectDTO` (snake→camel: `research_question→researchQuestion`, datas via `toISOString()` UTC); `createProject(db, actor, input: CreateProjectInput)` (insert com `owner_id: actor.userId` — NUNCA de `input`); `getProjectForActor(db, actor, id)` (UUID valido? senao null; `where and(eq(id), eq(owner_id, actor.userId))` — UMA query escopada, nunca findById+check depois); `listProjectsForActor(db, actor, { limit, cursor, status })` (status default `'active'`; `'all'` inclui arquivados; cursor via `decodeCursor` — malformado ignora e comeca do inicio; ordenacao estavel `created_at DESC, id DESC`; busca `limit+1` para `hasMore`; `nextCursor = encodeCursor(last.createdAt, last.id)` quando hasMore senao null); `updateProjectForActor` (PATCH parcial: so `title/researchQuestion/description/status`; `updated_at=now()`); `deleteProjectForActor` (delete escopado; retorna boolean found).
  2. `routes/projects.ts`: `buildProjectRoutes(app, db)` com `preHandler: requireAuth(db)` em TODAS: `POST /api/v1/projects` (201 + ProjectDTO), `GET /api/v1/projects` (query `paginationQuerySchema` + `status: z.enum(['active','archived','all']).default('active')`; 200 `{ items: ProjectDTO[], page: PageInfo }`), `GET /api/v1/projects/:id` (200 ou 404 NOT_FOUND "Recurso não encontrado." quando fora do escopo — SEM distinguir inexistente vs alheio), `PATCH /api/v1/projects/:id` (200 ou 404; `title` vazio → 400 VALIDATION_ERROR), `DELETE /api/v1/projects/:id` (sem `?confirm=true` → 400 CONFIRMATION_REQUIRED; com → 204 ou 404). Arquivar = `PATCH { status: 'archived' }`; reativar = `PATCH { status: 'active' }` (D-24). IDs opacos UUID; resposta JSON UTF-8 camelCase. NAO registrar em index.ts.</action>
  <acceptance_criteria>
    - `apps/core-api/src/lib/projects.ts contains owner_id` e `contains toProjectDTO`
    - `apps/core-api/src/routes/projects.ts contains /api/v1/projects` e `contains CONFIRMATION_REQUIRED` e `contains requireAuth`
    - `grep -rn "req.body.owner\|ownerId.*body\|query.owner" apps/core-api/src/routes/projects.ts apps/core-api/src/lib/projects.ts | wc -l` == 0 (ownerId nunca do cliente)
    - `pnpm --filter @uhhu/core-api typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>grep -n "owner_id\|toProjectDTO\|CONFIRMATION_REQUIRED\|requireAuth" apps/core-api/src/lib/projects.ts apps/core-api/src/routes/projects.ts | head -25; pnpm --filter @uhhu/core-api typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>CRUD isolado por owner com paginacao e arquivar/excluir conforme D-22--D-24.</done>
</task>

<task type="auto">
  <name>task 2: testes de integracao PG (dono/estranho/paginacao)</name>
  <files>tests/integration/projects.test.ts</files>
  <read_first>
    - apps/core-api/src/routes/projects.ts (rotas reais da task 1)
    - apps/core-api/src/lib/projects.ts (helpers escopados)
    - tests/integration/auth.test.ts (se o 02-02 ja escreveu — reutilizar helpers de usuario/sessao; senao criar usuarios direto via DB com argon2 do plano 02-02? NAO duplicar hash: inserir via SQL com hash argon2id valido gerado por `argon2.hash('SenhaTeste123!', ...)` no proprio teste)
    - vitest.workspace.ts (projetos smoke vs integration)
  </read_first>
  <action>Criar `tests/integration/projects.test.ts` contra PG real (skip gracioso offline, mesmo padrao do repo) que prova, com Fastify em memoria registrando `buildProjectRoutes` + stub de actor compativel (quando as sessoes reais do 02-02 nao estiverem disponiveis, o stub injeta `request.actor = { userId, role: 'member', requestId, authMethod: 'session' }` — o teste com sessoes reais e refeito no 02-04):
  1. CRUD feliz: cria (201, `title` obrigatorio; sem `title` → 400), le (200), atualiza titulo+pergunta (200, editavel a qualquer momento per D-22), lista pagina (200 com `page.{limit,nextCursor,hasMore}`).
  2. Isolamento: user A cria; user B `GET /:id` → 404 (nao 403, sem vazar existencia); B `PATCH` → 404; B `DELETE?confirm=true` → 404; A ainda le 200 (nao apagado por estranho).
  3. ID adulterado: UUID valido inexistente → 404 com envelope `{error:{code:'NOT_FOUND',message:'Recurso não encontrado.',requestId}}` + header `x-request-id` presente.
  4. Delete com confirmacao: sem `?confirm=true` → 400 CONFIRMATION_REQUIRED; com → 204 e GET seguinte 404; arquivar via PATCH status → some da lista default, aparece com `?status=archived`, reativa com `active`.
  5. Paginacao: criar 25 projetos, `GET ?limit=20` → 20 items + `hasMore:true` + `nextCursor` nao-nulo; seguir cursor → restantes + `hasMore:false`; `?limit=999` → clamp 100 (maximo D-27); default sem limit → 20.</action>
  <acceptance_criteria>
    - `tests/integration/projects.test.ts contains 404` e `contains nextCursor` e `contains CONFIRMATION_REQUIRED`
    - Integracao PG passa com banco real; sem PG pula sem falhar
    - Nenhum teste usa `any`; atores via ActorContext tipado
  </acceptance_criteria>
  <verify>
    <automated>grep -c "404\|nextCursor\|CONFIRMATION_REQUIRED" tests/integration/projects.test.ts; APP_DATABASE_URL="$APP_DATABASE_URL" pnpm vitest run --project integration tests/integration/projects.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>LAB-01 + PLAT-03 + CORE-01(paginacao) provados para projetos; matriz curl completa fica no 02-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/projects/:id | `:id`, `title`, `cursor`, `confirm` hostis; owner do body ignorado |
| actor→DB | `actor.userId` e a unica fonte de escopo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | E (Elevation/IDOR) | lib/projects.ts | mitigate | TODA query com `and(eq(id), eq(owner_id, actor.userId))`; fora do escopo → 404 identico a inexistente; teste dono/estranho/ID-adulterado |
| T-02-03-02 | T | PATCH/DELETE | mitigate | Zod em todo body/query; `title` 1..200; `confirm=true` obrigatorio; status allowlist `active\|archived` |
| T-02-03-03 | I | erros 404 | mitigate | Mensagem unica "Recurso não encontrado." sem distinguir motivo; envelope com requestId, sem stack/SQL |
| T-02-03-04 | D | listagem | mitigate | `limit` clamp 1..100 default 20; cursor opaco base64url com decode null-safe; ordenacao estavel evita pulos/duplicatas |
</threat_model>

<verification>
- Typecheck + integracao PG verde (ou skip gracioso); `curl` contra instancia real confirma 404 estranho e 400 sem confirm.
</verification>

<success_criteria>
- Projetos isolados por ownerId com paginacao e arquivar/excluir; nenhum ownerId vindo do cliente.
</success_criteria>

<output>
After completion, create `.planning/phases/02-plataforma-e-isolamento/02-03-SUMMARY.md`
</output>
