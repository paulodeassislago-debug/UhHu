---
phase: 03-buscas-e-adapters
plan: "04"
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - apps/core-api/src/lib/searches.ts
  - apps/core-api/src/lib/searchRuns.ts
  - apps/core-api/package.json
  - packages/integrations/src/index.ts
autonomous: true
requirements:
  - LAB-02
  - LAB-03
  - LAB-04
  - LAB-05
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "Search CRUD escopado por owner via cadeia →Project existe em lib owner-first"
    - "Executar busca cria SearchRun temporal com snapshot, diff de novos e métricas por fonte"
    - "1 fonte falha = partial com válidos preservados; 2 = failed; cancel preserva parciais"
    - "Idempotência 24h e rate-limit 10 runs/h por usuário funcionam"
  artifacts:
    - path: "apps/core-api/src/lib/searches.ts"
      provides: "CRUD searches owner-first + results paginados"
      contains: "ForActor"
    - path: "apps/core-api/src/lib/searchRuns.ts"
      provides: "motor de execução sync/async, retry, partial, diff, idempotência"
      contains: "Idempotency"
    - path: "packages/integrations/src/index.ts"
      provides: "barrel final incluindo adapters"
      contains: "adapters"
  key_links:
    - from: "apps/core-api/src/lib/searchRuns.ts"
      to: "packages/integrations/src/adapters.ts"
      via: "executor chama search por fonte"
      pattern: "getSourceAdapter"
    - from: "apps/core-api/src/lib/searchRuns.ts"
      to: "lab_search_runs"
      via: "drizzle insert/update de status e métricas"
      pattern: "labSearchRuns"
    - from: "apps/core-api/src/lib/searches.ts"
      to: "projects"
      via: "escopo owner via join search→project"
      pattern: "ownerId|owner_id"
---

<objective>
Lib de domínio lab: searches owner-first + motor de execução de runs (LAB-02/03/04/05, CORE-03/04, D-28–D-39-núcleo).

Purpose: Toda regra de busca vive aqui, uma vez só — rotas (03-05) são adaptadores finos HTTP. É o coração da fase.
Output: `lib/searches.ts`, `lib/searchRuns.ts`, barrel final de integrations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buscas-e-adapters/03-CONTEXT.md
@AGENTS.md
@dev-docs/03-lab-spec-v1.md
@dev-docs/07-core-contract.md
@apps/core-api/src/lib/projects.ts
@packages/contracts/src/lab.ts
@packages/db/src/schema.ts
@packages/integrations/src/types.ts
@packages/integrations/src/adapters.ts
@packages/integrations/src/postFilter.ts
@packages/integrations/src/health.ts

<interfaces>
Padrão owner-first a copiar de apps/core-api/src/lib/projects.ts:
```typescript
// TODA query filtra owner server-side; fora do escopo → null (rota vira 404).
// Cursor estável created_at DESC, id DESC + limit+1; 404 idêntico; Zod strip (ownerId nunca do body).
export async function getProjectForActor(db, actor, id): Promise<ProjectDTO | null>
```
Escopo lab: Search NÃO tem owner próprio — escopo via JOIN `lab_searches → projects WHERE projects.owner_id = actor.userId` em TODA query (get/list/update/delete de search, get de run, listagem de results). Result→Run→Search→Project reconstruível (LAB-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: dep workspace + lib searches owner-first + results paginados</name>
  <files>apps/core-api/package.json, apps/core-api/src/lib/searches.ts</files>
  <read_first>
    - apps/core-api/package.json (deps atuais — adicionar sem remover)
    - apps/core-api/src/lib/projects.ts (copiar padrão *ForActor, cursor, toDTO snake→camel, clamp limit)
    - packages/contracts/src/lab.ts (createSearchSchema, SearchDTO, ResultDTO, decodeResultsCursor)
    - packages/db/src/schema.ts (lab_searches, lab_results)
    - dev-docs/03-lab-spec-v1.md §3 (Search declarativa) e §5 (rotas)
  </read_first>
  <action>Criar `apps/core-api/src/lib/searches.ts` com `*ForActor(db, actor, ...)` e NENHUM acesso fora do escopo (D-22–D-24 herdados; ownerId sempre da sessão). Passo 0 (pré-requisito): adicionar `"@uhhu/integrations": "workspace:*"` às dependencies de `apps/core-api/package.json` e rodar `pnpm install`:
  1. `createSearchForActor(db, actor, input: CreateSearchInput)`: valida projectId pertence ao ator via `getProjectForActor` (reuso, não reimplementar); fora do escopo → retorna null (rota vira 404, sem revelar projeto alheio). Insere term/filters/sources.
  2. `getSearchForActor`, `listSearchesForActor(db, actor, projectId, {limit,cursor})` (cursor created_at|id como projetos, D-27), `updateSearchForActor` (term/filters/sources editáveis; runs antigos JÁ congelaram snapshot — D-33), `deleteSearchForActor` (cascade runs/results pelo FK).
  3. `getRunForActor(db, actor, runId)`: JOIN run→search→project com owner do ator; fora → null. `listRunsForActor(db, actor, searchId, {limit,cursor})` idem.
  4. `listResultsForActor(db, actor, runId, {limit, cursor})`: escopo via cadeia run→search→project; ordenação `source ASC, rank ASC, id ASC` + cursor `source|rank|id` (encode/decode de contracts, D-36); default 20 max 100; retorna `{items: ResultDTO[], page}` + `total` do run para o header da listagem. `getResultForActor` (ficha mínima do item — proveniência runId/source/sourceId/rawMetadata; decisão/elegibilidade é Phase 4).
  5. `toSearchDTO/toRunDTO/toResultDTO` snake→camel. `any` zero; UUID inválido → null (nunca throw).
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/core-api/package.json` contém `@uhhu/integrations: workspace:*`
    - `searches.ts` contém `createSearchForActor`, `getSearchForActor`, `listSearchesForActor`, `updateSearchForActor`, `deleteSearchForActor`, `getRunForActor`, `listRunsForActor`, `listResultsForActor`, `getResultForActor`
    - grep `ownerId` mostra filtro em TODA query (join com projects); grep `request.body.owner|input.ownerId` zero
    - ordenação results usa `source` + `rank` (grep `rank`)
  </acceptance_criteria>
  <done>Domínio de leitura/escrita de buscas isolado por owner via cadeia →Project</done>
</task>

<task type="auto">
  <name>task 2: motor de execução de runs (sync/async, retry, partial, diff, idempotência)</name>
  <files>apps/core-api/src/lib/searchRuns.ts</files>
  <read_first>
    - apps/core-api/src/lib/searches.ts (task 1 — getSearchForActor para escopo)
    - packages/integrations/src/adapters.ts (getSourceAdapter, ADAPTER_VERSIONs)
    - packages/integrations/src/postFilter.ts (postFilter)
    - packages/integrations/src/health.ts (recordSourceEvent)
    - packages/integrations/src/sourceClient.ts (SourceClient por fonte, challenge→renova 1×)
    - packages/db/src/schema.ts (lab_search_runs, lab_results, lab_idempotency_keys)
    - .planning/phases/03-buscas-e-adapters/03-CONTEXT.md (D-28, D-29, D-30, D-35, D-37, D-38, D-39 valores exatos)
  </read_first>
  <action>Criar `apps/core-api/src/lib/searchRuns.ts` com `executeSearchRun(db, actor, searchId, opts {idempotencyKey?: string, fetchFn?}): Promise<{run: SearchRunDTO, sync: boolean, replayed: boolean}>`. Fluxo exato:
  1. Escopo: search via `getSearchForActor` → null = fora do escopo (rota 404). `oasisbr` em sources → lança `SourceDisabledError` (rota 400).
  2. Rate-limit próprio (D-39): conta runs com `created_by=actor.userId AND executed_at > now-1h`; ≥10 → lança `RunRateLimitedError` (rota 429 RATE_LIMITED). Contagem no BANCO (sobrevive restart), não em memória.
  3. Idempotência (D-39): se `idempotencyKey` presente: `keyHash=sha256(userId|key|canonicalBody)` onde canonicalBody = JSON estável `{searchId, term, filters, sources}`; SELECT em lab_idempotency_keys com `expires_at>now`: hit + mesmo bodyHash → retorna run existente com `replayed:true` (rota responde 200 + header `Idempotent-Replayed: true`); hit + bodyHash diferente → lança `IdempotencyConflictError` (rota 422); miss/expirado → prossegue e insere a chave vinculada ao novo run com expires now+24h.
  4. Cria run `queued` com snapshot de term/filters/sources + executed_at=now + adapter_versions `{bdtd, capes}`; marca `running` com started_at.
  5. Execução por fonte (sequencial bdtd→capes, cortesia global): para cada fonte habilitada: `getSourceAdapter` → `search(client, def, {page:1, perPage:20}, {signal})` → challenge → renova jar 1× e retenta 1× (D-38) → falha persistente = fonte failed (registra `recordSourceEvent(ok:false)`); sucesso → `postFilter` → persiste items em lab_results (rank = índice da fonte, raw integral higienizado pelo adapter) → `recordSourceEvent(ok:true)`. Timeout de fila 60s (D-29): `AbortController` global do run com 60000ms; estouro → interrompe fontes restantes como failed sem apagar persistidos.
  6. Status final (D-37): 2 ok → `succeeded`; 1 ok → `partial`; 0 ok → `failed`. Métricas `RunMetrics`: perSource `{status,total,returned,durationMs}`, `newCount` = results cujo `(source,sourceId)` NÃO existe em NENHUM run anterior da mesma search (query anti-join — D-35, sem dedup cross-fonte), `coverage {bdtd, capes}` = counts do run atual (overlap exato é Phase 4/dedup — documentar). `error` jsonb só quando failed/partial com detalhe por fonte SEM cookies/hosts internos.
  7. Limite sync 25s (D-28): o CONTROLE sync/async vive na ROTA (03-05) via `opts.maxSyncMs=25000`: este lib expõe `executeSearchRun` que roda até o fim; a rota usa `Promise.race` com 25s — venceu a race sem concluir → responde 202 e a execução continua em background atualizando a mesma linha (run 1:1 com job — D-30; `GET /jobs/:id` lê a linha). Cancelamento (D-29): `cancelRunForActor(db, actor, runId)` — só `queued|running` → `cancelled` + finished_at, preservando results já persistidos (nunca deleta); executores verificam flag `cancelRequested` (coluna? usar status lido entre fontes — UPDATE concorrente + re-read; documentar a condição de corrida aceita no v1 monoprocesso).
  8. `fetchFn` injetável passado aos adapters (testes). `crypto.randomUUID` para ids se necessário (NUNCA Math.random).
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `searchRuns.ts` contém `executeSearchRun`, `cancelRunForActor`, literais `25000` (documentado como consumido pela rota), `60000`, `Idempotent-Replayed` mencionado, `newCount`, `coverage`
    - idempotência usa sha256 de `userId|key|body` com 24h; rate-limit conta no banco por `created_by` e janela 1h com limite 10
    - `Math.random` zero; `ownerId` do body zero
  </acceptance_criteria>
  <done>Motor decide sync/async-ready, partial, diff e idempotência com regras D-28–D-39 literais</done>
</task>

<task type="auto">
  <name>task 3: barrel final de integrations</name>
  <files>packages/integrations/src/index.ts</files>
  <read_first>
    - packages/integrations/src/index.ts (estado deixado pela 03-02)
    - packages/integrations/src/adapters.ts, packages/integrations/src/postFilter.ts (exports reais da 03-03)
  </read_first>
  <action>Estender o barrel `packages/integrations/src/index.ts` para exportar adapters + postFilter (`export * from './adapters.js'`, `'./postFilter.js'`) sem remover exports da 03-02; resolver colisões de nomes a favor dos nomes já exportados (renomear com `as` se preciso, nunca quebrar 03-02). Rodar typecheck do pacote + root.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/integrations run typecheck && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` contém `adapters.js` e `postFilter.js` além de sourceClient/registry/health/types
    - typechecks do pacote e do root verdes
  </acceptance_criteria>
  <done>Barrel único e estável para executor e rotas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota→lib | searchId/runId do cliente; lib revalida escopo owner em cada chamada |
| lib→fontes | Termo do usuário chega aos adapters; resultados externos entram no PG |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Elevation (IDOR) | searches.ts, searchRuns.ts | mitigate | TODA query com join owner via →Project; fora do escopo → null → 404; created_by server-side |
| T-03-04-02 | Tampering | idempotency key | mitigate | sha256(userId\|key\|body), janela 24h, conflito de body → 422 |
| T-03-04-03 | Denial of Service | execução de runs | mitigate | 10 runs/h por usuário (banco), timeout global 60s, perPage clamp, cancel cooperativo |
| T-03-04-04 | Information Disclosure | run.error, metrics | mitigate | sem cookies/hosts internos/corpos; rawMetadata já higienizado no adapter |
| T-03-04-05 | Repudiation | runs temporais | mitigate | snapshot congelado + executed/started/finished + adapter_versions = proveniência reconstruível |
</threat_model>

<verification>
Typecheck core-api verde; typecheck root verde; grep `Math.random|as any|: any` zero nos arquivos criados.
</verification>

<success_criteria>
Regra de busca existe uma vez só, isolada por owner, com partial/idempotência/rate-limit/diff implementados.
</success_criteria>

<output>
After completion, create `.planning/phases/03-buscas-e-adapters/03-04-SUMMARY.md`
</output>
