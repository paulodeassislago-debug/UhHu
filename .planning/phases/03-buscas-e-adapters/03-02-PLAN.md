---
phase: 03-buscas-e-adapters
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/integrations/package.json
  - packages/integrations/tsconfig.json
  - packages/integrations/src/sourceClient.ts
  - packages/integrations/src/registry.ts
  - packages/integrations/src/health.ts
  - packages/integrations/src/types.ts
  - packages/integrations/src/index.ts
autonomous: true
requirements:
  - SRC-03
  - SRC-04
  - LAB-12
must_haves:
  truths:
    - "Existe um SourceClient compartilhado por instância com cookie jar por fonte, mutex, rate limit, circuit breaker e timeout"
    - "Registry conhece bdtd+capes habilitadas e oasisbr desabilitada"
    - "Health de fonte é computável a partir de eventos recentes sem vazar segredos"
  artifacts:
    - path: "packages/integrations/src/sourceClient.ts"
      provides: "fetch corticado por fonte (jar, mutex, batch, breaker, timeout, UA)"
      min_lines: 100
    - path: "packages/integrations/src/registry.ts"
      provides: "bdtd+capes habilitadas, oasisbr desabilitada"
      contains: "oasisbr"
    - path: "packages/integrations/src/health.ts"
      provides: "ok|degraded|offline a partir de lab_source_events"
      contains: "degraded"
  key_links:
    - from: "packages/integrations/src/registry.ts"
      to: "packages/integrations/src/sourceClient.ts"
      via: "registry entrega client por fonte ao executor"
      pattern: "sourceClient|SourceClient"
    - from: "packages/integrations/src/health.ts"
      to: "@uhhu/db labSourceEvents"
      via: "leitura de eventos recentes por fonte"
      pattern: "labSourceEvents|lab_source_events"
---

<objective>
SourceClient compartilhado + registry de fontes + computação de health (SRC-03, SRC-04-parte, LAB-12-parte).

Purpose: Cortesia e resiliência contra BDTD/CAPES vivem numa camada só, testável e sem segredos — adapters (03-03) e executor (03-04) consomem, nunca reimplementam.
Output: Pacote `@uhhu/integrations` com SourceClient, registry e health.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buscas-e-adapters/03-CONTEXT.md
@AGENTS.md
@dev-docs/04-fontes-bdtd-capes.md
@dev-docs/07-core-contract.md
@dev-docs/08-security-baseline.md
@packages/contracts/src/lab.ts
@packages/db/src/schema.ts

<interfaces>
From 03-01 (devem existir após wave 1 — ler antes de codar):
```typescript
// packages/contracts/src/lab.ts
export interface SourceHealthDTO { source: string; status: 'ok'|'degraded'|'offline'; checkedAt: string; recent: { total: number; ok: number; failed: number; challenges: number } }
// packages/db/src/schema.ts
export const labSourceEvents; // { source, ok, isChallenge, at }
```
Padrão de pacote (copiar de packages/db/package.json): name `@uhhu/<nome>`, type module, main `./src/index.ts`, workspace deps, script `typecheck: tsc --noEmit -p tsconfig.json`, tsconfig extends `../../tsconfig.base.json` include `["src"]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: pacote + SourceClient compartilhado + registry</name>
  <files>packages/integrations/package.json, packages/integrations/tsconfig.json, packages/integrations/src/types.ts, packages/integrations/src/sourceClient.ts, packages/integrations/src/registry.ts, packages/integrations/src/index.ts</files>
  <read_first>
    - packages/db/package.json (molde exato de package de workspace)
    - packages/db/tsconfig.json (molde exato de tsconfig)
    - dev-docs/04-fontes-bdtd-capes.md §1–§2 (cortesia: batch ≤10 + wait 2s, UA, cookie jar, renovação adaptativa)
    - dev-docs/07-core-contract.md §15.2 (SourceClient: jar por fonte, mutex, rate limit global, breaker, timeout 60s, UA, sem credencial ao cliente)
    - dev-docs/08-security-baseline.md §2.5 (SSRF: allowlist de hosts, sem URL arbitrária)
  </read_first>
  <action>Criar o pacote `@uhhu/integrations` (substituir o README-reserva por código real, mantendo uma linha de fronteira no header de cada arquivo). Valores concretos, sem "configurável depois":
  1. `package.json`: name `@uhhu/integrations`, version 0.0.0, type module, main `./src/index.ts`, types idem, script typecheck, deps `zod ^4.5.4` + `@uhhu/db: workspace:*` (tipos Db + labSourceEvents p/ health.ts) + `drizzle-orm ^0.45.2` (query builder no health.ts); devDeps `tsx`. `tsconfig.json` idêntico ao de `@uhhu/db`.
  2. `src/types.ts`: `SourceName = 'bdtd' | 'capes' | 'oasisbr'`; `SourcePage { total: number | null, items: NormalizedItem[], sourceStatus: 'ok'|'failed'|'challenge' }`; `NormalizedItem { sourceId, title, authors: string[], year: number | null, docType: string | null, institution: string | null, program: string | null, abstract: string | null, originUrl: string | null, sourceUrl: string | null, rawMetadata: Record<string, unknown> }`; `SourceAdapter { name, level: 'official'|'unofficial', enabled, search(def, page, ctx): Promise<SourcePage>, enrich(sourceId, ctx): Promise<NormalizedItem> }`; `SearchDef { term, yearFrom?, yearTo?, docTypes?, area?, institution?, program? }`; `SourceClientContext { signal: AbortSignal, fetchFn?: typeof fetch }` (fetchFn injetável = testes determinísticos sem rede).
  3. `src/sourceClient.ts`: classe `SourceClient` UMA instância por fonte (construída pelo registry): cookie jar em memória (array de `name=value` por fonte, enviado em `Cookie`, atualizado de `set-cookie` nas respostas); `fetchText(url, init)` que (a) REJEITA qualquer host fora da allowlist `['bdtd.ibict.br','catalogodeteses.capes.gov.br']` com `SourceHostBlockedError` (SSRF — D-38 sem `rejectUnauthorized:false`; TLS default do Node, nunca `rejectUnauthorized:false` em lugar algum); (b) serializa concorrência por mutex simples (fila promise por fonte — anti thundering-herd); (c) rate limit global batch ≤10 + wait 2000ms entre batches (contador por fonte); (d) timeout por request 15000ms via AbortSignal.any com o signal do chamador; (e) header `User-Agent: UhHu-Lab/0.1.0-fase3` constante `UH_HU_UA` exportada; (f) detecta challenge (content-type text/html onde se esperava JSON, ou corpo contendo `OasisbrVerify`) e retorna `{ challenged: true }` para o chamador renovar o jar (limpa jar e sinaliza 1 retry com jar novo); (g) NUNCA loga cookie/set-cookie/corpo (logs só `{source, status, durationMs}`).
  4. `src/registry.ts`: `SOURCE_REGISTRY = [{name:'bdtd',level:'unofficial',enabled:true},{name:'capes',level:'unofficial',enabled:true},{name:'oasisbr',level:'unofficial',enabled:false}]` (dev-docs/04 §4); `getAdapter(name)` retorna client+meta ou lança `SourceDisabledError` quando `enabled:false` (a rota traduz para 400 SOURCE_DISABLED — D-33); `listSources()` para `GET /lab/sources`.
  5. `src/index.ts`: exporta types, SourceClient, registry, health (task 2). NÃO exportar adapters (03-03 cria os arquivos mas este plano detém o barrel; 03-04 fará o wiring final do barrel se preciso — sem conflito de wave).
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/integrations run typecheck && pnpm lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `packages/integrations/src/sourceClient.ts` contém `UH_HU_UA`, `bdtd.ibict.br`, `catalogodeteses.capes.gov.br`, `rejectUnauthorized` aparece ZERO vezes
    - `packages/integrations/src/registry.ts` contém `oasisbr` com `enabled:false` e `SourceDisabledError`
    - `packages/integrations/src/types.ts` contém `SourceAdapter`, `NormalizedItem`, `fetchFn`
    - typecheck do pacote verde sob strict
  </acceptance_criteria>
  <done>Pacote compila; hosts externos restritos à allowlist; oasisbr desabilitada no registry</done>
</task>

<task type="auto">
  <name>task 2: health ok|degraded|offline a partir de eventos</name>
  <files>packages/integrations/src/health.ts</files>
  <read_first>
    - packages/db/src/schema.ts (tabela lab_source_events criada na 03-01)
    - packages/contracts/src/lab.ts (SourceHealthDTO)
    - .planning/phases/03-buscas-e-adapters/03-CONTEXT.md (D-38: 1 retry/fonte; health por taxa recente de falhas/challenges, counts sem segredos)
  </read_first>
  <action>Criar `packages/integrations/src/health.ts` com `computeSourceHealth(db, source, now): Promise<SourceHealthDTO>` e `recordSourceEvent(db, source, ok, isChallenge)`. Regras numéricas exatas (discrição OpenCode concretizada, documentar no header): janela = últimos 50 eventos da fonte OU última 1h (o que for menor em cobertura, i.e. `WHERE source AND at > now-1h ORDER BY at DESC LIMIT 50`); 0 eventos → `{status:'ok', recent:{total:0,ok:0,failed:0,challenges:0}}`; senão failureRate = failed/total: 0 → ok; <0.5 → degraded; ≥0.5 → offline; qualquer evento com isChallenge conta em challenges E em failed. Retorno NUNCA inclui cookies, URLs internas, corpos ou connection strings — só counts + status + checkedAt ISO. `recordSourceEvent` insere 1 linha (chamado pelo executor 03-04, não por este plano).
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/integrations run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/integrations/src/health.ts` contém `computeSourceHealth`, `recordSourceEvent`, literais `degraded` e `offline`, e o threshold `0.5`
    - função não referencia `cookie`, `set-cookie`, `Authorization` ou `DATABASE_URL` (grep zero)
  </acceptance_criteria>
  <done>Health computável por fonte com thresholds documentados e sem vazamento</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integrations→BDTD/CAPES | Respostas externas hostis (HTML challenge, JSON malformado, TLS quebrado) |
| executor→SourceClient | Definição de busca vinda do usuário chega ao fetch da fonte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing/Tampering | sourceClient fetch | mitigate | allowlist de 2 hosts, `SourceHostBlockedError` fora dela; nunca `rejectUnauthorized:false` |
| T-03-02-02 | Information Disclosure | cookie jar / logs | mitigate | jar em memória, nunca em resposta/DB/log; logs só source+status+durationMs |
| T-03-02-03 | Denial of Service | fontes lentas | mitigate | timeout 15s por request, mutex por fonte, batch ≤10 + wait 2s, UA identificado |
| T-03-02-04 | Elevation | registry oasisbr | mitigate | `enabled:false` + `SourceDisabledError` traduzido para 400 (não 404 que vaza/oculta mal) |
</threat_model>

<verification>
`pnpm --filter @uhhu/integrations run typecheck` verde; `pnpm lint` limpo; grep `rejectUnauthorized` zero no pacote.
</verification>

<success_criteria>
Adapters futuros têm de onde herdar cortesia; health tem semântica numérica fixa; nenhum segredo atravessa a fronteira.
</success_criteria>

<output>
After completion, create `.planning/phases/03-buscas-e-adapters/03-02-SUMMARY.md`
</output>
