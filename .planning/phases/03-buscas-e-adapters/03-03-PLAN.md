---
phase: 03-buscas-e-adapters
plan: "03"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/integrations/src/bdtd.ts
  - packages/integrations/src/capes.ts
  - packages/integrations/src/postFilter.ts
  - packages/integrations/src/adapters.ts
  - tests/integration/lab-sources-contract.test.ts
  - tests/integration/fixtures/bdtd-search.json
  - tests/integration/fixtures/capes-busca.json
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-04
  - SRC-05
must_haves:
  truths:
    - "Adapter BDTD traduz SearchDef para VuFind lookfor+type+filter[] e normaliza para NormalizedItem"
    - "Adapter CAPES traduz SearchDef para POST rest/busca com Ano expandido e normaliza para NormalizedItem"
    - "Pós-filtro Core aplica localmente o que a fonte não honrou"
    - "Testes de contrato com fixtures passam no CI sem rede externa"
  artifacts:
    - path: "packages/integrations/src/bdtd.ts"
      provides: "search+enrich BDTD VuFind"
      contains: "vufind/api/v1/search"
    - path: "packages/integrations/src/capes.ts"
      provides: "search+enrich CAPES rest/busca"
      contains: "rest/busca"
    - path: "packages/integrations/src/postFilter.ts"
      provides: "pós-filtro local redundante"
      contains: "postFilter"
    - path: "tests/integration/lab-sources-contract.test.ts"
      provides: "contrato BDTD/CAPES com fixtures"
      min_lines: 60
  key_links:
    - from: "packages/integrations/src/bdtd.ts"
      to: "packages/integrations/src/sourceClient.ts"
      via: "fetch corticado (jar, mutex, timeout)"
      pattern: "SourceClient|fetchText"
    - from: "packages/integrations/src/postFilter.ts"
      to: "packages/integrations/src/types.ts"
      via: "opera sobre NormalizedItem[]"
      pattern: "NormalizedItem"
---

<objective>
Adapters BDTD + CAPES + pós-filtro Core + testes de contrato com fixtures (SRC-01, SRC-02, SRC-04, SRC-05).

Purpose: A busca real multi-fonte com compatibilidade máxima às APIs (D-31) e redundância fonte+Core (D-32). Fixtures congelam os shapes de 06/09/2026 para o CI não depender de rede.
Output: `bdtd.ts`, `capes.ts`, `postFilter.ts`, `adapters.ts`, fixtures + teste de contrato.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buscas-e-adapters/03-CONTEXT.md
@AGENTS.md
@dev-docs/04-fontes-bdtd-capes.md
@dev-docs/08-security-baseline.md
@packages/integrations/src/types.ts
@packages/integrations/src/sourceClient.ts
@packages/contracts/src/lab.ts

<interfaces>
From 03-01/03-02 (ler antes de codar — mesma wave que 03-02, então importar SÓ de types.ts/sourceClient.ts se já existirem; se 03-02 ainda não rodou, codar contra as assinaturas abaixo e o executor paralelo resolve no wiring 03-04):
```typescript
// packages/integrations/src/types.ts
export interface SearchDef { term: string; yearFrom?: number; yearTo?: number; docTypes?: string[]; area?: string; institution?: string; program?: string }
export interface NormalizedItem { sourceId: string; title: string; authors: string[]; year: number|null; docType: string|null; institution: string|null; program: string|null; abstract: string|null; originUrl: string|null; sourceUrl: string|null; rawMetadata: Record<string, unknown> }
export interface SourcePage { total: number|null; items: NormalizedItem[]; sourceStatus: 'ok'|'failed'|'challenge' }
export interface SourceClientContext { signal: AbortSignal; fetchFn?: typeof fetch }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: adapters BDTD e CAPES com enrich sob demanda</name>
  <files>packages/integrations/src/bdtd.ts, packages/integrations/src/capes.ts, packages/integrations/src/adapters.ts</files>
  <read_first>
    - dev-docs/04-fontes-bdtd-capes.md (shapes VuFind + rest/busca INTEIROS — §1 e §2 são a spec)
    - packages/integrations/src/sourceClient.ts (fetchText, challenge, SourceDisabledError) — se inexistente (03-02 em paralelo), codar contra a interface acima e registrar a assunção no SUMMARY
    - packages/contracts/src/lab.ts (RunMetrics perSource para preencher adapterVersions)
  </read_first>
  <action>Implementar os dois adapters com `ADAPTER_VERSION = 'bdtd/1.0-fase3'` e `'capes/1.0-fase3'` exportadas (vão para `adapter_versions` do run — D-34/proveniência):
  1. `bdtd.ts` — `searchBdtd(client, def, page {page, perPage}, ctx)`: GET `https://bdtd.ibict.br/vufind/api/v1/search?lookfor=<encodeURIComponent(def.term)>&type=AllFields` + `filter[]=format:"masterThesis"|"doctoralThesis"` (de docTypes; ausente = sem filtro de formato) + `filter[]=publishDate:[yearFrom TO yearTo]` (só quando ao menos um presente; ausente = sem filtro) + `page` + `limit` (perPage clamp 5..50, default 20). `type=AllFields` fixo (D-31: string livre, sem tradução lossy). Parse defensivo com Zod `.safeParse` do JSON (records, resultCount); item→NormalizedItem (id→sourceId, title, authors array, publishDate ano→year, format→docType, institution, link→originUrl, rawMetadata = registro INTEGRAL sem cookies). Challenge (HTML/OasisbrVerify) → `{sourceStatus:'challenge', items:[]}` para o executor renovar o jar e retentar 1× (D-38). `enrichBdtd(client, sourceId, ctx)`: GET `https://bdtd.ibict.br/vufind/Record/<encodeURIComponent(sourceId)>` HTML e extrai orientador/banca/programa/keywords/resumo via regexes ancoradas em marcadores estáveis (sem cheerio novo — regex + substring para não adicionar dependência); falha de parse → NormalizedItem parcial com rawMetadata `{recordUrl}` e abstract null (nunca lança em campo opcional).
  2. `capes.ts` — `searchCapes(client, def, page, ctx)`: POST `https://catalogodeteses.capes.gov.br/catalogo-teses/rest/busca` JSON `{termo: def.term, filtros: [...], pagina: page, registrosPorPagina: perPage}` onde filtros = um `{campo:'Ano', valor:'AAAA'}` POR ANO de yearFrom..yearTo expandido (cap em 30 anos; range maior → 422 local `RangeTooWideError` antes da rede), docTypes→`{campo:'Grau Acadêmico', valor}` (`masterThesis`→'Mestrado', `doctoralThesis`→'Doutorado'), area→`Grande Área Conhecimento`, program→`Área Conhecimento` (institution NÃO vai à fonte — só pós-filtro). perPage clamp 5..50 default 20 (mínimo operacional 5). Parse `{pagina, total, tesesDissertacoes[]}` defensivo; sem-divulgação (sinal da fonte) → sourceUrl null + rawMetadata.flag `semDivulgacao:true` (D: sem link quando sem divulgação). `enrichCapes(client, sourceId|link, ctx)`: GET ficha HTML, extrai `#resumo`, `#palavras`, presença de `#download:link_download_arquivo` (sem ele → sourceUrl null).
  3. Higiene D-34/segurança: antes de montar NormalizedItem, REMOVER de rawMetadata quaisquer chaves matching `/cookie|set-cookie|authorization|token|session/i` (função `sanitizeRaw()` exportada, testada). `any` proibido; `unknown` + narrowing.
  4. `adapters.ts`: `getSourceAdapter('bdtd'|'capes')` → `{search, enrich, version}`; `oasisbr` → lança o mesmo `SourceDisabledError` do registry (D-33). NÃO tocar `src/index.ts` (dono: 03-02/03-04) — este arquivo é importado direto pelo executor 03-04.
  5. `enrich*` implementadas + testadas, mas NÃO chamadas durante o run (enrich sob demanda/batch de elegíveis é Phase 4 — 03-lab-spec §6); documentar no header.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/integrations run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bdtd.ts` contém `vufind/api/v1/search`, `lookfor`, `publishDate`, `Record/` e `ADAPTER_VERSION`
    - `capes.ts` contém `rest/busca`, `registrosPorPagina`, `Grande Área Conhecimento`, `resumo`, `link_download_arquivo`
    - `sanitizeRaw` existe e remove chaves cookie/authorization (coberta por teste na task 3)
    - `rejectUnauthorized` zero no pacote; `any` zero nos 3 arquivos
  </acceptance_criteria>
  <done>Adapters traduzem SearchDef sem perda (string livre) e normalizam com raw integral higienizado</done>
</task>

<task type="auto">
  <name>task 2: pós-filtro Core redundante fonte+Core</name>
  <files>packages/integrations/src/postFilter.ts</files>
  <read_first>
    - packages/integrations/src/types.ts (SearchDef, NormalizedItem)
    - dev-docs/03-lab-spec-v1.md §2 (filtros por busca; Core garante o filtro)
    - .planning/phases/03-buscas-e-adapters/03-CONTEXT.md (D-32: cada fonte honra o que pode, Core pós-filtra o resto)
  </read_first>
  <action>Criar `postFilter.ts` com `postFilter(items: NormalizedItem[], def: SearchDef): { kept: NormalizedItem[]; dropped: number }` — redundância desejada D-32: aplica TODOS os filtros de def sobre CADA item mesmo que a fonte já tenha filtrado (idempotente por construção): yearFrom/yearTo sobre `year` (null → MANTÉM, nunca descarta por dado ausente); docTypes sobre `docType` (mapear `Mestrado/masterThesis`, `Doutorado/doctoralThesis` case-insensitive, resto compara direto); institution/program/area como substring case-insensitive sobre os campos correspondentes (area casa com institution+program); `source` (bdtd|capes) NÃO filtra aqui (já separado por adapter). Puro, síncrono, sem I/O, sem `any`. Exportar `describePostFilter(def): string` PT-BR para métricas/logs ('pós-filtro: ano 2020–2024, tipo tese').
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/integrations run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `postFilter.ts` contém `postFilter` e `describePostFilter`; year null é mantido (teste cobre)
    - redundância explícita: filtros aplicados incondicionalmente, não só "o que falta"
  </acceptance_criteria>
  <done>Pós-filtro garante o contrato declarado ao usuário independente da fonte</done>
</task>

<task type="auto">
  <name>task 3: fixtures + testes de contrato SRC-05 sem rede</name>
  <files>tests/integration/fixtures/bdtd-search.json, tests/integration/fixtures/capes-busca.json, tests/integration/lab-sources-contract.test.ts</files>
  <read_first>
    - dev-docs/04-fontes-bdtd-capes.md §1–§2 (shapes mínimos para as fixtures)
    - tests/integration/projects.test.ts (molde do harness: Fastify? aqui NÃO precisa de app — teste puro de adapter com fetchFn injetado; usar só o padrão de skip gracioso se precisar de PG — este teste NÃO usa PG)
    - packages/integrations/src/bdtd.ts, packages/integrations/src/capes.ts (assinaturas reais)
  </read_first>
  <action>Criar fixtures mínimas fiéis aos shapes de 06/09/2026 (2–3 registros cada, com 1 registro sem-divulgação na CAPES e 1 registro com campo ausente na BDTD) e `tests/integration/lab-sources-contract.test.ts` (vitest, projeto `integration`, SEM PG e SEM rede — `fetchFn` fake injeta as fixtures; roda sempre, nunca pula):
  1. BDTD: `lookfor`+`filter[]` montados corretamente (assert na URL chamada pelo fake), `total` parseado, items normalizados, registro com campo ausente → null sem lançar, challenge HTML → `sourceStatus:'challenge'`.
  2. CAPES: payload com `termo` intacto (aspas preservadas), `Ano` expandido por ano (2020–2022 → 3 filtros), registra `registrosPorPagina`≥5, sem-divulgação → sourceUrl null + flag, total parseado.
  3. `sanitizeRaw` remove `cookie`/`authorization`/`token` de rawMetadata.
  4. `postFilter`: ano mantém null, docType mapeia Mestrado→masterThesis, institution substring.
  5. `getSourceAdapter('oasisbr')` lança `SourceDisabledError`.
  Como o pacote `@uhhu/integrations` é novo, o teste importa os adapters por PATH RELATIVO (molde projects.test.ts que usa `../../apps/...`): `../../packages/integrations/src/bdtd.js`, `.../capes.js`, `.../postFilter.js`, `.../adapters.js` (com extensão `.js` sob NodeNext, igual aos imports internos). Cobertura SRC-05 parcial aqui (só-BDTD/só-CAPES/ambas em nível de adapter); relatório por run entra no executor 03-04 via `metrics.coverage`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/lab-sources-contract.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/lab-sources-contract.test.ts` existe com ≥8 its e todos verdes SEM rede e SEM PG
    - fixtures contêm `resultCount` (BDTD) e `tesesDissertacoes` (CAPES)
    - `pnpm test` (workspace) continua verde
  </acceptance_criteria>
  <done>Contrato dos adapters congelado em fixtures; CI prova sem rede externa</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BDTD/CAPES→adapter | JSON/HTML externo parseado defensivamente (Zod safeParse, regex ancorada) |
| adapter→DB (via executor) | rawMetadata higienizado antes de persistir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | parse VuFind/rest/busca | mitigate | Zod safeParse; campos ausentes → null, nunca throw em opcional |
| T-03-03-02 | Information Disclosure | rawMetadata | mitigate | `sanitizeRaw` remove cookie/authorization/token/session antes de NormalizedItem |
| T-03-03-03 | Denial of Service | range de anos gigante | mitigate | cap 30 anos, `RangeTooWideError` antes da rede; perPage clamp 5..50 |
| T-03-03-04 | Spoofing | TLS CAPES com cadeia quebrada | mitigate | NUNCA `rejectUnauthorized:false`; falha de TLS vira source failed → partial (D-37) |
</threat_model>

<verification>
Typecheck do pacote verde; teste de contrato verde sem rede/PG; `pnpm test` workspace verde; grep `rejectUnauthorized|any` zero no pacote.
</verification>

<success_criteria>
Busca real traduzível nas duas fontes; enrich pronto para Phase 4; contrato congelado em fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-buscas-e-adapters/03-03-SUMMARY.md`
</output>
