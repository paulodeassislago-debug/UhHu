---
phase: 03-buscas-e-adapters
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/lab.ts
  - packages/contracts/src/errors.ts
  - packages/contracts/src/index.ts
  - packages/db/src/schema.ts
  - packages/db/drizzle/0002_*.sql
autonomous: true
requirements:
  - LAB-02
  - LAB-03
  - LAB-04
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "Tipos e schemas Zod de Search/SearchRun/Result/Source existem em definição única compartilhada"
    - "Migration 0002 cria as tabelas lab com CHECKs e índices e aplica sem erro contra PG DEV"
    - "Códigos de erro novos (SOURCE_DISABLED, SOURCE_UNAVAILABLE, IDEMPOTENCY_CONFLICT) vêm do catálogo PT-BR"
  artifacts:
    - path: "packages/contracts/src/lab.ts"
      provides: "Schemas Zod + DTOs de busca/run/result/sources/health"
      min_lines: 120
    - path: "packages/db/src/schema.ts"
      provides: "Tabelas lab_searches, lab_search_runs, lab_results, lab_idempotency_keys, lab_source_events"
      contains: "labSearches"
    - path: "packages/db/drizzle/0002_*.sql"
      provides: "Migration versionada das tabelas lab"
      contains: "lab_searches"
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/lab.ts"
      via: "export * from './lab.js'"
      pattern: "lab\\.js"
    - from: "packages/db/src/schema.ts"
      to: "packages/db/drizzle/0002_*.sql"
      via: "drizzle-kit generate"
      pattern: "lab_search_runs"
---

<objective>
Fundação de tipos e persistência da Phase 3: contratos compartilhados de buscas/runs/results/sources + tabelas lab + migration 0002.

Purpose: Sem tipos únicos e sem tabelas, nenhum adapter ou rota pode existir (D-28–D-39 dependem destes artefatos). Este plano é a base dos waves 2–5.
Output: `packages/contracts/src/lab.ts`, catálogo de erros estendido, schema Drizzle estendido, migration 0002 gerada e aplicável.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buscas-e-adapters/03-CONTEXT.md
@AGENTS.md
@dev-docs/03-lab-spec-v1.md
@dev-docs/07-core-contract.md
@packages/contracts/src/errors.ts
@packages/contracts/src/pagination.ts
@packages/contracts/src/projects.ts
@packages/db/src/schema.ts

<interfaces>
Executor deve usar estes padrões diretamente, sem explorar a codebase:

From packages/contracts/src/errors.ts — catálogo PT-BR, nunca montar mensagem inline:
```typescript
export type ErrorCode = 'INVALID_CREDENTIALS' | ... | 'INTERNAL_ERROR';
export function buildEnvelope(code: ErrorCode, requestId: string, details?: unknown): ErrorEnvelope;
```

From packages/contracts/src/pagination.ts — paginação de projetos (molde; results usa cursor próprio fonte|rank|id definido neste plano):
```typescript
export const paginationQuerySchema = z.object({
  limit: z.coerce.number().int().min(1).max(100).default(20),
  cursor: z.string().max(512).optional(),
});
export function encodeCursor(createdAt: string, id: string): string;
export function decodeCursor(cursor: string): { createdAt: string; id: string } | null;
```

From packages/db/src/schema.ts — convenções: snake_case no banco, `uuid(...).primaryKey().defaultRandom()`, `timestamp(..., { withTimezone: true })`, CHECKs via `check()` + `sql`, índices via `index()`, FK com `{ onDelete: 'cascade' }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: contratos lab em packages/contracts</name>
  <files>packages/contracts/src/lab.ts, packages/contracts/src/errors.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/errors.ts (catálogo atual, para estender sem duplicar)
    - packages/contracts/src/pagination.ts (convenção limit default 20 max 100)
    - dev-docs/03-lab-spec-v1.md §3 (modelo Search/SearchRun/Result) e §5 (rotas lab)
    - dev-docs/07-core-contract.md §9.2 (status HTTP), §12 (domínio Lab), §14 (Job)
  </read_first>
  <action>Criar `packages/contracts/src/lab.ts` como ÚNICA definição de tipos de busca (nenhum outro pacote duplica; `any` proibido). Conteúdo exato:
  1. `searchTermSchema = z.string().trim().min(1).max(500)` + refinamento de aspas balanceadas (conta de `"` par) com mensagem PT-BR 'Termo com aspas desbalanceadas.' — string livre pass-through AND/OR/NOT + frase exata per D-31, sem tradução estruturada.
  2. `searchFiltersSchema = z.object({ yearFrom: z.number().int().min(1800).max(2100).optional(), yearTo: z.number().int().min(1800).max(2100).optional(), docTypes: z.array(z.enum(['masterThesis','doctoralThesis'])).max(2).optional(), source: z.enum(['bdtd','capes']).optional(), area: z.string().trim().max(200).optional(), institution: z.string().trim().max(300).optional(), program: z.string().trim().max(300).optional() })` com refinamento yearFrom<=yearTo (D-32).
  3. `searchSourcesSchema = z.array(z.enum(['bdtd','capes','oasisbr'])).min(1).max(2)` — `oasisbr` passa no schema mas a ROTA retorna 400 SOURCE_DISABLED (D-33; decisão de rota, não de schema).
  4. `createSearchSchema = z.object({ projectId: z.string().uuid(), term: searchTermSchema, filters: searchFiltersSchema.default({}), sources: z.array(z.enum(['bdtd','capes'])).min(1).max(2).default(['bdtd','capes']) })`; `updateSearchSchema` com term/filters/sources opcionais (runs antigos congelam snapshot — D-33).
  5. `runStatusSchema = z.enum(['queued','running','succeeded','partial','failed','cancelled'])` (D-30, Job=Run 1:1, mesmos estados).
  6. DTOs: `SearchDTO {id, projectId, term, filters, sources, createdAt, updatedAt}`, `SearchRunDTO {id, searchId, status, termSnapshot, filtersSnapshot, sourcesSnapshot, executedAt, startedAt, finishedAt, metrics, error}`, `ResultDTO {id, runId, source, sourceId, title, authors, year, docType, institution, program, abstract, originUrl, sourceUrl, retrievedAt}`, `RunMetrics {perSource: Record<'bdtd'|'capes', {status, total, returned, durationMs}>, newCount, coverage: {bdtd: number, capes: number}}`, `SourceHealthDTO {source, status: 'ok'|'degraded'|'offline', checkedAt, recent: {total, ok, failed, challenges}}`, `JobDTO {id, type: 'lab.search.execute', status, progress: {doneSources, totalSources} | null, createdAt, startedAt, finishedAt, resultRef: {runId} | null, error}`.
  7. `encodeResultsCursor(source: string, rank: number, id: string): string` (base64url de `source|rank|id`) + `decodeResultsCursor` retornando null em malformado sem lançar (molde pagination.ts, D-36: ordenação fonte + ordem da fonte).
  8. Estender `ErrorCode` em errors.ts com exatamente: `SOURCE_DISABLED` ('Fonte desabilitada nesta instalação.'), `SOURCE_UNAVAILABLE` ('Fonte temporariamente indisponível. Resultados parciais foram preservados.'), `IDEMPOTENCY_CONFLICT` ('Chave de idempotência reutilizada com corpo diferente.') — mensagens PT-BR no ERROR_CATALOG, sem vazar cookies/hosts internos.
  9. Exportar `./lab.js` em `packages/contracts/src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/contracts exec tsc --noEmit -p tsconfig.json 2>&1 || pnpm typecheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/lab.ts` contém `searchTermSchema`, `searchFiltersSchema`, `createSearchSchema`, `runStatusSchema`, `encodeResultsCursor`, `decodeResultsCursor`
    - `packages/contracts/src/errors.ts` contém `SOURCE_DISABLED`, `SOURCE_UNAVAILABLE`, `IDEMPOTENCY_CONFLICT` no type E no ERROR_CATALOG
    - `packages/contracts/src/index.ts` contém `lab.js`
    - grep `any` em lab.ts retorna zero ocorrências fora de comentários
  </acceptance_criteria>
  <done>Contratos lab compilam sob strict + exactOptionalPropertyTypes; typecheck verde</done>
</task>

<task type="auto">
  <name>task 2: schema Drizzle + migration 0002 das tabelas lab</name>
  <files>packages/db/src/schema.ts, packages/db/drizzle/0002_*.sql</files>
  <read_first>
    - packages/db/src/schema.ts (tabelas projects como molde: owner, CHECKs, índices)
    - packages/contracts/src/lab.ts (tipos criados na task 1 — espelhar no banco)
    - dev-docs/03-lab-spec-v1.md §3 (campos de Search/SearchRun/Result) e §6 (SourceClient 60s)
  </read_first>
  <action>Estender `packages/db/src/schema.ts` com exatamente estas 5 tabelas (snake_case, uuid PK defaultRandom, timestamptz, CHECKs no banco não só no Zod, tipos exportados `LabSearch`, `LabSearchRun`, `LabResult`, `LabIdempotencyKey`, `LabSourceEvent`):
  1. `lab_searches`: id, project_id FK→projects.id cascade, term text not null (CHECK char_length 1..500), filters jsonb not null default '{}', sources text[] not null default '{bdtd,capes}', created_at, updated_at ($onUpdate). CHECK `sources <@ ARRAY['bdtd','capes','oasisbr']`.
  2. `lab_search_runs`: id, search_id FK→lab_searches.id cascade, created_by uuid not null FK→users.id (escopo p/ rate-limit 10/h + idempotência por usuário, D-39), status text default 'queued' CHECK nos 6 estados D-30, term_snapshot text not null, filters_snapshot jsonb not null, sources_snapshot jsonb not null (runs congelam o usado — D-33), executed_at/started_at/finished_at timestamptz, metrics jsonb not null default '{}', error jsonb, adapter_versions jsonb not null default '{}', idempotency_key_hash text. Índices: (search_id, executed_at desc), (created_by, executed_at desc).
  3. `lab_results`: id, run_id FK→lab_search_runs.id cascade, source text CHECK (bdtd|capes), source_id text not null, title text not null, authors jsonb not null default '[]', year integer nullable, doc_type text nullable, institution/program/abstract/origin_url/source_url text nullable, raw_metadata jsonb not null (integral da fonte — D-34), rank integer not null (ordem da fonte — D-36), retrieved_at timestamptz default now. UNIQUE(run_id, source, source_id) (base do diff de novos D-35). Índice (run_id, source, rank).
  4. `lab_idempotency_keys`: key_hash text PK (sha256 de `userId|key|bodyHash`), user_id FK→users cascade, body_hash text not null, run_id FK→lab_search_runs null, expires_at timestamptz not null (24h — D-39), created_at. Índice expires_at.
  5. `lab_source_events`: id uuid PK, source text CHECK (bdtd|capes), ok boolean not null, is_challenge boolean default false, at timestamptz default now (base do health D-38). Índice (source, at desc).
  Depois gerar SQL com `pnpm --filter @uhhu/db db:generate` e conferir que o arquivo `packages/db/drizzle/0002_*.sql` contém `lab_searches` E `lab_search_runs` E `lab_results` E `lab_idempotency_keys` E `lab_source_events`. NÃO aplicar contra prod; aplicar contra PG DEV para prova (`pnpm --filter @uhhu/db db:migrate` com env DEV) somente se PG DEV alcançável, senão registrar skip com motivo.
  </action>
  <verify>
    <automated>ls packages/db/drizzle/0002_*.sql && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/schema.ts` contém `labSearches`, `labSearchRuns`, `labResults`, `labIdempotencyKeys`, `labSourceEvents`
    - arquivo `packages/db/drizzle/0002_*.sql` existe e contém os 5 nomes de tabela
    - `pnpm typecheck` verde (root + pacotes)
    - migration aplica contra PG DEV (ou skip documentado com erro de conexão, nunca falha silenciosa)
  </acceptance_criteria>
  <done>Migration 0002 versionada no monorepo e aplicável; schema espelha contratos da task 1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→contracts | Termo/filtros hostis (strings longas, aspas, unicode) entram via Zod |
| migrate→PG DEV | Migration DDL roda com role uhhu_migrate, nunca uhhu_app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | searchTermSchema | mitigate | max 500 + aspas balanceadas no Zod; CHECK char_length no banco |
| T-03-01-02 | Information Disclosure | raw_metadata jsonb | mitigate | coluna existe mas NUNCA serializada com cookies/segredos — adapters (03-03) higienizam antes de persistir; documentado no header do schema |
| T-03-01-03 | Denial of Service | filters arrays | mitigate | docTypes max(2), strings com max, year range 1800–2100 |
| T-03-01-04 | Elevation | lab_search_runs.created_by | mitigate | preenchido server-side do ActorContext, nunca do body (lib 03-04 aplica) |
</threat_model>

<verification>
`pnpm typecheck` verde; `pnpm lint` sem erros nos arquivos tocados; migration 0002 presente e aplicável em DEV.
</verification>

<success_criteria>
Contratos lab importáveis por qualquer pacote; 5 tabelas criadas via migration versionada; novos códigos de erro no catálogo PT-BR.
</success_criteria>

<output>
After completion, create `.planning/phases/03-buscas-e-adapters/03-01-SUMMARY.md`
</output>
