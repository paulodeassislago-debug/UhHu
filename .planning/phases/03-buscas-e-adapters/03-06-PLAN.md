---
phase: 03-buscas-e-adapters
plan: "06"
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - tests/integration/lab-search-runs.test.ts
  - scripts/curl-lab.sh
autonomous: false
requirements:
  - LAB-02
  - LAB-03
  - LAB-04
  - LAB-05
  - LAB-12
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "Suite de integração prova busca→run→results→proveniência contra PG real com adapters mockados"
    - "Matriz IDOR cobre searches/runs/results/jobs (dono, estranho, adulterado via curl)"
    - "Script curl-lab.sh prova o fluxo manual ponta a ponta"
    - "Humano confirma busca REAL BDTD+CAPES ao vivo (única prova que exige rede externa)"
  artifacts:
    - path: "tests/integration/lab-search-runs.test.ts"
      provides: "integração lab contra PG real"
      min_lines: 150
    - path: "scripts/curl-lab.sh"
      provides: "prova manual executável do fluxo lab"
      contains: "ALL PASS"
  key_links:
    - from: "tests/integration/lab-search-runs.test.ts"
      to: "apps/core-api/src/routes/lab.ts"
      via: "Fastify inject com rotas reais"
      pattern: "buildLabRoutes"
    - from: "scripts/curl-lab.sh"
      to: "tests/integration/lab-search-runs.test.ts"
      via: "mesma cobertura dono/estranho/adulterado"
      pattern: "404"
---

<objective>
Prova da fase: integração PG + matriz IDOR + curl + auditoria adversarial + checkpoint humano de busca real (LAB-02–05/12, SRC-01–05, CORE-03/04).

Purpose: Transformar "código pronto" em "fase provada" no padrão 02-04 (teste + curl + auditoria + humano), com a única exceção honesta: rede BDTD/CAPES real só no checkpoint.
Output: Teste de integração, script curl, auditoria documentada, aprovação humana da busca viva.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buscas-e-adapters/03-CONTEXT.md
@AGENTS.md
@dev-docs/08-security-baseline.md
@tests/integration/idor-matrix.test.ts
@tests/integration/projects.test.ts
@scripts/curl-idor.sh
@apps/core-api/src/routes/lab.ts
@apps/core-api/src/lib/searchRuns.ts

<interfaces>
Molde de prova (copiar de tests/integration/idor-matrix.test.ts + scripts/curl-idor.sh): harness Fastify em memória + `buildAuthRoutes` + `buildProjectRoutes` + `buildLabRoutes` no mesmo app com cookie `uhhu_session` real; 2 usuários (dono A, estranho B); asserts de `x-request-id` + regex negativa `stack|passwordHash|token|set-cookie|cookie` nos corpos; skip gracioso sem PG (nunca falha offline); NUNCA imprime connection string. Adapters com `fetchFn` fake (fixtures da 03-03) — determinístico, sem rede.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: integração lab contra PG real + curl-lab.sh</name>
  <files>tests/integration/lab-search-runs.test.ts, scripts/curl-lab.sh</files>
  <read_first>
    - tests/integration/idor-matrix.test.ts (molde da matriz: 2 usuários, 404 triplo, asserts de envelope)
    - tests/integration/projects.test.ts (molde do harness: migrate, truncate, bootstrap de usuários)
    - scripts/curl-idor.sh (molde do script: assert_code, jars, 12 passos)
    - apps/core-api/src/routes/lab.ts (paths e status exatos a provar)
    - apps/core-api/src/lib/searchRuns.ts (como injetar fetchFn fake no harness — se a rota não expõe injeção, usar override de módulo ou flag de teste documentada; NUNCA rede real no teste)
  </read_first>
  <action>1. Criar `tests/integration/lab-search-runs.test.ts` (PG real, adapters com fetchFn fake das fixtures 03-03; skip gracioso sem PG) com estes its mínimos, todos com `x-request-id` + regex negativa de vazamento:
  - LAB-02: cria search declarativa (termo com aspas + filtros ano/tipo) SEM executar → 201 + GET lista; edição de termo → 200 e run antigo mantém snapshot (cria run, edita search, GET run mostra termSnapshot antigo — D-33).
  - LAB-03+SRC-05: executa run com ambas fontes ok (fake) → 201 `succeeded`, metrics.coverage `{bdtd:2, capes:2}` (counts das fixtures), reexecução → novo run com `newCount` correto (2ª execução com 1 item novo no fake → newCount 1 — D-35).
  - LAB-05+D-37: fake CAPES falha → 201 `partial` com items BDTD preservados; ambas falham → 201 `failed`; results do partial continuam listáveis.
  - LAB-04: cadeia Result→Run→Search→Project reconstruível (GET result mostra runId; GET run mostra searchId; GET search mostra projectId do dono) + rawMetadata integral presente + `source/sourceId` corretos.
  - D-36: GET results ordenado fonte+rank com cursor (`limit=1` pagina sem repetir nem pular).
  - CORE-03/D-39: mesmo `Idempotency-Key` + mesmo body → 200 replay com `Idempotent-Replayed: true` e MESMO runId; mesmo key + body diferente → 422; 11º run na hora → 429 (criar 10 runs rápidos com fakes).
  - CORE-04/D-29/D-30: `GET /jobs/:runId` espelha o run; cancel de run `running` (fake lento com delay 100ms) → `cancelled` com parciais preservados; `oasisbr` → 400 SOURCE_DISABLED.
  - LAB-12: `GET /lab/sources` lista 3 com oasisbr desabilitada; health após runs → `ok` com counts>0.
  - IDOR: estranho B → 404 em GET search/run/result/job alheios + PATCH/DELETE search alheia + POST run em search alheia; UUID adulterado → 404; sem cookie → 401; `ownerId`/`projectId` no body ignorados (resposta não contém ids alheios).
  2. Criar `scripts/curl-lab.sh` (chmod +x, molde curl-idor.sh): bootstrap→2 usuários→projeto→search→run→poll job→results→rerun(newCount)→cancel→health→4 provas 404 de estranho; cada passo com `assert_code`; final `echo "ALL PASS"`. Executar contra boot local + PG DEV e colar o `ALL PASS` no SUMMARY.
  </action>
  <verify>
    <automated>pnpm vitest run --project integration tests/integration/lab-search-runs.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - teste contém `Idempotent-Replayed`, `newCount`, `partial`, `cancelled`, `SOURCE_DISABLED`, `404` (IDOR) e regex negativa `set-cookie|passwordHash|token`
    - suite integração completa verde (`pnpm test:integration`) ou skips só por PG inalcançável documentado
    - `scripts/curl-lab.sh` executável com `ALL PASS` contra PG DEV (ou falha de infra documentada, nunca falso-verde)
  </acceptance_criteria>
  <done>Fluxo busca→run→results→proveniência provado por teste + curl, com IDOR triplo</done>
</task>

<task type="auto">
  <name>task 2: auditoria adversarial + gates da fase</name>
  <files>tests/integration/lab-search-runs.test.ts</files>
  <read_first>
    - dev-docs/08-security-baseline.md §4 (roteiro de auditoria arquivo-a-arquivo) e §7 (gate de merge)
    - apps/core-api/src/lib/searchRuns.ts, apps/core-api/src/routes/lab.ts, packages/integrations/src/bdtd.ts, packages/integrations/src/capes.ts, packages/integrations/src/sourceClient.ts (alvos da auditoria)
  </read_first>
  <action>Executar auditoria adversarial (roteiro §4: autorização decorativa, IDOR, confiança no cliente, segredos, XSS/input/SSRF, sessões, rate limit, SQL injection, isolamento) SOMENTE leitura + relatório primeiro: listar achados com severidade/arquivo+linha/exploração local/teste que provaria; controles que funcionam. Depois corrigir apenas crítico/alto (médio/baixo documentados no SUMMARY como advisory), cada correção com teste. Rodar gates: `pnpm typecheck`, `pnpm lint`, `pnpm format`, `pnpm test`, `pnpm audit --audit-level high`, Gitleaks tree (`gitleaks detect --source .` se instalado, senão registrar ausência), OpenGrep/SAST se disponível. Anexar tabela de gates (comando → resultado) no SUMMARY. `any` proibido inclusive nos testes novos.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint 2>&1 | tail -3 && pnpm audit --audit-level high 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contém seção de auditoria com achados (ou "0 crit/0 high") + controles verificados
    - gates typecheck/lint/format/test/audit verdes ou com aceite formal documentado
    - nenhum crítico/alto aberto sem correção + teste
  </acceptance_criteria>
  <done>Auditoria antes de patch, gates verdes, relatório revisável por humano</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Buscas BDTD+CAPES com runs temporais, partial, proveniência e health — provadas com adapters mockados (teste+curl verdes). Falta SÓ a prova que exige internet real: 1 busca viva em cada fonte.</what-built>
  <how-to-verify>
    1. Suba o boot contra PG DEV (`pnpm --filter @uhhu/core-api start` com `.env.dev.cs`) e autentique (use `scripts/curl-idor.sh` para obter sessão, ou login manual).
    2. Crie um projeto e uma search de teste: `POST /api/v1/lab/searches {"projectId":"<id>","term":"\"ensino de química\"","filters":{"yearFrom":2020,"yearTo":2024},"sources":["bdtd","capes"]}`.
    3. Execute: `POST /api/v1/lab/searches/<searchId>/runs` e aguarde o 201 (até 25s) ou faça polling em `GET /api/v1/jobs/<runId>`.
    4. Confirme: (a) ao menos 1 resultado de CADA fonte (`metrics.coverage` com bdtd≥1 e capes≥1, ou `partial` justificando qual fonte falhou e por quê); (b) `GET .../results` lista com `source/sourceId/rawMetadata`; (c) `GET /api/v1/lab/sources/capes/health` e `/bdtd/health` respondem `ok|degraded|offline` com counts.
    5. Se alguma fonte falhar por bloqueio de rede local (não por bug): anote qual e responda "approved with <fonte> blocked" — o contrato com fixtures continua valendo e o re-teste entra no próximo ciclo.
  </how-to-verify>
  <resume-signal>Type "approved" (busca viva nas duas fontes) ou "approved with <fonte> blocked" (descreva o erro observado) ou descreva issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste→PG DEV | Credenciais DEV via env, nunca impressas; truncate só em tabelas lab+platform de teste |
| humano→produção BDTD/CAPES | Checkpoint usa cortesia real (1 busca, não rajada) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | logs de teste/curl | mitigate | sem connection string, sem cookie, sem corpos de fonte nos asserts |
| T-03-06-02 | Denial of Service | checkpoint humano | mitigate | 1 busca de teste por fonte (critério de aceite), sem laço nem rajada |
| T-03-06-03 | Repudiation | prova da fase | mitigate | ALL PASS do curl + suite verde + approval humano registrados no SUMMARY |
</threat_model>

<verification>
Suite integração verde (ou skip infra documentado); curl ALL PASS; auditoria sem crítico/alto aberto; checkpoint humano approved.
</verification>

<success_criteria>
Phase 3 atinge os 4 critérios do ROADMAP: busca real por fonte (humano), runs temporais com diff (teste), partial+jobs+cancel (teste), proveniência+health (teste+humano).
</success_criteria>

<output>
After completion, create `.planning/phases/03-buscas-e-adapters/03-06-SUMMARY.md`
</output>
