---
phase: 03-buscas-e-adapters
plan: "05"
type: execute
wave: 4
depends_on:
  - 03-04
files_modified:
  - apps/core-api/src/routes/lab.ts
  - apps/core-api/src/plugins/rateLimit.ts
  - apps/core-api/src/index.ts
autonomous: true
requirements:
  - LAB-02
  - LAB-03
  - LAB-05
  - LAB-12
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "CRUD de searches e execução de runs respondem sob /api/v1/lab com envelope PT-BR"
    - "POST runs responde 201 sync / 202 async+polling / 200 replay idempotente"
    - "GET /jobs/:id e POST /jobs/:id/cancel espelham o run 1:1"
    - "GET /lab/sources e /lab/sources/:name/health reportam ok|degraded|offline"
  artifacts:
    - path: "apps/core-api/src/routes/lab.ts"
      provides: "rotas lab + jobs"
      contains: "/lab/searches"
    - path: "apps/core-api/src/plugins/rateLimit.ts"
      provides: "throttle 10 runs/h (fallback) + global herdado"
      contains: "lab"
    - path: "apps/core-api/src/index.ts"
      provides: "boot com buildLabRoutes"
      contains: "buildLabRoutes"
  key_links:
    - from: "apps/core-api/src/routes/lab.ts"
      to: "apps/core-api/src/lib/searchRuns.ts"
      via: "executeSearchRun + cancelRunForActor"
      pattern: "executeSearchRun"
    - from: "apps/core-api/src/index.ts"
      to: "apps/core-api/src/routes/lab.ts"
      via: "register no boot único"
      pattern: "buildLabRoutes"
    - from: "apps/core-api/src/routes/lab.ts"
      to: "packages/integrations/src/health.ts"
      via: "computeSourceHealth nos endpoints"
      pattern: "computeSourceHealth"
---

<objective>
Superfície HTTP do lab + jobs + health + wiring no boot (LAB-02/03/05/12, CORE-03/04, D-28–D-30/D-33/D-37–D-39-visível).

Purpose: Expor o motor 03-04 por REST nos contratos 07-core-contract §11.2, sem duplicar regra — rotas adaptam E/S, lib decide.
Output: `routes/lab.ts`, throttle de runs, boot com lab registrado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buscas-e-adapters/03-CONTEXT.md
@AGENTS.md
@dev-docs/07-core-contract.md
@apps/core-api/src/routes/projects.ts
@apps/core-api/src/lib/searches.ts
@apps/core-api/src/lib/searchRuns.ts
@apps/core-api/src/plugins/rateLimit.ts
@apps/core-api/src/index.ts
@packages/contracts/src/lab.ts

<interfaces>
Copiar de apps/core-api/src/routes/projects.ts: `buildXRoutes(app, db)`, `requireAuth(db)` preHandler, `resolveRequestId` + header `x-request-id`, `withClampedLimit`, params UUID → 404 NOT_FOUND, body Zod → 400 VALIDATION_ERROR, fora do escopo → 404 idêntico, DELETE com `?confirm=true` → 400 CONFIRMATION_REQUIRED.
Mapa de status da execução (D-28/D-30/D-37/D-39): sync concluído (succeeded|partial|failed) → 201 com SearchRunDTO; excedeu 25s → 202 com SearchRunDTO + header `Location: /api/v1/jobs/<runId>`; replay idempotente → 200 + header `Idempotent-Replayed: true`; cancel → 200 com run cancelled; run failed NÃO vira 502 (o recurso run foi criado — status carrega o erro por fonte).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: rotas lab (searches, runs, results, sources, jobs)</name>
  <files>apps/core-api/src/routes/lab.ts</files>
  <read_first>
    - apps/core-api/src/routes/projects.ts (molde INTEGRAL de rota: requestId, actor, Zod, 404, clamp)
    - apps/core-api/src/lib/searches.ts (assinaturas *ForActor reais)
    - apps/core-api/src/lib/searchRuns.ts (executeSearchRun, cancelRunForActor, erros tipados)
    - packages/contracts/src/lab.ts (schemas de body/query e DTOs)
    - dev-docs/07-core-contract.md §11.2 (paths exatos) e §9.2 (status)
  </read_first>
  <action>Criar `apps/core-api/src/routes/lab.ts` com `buildLabRoutes(app, db)`, todas atrás de `requireAuth(db)`, envelope PT-BR e `x-request-id` em TODA resposta:
  1. `POST /api/v1/lab/searches` (createSearchSchema) → 201 SearchDTO; projectId alheio/inexistente → 404 NOT_FOUND idêntico.
  2. `GET /api/v1/lab/searches?projectId=` (projectId obrigatório, UUID; clamp limit) → 200 `{items, page}`; projeto alheio → 404.
  3. `GET/PATCH/DELETE /api/v1/lab/searches/:searchId` → 200/200/204; DELETE exige `?confirm=true` (400 CONFIRMATION_REQUIRED, molde projects); fora do escopo → 404.
  4. `POST /api/v1/lab/searches/:searchId/runs`: lê `Idempotency-Key` (string 1..128, opcional); chama `executeSearchRun` numa `Promise.race` contra timeout de 25000ms (D-28): concluiu → 201 SearchRunDTO (qualquer status final, inclusive partial/failed — D-37); estourou → 202 SearchRunDTO (estado corrente `running`) + `Location: /api/v1/jobs/<runId>`, execução continua em background atualizando a linha; replay idempotente → 200 + `Idempotent-Replayed: true`; `SourceDisabledError` (oasisbr) → 400 SOURCE_DISABLED (D-33); `IdempotencyConflictError` → 422 IDEMPOTENCY_CONFLICT; `RunRateLimitedError` → 429 RATE_LIMITED (D-39).
  5. `GET /api/v1/lab/searches/:searchId/runs` → 200 `{items, page}` histórico temporal (D: reexecução = novo run).
  6. `GET /api/v1/lab/runs/:runId` → 200 SearchRunDTO (estado corrente p/ polling). `GET /api/v1/lab/runs/:runId/results` → 200 `{items, page, total, newCount}` (newCount do run corrente vs anterior — D-35 visível).
  7. `GET /api/v1/lab/results/:resultId` → 200 ResultDTO com `runId/source/sourceId/rawMetadata` (proveniência LAB-04).
  8. `GET /api/v1/lab/sources` → 200 lista do registry `{name, level, enabled}` (sem segredos). `GET /api/v1/lab/sources/:sourceName/health` → 200 SourceHealthDTO via `computeSourceHealth`; source desconhecida → 404 NOT_FOUND.
  9. `GET /api/v1/jobs/:jobId` → 200 JobDTO (jobId = runId; D-30 1:1; job inexistente/fora do escopo → 404). `POST /api/v1/jobs/:jobId/cancel` → 200 SearchRunDTO cancelled (já-final → 200 com estado atual, sem erro; D-29 preserva parciais).
  10. Comparação entre buscas, decisão/divergência, corpus e exportação NÃO entram (Phase 4 — 404 natural do notFoundHandler, sem stub).
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lab.ts` contém todos os 12 paths (`/lab/searches`, `/runs`, `/results`, `/sources`, `/jobs/:jobId`, `/cancel`) e literais `25000`, `Idempotent-Replayed`, `Location`
    - `compare`, `decision`, `corpus`, `export` aparecem ZERO vezes como rota registrada
    - `requireAuth(db)` em todas as rotas; `x-request-id` em todas as respostas
  </acceptance_criteria>
  <done>Superfície §11.2 (escopo Phase 3) exposta com semântica 201/202/200 correta</done>
</task>

<task type="auto">
  <name>task 2: throttle de runs + wiring no boot</name>
  <files>apps/core-api/src/plugins/rateLimit.ts, apps/core-api/src/index.ts</files>
  <read_first>
    - apps/core-api/src/plugins/rateLimit.ts (duas camadas: global 200/min + hook fino por IP — estender SEM quebrar)
    - apps/core-api/src/index.ts (boot único: cookie+requestId+errorHandler+rateLimit diretos no root + registers)
    - apps/core-api/src/lib/searchRuns.ts (rate-limit principal é no banco — este hook é fallback anti-rajada)
  </read_first>
  <action>1. Em `rateLimit.ts`: estender o hook fino com bucket `lab-runs` — `POST /api/v1/lab/searches/*/runs` (match por prefixo `/api/v1/lab/searches/` + sufixo `/runs`) limite 30/min por IP (anti-rajada; o limite contratual 10 runs/h por usuário D-39 vive no banco em searchRuns.ts e NÃO é substituído por este). Documentar no header a distinção (IP/minuto aqui vs usuário/hora no banco). Exportar `labRunsRateLimit = {max: 30, timeWindow: '1 minute'}`.
  2. Em `index.ts`: `await app.register(async (child) => buildLabRoutes(child, db))` ao lado de auth/projects (mesmo db único uhhu_app). NÃO reordenar plugins globais; health continua antes do produto.
  </action>
  <verify>
    <automated>pnpm --filter @uhhu/core-api run typecheck && pnpm lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `rateLimit.ts` contém bucket `lab-runs` com max 30 e comentário distinguindo do limite 10/h do banco
    - `index.ts` contém `buildLabRoutes` registrado com `db`
    - typecheck + lint verdes
  </acceptance_criteria>
  <done>Boot único serve lab com dupla proteção de taxa sem quebrar limites existentes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→lab routes | IDs, termo, filtros, Idempotency-Key — tudo validado no Zod antes da lib |
| background run→DB | Execução 202 continua após resposta; erros viram status failed, nunca 500 vazado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Elevation (IDOR) | todas as rotas lab/jobs | mitigate | requireAuth + *ForActor; fora do escopo → 404 idêntico (inclui jobs) |
| T-03-05-02 | Information Disclosure | health/sources | mitigate | só name/level/enabled + counts; erro de fonte sem hosts/corpos |
| T-03-05-03 | Denial of Service | POST runs | mitigate | 30/min por IP (hook) + 10/h por usuário (banco) + timeout 60s + cancel |
| T-03-05-04 | Tampering | Idempotency-Key | mitigate | charset/tamanho validados; conflito de body → 422 explícito |
| T-03-05-05 | Spoofing | X-Request-Id | mitigate | mesmo pattern allowlist das rotas existentes, fallback randomUUID |
</threat_model>

<verification>
Typecheck + lint verdes; boot sobe localmente e `GET /api/v1/lab/sources` responde 401 sem cookie (prova rápida manual do executor).
</verification>

<success_criteria>
Cliente executa busca, faz polling do job, cancela e consulta health — tudo por REST autenticado e isolado.
</success_criteria>

<output>
After completion, create `.planning/phases/03-buscas-e-adapters/03-05-SUMMARY.md`
</output>
