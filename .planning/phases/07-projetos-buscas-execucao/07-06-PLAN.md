---
phase: 07-projetos-buscas-execucao
plan: "06"
type: execute
wave: 5
depends_on:
  - 07-01
gap_closure: false
gap_source: null
ux_request: Pedido Paulo 12/09/2026 (verificação externa fase 7) — lápis de edição inline no cabeçalho do projeto
files_modified:
  - apps/lab/app/project/[id].tsx
  - apps/lab/src/projects/__tests__/projects-ui.test.ts
  - dev-docs/10-lab-esqueleto-telas.md
autonomous: true
requirements:
  - UI-10
  - UI-11
must_haves:
  truths:
    - User edita o TÍTULO pelo lápis ao lado do texto (TextInput inline + Salvar/Cancelar) com PATCH real persistindo
    - User edita a PERGUNTA pelo lápis ao lado do texto (mesmo fluxo saveQuestion atual), sem botão separado
    - Nenhuma chamada extra de API além do PATCH existente; zero dependência nova
  artifacts:
    - path: apps/lab/app/project/[id].tsx
      provides: Cabeçalho com edição inline por lápis
      contains: ✎
  key_links:
    - from: apps/lab/app/project/[id].tsx
      to: apps/lab/src/api/projects.ts
      via: título e pergunta salvam via projectsApi.update existente
      pattern: projectsApi.update
---

<objective>
UX Paulo: trocar botão "Editar pergunta" separado por lápis ✎ inline + tornar o título editável da mesma forma.

Purpose: Mutabilidade visível direto no texto (esqueleto §8); cabeçalho do projeto fica pronto antes do fechamento da fase.
Output: Título e pergunta editáveis inline com Salvar/Cancelar, testes, doc de telas anotada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projetos-buscas-execucao/07-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/08-security-baseline.md
@apps/lab/app/project/[id].tsx
@apps/lab/src/api/projects.ts

<interfaces>
Estado atual (07-01): pergunta edita via botão separado + saveQuestion (projectsApi.update com { researchQuestion }); título é estático. Pedido: lápis "✎" (Text, sem lib de ícones) ao lado do TÍTULO → TextInput inline (Salvar/Cancelar) → PATCH real via projectsApi.update({ title }) — updateProject já existe no CORE; e lápis ao lado de "Pergunta: ..." reusando o fluxo saveQuestion atual, removendo o botão separado. Manter: zod/limites atuais (mesmo schema de update; título segue limites do contrato), estados vazio/erro, mutabilidade visível imediata na UI após PATCH, ScrollView raiz, zero `any`, sem chamadas extras de API.
Doc: anotar em dev-docs/10-lab-esqueleto-telas.md §4 (cabeçalho) a affordance ✎ inline para título+pergunta — adendo mínimo, sem reescrever o validado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: lápis inline título + pergunta com PATCH real</name>
  <files>apps/lab/app/project/[id].tsx</files>
  <read_first>
    - apps/lab/app/project/[id].tsx (fluxo saveQuestion atual, estados, ScrollView; onde está o botão "Editar pergunta")
    - apps/lab/src/api/projects.ts update (assinatura UpdateProjectInput; confirmar que title é aceito pelo schema)
  </read_first>
  <action>
    1. Título: adicionar "✎" (Text com acessibilidade/hitSlop) ao lado do título → troca por TextInput com valor atual + botões Salvar/Cancelar → Salvar chama `projectsApi.update(project.id, { title: value.trim() }, { getToken })` (validar não-vazio + limite do contrato antes; erro de validação local, sem request) → atualiza estado local com o DTO retornado (mutabilidade visível) → volta ao modo leitura. Cancelar descarta sem request.
    2. Pergunta: remover o botão "Editar pergunta" separado; adicionar "✎" ao lado de "Pergunta: ..." reusando o MESMO fluxo saveQuestion existente (mesma validação, mesmo PATCH, mesmos estados).
    3. Preservar: ScrollView raiz, estados §11 (vazio/erro), arquivar em menu (D-14 intacto), FlatList/tripwire (sem lista nova aqui). Se `updateProjectSchema` não aceitar `title`, NÃO ampliar schema neste plano — registrar como bloqueador no SUMMARY e parar (decisão de contrato volta ao Paulo).
  </action>
  <acceptance_criteria>
    - `grep -c "✎" "apps/lab/app/project/[id].tsx"` >= 2 (título + pergunta)
    - `grep -c "Editar pergunta" "apps/lab/app/project/[id].tsx"` == 0 (botão separado removido; "Pergunta:" como label pode restar)
    - `grep -c "projectsApi.update" "apps/lab/app/project/[id].tsx"` >= 2 (título + pergunta salvam pelo PATCH existente)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit</automated>
  </verify>
  <done>Título e pergunta editáveis inline por lápis, com PATCH real e sem botão separado</done>
</task>

<task type="auto">
  <name>task 2: testes + gates + adendo doc + auditoria</name>
  <files>apps/lab/src/projects/__tests__/projects-ui.test.ts, dev-docs/10-lab-esqueleto-telas.md</files>
  <read_first>
    - apps/lab/src/projects/__tests__/projects-ui.test.ts (padrão de teste da fase 7)
    - dev-docs/10-lab-esqueleto-telas.md §4 (cabeçalho + abas — ponto do adendo)
  </read_first>
  <action>
    1. Testes (vitest, sem rede, zero `any`): título com ✎ abre edição e Salvar chama update com { title }; Cancelar não chama; botão "Editar pergunta" separado ausente (fonte contém ✎ para pergunta); validação local bloqueia título vazio sem request.
    2. Gates: typecheck + lint + test do app exit 0; `grep -rn ": any\|as any" apps/lab/app/project apps/lab/src/projects | wc -l` == 0.
    3. Adendo mínimo em dev-docs/10-lab-esqueleto-telas.md §4: nota datada (12/09/2026, pedido Paulo) de que título e pergunta editam por lápis ✎ inline — sem reescrever o validado.
    4. Auditoria adversarial rápida do diff como seção do SUMMARY.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @uhhu/lab run typecheck && pnpm --filter @uhhu/lab run lint && pnpm --filter @uhhu/lab run test` todos exit 0
    - `grep -c "12/09/2026" dev-docs/10-lab-esqueleto-telas.md` >= 1 (adendo datado)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test 2>&1 | tail -n 6</automated>
  </verify>
  <done>UX do lápis testada, gates verdes, doc de telas anotada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TextInput → PATCH | mesma validação zod/limites do update existente; servidor revalida e autoriza por owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | título com HTML/script | mitigate | Text RN não interpreta HTML; servidor valida limites; sem dangerouslySetInnerHTML em nenhum caminho |
| T-07-06-02 | Elevation | editar projeto alheio | mitigate | mesmo projectsApi.update com owner do servidor (404 fora do escopo); sem ID novo confiado |
</threat_model>

<verification>
- Título e pergunta editam por ✎ inline com PATCH real; botão separado removido
- Testes + typecheck + lint exit 0, zero `any`
- Adendo datado na doc de telas §4
</verification>

<success_criteria>
- UI-10/UI-11: cabeçalho com edição inline por lápis (pedido Paulo 12/09)
- UI-04: gates mantidos
</success_criteria>

<output>
After completion, create `.planning/phases/07-projetos-buscas-execucao/07-06-SUMMARY.md`
</output>
