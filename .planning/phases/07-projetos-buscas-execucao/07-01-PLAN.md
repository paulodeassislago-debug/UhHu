---
phase: 07-projetos-buscas-execucao
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/lab/src/ui/ProjectModal.tsx
  - apps/lab/app/projects.tsx
  - apps/lab/app/project/[id].tsx
  - apps/lab/src/projects/__tests__/projects-ui.test.ts
autonomous: true
requirements:
  - UI-09
  - UI-10
  - UI-11
must_haves:
  truths:
    - User cria projeto via modal (título + pergunta) e a lista atualiza sem reload manual
    - User edita a pergunta inline no cabeçalho do projeto e arquiva/reativa por menu sem tela de configurações
    - User navega por 3 abas (Estratégias / Comparação / Corpus com contador vivo) sem perder posição
  artifacts:
    - path: apps/lab/src/ui/ProjectModal.tsx
      provides: Modal criar projeto com validação Zod client-side
      contains: createProjectSchema
    - path: apps/lab/app/projects.tsx
      provides: Lista com criar/arquivar/reativar + estados §11 em FlatList
      contains: FlatList
    - path: apps/lab/app/project/[id].tsx
      provides: Cabeçalho editável + TabBar de 3 abas + contador vivo do corpus
      contains: TabBar|Corpus:
  key_links:
    - from: apps/lab/src/ui/ProjectModal.tsx
      to: projectsApi.create
      via: createProjectSchema.parse antes do POST /api/v1/projects
      pattern: projectsApi\.create
    - from: apps/lab/app/project/[id].tsx
      to: labApi.getCorpus
      via: contador vivo da aba Corpus
      pattern: getCorpus
    - from: apps/lab/app/projects.tsx
      to: projectsApi.update
      via: arquivar/reativar com PATCH status
      pattern: projectsApi\.update
---

<objective>
Projetos na UI: modal de criação, lista gerenciável e cabeçalho do projeto com edição inline + abas (D-13, D-14, UI-09, UI-10, UI-11).

Purpose: Sem CRUD de projetos na UI o slice vertical trava no primeiro passo — a lista da fase 6 tem CTA desabilitado e o cabeçalho é só leitura.
Output: ProjectModal reutilizável, projects.tsx com criar/arquivar/reativar, [id].tsx com pergunta editável + TabBar de 3 abas + contador vivo.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projetos-buscas-execucao/07-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/08-security-baseline.md
@apps/lab/app/projects.tsx
@apps/lab/app/project/[id].tsx
@apps/lab/src/api/projects.ts
@apps/lab/src/api/lab.ts

<interfaces>
Esqueleto §3 (textos exatos, sem reinventar):
- Card: título, pergunta 1 linha truncada, status ativo/arquivado, contagem leve opcional v1.
- Vazio: "Nenhum projeto ainda — comece uma pesquisa". Criar via modal (título + pergunta).
Esqueleto §4 (cabeçalho + abas):
- Cabeçalho: título, "Pergunta: ...", status, data. Abas: [ Estratégias ] [ Comparação ] [ Corpus: N ].
- Corpus com contador vivo (número reflete decisões; aqui reflete o GET corpus).

Contrato real (packages/contracts/src/projects.ts + lab.ts):
- createProjectSchema: { title: string(1..200 trim), researchQuestion?: string(≤2000), description?: string(≤5000) }.
- updateProjectSchema: partial + { status?: 'active'|'archived', referenceSearchId?: uuid|null }.
- projectsApi.list({status:'active'|'archived'|'all'}) / .create(input) / .update(id,patch) / .listById(id).
- Arquivar = PATCH { status:'archived' }; reativar = PATCH { status:'active' } (sem tela de config — D-14).
- labApi.getCorpus(projectId, {limit}) → { items, page:{limit,nextCursor,hasMore} } — SEM total; contagem = items.length (+ "+" se hasMore).
- labApi.listSearches(projectId, {limit}) → { items, page } — SEM total; nº buscas = items.length (+ "+" se hasMore); ordenado desc(createdAt), items[0] é o mais recente.
- ProjectDTO: { id, title, researchQuestion: string|null, description, status, referenceSearchId: string|null, createdAt, updatedAt }.
- 401 com sessão prévia → markExpired() + router.replace /login?expired=1&next=<rota atual> (padrão 06-03/06-04, isSafeNext em session.tsx).
- Estados §11: Empty / CardSkeleton / ErrorBanner (onRetry refaz fetch) — nunca spinner solitário; lista = FlatList (tripwire 06-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: ProjectModal + lista de projetos gerenciável</name>
  <files>apps/lab/src/ui/ProjectModal.tsx, apps/lab/app/projects.tsx</files>
  <read_first>
    - apps/lab/app/projects.tsx (estado atual: FlatList, CTA desabilitado, texto "disponível na fase 7")
    - apps/lab/src/api/projects.ts (projectsApi.create/update/list com schemas)
    - apps/lab/src/ui/Empty.tsx (props title/message/actionLabel/onAction/disabled)
    - packages/contracts/src/projects.ts (createProjectSchema exato)
  </read_first>
  <action>Implementar D-13 + UI-09 + UI-10 (lista) sem inventar contrato:
  1. Criar apps/lab/src/ui/ProjectModal.tsx: `ProjectModal({visible, onClose, onCreated})` com React Native `Modal` (transparent, animationType slide) + card central com dois campos — Título (obrigatório, maxLength 200) e Pergunta de pesquisa (opcional, multiline, maxLength 2000). Validar no submit com `createProjectSchema.parse({title: title.trim(), researchQuestion: question.trim().length>0 ? question.trim() : undefined})` e exibir a primeira mensagem Zod (flatten fieldErrors) em texto vermelho acima dos botões. Botões: "Criar projeto" (chama `projectsApi.create(parsed, {getToken})`, fecha e chama onCreated(created)) e "Cancelar". Erro de API (ApiError) exibido verbatim (message + requestId caption). Props tipadas com interface exportada; `import type { ProjectDTO }` de contracts; zero `any`.
  2. Em apps/lab/app/projects.tsx: habilitar o CTA — botão "Nova pesquisa (+)" abre o modal; onCreated insere o criado no topo da lista local (sem refetch full) e fecha o modal. Cada card FlatList ganha linha de ações: [Abrir] (Link /project/[id]) + [Arquivar]/[Reativar] (projectsApi.update(id,{status}, {getToken}) + atualiza item local; arquivar remove da lista `active`, reativar idem). Adicionar filtro de status no topo: três botões Ativos/Arquivados (chamam list com status correspondente; 'all' NÃO usado). Contagem leve por card: `N busca(s)` via `labApi.listSearches(project.id, {limit: 100})` por projeto com cache em Map local (um fetch por projeto por mount; erro de contagem → omite a linha, nunca quebra a lista). Nº de elegíveis NÃO exibido (fase 9; comentário no código citando UI-23). Manter FlatList + keyExtractor id + ListEmpty verbatim + CardSkeleton + ErrorBanner com Repetir + 401→expired/next=/projects. Nenhum `.map(` de lista na tela (tripwire).
  </action>
  <acceptance_criteria>
    - `test -f apps/lab/src/ui/ProjectModal.tsx` e `grep -c "createProjectSchema" apps/lab/src/ui/ProjectModal.tsx` >= 2 (import + parse)
    - `grep -c "ProjectModal\|projectsApi.create\|projectsApi.update" apps/lab/app/projects.tsx` >= 4
    - `grep -c "fase 7" apps/lab/app/projects.tsx` == 0 (nenhum CTA desabilitado restante da fase 6)
    - `grep -rn ": any\|as any\|@ts-ignore" apps/lab/src/ui/ProjectModal.tsx apps/lab/app/projects.tsx | wc -l` == 0
    - `grep -c "FlatList" apps/lab/app/projects.tsx` >= 2 (import + uso; tripwire preservado)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit -p tsconfig.json && pnpm --filter @uhhu/lab exec eslint apps/lab/src/ui/ProjectModal.tsx apps/lab/app/projects.tsx</automated>
  </verify>
  <done>Modal cria projeto e volta à lista atualizada; arquivar/reativar por card e filtro Ativos/Arquivados funcionam; typecheck+lint verdes</done>
</task>

<task type="auto">
  <name>task 2: cabeçalho editável + TabBar com contador vivo + testes</name>
  <files>apps/lab/app/project/[id].tsx, apps/lab/src/projects/__tests__/projects-ui.test.ts</files>
  <read_first>
    - apps/lab/app/project/[id].tsx (cabeçalho + placeholder de abas atual)
    - apps/lab/src/api/projects.ts (update com researchQuestion/status) + apps/lab/src/api/lab.ts (getCorpus)
    - apps/lab/src/auth/session.tsx (useAuth, markExpired, isSafeNext)
    - apps/lab/src/ui/__tests__/scroll-containers.test.ts (padrão tripwire — NÃO modificar aqui; só referência)
  </read_first>
  <action>Implementar D-14 + UI-11 (cabeçalho + navegação por abas) com valores concretos:
  1. Em apps/lab/app/project/[id].tsx: cabeçalho real — título (24/semibold), linha "Pergunta: {researchQuestion ?? '—'}" com botão "Editar pergunta" que troca para TextInput multiline + botões Salvar/Cancelar; Salvar chama `projectsApi.update(projectId, {researchQuestion: value.trim().length>0 ? value.trim() : null}, {getToken})` — string vazia salva null (limpa a pergunta). Menu do cabeçalho: botões [Arquivar]/[Reativar] (update status + setProject local) — sem tela de configurações. Abaixo do cabeçalho, TabBar com 3 itens: [Estratégias] (Link /project/[id]/strategies), [Comparação] (botão disabled com hint "fase 9"), [Corpus: N] (Link /project/[id]/corpus) onde N = contador vivo via `labApi.getCorpus(projectId, {limit: 100}, {getToken})` → `${items.length}${page.hasMore ? '+' : ''}`; erro do contador → "Corpus" sem número (nunca quebra o cabeçalho). Remover o Empty placeholder "fase 7" e o PartialBanner placeholder da fase 6; manter ScrollView raiz + CardSkeleton + ErrorBanner com Repetir + 401→expired/next=`/project/${projectId}`. Tab ativa derivada da rota atual (prop opcional `activeTab` default 'none' nesta tela).
  2. Criar apps/lab/src/projects/__tests__/projects-ui.test.ts (vitest, sem rede — mock global.fetch tipado como em client.test.ts): cobre (a) update de pergunta vazia serializa `{researchQuestion: null}` no body do PATCH; (b) arquivar serializa `{status:'archived'}`; (c) contador vivo monta "Corpus: 3" com items.length 3/hasMore false e "Corpus: 100+" com hasMore true — testar a função pura de formatação exportada de [id].tsx ou de um helper `formatCorpusCount(items.length, hasMore)` em `apps/lab/src/projects/counts.ts` (criar se necessário; se criar, adicionar aos files_modified). Sem `any` (unknown + narrowing).
  </action>
  <acceptance_criteria>
    - `grep -c "Editar pergunta\|Arquivar\|Reativar" "apps/lab/app/project/[id].tsx"` >= 3
    - `grep -c "getCorpus\|Corpus:" "apps/lab/app/project/[id].tsx"` >= 2
    - `grep -c "fase 7" "apps/lab/app/project/[id].tsx"` == 0 (placeholder removido)
    - `grep -rn ": any\|as any" apps/lab/src/projects/ "apps/lab/app/project/[id].tsx" | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test -- src/projects/__tests__/projects-ui.test.ts 2>&1 | tail -n 12</automated>
  </verify>
  <done>Pergunta edita inline e persiste; arquivar/reativar no cabeçalho; 3 abas com contador vivo; testes verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal → CORE | Título/pergunta são input hostil; servidor revalida via Zod (client valida por UX) |
| lista → outro usuário | IDs fora do escopo retornam 404 idêntico; app nunca distingue "inexistente" de "alheio" |
| contador → getCorpus | Contagem éEnriquecimento de leitura; nunca decide elegibilidade no client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | ProjectModal title/question | mitigate | createProjectSchema.parse no client (UX) + Zod no servidor; trim + maxLength nos inputs |
| T-07-01-02 | Information Disclosure | IDOR em projects/:id | mitigate | ownerId deriva do ator no servidor; app trata 404 como "não encontrado" genérico; teste cobre 404 |
| T-07-01-03 | Spoofing | guarda de frontend como auth | accept | guards são UX (AGENTS.md); 401 real do servidor dispara expired+next; sem decisão de privilégio no client |
| T-07-01-04 | Tampering | XSS via título/pergunta | mitigate | React Native escapa Text por padrão; sem dangerouslySetInnerHTML/WebView; sem eval |
</threat_model>

<verification>
- typecheck + lint do app exit 0, zero `any` nos arquivos do plano
- vitest do novo teste verde; tripwire de scroll intacto (FlatList em projects, ScrollView em [id])
- Fluxo manual: criar via modal → aparece no topo; arquivar some de Ativos e aparece em Arquivados; pergunta edita e persiste após reload
</verification>

<success_criteria>
- UI-09: lista com título + pergunta truncada + status + contagem leve de buscas + vazio orientador
- UI-10: criar via modal, pergunta editável inline, arquivar/reativar sem tela de config
- UI-11 (parcial deste plano): 3 abas navegáveis com contador vivo do corpus
</success_criteria>

<output>
After completion, create `.planning/phases/07-projetos-buscas-execucao/07-01-SUMMARY.md`
</output>
