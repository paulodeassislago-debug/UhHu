---
phase: 07-projetos-buscas-execucao
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/lab/src/search/searchTerm.ts
  - apps/lab/src/search/SearchForm.tsx
  - apps/lab/src/search/SearchCard.tsx
  - apps/lab/app/project/[id]/search-form.tsx
  - apps/lab/app/project/[id]/strategies.tsx
  - apps/lab/src/search/__tests__/searchTerm.test.ts
  - apps/lab/src/ui/__tests__/scroll-containers.test.ts
autonomous: true
requirements:
  - UI-12
  - UI-13
must_haves:
  truths:
    - User vê cards de estratégia com termos legíveis, filtros, fontes, nº de runs e última execução (ou vazio orientador)
    - User monta a busca em linhas com AND/OR/NOT (sem expressão livre) e o app compõe o termo do contrato
    - User salva a estratégia OU salva-e-executa, vendo o selo de filtro Core + status das fontes antes de executar
  artifacts:
    - path: apps/lab/src/search/searchTerm.ts
      provides: Composição linhas → termo pass-through do contrato
      contains: buildSearchTerm
    - path: apps/lab/app/project/[id]/search-form.tsx
      provides: Formulário fiel ao §6 com selo Core + status fontes + 2 CTAs
      contains: filtro garantido pelo Core
    - path: apps/lab/app/project/[id]/strategies.tsx
      provides: Lista de cards de estratégia em FlatList
      contains: FlatList
  key_links:
    - from: apps/lab/src/search/SearchForm.tsx
      to: labApi.createSearch/updateSearch
      via: createSearchSchema.parse({projectId, term, filters, sources})
      pattern: createSearch
    - from: apps/lab/src/search/SearchCard.tsx
      to: labApi.listRuns
      via: runs count + última execução (items[0], desc executedAt)
      pattern: listRuns
    - from: apps/lab/app/project/[id]/search-form.tsx
      to: labApi.listSources/getSourceHealth
      via: status das fontes antes de executar
      pattern: listSources
---

<objective>
Estratégias: lista de cards + formulário de busca fiel ao §6 com salvar vs salvar-e-executar (D-09, D-10, UI-12, UI-13).

Purpose: O pesquisador define estratégias declarativas e as executa — sem este plano não há o que acompanhar (07-03) nem historiar (07-04).
Output: Helper searchTerm, SearchForm + tela search-form, SearchCard + tela strategies reescrita, testes unitários, tripwire estendido.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projetos-buscas-execucao/07-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/08-security-baseline.md
@apps/lab/app/project/[id]/strategies.tsx
@apps/lab/src/api/lab.ts
@apps/lab/src/auth/session.tsx

<interfaces>
Esqueleto §5 (card de estratégia — textos e ações):
- Card: descritores/termos legível com operadores, resumo filtros, fontes (BDTD/CAPES/ambas), "runs: N · último: <data> · <total> resultados".
- Ações: [Executar] [Comparar(disabled hint "fase 9")] [⋯ menu: editar, duplicar, excluir(disabled hint "neste plano só visual — excluir na 07-04")].
- Vazio: "Nenhuma estratégia — defina a primeira busca".
Esqueleto §6 (formulário — blocos e selo):
- Termos em linhas com operador AND/OR/NOT entre elas (SEM campo de expressão livre — D-09).
- Filtros: Ano [de]—[até] (opcional), Tipo [x] tese [x] dissertação, Área/Instituição/Programa (opcionais, pós-filtro Core quando a fonte não honra).
- Fontes: (x) BDTD (x) CAPES default ambas marcadas + "[status das fontes: ...]" + selo "filtro garantido pelo Core (pós-filtro)".
- CTAs: [Salvar estratégia] [Executar agora] (salvar ≠ executar — D-10).

Contrato real (packages/contracts/src/lab.ts) — FASE 7 NÃO MUDA CONTRATO:
- createSearchSchema: { projectId: uuid, term: searchTermSchema(1..500 trim, aspas balanceadas), filters: {yearFrom?, yearTo?(yearFrom≤yearTo), docTypes?: ('masterThesis'|'doctoralThesis')[], source?: 'bdtd'|'capes', area?≤200, institution?≤300, program?≤300}, sources: ('bdtd'|'capes')[1..2] default ['bdtd','capes'] }.
- NÃO existe campo booleanOperators: as linhas AND/OR/NOT da UI são COMPOSTAS num único `term` pass-through (D-31: Core repassa sem tradução estruturada). Regra de composição (searchTerm.ts): cada linha {text, op} onde op é o operador ANTES da linha (primeira sem op); texto com espaço e sem aspas → envolver em `"..."`; juntar com ` AND `/` OR `/` NOT `; validar o resultado com searchTermSchema.parse (erro → mensagem PT-BR do Zod na tela, sem POST).
- Mapeamento de tipo UI→contrato: tese → 'doctoralThesis', dissertação → 'masterThesis' (NÃO inverter).
- labApi.createSearch(input) / .updateSearch(id,patch) / .listSearches(projectId,{limit,cursor}) / .executeSearch(id,{idempotencyKey}) / .listRuns(searchId,{limit}) / .listSources() → LabSourceEntry[{name,level,enabled}] / .getSourceHealth(source) → {status:'ok'|'degraded'|'offline'}.
- SearchRunDTO.metrics: { perSource:{bdtd:{status,total,returned,durationMs}, capes:{...}}, newCount, coverage:{bdtd,capes} }; total do run = coverage.bdtd + coverage.capes. listRuns ordenado desc(executedAt): items[0] = última execução. Run status reais: 'queued'|'running'|'succeeded'|'partial'|'failed'|'cancelled' (rótulo "ok" = succeeded na UI).
- Executar: POST .../runs com Idempotency-Key = `globalThis.crypto.randomUUID()` por toque (sem Math.random — AGENTS.md); 201 = concluiu sync (QUALQUER status final), 202 = running (polling na 07-03), 200 = replay idempotente, 429 = 10 runs/h (mensagem verbatim + sem retry auto), 422 = conflito idempotência. Após execute → router.push('/project/[id]/run?runId=&searchId=') — rota como STRING literal (tela criada na 07-03; sem import).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: helper searchTerm + formulário §6 com salvar/salvar-e-executar</name>
  <files>apps/lab/src/search/searchTerm.ts, apps/lab/src/search/SearchForm.tsx, apps/lab/app/project/[id]/search-form.tsx, apps/lab/src/search/__tests__/searchTerm.test.ts</files>
  <read_first>
    - packages/contracts/src/lab.ts (createSearchSchema, searchTermSchema, filtros, sources — valores exatos acima)
    - apps/lab/src/api/lab.ts (createSearch/updateSearch/executeSearch/listSources/getSourceHealth + LabRequestOptions.idempotencyKey)
    - dev-docs/10-lab-esqueleto-telas.md §6 (blocos do formulário)
  </read_first>
  <action>Implementar D-09 + D-10 + UI-13 com valores concretos (sem mudar contrato):
  1. Criar apps/lab/src/search/searchTerm.ts: `export interface TermRow { text: string; op: 'AND'|'OR'|'NOT'; }` + `export function buildSearchTerm(rows: TermRow[]): string` — trim por linha, descarta vazias, envolve em aspas duplas se contém espaço e ainda não está entre aspas, junta a partir da 2ª linha com ` ${row.op} `; `export function splitSearchTerm(term: string): TermRow[]` faz o inverso aproximado para edição (split por ` AND | OR | NOT ` com regex, primeira sem op, demais com op capturado; fallback linha única). Erro de validação via `searchTermSchema.parse` no chamador (não neste arquivo). Zero `any`.
  2. Criar apps/lab/src/search/SearchForm.tsx: componente controlado `SearchForm({projectId, initial?, sourcesHealth, onSave, onSaveAndRun, saving})` com: (a) linhas de termos — FlatList aninhada `scrollEnabled={false}` de rows {text, op} (primeira sem seletor de op; demais com 3 botões AND/OR/NOT; botão + adiciona linha, × remove, mínimo 1); (b) Ano de/até (TextInput numérico, vazio = indefinido); Tipo: dois toggles tese/dissertacão → doctoralThesis/masterThesis; Área/Instituição/Programa (TextInput opcionais); (c) Fontes: dois toggles BDTD/CAPES (mínimo 1; default ambas) + selo fixo "filtro garantido pelo Core (pós-filtro)" + status de cada fonte recebido por prop (`{bdtd: 'ok'|..., capes: ...}` → texto "BDTD: operacional/degradada/offline"); (d) dois CTAs: "Salvar estratégia" → onSave(payload) e "Executar agora" → onSaveAndRun(payload), onde payload = {term: buildSearchTerm(rows), filters, sources}. Validação local: buildSearchTerm vazio ou parse Zod falho → erro PT-BR na tela sem chamar API.
  3. Criar apps/lab/app/project/[id]/search-form.tsx: ScrollView raiz; carrega saúde das fontes (`listSources` + `getSourceHealth` por fonte, {getToken}; falha → status "indisponível" sem bloquear o form); modo criar (sem ?searchId=) ou editar (?searchId=: getSearch + splitSearchTerm); Salvar → createSearch ou updateSearch + router.back(); Executar agora → salva (cria/atualiza) e em seguida `executeSearch(searchId, {getToken, idempotencyKey: globalThis.crypto.randomUUID()})` + push `/project/[id]/run?runId=<id>&searchId=<sid>`; 429/422/400 → ErrorBanner verbatim + sem navegação; 401 → expired/next. Estados §11 (skeleton no load de edição, vazio n/a, erro com Repetir).
  4. Criar apps/lab/src/search/__tests__/searchTerm.test.ts: buildSearchTerm com 1 linha ("química" → `química`), 2 linhas com espaço (AND → `"ensino de química" AND gamificação`), NOT, linha vazia descartada, aspas balanceadas (parse do contrato passa); splitSearchTerm round-trip. Vitest puro, sem rede, sem `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "buildSearchTerm\|splitSearchTerm" apps/lab/src/search/searchTerm.ts` >= 3 e `grep -c "booleanOperators" apps/lab/src/search/*.ts* "apps/lab/app/project/[id]/search-form.tsx" 2>/dev/null | grep -v "^.*:0" | wc -l` == 0 (nenhum campo novo de contrato)
    - `grep -c "filtro garantido pelo Core" apps/lab/src/search/SearchForm.tsx` >= 1 e `grep -c "Salvar estratégia\|Executar agora" apps/lab/src/search/SearchForm.tsx` >= 2
    - `grep -c "randomUUID" "apps/lab/app/project/[id]/search-form.tsx"` >= 1 e `grep -c "Math.random" apps/lab/src/search/*.ts* "apps/lab/app/project/[id]/search-form.tsx" | grep -v ":0" | wc -l` == 0
    - `grep -c "doctoralThesis.*tese\|tese.*doctoralThesis" apps/lab/src/search/SearchForm.tsx; grep -c "masterThesis" apps/lab/src/search/SearchForm.tsx` >= 1 (mapeamento tese→doctoral presente)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test -- src/search/__tests__/searchTerm.test.ts 2>&1 | tail -n 12</automated>
  </verify>
  <done>Linhas AND/OR/NOT compõem termo válido do contrato; form §6 com selo + saúde das fontes + 2 CTAs; salvar e salvar-e-executar funcionam; testes verdes</done>
</task>

<task type="auto">
  <name>task 2: lista de estratégias com cards + tripwire estendido</name>
  <files>apps/lab/src/search/SearchCard.tsx, apps/lab/app/project/[id]/strategies.tsx, apps/lab/src/ui/__tests__/scroll-containers.test.ts</files>
  <read_first>
    - apps/lab/app/project/[id]/strategies.tsx (placeholder atual a substituir)
    - apps/lab/src/api/lab.ts (listSearches, listRuns, executeSearch)
    - apps/lab/src/ui/Empty.tsx + ErrorBanner.tsx + Skeleton.tsx (estados §11)
    - apps/lab/src/ui/__tests__/scroll-containers.test.ts (estender com a nova tela de form + strategies real)
  </read_first>
  <action>Implementar UI-12 (cards + vazio) com dados reais e sem fingir métrica:
  1. Criar apps/lab/src/search/SearchCard.tsx: `SearchCard({search, getToken, onChanged})` — exibe term legível (string verbatim, numberOfLines 2), resumo filtros (`2020–2025 · tese, dissertação · área X` montado só com filtros presentes; vazio → "sem filtros"), fontes (`BDTD + CAPES` a partir de search.sources), e bloco de runs: `listRuns(search.id, {limit: 100}, {getToken})` → `runs: N${hasMore?'+':''}` + última = items[0] (`hoje HH:MM` se mesmo dia senão `DD/MM` + `· <coverage.bdtd+coverage.capes> resultados` + rótulo de status: succeeded→"ok", demais verbatim). Erro no fetch de runs → linha "histórico indisponível" (card continua). Ações: [Executar] (executeSearch + idempotencyKey randomUUID + push run como na task 1; 429 → mensagem verbatim no card), [Comparar] disabled hint "fase 9", [⋯] menu com [Editar] (push search-form?searchId=), [Duplicar] (createSearch com mesmo projectId/term/filters/sources verbatim, sem sufixo — Search não tem título), [Excluir] disabled hint "diálogo de cascata na 07-04" (NÃO excluir aqui). Zero `any`, `import type` dos DTOs.
  2. Reescrever apps/lab/app/project/[id]/strategies.tsx: FlatList raiz de SearchDTO via listSearches(projectId) + botão "Nova estratégia (+)" → push search-form; vazio → Empty "Nenhuma estratégia"/"Defina a primeira busca"; loading → CardSkeleton; erro → ErrorBanner + Repetir; 401 → expired/next=`/project/${id}/strategies`. Nenhum `.map(` na tela (cards via renderItem; linhas internas vivem no componente).
  3. Estender scroll-containers.test.ts: adicionar STRATEGIES_PATH e SEARCHFORM_PATH (lê search-form.tsx: importa+usa ScrollView; strategies.tsx: importa+usa FlatList + keyExtractor + renderItem) e incluí-los no teste "nenhum .map fora de FlatList/renderItem". Manter os 4 paths existentes intactos.
  </action>
  <acceptance_criteria>
    - `grep -c "FlatList" "apps/lab/app/project/[id]/strategies.tsx"` >= 2 e `grep -c "\.map(" "apps/lab/app/project/[id]/strategies.tsx" "apps/lab/app/project/[id]/search-form.tsx"` == 0
    - `grep -c "Nenhuma estratégia" "apps/lab/app/project/[id]/strategies.tsx"` >= 1
    - `grep -c "strategies\|search-form" apps/lab/src/ui/__tests__/scroll-containers.test.ts` >= 4
    - `grep -rn ": any\|as any" apps/lab/src/search/SearchCard.tsx "apps/lab/app/project/[id]/strategies.tsx" | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test -- src/ui/__tests__/scroll-containers.test.ts 2>&1 | tail -n 8 && pnpm --filter @uhhu/lab exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Cards com runs/última execução reais; Executar navega ao run; tripwire cobre as 2 telas novas; typecheck verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linhas de termo → term | Input hostil composto no client; servidor valida tamanho + aspas (400 em inválido) |
| Executar → rate limit | 10 runs/h por usuário no banco; client exibe 429 verbatim sem auto-retry (sem DoS auto-infligido) |
| saúde das fontes → UI | Status é informativo; nunca autoriza nem bloqueia execução (decisão é do servidor na execução) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | term composto | mitigate | searchTermSchema.parse antes do POST + Zod no servidor; sem eval/new Function na composição |
| T-07-02-02 | Denial of Service | duplo-tap em Executar | mitigate | Idempotency-Key única por toque (replay vira 200 sem novo run); sem auto-retry em 429 |
| T-07-02-03 | Information Disclosure | runs de outro usuário | mitigate | listRuns owner-scoped no servidor; 404 genérico; sem exibir IDs de outros escopos |
| T-07-02-04 | Tampering | XSS via term/filtros | mitigate | Text escapa por padrão; sem WebView/innerHTML; nota de divergência fora desta fase |
</threat_model>

<verification>
- typecheck + lint + testes (searchTerm + tripwire) verdes, zero `any`
- Fluxo manual: criar estratégia em linhas → card aparece com termos legíveis; Executar agora → run criado e navega (tela do run chega na 07-03)
- `grep -c "booleanOperators\|migrate\|drizzle" apps/lab/src/search/* | wc -l` == 0 (nenhuma mudança de contrato)
</verification>

<success_criteria>
- UI-12: cards com termos/filtros/fontes/runs/última execução + vazio orientador
- UI-13: salvar vs salvar-e-executar com selo Core + status das fontes visível
- D-09/D-10 implementados sem expressão livre e sem campo novo no contrato
</success_criteria>

<output>
After completion, create `.planning/phases/07-projetos-buscas-execucao/07-02-SUMMARY.md`
</output>
