---
phase: 07-projetos-buscas-execucao
plan: "05"
type: execute
wave: 4
depends_on:
  - 07-02
  - 07-03
gap_closure: true
gap_source: 07-HUMAN-UAT.md item 1 (teste humano real 12/09/2026 — EXECUTAR AGORA quebra na web com crypto.randomUUID is not a function; review LW já sinalizava)
files_modified:
  - apps/lab/package.json
  - apps/lab/src/utils/uuid.ts
  - apps/lab/app/project/[id]/search-form.tsx
  - apps/lab/app/project/[id]/run.tsx
  - apps/lab/src/search/SearchCard.tsx
  - apps/lab/src/utils/__tests__/uuid.test.ts
  - apps/lab/src/ui/__tests__/scroll-containers.test.ts
autonomous: true
requirements:
  - UI-14
must_haves:
  truths:
    - User toca EXECUTAR AGORA na web via HTTP inseguro (tailnet) e o run é criado (navega para /run, polling inicia) sem exceção de crypto
    - Idempotency-Key continua única por toque e 429 continua sem auto-retry (T-07-02-02 preservado)
    - Nenhum `Math.random` no caminho; nenhum `crypto.randomUUID` nu restante no app
  artifacts:
    - path: apps/lab/src/utils/uuid.ts
      provides: Geração de UUID cross-platform (web+nativo)
      contains: expo-crypto|getRandomValues
    - path: apps/lab/src/utils/__tests__/uuid.test.ts
      provides: Teste do gerador + formato UUID
      contains: uuid
  key_links:
    - from: apps/lab/app/project/[id]/search-form.tsx
      to: apps/lab/src/utils/uuid.ts
      via: EXECUTAR AGORA gera Idempotency-Key pelo wrapper compatível
      pattern: uuid|newIdempotencyKey
    - from: apps/lab/src/ui/__tests__/scroll-containers.test.ts
      to: apps/lab/app/project/[id]/search-form.tsx
      via: tripwire falha se crypto.randomUUID nu voltar às telas
      pattern: randomUUID
---

<objective>
Corrigir o blocker do UAT: trocar `globalThis.crypto.randomUUID()` (só existe em contexto seguro; beta é HTTP via tailnet) por geração compatível web+nativo nos 3 toques, com tripwire.

Purpose: O caminho Executar da UI está quebrado na web; sem este fix a fase 7 não fecha.
Output: Wrapper UUID cross-platform, 3 call sites migrados, teste + tripwire, bundle web reexportado, SUMMARY com auditoria.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projetos-buscas-execucao/07-CONTEXT.md
@dev-docs/08-security-baseline.md
@apps/lab/src/api/client.ts
@apps/lab/app/project/[id]/search-form.tsx
@apps/lab/app/project/[id]/run.tsx
@apps/lab/src/search/SearchCard.tsx

<interfaces>
Evidência (12/09/2026, Chrome headless + preview tailnet HTTP): EXECUTAR AGORA lança "globalThis.crypto.randomUUID is not a function" — sem navegação, sem run. SALVAR funciona (não gera key). 3 call sites nus: search-form.tsx:220, run.tsx:176, SearchCard.tsx:165. client.ts:newRequestId já degrada (null) — só os toques quebram.
Abordagem: `pnpm --filter @uhhu/lab add expo-crypto` (Expo SDK 57 — resolver versão compatível ~15.x; 0 deps novas além dela) e wrapper `newIdempotencyKey()` em apps/lab/src/utils/uuid.ts usando expo-crypto (`randomUUID()` síncrono, web+nativo) — SEM `Math.random` em nenhum caminho (grep bloqueante). Propriedades preservadas: key única por toque (UUIDv4), sem auto-retry em 429, header Idempotency-Key como antes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: wrapper UUID + migração dos 3 toques</name>
  <files>apps/lab/package.json, apps/lab/src/utils/uuid.ts, apps/lab/app/project/[id]/search-form.tsx, apps/lab/app/project/[id]/run.tsx, apps/lab/src/search/SearchCard.tsx</files>
  <read_first>
    - apps/lab/src/api/client.ts newRequestId (padrão de degradação existente; o wrapper segue o mesmo espírito mas nunca retorna null — expo-crypto sempre gera)
    - os 3 call sites (contexto do handler: idempotencyKey por toque, sem retry)
    - apps/lab/package.json (expo 57.0.22 — compatibilidade da versão expo-crypto)
  </read_first>
  <action>
    1. Adicionar `expo-crypto` ao app (`pnpm --filter @uhhu/lab add expo-crypto`, versão compatível com SDK 57). Se offline/sem registro, fallback documentado: UUIDv4 manual via `globalThis.crypto.getRandomValues` (disponível em contexto inseguro) com o mesmo wrapper e sem `Math.random` — nunca travar a fase por rede; registrar a escolha no SUMMARY.
    2. Criar apps/lab/src/utils/uuid.ts exportando `newIdempotencyKey(): string` (UUIDv4 via expo-crypto; comentário citando o gap UAT 12/09/2026 e a restrição de contexto seguro). Zero `any`.
    3. Migrar os 3 call sites para o wrapper, preservando semântica (key por toque, header igual, sem retry).
    4. Typecheck + lint do app exit 0; `grep -rn "crypto.randomUUID" apps/lab/app apps/lab/src --include="*.ts*" | grep -v "utils/uuid.ts\|__tests__" | wc -l` == 0; `grep -rn "Math.random" apps/lab/app apps/lab/src | wc -l` == 0.
  </action>
  <acceptance_criteria>
    - `test -f apps/lab/src/utils/uuid.ts && grep -c "newIdempotencyKey" apps/lab/src/utils/uuid.ts` >= 2
    - `grep -rn "crypto.randomUUID" apps/lab/app apps/lab/src --include="*.ts*" | grep -v "utils/uuid.ts\|__tests__" | wc -l` == 0
    - `grep -rn "Math.random" apps/lab/app apps/lab/src --include="*.ts*" | wc -l` == 0
    - `pnpm --filter @uhhu/lab exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit && pnpm --filter @uhhu/lab exec eslint apps/lab/src/utils apps/lab/app/project apps/lab/src/search 2>&1 | tail -n 5</automated>
  </verify>
  <done>EXECUTAR AGORA gera key em qualquer contexto (HTTPS/localhost/HTTP tailnet/Hermes) sem exceção</done>
</task>

<task type="auto">
  <name>task 2: teste + tripwire + reexport + auditoria</name>
  <files>apps/lab/src/utils/__tests__/uuid.test.ts, apps/lab/src/ui/__tests__/scroll-containers.test.ts</files>
  <read_first>
    - apps/lab/src/api/__tests__/client.test.ts (padrão de teste: vitest, zero `any`, sem rede)
    - apps/lab/src/ui/__tests__/scroll-containers.test.ts (tripwire existente — estender ou adicionar asserção irmã no mesmo arquivo)
  </read_first>
  <action>
    1. Criar apps/lab/src/utils/__tests__/uuid.test.ts: `newIdempotencyKey()` retorna string no formato UUIDv4 (`/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/`), duas chamadas seguidas diferem, e 1000 chamadas não colidem. Simular ausência de `crypto.randomUUID` (delete/stub) provando que o wrapper continua gerando (é exatamente o cenário do beta HTTP).
    2. Estender o tripwire (scroll-containers.test.ts ou novo bloco): falha se `crypto.randomUUID` nu aparecer em apps/lab/app ou apps/lab/src fora de `utils/uuid.ts` e fora de `__tests__`.
    3. Gates: typecheck + lint + `vitest run` do app exit 0. Reexportar bundle web + grep de segredos em dist == 0.
    4. Auditoria adversarial rápida do diff (o wrapper vaza algo? a key vai só para o header Idempotency-Key como antes? nenhuma mudança em auth/sessão?) como seção do SUMMARY.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @uhhu/lab run typecheck && pnpm --filter @uhhu/lab run lint && pnpm --filter @uhhu/lab run test` todos exit 0
    - `test -f apps/lab/src/utils/__tests__/uuid.test.ts`
    - `grep -rE "uhhu_pat|COOKIE_SECRET|DATABASE_URL" apps/lab/dist/ 2>/dev/null | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test 2>&1 | tail -n 8</automated>
  </verify>
  <done>Gap fechado com prova automatizada do cenário HTTP-inseguro; bundle reexportado limpo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper → header | key opaca por toque, mesma que antes; sem dado de usuário dentro |
| nova dep expo-crypto | lib oficial Expo, sem rede, sem segredo; só entropia local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Information Disclosure | Idempotency-Key previsível | mitigate | UUIDv4 via CSPRNG (expo-crypto/getRandomValues); `Math.random` proibido com grep bloqueante |
| T-07-05-02 | Tampering | dep maliciosa | mitigate | só expo-crypto oficial, versão pinada no lockfile; `pnpm audit` registrado |
| T-07-05-03 | Denial of Service | exceção no handler | mitigate | wrapper nunca lança no caminho do toque (é o próprio bug); teste simula ausência de randomUUID |
</threat_model>

<verification>
- EXECUTAR AGORA gera key sem `crypto.randomUUID` global (teste simula o beta HTTP)
- typecheck + lint + testes exit 0, zero `any`, zero `Math.random`
- `expo export --platform web` ok + dist sem segredos
</verification>

<success_criteria>
- UI-14: caminho Executar funciona na web HTTP (gap UAT fechado no código; re-teste humano pendente)
- UI-04: gates mantidos + tripwire estendido
</success_criteria>

<output>
After completion, create `.planning/phases/07-projetos-buscas-execucao/07-05-SUMMARY.md`
</output>
