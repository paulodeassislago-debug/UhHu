---
phase: 07-projetos-buscas-execucao
plan: "04"
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - apps/lab/src/search/RunHistory.tsx
  - apps/lab/src/search/DeleteSearchDialog.tsx
  - apps/lab/src/search/SearchCard.tsx
  - apps/lab/src/search/__tests__/runHistory.test.ts
autonomous: true
requirements:
  - UI-15
  - UI-16
must_haves:
  truths:
    - User exclui busca somente após diálogo que lista a cascata (runs, resultados, decisões perdidas)
    - User consulta o histórico de runs expansível dentro do card da estratégia (sem quarta aba)
    - Cada entrada do histórico mostra data/hora, status, total, novos, duração e detalhe do parcial; toque abre o run
  artifacts:
    - path: apps/lab/src/search/DeleteSearchDialog.tsx
      provides: Diálogo de cascata antes do hard-delete
      contains: hard-delete|serão perdidas|serão perdidos
    - path: apps/lab/src/search/RunHistory.tsx
      provides: Histórico expansível por estratégia
      contains: RunHistory|histórico
    - path: apps/lab/src/search/SearchCard.tsx
      provides: Card com histórico + excluir integrados
      contains: RunHistory|DeleteSearchDialog
  key_links:
    - from: apps/lab/src/search/DeleteSearchDialog.tsx
      to: labApi.deleteSearch
      via: DELETE ?confirm=true só após confirmação explícita
      pattern: deleteSearch
    - from: apps/lab/src/search/RunHistory.tsx
      to: labApi.listRuns
      via: histórico paginado por searchId (items[0] = mais recente)
      pattern: listRuns
    - from: apps/lab/src/search/RunHistory.tsx
      to: run.tsx route
      via: toque na entrada navega ao run (string literal, sem import da tela)
      pattern: /run\?runId=
---

<objective>
Excluir com cascata explícita + histórico de runs expansível no card (D-11, D-12, UI-15, UI-16).

Purpose: Fecha a fase com segurança (hard-delete consciente) e memória temporal consultável — sem estes, excluir é perigoso e reexecutar perde contexto.
Output: RunHistory + DeleteSearchDialog integrados ao SearchCard, testes, card final da fase.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projetos-buscas-execucao/07-CONTEXT.md
@dev-docs/10-lab-esqueleto-telas.md
@dev-docs/08-security-baseline.md
@apps/lab/src/search/SearchCard.tsx
@apps/lab/src/api/lab.ts

<interfaces>
Decisões D-11/D-12 (valores exatos):
- Histórico mora como EXPANSÍVEL dentro do card de cada estratégia — NÃO é quarta aba (3 abas mantidas: decisão CONTEXT; esqueleto §1 citado como ambíguo e resolvido).
- Entrada: data/hora (executedAt), status (rótulo PT da 07-03), total (coverage.bdtd+capes), novos desde o anterior (metrics.newCount), duração (finishedAt−startedAt, "—" se ausente), parcial mostra o que faltou (fontes failed/skipped + error.message). Toque → abre os resultados do run (placeholder fase 8 = tela run.tsx da 07-03 com link "lista na fase 8").
- Excluir (§14-3, hard-delete com cascata): diálogo lista Nº de runs, Nº de resultados e decisões perdidas ANTES do hard-delete. Sem soft-delete no v1.

Contrato real (sem endpoint de contagem — computar no client):
- labApi.deleteSearch(searchId,{getToken}) → DELETE `/api/v1/lab/searches/:id?confirm=true` → 204 (sem corpo); sem ?confirm=true → 400 CONFIRMATION_REQUIRED. Hard-delete em cascata no banco (runs + resultados + decisões).
- Contagens do diálogo (honestas, sem inventar endpoint): runs = `listRuns(searchId,{limit:100})` → items.length (+ "+" se hasMore); resultados = soma de (coverage.bdtd + coverage.capes) dos runs fetched; decisões = texto qualitativo "decisões de elegibilidade e associações de tags ligadas a estes runs serão perdidas" (sem número — sem endpoint barato na fase 7; NÃO afirmar número). Diálogo: título "Excluir estratégia?", corpo com as 3 linhas de cascata + termo da busca, botões [Cancelar] e [Excluir] destrutivo; Excluir → deleteSearch → onDeleted(searchId) (lista remove o card) ; erro → mensagem verbatim no diálogo, sem fechar.
- RunHistory props: `RunHistory({searchId, getToken, projectId})`; expansível via useState (botão "Histórico (N) ▸/▾"); lista paginada limit 20 + botão "Ver mais" quando hasMore (cursor); cada entrada toca → router.push(`/project/${projectId}/run?runId=${run.id}&searchId=${searchId}`) — string literal, sem importar run.tsx (paralelo seguro com 07-03).
- Habilitar [Excluir] no menu ⋯ do SearchCard (remover o hint disabled da 07-02) abrindo o diálogo; habilitação real do [Comparar] fica na fase 9 (manter disabled).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: RunHistory expansível com entradas completas</name>
  <files>apps/lab/src/search/RunHistory.tsx, apps/lab/src/search/__tests__/runHistory.test.ts</files>
  <read_first>
    - apps/lab/src/api/lab.ts (listRuns + SearchRunDTO/metrics/coverage exatos)
    - apps/lab/src/search/useRunPolling.ts (reusar runStatusLabel/formatDurationMs/isTerminalStatus — NÃO duplicar)
    - apps/lab/src/search/SearchCard.tsx (onde o histórico será embutido na task 2)
  </read_first>
  <action>Implementar D-11 + D-12 + UI-16 (histórico no card):
  1. Criar apps/lab/src/search/RunHistory.tsx: `RunHistory({searchId, projectId, getToken})` — botão cabeçalho `Histórico (N) ▸` (N do primeiro fetch, "+" se hasMore) que expande/colapsa (useState, default colapsado); expandido: lista (`.map` limitado ao fetch atual ≤20 + botão "Ver mais" quando page.hasMore, append com cursor) de entradas tocáveis; cada entrada: linha 1 `DD/MM HH:MM · <rótulo PT> · <total> resultados`, linha 2 `+<newCount> novos · <duração>` (duração via formatDurationMs reutilizado), linha 3 se partial: `faltou: <fontes failed/skipped>` + motivo error.message quando presente; toque → push run como acima. Loading → texto "carregando histórico…" (sem spinner solitário: acompanha o botão); erro → texto + [tentar de novo] (refaz listRuns); vazio → "Nenhuma execução ainda". Formatação de data em helper exportado `formatRunWhen(iso: string): string` (mesmo dia → "hoje HH:MM", senão "DD/MM HH:MM", fallback "—" em inválida). Reusar runStatusLabel/formatDurationMs de useRunPolling (import, sem copiar). Zero `any`.
  2. Criar apps/lab/src/search/__tests__/runHistory.test.ts: formatRunWhen (hoje vs outra data vs inválida), total = coverage soma, parcial detecta fontes faltantes — via helpers puros exportados (`formatRunWhen`, `summarizeRun(run): {total, missing: string[], durationArgs}`), sem rede/timers, sem `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "formatRunWhen\|summarizeRun" apps/lab/src/search/RunHistory.tsx` >= 4 (defs + usos + export)
    - `grep -c "runStatusLabel\|formatDurationMs" apps/lab/src/search/RunHistory.tsx` >= 2 (reuso da 07-03, sem duplicar mapa de rótulos)
    - `grep -c "quarta aba\|TabBar\|/history" apps/lab/src/search/RunHistory.tsx` == 0 (histórico no card, sem aba nova)
    - `grep -rn ": any\|as any" apps/lab/src/search/RunHistory.tsx apps/lab/src/search/__tests__/runHistory.test.ts | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab run test -- src/search/__tests__/runHistory.test.ts 2>&1 | tail -n 8</automated>
  </verify>
  <done>Histórico expansível no card com todos os campos D-12 + ver-mais paginado + toque abre o run; testes verdes</done>
</task>

<task type="auto">
  <name>task 2: diálogo de cascata + integração final do card</name>
  <files>apps/lab/src/search/DeleteSearchDialog.tsx, apps/lab/src/search/SearchCard.tsx</files>
  <read_first>
    - apps/lab/src/search/SearchCard.tsx (menu ⋯ com Excluir disabled da 07-02 a habilitar)
    - apps/lab/src/search/RunHistory.tsx (props da task 1)
    - apps/lab/src/api/lab.ts (deleteSearch com ?confirm=true + listRuns para contagens)
  </read_first>
  <action>Implementar UI-15 + integração (sem quarta aba, sem tela nova):
  1. Criar apps/lab/src/search/DeleteSearchDialog.tsx: `DeleteSearchDialog({search, visible, onClose, onDeleted, getToken})` — ao abrir (visible true): fetch `listRuns(search.id,{limit:100},{getToken})` → runsN, resultados = soma coverage, erro → "contagem indisponível" mas diálogo continua (nunca bloqueia por falha de contagem). Corpo: título "Excluir estratégia?", termo verbatim, 3 linhas de cascata: `• ${runsN} execução(ões)`, `• ~${resultados} resultado(s) (soma da cobertura)`, `• decisões de elegibilidade e tags associadas serão perdidas`; aviso "Esta ação não pode ser desfeita (hard-delete).". Botões [Cancelar] e [Excluir] (destrutivo; disabled durante o DELETE). Excluir → `deleteSearch(search.id,{getToken})` → 204 → onDeleted(search.id) + onClose; ApiError → mensagem verbatim dentro do diálogo (sem fechar). Modal transparent como ProjectModal.
  2. Em apps/lab/src/search/SearchCard.tsx: embutir `<RunHistory searchId projectId getToken />` abaixo das ações (sempre montado, colapsado por default); habilitar [Excluir] no menu ⋯ abrindo o diálogo (remover hint "07-04", manter [Comparar] disabled fase 9); receber prop `onDeleted` repassada da strategies (que remove o card da lista local). Manter Executar/Editar/Duplicar da 07-02 intactos. Zero `any`.
  </action>
  <acceptance_criteria>
    - `grep -c "serão perdidas\|hard-delete\|Excluir estratégia" apps/lab/src/search/DeleteSearchDialog.tsx` >= 3
    - `grep -c "RunHistory\|DeleteSearchDialog" apps/lab/src/search/SearchCard.tsx` >= 4 (imports + usos)
    - `grep -c "disponível na fase 7\|diálogo de cascata na 07-04" apps/lab/src/search/SearchCard.tsx` == 0 (hints removidos onde implementado)
    - `grep -rn ": any\|as any" apps/lab/src/search/DeleteSearchDialog.tsx apps/lab/src/search/SearchCard.tsx | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @uhhu/lab exec tsc --noEmit -p tsconfig.json && pnpm --filter @uhhu/lab exec eslint apps/lab/src/search/RunHistory.tsx apps/lab/src/search/DeleteSearchDialog.tsx apps/lab/src/search/SearchCard.tsx && pnpm --filter @uhhu/lab run test 2>&1 | tail -n 6</automated>
  </verify>
  <done>Excluir só após cascata explícita; histórico vive no card; suite do app toda verde; card final da fase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diálogo → DELETE | ?confirm=true só após toque explícito em [Excluir]; sem exclusão por gesto ambíguo |
| contagens → UI | Números são informativos (podem ter "+" quando paginado); nunca autorizam nem substituem confirmação |
| histórico → runs alheios | listRuns owner-scoped; toque navega só com ids já visíveis no card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | exclusão acidental | mitigate | diálogo com cascata + [Excluir] explícito + sem auto-delete; 400 sem confirm no servidor |
| T-07-04-02 | Information Disclosure | contagem vaza existência | mitigate | contagens vêm de endpoints já owner-scoped; 404 genérico; sem distinguir alheio |
| T-07-04-03 | Denial of Service | ver-mais infinito | mitigate | paginação cursor limit 20; append sob ação manual; sem prefetch |
| T-07-04-04 | Repudiation | delete sem rastro | accept | servidor registra actor/requestId; client exibe requestId em erro; auditoria de deletes é fase futura |
</threat_model>

<verification>
- typecheck + lint + suite completa do app verdes, zero `any`
- Fluxo manual: excluir pede cascata com números; confirma → card some; cancela → nada muda; histórico expande com entradas completas e toque abre o run
</verification>

<success_criteria>
- UI-15: hard-delete somente após diálogo com runs/resultados/decisões listados
- UI-16: histórico expansível no card (sem quarta aba) com todos os campos D-12
</success_criteria>

<output>
After completion, create `.planning/phases/07-projetos-buscas-execucao/07-04-SUMMARY.md`
</output>
